# Агент «Доступность и единицы: rem против px» — ответ не собрался (пять попыток
# отдать JSON со сломанным экранированием), полного текста в журнале нет.
# Уцелела одна формулировка из последней попытки, дословно:
#
#   «rem — для отступов, которые служат читаемости текста и его связи со
#   шрифтом (padding вокруг текстового блока, margin между параграфами,
#   line-height); px — для того, что не должно расти вместе с текстом …»
#
# Оценка: УМЕРЕННО — источник (Josh Comeau, «The Surprising Truth About Pixels
# and Accessibility») лежит в raw/sources/method/josh-comeau-pixels-a11y.html и
# читается напрямую. Тема не перезапускалась ради экономии лимита; в LAYERS.md
# развилка «rem/px» разобрана по остальным агентам и этому источнику.
