/**
 * Имена семей проверки переносимости — один список на проверку и на сборщик
 * набора.
 *
 * Отдельным файлом по той же причине, что `css-families.mjs` и
 * `code-families.mjs`: сборщику набора нельзя ВВОЗИТЬ саму проверку —
 * она запускается прямо при чтении, и сборка обернулась бы прогоном по
 * чужому проекту. А список нужен обоим: проверке — чтобы считать, сборщику —
 * чтобы положить новому проекту базу, где названы ВСЕ семьи, а не те, что
 * помнил автор в день сборки.
 */

export const PORT_FAMILIES = [
  'varGone', 'twoValues', 'markupDrift',
  'dataInView', 'moneyLiteral', 'engineInShared',
]

/** Пустая база: ноль по каждой семье. На новом проекте долга нет, и первое
 *  же нарушение обязано валить сборку. */
export const emptyPortBaseline = () =>
  Object.fromEntries(PORT_FAMILIES.map((k) => [k, 0]))
