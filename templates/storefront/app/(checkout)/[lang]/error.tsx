'use client'
/* Сбой на шаге оформления — тот же экран сбоя, что у магазина: у корневого
   макета кассы своя граница, и экран берётся оттуда, а не пишется второй раз. */
export { default } from '../../[lang]/error.tsx'
