# Открытые вопросы

Что ждёт слова заказчика или следующего шага. Закрытый вопрос уезжает в
`docs/decisions.md` (решение) или в `docs/rules.md` (правило с дефектом).

## Держать в уме

- ~~**Основа объявляет шрифт и тени у себя**~~ — решено (И385): шрифт и роли
  тени объявлены один раз, в `styles/look.css` (у набора рукой, у витрины —
  выпущены из вида), тени — на списке полов; `check:look` больше ничего не
  пропускает, стенды и ворота этапа читают `look.css`.
- **Выпуск стилей вида читает опубликованный вид образца** (`look.json`);
  у Payload (план 4) источником станет global «look» — `scripts/look-slots.mjs`
  должен будет брать его из источника, а не с диска.

## Пересмотреть при смене модели

Подэтапы основания **9 (стенд формы), 10 и 13** и правила **И228–И230**
сделаны не той моделью, что вела подэтапы 0–8: лимит первой кончился на
слое 9. Машиной всё проверено — семьи на нуле, тесты зелёные, — но разбор и
формулировки стоит перечитать, когда лимит вернётся. Найти правки:
`git log --grep 'Co-Authored-By: Claude Opus'`. Заведено 21.09.2026 по слову
заказчика: «фабл закончился, ты делаешь опусом, помечай что сделано опусом,
чтоб потом пересмотреть».

## Ждут заказчика

- **Форма главной кнопки для магазина** — шесть форм в каталоге (И276),
  одобрены стрелка и шеврон; какую носит демо, заказчик ещё не назвал.
  Тона хвоста с 24.09.2026 — роли строителя палитры по полу (И295, И296):
  на тёмном герое хвост сходит к тёмному полу, ручки `--btn-ground` больше
  нет. Подписи вариантов в каталоге («each a step lighter») написаны про пол
  страницы; на тёмной полосе тона темнее заливки — подпись править словом
  заказчика, если форма с хвостом будет выбрана.
- **Черновик вида демо** (`lib/source/sample/look.draft.json`, 11:30) хранит
  прежние значения кнопки «Строгий угол»: он не тронут (черновик заказчика);
  первое же нажатие в панели перепишет его новыми осями.

- **Шрифт витрины** — показан на её же тексте (`face:stand`), не назван.
  Замер сузил выбор до трёх: Manrope, IBM Plex Sans, Source Serif 4 —
  таблица и почему выбыли трое в `docs/decisions.md`, «Пространство и текст».

Закрыто 21.09.2026: **набор ритма** — «Нынешний»; **характер витрины** —
«Ровный магазин». Оба — `docs/gate.md` и `docs/decisions.md`.

## Румынская запятая — в данных, не в шрифте (слою 12, слова)

Замер 21.09.2026 в Chromium: у всех шести кандидатов `ș ț ă î â` свои, а
не подменные. Значит, шрифт румынский текст не ломает ни один — ломают
ДАННЫЕ, если в них стоят седильные `ş ţ` (U+015E / U+0162) вместо своих
`ș ț` (U+0219 / U+021B). Разница видна румыну и не видна нам: хвостик
вместо запятой.

Меряется одной строкой по данным, а не глазом, — поэтому в день, когда
заводится словарь слов (`docs/words.md`, слой 12), это становится проверкой:
седильный знак в румынском тексте — дефект. Сегодня проверять нечего: своего
текста в наборе нет.

## Найдено стендами 21.09.2026 (ждёт решения)

Три находки со стендов характера и шрифта. Ни одна не чинилась на месте:
каждая меняет договор набора, и правится отдельным заходом с отрисованной
проверкой на обеих темах.

- **Лист обещает пол и не красит его.** Закон `craft` называет примитив так:
  «`sheet` — **лист**: предмет со своим полом — поле, угол, отмена линии
  страницы внутри». В `styles/primitives.module.css` у `.sheet` есть поле,
  угол и отмена линии — и нет заливки. Поэтому оба стенда, показывающие
  лист, дописывают ему `background: var(--plate)` своей строкой. Атрибут
  `[data-plate]` тут ни при чём и работает как задумано: его собственный
  комментарий говорит «предмет, который красится `--plate` и потому за полом
  не идёт, ставит `data-plate`», то есть красит предмет, а атрибут возвращает
  роли. Решить надо одно: красит ли `.sheet` пол сам (тогда роли листа
  переезжают в примитив и перестают жить в двух местах) или закон
  переписывается под сегодняшний код.
- **На ширине перетока карточка полки не вмещает кнопку покупки.** Пол ряда
  у примитива `grid` — две ячейки («карточка во всю ширину телефона читается
  как баннер», решение заказчика). На 320, куда набор обязан складываться по
  норме перетока, каждой карточке достаётся около 112px, а кнопка «В
  количката» просит 138 и не помещается никогда. Решение заказчика
  принималось на 360 и шире. Либо полка объявляет `--cols-min: 1` ниже
  первого шва, либо на 320 у карточки не кнопка, а ссылка.
- **Пилюля не переносит подпись.** У `.chip` стоит `white-space: nowrap`, и
  «широк спектър» на 360 вылезает из своей колонки. Стенд разрешил перенос у
  себя. Стоит решить, не должно ли это жить в самом примитиве: рост пилюли
  держится ролью размера, и перенос его не ломает.

## Следующие шаги основания (по слоям, `npm run stage`)

- **Слой 2 · оси в проекте**: `<meta name="color-scheme">` раньше стилей и
  `theme-color` по теме, cookie `theme=` с сервера, проверка трёх состояний
  темы в браузере, `lang` на узлах с чужим языком, фикстура самых длинных
  слов каждого языка на 320 — всё это проект (Next), не набор.
  `any-pointer: coarse` вместо `pointer: coarse` — при первом планшете с
  трекпадом в замере. Масштаб 1 : 1.25 под пальцем (Spectrum) — вопрос
  заказчику, если кнопки на телефоне покажутся мелкими.

- **Слой 1 · словарь имён**: реестр держит форму и ярусы, но словарь
  остался коротким (`--ink`, `--pop`, `--plate`), а не строгой грамматикой
  Curtis / Style Dictionary (`--color-action-background-primary-hover`).
  Переименовывать ли — решение заказчика; если да — псевдоним со сроком на
  каждое старое имя. Кривые `--hero-size`, `--pagehead-size`, `--intro-size`
  (от контейнера, `cqi`) пока в `tokens.css` рукой — перевести в строитель.
  Семья `unknownVar` (ссылка на несуществующее имя без запасного) ждёт
  своего дефекта.

- **Слой 7 · размер органов в проекте**: отрисованная проверка «органы
  одного размера в одной строке равны по высоте» (Curtis) — когда появятся
  узлы (`touch-action: manipulation` с И229 стоит в основании).
- **Замер на рыночных ширинах**: 393, 384, 414, 360 (Румыния, Хорватия);
  375 нет в первой шестёрке — коридоры воздуха снимались на 375 и 1440.
- **Пороги контейнерных запросов — свой реестр**: у примитивов пороги
  (`--switch-at` 640, `--lede-at` 789, `--side-w` 360) стоят числом с
  причиной в комментарии; Tailwind и Radix держат их отдельным списком
  («container seams» — имя и причина у каждого). Завести, когда порогов
  станет больше пяти или два примитива сойдутся на одном числе.

## Витрина RO — отложено после плана 1 (23.09.2026)

Найдено итоговым разбором ветки витрины (`templates/storefront`). Каждая
строка — что и куда: план витрины или скилл, в котором это чинится.

- **Карта сайта собирается при сборке** и падает, если источник не ответил:
  с живыми источниками — на запрос и с обновлением (`revalidate`). План 4.
- **Промах данных** (нет товара, полки, документа) отвечает 404 и
  `noindex`, а своя страница появляется только после гидратации; полная
  серверная 404 — вместе с живыми источниками и `cacheComponents`. Каждая
  такая 404 спрашивает источник дважды (`shellData`) — кэш. План 4.
- **Пол «бумага»**, описанный в `styles/tokens.css`, не имеет роли краски;
  подвал стоит на `--surface`. Скилл `palette`.
- **Кандидат в семью `check:css`**: `margin:0` после ребёнка `stack` при
  равном весе отменяет ритм примитива (найдено на приёмке). Скилл `craft`.
- **JSON-LD товара с вариантами**: на каноническом адресе без выбранного
  варианта предложения нет — `AggregateOffer`, валюта из
  `variant.price.currency`, сумма через `toAmount()` набора. До того, как
  `PRICES_ARE_REAL` станет `true`. Скилл `shop`.
- **Canonical страниц листания** ведёт на первую страницу, а по договору —
  на себя (отфильтрованная полка — на полку без фильтра). План 5, проход СЕО.
- **`LangSwitch`** строит адреса мимо `hrefFor`, теряет строку запроса
  (вариант и фильтры пропадают при смене языка) и на случайной 404 ведёт на
  неё же — альтернативы от страницы. План 4.
- **Подвал** сам ввозит реквизиты и контакты и сам решает колонки
  документов — в `ShellData` и договор `Market`. План 4.
- **Договор и живые данные**: `images[0]`, `variants.reduce` без начального
  значения, обязательный `Card.image` — допускают ли пустое адаптеры. План 4.
- **Нет своей `app/global-error.tsx` на языке рынка**; `source()` бросает
  синхронно на неизвестном `SOURCE`. План 4.
- **`install.mjs --update` не обновляет** скопированные помощники Vendure и
  коммерции в `lib/source/vendure/core` и `lib/commerce`. План 5.
- **Повторная установка `--force`** не удаляет файлы, убранные из шаблона.
  План 5.
- **Ссылка SOL**: сообщается, что платформа споров ЕС (ODR) закрыта
  20.07.2025 (Регламент (ЕС) 2024/3228) и обязанность ссылаться на неё
  отпала; приказ ANPC 449/2022 говорит о пиктограммах SAL/SOL, а шаблон
  рисует текстовые ссылки. **Решают заказчик и юрист**; до их слова шаблон
  держит обе ссылки (`lib/company.ts`).

## Витрина — вид из админки (план 4)

- **Global «look» в Payload** — источник `content().look()` живого сайта
  вместо `lib/source/sample/look.json`. Хранит ОДИН вид готовыми
  значениями, как образец: `vars` (свойства из закрытого списка сайта
  `lib/look-slots.json`), `header`, `fonts` (свои woff2), `names`. Хук
  `afterChange` зовёт `POST /api/revalidate` с заголовком
  `x-revalidate-secret` (`REVALIDATE_SECRET`) и телом `{ "tag": "look" }` —
  вид меняется через секунды, без сборки (профиль `max`: первый запрос ещё
  получает прежнюю статическую страницу, И270).
- **Global «look» проверяет при сохранении чистым правилом сайта**:
  `acceptLook` из `lib/look-rule.ts` (значения — `lib/look-values.ts`:
  имя из списка, значение по роду, без знаков, которыми внедряют CSS;
  сочетание — пары групп, которые не носятся) по фактам
  `lib/look-slots.json`. Не прошедшее не сохраняется, с причиной словами —
  той же, что пишет журнал сайта и показывает панель вида (И270).
- **Черновик и публикация из панели в Payload.** Сейчас панель вида пишет
  черновик и публикует в файлы образца (`look.draft.json`, `look.json`);
  живому магазину — черновая версия global «look» (у Payload черновики и
  живой предпросмотр свои) и публикация через его API с тем же
  `check:choice` перед записью. Там же решить, где лежат шрифты
  опубликованного вида: `public/fonts/` сайта (сейчас) или медиа Payload.
- **Кэш данных Next переживает сборку**: `unstable_cache` вида лежит в
  `.next/cache` и после `next build` отдаёт последнее закэшированное, а не
  то, что сейчас в источнике, — пока не придёт `POST /api/revalidate`.
  Правка `look.json` руками без сброса после пересборки не видна
  (найдено 24.09.2026 на копии витрины). Сборке — сбрасывать тег `look`
  после запуска или чистить кэш данных; решить в плане 4 вместе с хуком.
- **Установщик не удаляет файлы, которые шаблон перестал носить**:
  `install.mjs --storefront --force` поверх демо оставил прежние
  `components/Look.tsx`, `lib/look-data.ts` и шрифты, и `tsc` демо
  краснел, пока их не убрали руками (24.09.2026). Нужна запись
  поставленных файлов шаблона и уборка отставших — как у инструментов
  (`.site-kit-install.json`).

## Витрина — карта товара: галерея, цена в кнопке, евро (24.09.2026)

- **Снимки товара из Vendure.** `Product.images` (первый — главный) —
  `featuredAsset`, за ним `assets` без него (тот же ассет в `assets` бывает
  и главным — без повтора), размеры — из пресета ассета. Сейчас снимки
  рисует образец (`lib/source/sample/art.ts`, три-четыре вида с пометкой
  «sample»). План 4.
- **Прежняя цена из Vendure.** `Variant.was` — своё поле варианта (у Vendure
  его нет из коробки; `compareAtPrice` в минорных единицах, цена с налогом —
  как `priceWithTax`); пусто — `null`, плашки и зачёркнутой цены нет. Полка
  (`Card`) прежней цены пока не знает — плашка только на карте товара.
  План 4.
- **Условия продажи образца говорят «цены в евро»** (`lib/docs.json`).
  Магазин, поставленный с `--currency RON`, правит эту строку в своих
  условиях — они приходят из админки; ставщик текст не переписывает.
- **Крошки в одну колонку карты товара, 560–820.** На телефоне уже 560 крошки
  — один шаг назад (сделано 24.09.2026), а шире, пока карта в одну колонку,
  цепочка стоит по линии страницы, а колонка галереи и покупки — по
  середине ряда шириной `--gallery-w`. Решить, чья линия у крошек на карте в
  одну колонку. `components/Breadcrumbs.tsx`, `ProductView.module.css`.

## Витрина — найдено проверкой выбранного вида (24.09.2026)

`check:craft` на трёх страницах `/en` и `check:choice` по каталогу панели.
Ни одна находка не чинилась на месте: каждая — устройство вёрстки, не вида.

- **Громкая кнопка-контур на палубе героя не читается.** Стили «Контур» и
  «Тонкий люкс» красят надпись `--pop-ink`, а роль кнопки
  (`--ctrl-btn-ink-pop: var(--pop-ink)`) раскрывается на корне — палуба
  героя (`data-ground="deck"`) её не переназначает. «Pharmacy blue» +
  «Quiet luxury»: «See the products» 1,9 : 1 при 4,5. Правило сочетаний
  меряет пол страницы и карточки, палубу со снимком — нет; ловит
  `check:choice`. Нужны роли кнопки, которые пол переназначает, как прочие.
- **Ритм «Тихий» теснит карточки полки на 1024.** Поле карточки 24–28 px
  при карточке 216 px — 26 % ширины (`lane`, 16 находок). Ловит
  `check:choice`; решить, мерить ли поле карточки от её ширины.

## Главная витрины — после разбора (24.09.2026, пакет C)

Разбор главной и переделка блоков — И279–И283. Здесь то, что не сделано
или ждёт другого.

- **Слова расходятся с данными о сроке доставки.** Данные образца
  (`lib/shipping.ts`): курьер и постамат — 1–2 рабочих дня. Слова: ответ
  «How long does delivery take?» в вопросах главной (`lib/pages.ts`) и
  полоса обещания шапки `header.promise` (`lib/i18n/*.ts`) — «1–3».
  Наполнение не правилось: решает заказчик, какое число верно.
- **Протокол у образца — заглушка.** `public/sample/lab-RO-2409-10.pdf`
  помечен SAMPLE; настоящий документ по каждой партии даёт лаборатория и
  магазин (Vendure — файл партии). У товаров образца (`lib/source/sample/catalog.ts`)
  адрес протокола — всё ещё якорь `#lab-…` без цели.
- **Поиск по номеру партии (разбор H5) не сделан.** Форма «номер с
  этикетки → протокол» без скрипта требует адреса, который знает партии:
  поиск ищет только по имени (`lib/source/sample/catalog.ts`), а маршрута
  `/lab/<партия>` нет. Решить вместе с данными партий в Vendure.
- **Полоса доверия под героем (разбор H3) не сделана.** «Доставка от
  12,99 lei» — наименьшая цена платного способа: это функция вида в `lib/`
  (не в компоненте), а оплата при получении без сессии данными не
  приходит (`paymentMethods` просит сессию). Решить с пакетом D.
- **Герой на телефоне — выбор заказчика.** Стоит «снимок сверху, текст
  под ним»; вторая раскладка — текст поверх снимка с длинной вуалью снизу.
  Оба показаны снимками первого экрана 375 (отчёт пакета C, 24.09.2026).
- ~~**`plateGap` пропускает модуль по любому потребителю.**~~ — решено (И384):
  привязка к классу правила, а не к модулю. Семья в
  `tools/check-css.mjs` считает лист объявленным, если ХОТЬ ОДИН файл,
  берущий модуль, несёт `data-plate`. `data-plate` листа лаборатории
  (`components/blocks/Lab.tsx`) погасил находку о лотке-листе
  `styles/primitives.module.css:842` (`.tray[data-tray='plate'] > *`),
  который так и не объявлен. Нужна привязка к классу, а не к модулю.

## Каркас и шрифт витрины — после разбора (24.09.2026, пакет A)

Рама страницы, лестница заголовков, документы, «не найдено», подвал —
И287–И294. Здесь то, что не сделано или ждёт другого.

- **Первая фраза «Доставки и оплаты» — текст заказчика.** Скилл `shop`,
  «Обязательные страницы»: первая фраза отвечает на цену, срок, порог
  бесплатной и способ оплаты. Сейчас строка о документе — «Courier, pickup
  points and cash on delivery», без цены и срока (`lib/docs.json`). Не
  правилось: наполнение.
- ~~**Закрытая касса (разбор S2) — с пакетом D.**~~ Сделано пакетом D
  (И325): своя группа с корневым макетом `app/(checkout)/[lang]` и рама
  `chrome` у `Shell`.
- **Поиск в выдвижном меню (разбор S4) не сделан.** Шапка — варианты вида с
  метками снятия (`look-header:*`), у варианта `search` поле уже есть;
  поле в шторке `classic` и `boutique` — после того как правка шапки на
  main устоится.
- **Строка оплаты в подвале (разбор S5) — без данных.** Способы оплаты
  приходят только с сессией (`paymentMethods(session)`); подвалу нужен
  список магазина без корзины — то же, что полосе доверия пакета C. Место
  под пиктограммы ANPC SAL/SOL — после ответа юриста (выше, «Ждут
  заказчика»).
- **Остаток `check:design` после пакетов B и D — 1:** `flatRhythm` 1 —
  плитки категорий (главная, пакет C; `components/blocks/Categories.tsx:21`,
  между плитками `--gap-grid`, внутри плитки `--air-row`). Заголовок фильтров
  снят пакетом B (И318); корзина, касса и «спасибо» — пакетом D (И328,
  И333). Финальный проход 24.09.2026 его не трогал: главную в этот день
  переделывает другая сессия (варианты главной) — находка передана ей.
- ~~**`.more` рисует движение стрелки на месте**~~ — решено (И383): `.more`
  без потребителя снят, дом всего, что ведёт, — `go.module.css`.

## Детектор impeccable на витрине — первый замер (24.09.2026, И310)

База `check:detect` записана с витрины после пакета A: 12 находок.

- **`headCrowd` 9 — разделы документа на 1440** («Контакты» на каждом
  языке, «Доставка» по-венгерски). Раздел — `sidebar` (`DocView.module.css`,
  `.part`): заголовок узкой колонкой слева, текст справа, первые строки на
  одной линии шрифта. Детектор меряет воздух над заголовком и под ним по
  вертикали и в раскладке «рядом» читает заголовок прижатым к чужому
  разделу; на 375, где пара сложена, находок нет. Решить глазом (critique
  по порядку скиллов): если раскладка верна — выключить `heading-rhythm` в
  `tools/detect-families.mjs` с записью причины (разбор, шаг E4: решение
  набора сильнее умолчания детектора); если нет — чинить раздел.
  **Решено (И302):** заголовок сбоку от своего блока — не «прижатый»,
  `headCrowd` 9 → 0, база 12 → 3.
- ~~**`firstScreen` 3 — полка каталога на 1440**~~ — закрыто пакетом B
  (И318). Замер был о другой колонке, чем казалось: высокой была ПОЛКА
  (147 % окна), а панель фильтров помещалась (77 %). Колонки сбоку больше
  нет — грани полосой раскрытий над полкой, полка во всю ширину;
  `check:detect` на `/en/catalog`, `/en/catalog/uleiuri`, `/en/search`,
  `/en/search?q=oil`, `/en/search?q=zzzz` — 0 находок.

## Полка, пакет B — что осталось (24.09.2026)

- **Миллиграммовая шкала грани силы** (cbd-facet, §5: мг диапазонами по
  числу товаров) не сделана: грань «Сила» образца — только проценты, у
  капсул и косметики силы в фильтре нет, хотя мг у них теперь в данных
  (`Pack.mg`). Скилл `shop`, когда магазину понадобится фильтр по мг.
- **Упаковка у Vendure** (`Card.strength`, `Card.packs`): у образца — поля
  варианта в `lib/products.ts`; у адаптера — пользовательские поля варианта
  (мг, мера, единица) и ось силы у товара. План 4, вместе с картой товара.
- **Счёт граней у Vendure** — «против всех, кроме своей» (cbd-facet, §3):
  `search.facetValues` отдаёт счёт по текущей выборке; адаптеру нужен второй
  запрос на грань без её собственного выбора. План 4.
- ~~**Поиск с запросом не в дереве маршрутов**~~ — решено при вливании
  (И345): ключ `queries` в `kit.config.json`, у витрины
  `/[lang]/search` с `q=cbd` (находки на всех трёх языках) и `q=zzzz`
  (пусто); `check:craft`, `check:detect` и `check:open` берут их из
  дерева маршрутов.
- ~~**Запись числа в английском — рынка**~~ — решено при вливании (И347):
  число пишет язык страницы, одной функцией `lib/format.ts` — en «2.5 %»,
  ro и hu «2,5 %»; цена, строка фактов, грань, протокол и скидка — ею.
- ~~**Третий вариант карточки: «Outlined» или «Compact»**~~ — решено при
  вливании (24.09.2026): «Outlined» везде — `lib/cards.ts`, стили
  `ProductCard.module.css`, строка `CARD_LINES` панели. Одежда отличается
  контуром (волосок вместо тени), а «Compact» в той же панели уже называет
  набор ритма «Тесный» — опубликованный выбор заказчика (`docs/decisions.md`).
  Строка панели больше не обещает «плотную полку»: плотность полки у трёх
  одежд одна.

## Корзина и оформление — после разбора (24.09.2026, пакет D)

Коридор кассы, товар на каждом шаге, кнопка заказа в первом экране, один
счётчик, обещания из данных — И325–И334. Здесь то, что не сделано или ждёт
другого.

- **Срок возврата образца — 14 дней** (`lib/source/sample/content.ts`,
  `facts()`): законный минимум ЕС, а не слово магазина. Настоящий срок
  называет заказчик — и число в данных обязано совпасть с текстом его
  страницы «Retur» (сейчас там образец). Ждёт заказчика.
- **«Защищённое оформление» в закрытой шапке не написано.** Разбор (S2)
  предлагал надпись «Secure checkout»; это утверждение магазина, а не
  вёрстки, — место в шапке есть, слово — за заказчиком.
- ~~**Порядок «фамилия, имя» по-венгерски.**~~ — решено (И381): порядок частей
  имени — факт языка `NAME_ORDER` (`lib/locale.ts`); по нему стоят поля формы
  и пишется имя в сверке, шаблона `order.name` в словаре больше нет.
- **Полка пустой корзины берёт сортировку источника `popular`.** У образца
  это порядок каталога; у Vendure «ходовые» — отдельный сигнал (продажи или
  ручной список в Payload). План 4.
- ~~**Числа рвутся с единицей и в оформлении**~~ — решено (И387): объём и
  штуки образца — через `BIND`, сроки в словарях — `{n}\u00a0` перед единицей.
  Имена вариантов живого магазина заводит админка — правило для неё то же.
- **Кнопка шага доставки прячется `:has`.** Браузер без `:has` (до Safari
  15.4, Firefox 121) показывает и «Choose delivery», и подробности прежнего
  способа — путь работает, громких кнопок две. Замер доли таких браузеров —
  перед сдачей.
- ~~**Переустановка не убирает файлы, которые шаблон перенёс**~~ — решено
  при вливании (И340): `.site-kit-install.json` держит список файлов
  шаблона с хешами, `--storefront --force` снимает нетронутые файлы,
  которых шаблон больше не везёт, с опустевшими папками и
  `.next/dev/types`; поправленные сайтом — оставляет и называет.

## Вливание пакета B и гигиена набора — что осталось (24.09.2026, И340–И347)

- **Просители из каталога после снятия панели.** Набор ритма витрины
  называет просителей общего ряда из каталога панели (`каталог`, И343). В
  магазине после `look:remove` каталога нет, а запись остаётся — правдой о
  том, откуда пришёл опубликованный вид (он несёт весь ряд). Убрать
  ступени, которых не просит никто, можно только вместе с опубликованным
  видом: это работа `look:remove`, её там пока нет.
- **База `check:css` набора — она же база новой витрины.** `check:css`
  набора видит только свои `styles/`, а `tools/css-baseline.json` набора
  едет новому сайту: долг шаблона обязан помещаться в эту базу, а
  `--update` в наборе опустит её до долга одного набора. Сейчас у шаблона
  ни одной находки сверх базы (knobTie — 0 и там, и там); сторожить это
  стоит прогоном `check:css` установленной витрины в `npm test` набора.
- ~~**Опубликованный вид демо старше списка свойств сайта.**~~ — решено
  (И352, И353): сборка каталога пересчитывает опубликованный вид из его имён
  нынешним каталогом, прежний файл — копией рядом; `check:choice` не
  пропускает вид, который оставляет свойство сайта без значения. У демо 56
  свойств получили значения «Тёплого листа», имена те же.
- **Черновик демо старше каталога.** `look.draft.json` заказчика несёт
  прежнее поле `button: «Пилюля»` и 162 значения из 213; пересчёт его не
  трогает (И352: черновик — работа в руках заказчика). Первый щелчок в
  панели соберёт черновик из имён заново; до того `check:choice -- --draft`
  скажет «НЕ ГОДИТСЯ» — покрытие (И353).
- **Панель вида ходит к Google за шрифтами образцов.** `check:detect` с
  панелью видит четыре запроса `fonts.googleapis.com` — это образцы гарнитур
  в панели; страница покупателя шрифты берёт со своего адреса. Только при
  `LOOK_PICKER=on`.
- ~~**Вход в панель — после страницы** (И344)~~ — решено (И354): вход —
  тонкая полоса «Look» у низа окна, место под неё страница держит сама
  (резерв нижней полосы `--dock`).

## Витрина RO — отложено после плана 2 (23.09.2026)

Приёмка корзины и оформления (`templates/storefront`, план 2). Строка — что
и куда.

- **Письмо с подтверждением заказа** шлёт сервер (Vendure). «Спасибо» его
  не обещает. План 4.
- **Точки выдачи от настоящих служб**: адаптер к списку точек службы
  (`api.points` в справочнике `carriers.json`) или её виджет, кэш списка.
  План 4.
- **Оплата онлайн** (`kind: 'online'`): в договоре есть, а в оформлении нет
  перехода к провайдеру и возврата от него. Провайдер живёт на сервере
  Vendure. План 4 и позже.
- **Счёт на фирму**: румынский рынок, CUI и название фирмы в заказе. Нужны
  поля и договор. План 3 или 4.
- **Порог бесплатной доставки**: число в данных магазина и строка в корзине
  «до бесплатной доставки осталось…». Скилл `shop`, план 4.
- **Службы образца** (FAN Courier, Sameday, магазин) выбрал исполнитель из
  справочника. Настоящие выбирает заказчик (замысел, «Решения, которые ждут
  заказчика»).
- **Справочник служб** перепроверять перед запуском каждого магазина:
  службы сливаются и закрываются (`references/delivery.md`, конец раздела
  «Справочник по странам»).
- **Пересылка CBD**: до запуска письменно спросить выбранные службы
  (`references/delivery.md`, «CBD и службы доставки»). Заказчик.
- **Формулировку кнопки заказа и сообщений** утверждает заказчик в рамке
  И262.
- ~~**Ручка `--side-align` примитива `sidebar`**~~ — решено раньше (разбор
  24.09.2026, D3): `align` стоит в `MODIFIERS` `tools/names.mjs`, ручку первым
  объявил документ; проверено финальным проходом.
- **Панель фильтров одна на страницу**: у формы постоянный `id="filters"`,
  и кнопка открытия ссылается на него. Вторая `Filters` на странице
  потребует свой `id`. Скилл `craft`, когда понадобится.
- **Шторка фильтров на телефоне**: не закрывается смахиванием, не
  выезжает движением, страница под ней прокручивается. Родной `popover`
  даёт Escape, закрытие нажатием мимо и кнопку ×. Смахивание требует
  скрипта; выезд — `@starting-style` с ролями `--open-t` / `--ease`; запрет
  прокрутки под шторкой — вопрос модальности (правило 8). Скилл `craft`.
- **Без скрипта «Назад» после ошибки формы** (найдено приёмкой). Ошибка
  приходит ответом на POST: `useActionState` с адресом формы. Эта запись
  остаётся в истории. Путь «пустая отправка контактов → исправил →
  доставка → Назад» ведёт на неё, и браузер спрашивает о повторной отправке
  (`ERR_CACHE_MISS`). Если контакты введены с первого раза, «Назад»
  работает: поля приходят с сервера заполненными. Починка — «POST → переход
  → GET» и для ошибки. Состояние формы кладётся на короткое время в cookie
  этого же браузера, а страница читает его при показе. Это касается пяти
  форм оформления, поэтому это решение уровня замысла, а не приёмки. План 3.
- **Снимки строк корзины и полки без `srcset`**: образец рисует товар SVG,
  и у вектора веса нет (И264). Живой источник отдаёт растр: миниатюре
  строки корзины (96–112px с пакета D, И326) и сводки оформления (53–62px)
  нужен малый вариант (у Vendure — `preset`
  ассета), полке и карте товара — `srcset` и `sizes`. Проверка `weight`
  замерит это на живых данных. План 4.
- **Предел количества в строке корзины** не в договоре: вид корзины и
  образец держат 99 (`MAX` в `sample/commerce.ts`, слова
  `cart.error.quantity`). Нужно поле договора (`Cart` или строка) или
  постоянная адаптера. План 4.
- **Договор покупки под Vendure** (итоговый разбор плана 2) — что придётся
  поменять, когда встанет живой источник. План 4:
  - `lastOrder(session)` → `order(session, code)`: у гостя Vendure нет
    «последнего заказа», подтверждение — `orderByCode`. Код поставленного
    заказа хранится в cookie `httpOnly` этого браузера; окно — два часа
    (`DefaultOrderByCodeAccessStrategy`), как у образца.
  - Таблица способов на странице «Доставка и оплата» просит
    `shippingMethods(lang)` без активного заказа
    (`eligibleShippingMethods` Vendure требует заказ) — или строки таблицы
    ведёт содержание.
  - `Cart.discounts` делится на `couponCodes` и `discounts: { label,
    amount }`: автоматическая акция Vendure — не код, который покупатель
    может убрать.
  - `Order.payment` сужается до `{ code, kind, name }`: у поставленного
    заказа Vendure есть только код способа.
  - `CommerceError` `'contact-conflict'` — `EmailAddressConflictError`
    (адрес принадлежит зарегистрированному покупателю).
  - Оплата `'online'` требует у `placeOrder` исхода «переход к
    провайдеру», а не только заказа или отказа.
- **Имена заготовленных сессий есть только у образца**: под живым
  источником `sample-*` не существует, и `#as=` открывает экран «пусто».
  `kit.config.json` → `sessions` правится под проект, либо проверки
  убеждаются, что страница с `#as=` не легла на пустой экран. Скилл
  `craft`, план 4.

## Разбор чужих дизайн-скиллов (24.09.2026, И300–И301)

- **PRODUCT.md ждёт слова заказчика.** Черновик в `templates/storefront/PRODUCT.md`
  собран по репозиторию; каждый выведенный факт помечен «подтвердить словом
  заказчика». Подтверждённый теряет пометку, отменённый вычёркивается.
- **Шапка считает свою ширину от `--gut` сама** (`components/Header.module.css`,
  строка ряда: `min(var(--wrap) - var(--gut) * 2, 100% - var(--gut) * 2)`) —
  копия формулы `.wrap` и потому не берёт `--edge-x`: в альбомной ориентации
  на iPhone знаки шапки могут встать под вырез камеры. Чинится переходом ряда
  шапки на `.wrap` или на ту же запись с `--edge-x` — правка шапки (пакет A).
- **«Не найдено» — свой корень** (`app/global-not-found.tsx`): у него нет
  `export const viewport` корневого макета, и его мета окна без
  `viewport-fit=cover` — на этой странице вырез ноль. `check:seo` страницы
  «не найдено» не мерит (служебный лист). Одна запись окна на оба корня —
  правка страницы «не найдено» (пакет A).

## Варианты главной — что осталось (24.09.2026, И360–И364)

- **Слово магазина пусто.** Блок `story` (`templates/storefront/lib/pages.ts`,
  `STORY`) — место заказчика: заголовок, два-три предложения о магазине и
  снимок со своим `alt`. Нужен варианту Headline first; пока пуст — молчит
  во всех вариантах (`docs/design/home.md`, раздел 5).
- **Полоса «Look» срезает длинное имя палитры на телефоне.** `check:choice`
  с палитрой «Pharmacy blue» на 390 — «НЕ ГОДИТСЯ»: `span.lp-band-words`
  срезан `nowrap` на 14 px на всех трёх страницах, у опубликованного вида —
  ноль. Вариантами главной не вызвано: так на любой главной. Правка полосы
  панели (`look-panel/ui/look.css`, `.lp-band-words`) — у сессии панели.
- **Плакатный заголовок для Headline first** — новая роль крупного текста в
  строителе шкал, а не число на месте; ждёт выбора направления заказчиком
  (`docs/design/home.md`, раздел 7).
- **Заголовок полок у Cabinet стоит у края**, а не по середине, как всё
  остальное в первом экране: `sectionHead` — примитив, и встать по середине
  он может только силой места через класс, который есть у всех вариантов
  плиток. Решится, если заказчик выберет Cabinet.

- ~~**Шапка считает свою ширину от `--gut` сама**~~ — решено (И382): ряды шапки
  уже стояли на `.wrap`, копия формулы оставалась у панели полок «boutique»
  и у запасного края списка языка; коробка страницы — роли `--page-box` и
  `--page-edge` (`styles/tokens.css`), их берут `.wrap` и шапка.
- ~~**«Не найдено» — свой корень**~~ — решено раньше: `app/global-not-found.tsx`
  берёт `docViewport` (`components/Shell.tsx`), мета окна отдаёт
  `viewport-fit=cover`; проверено финальным проходом на `/en/nu-exista`.

## Финальный проход перед сдачей (24.09.2026, И380–И387)

Полный прогон на демо (main 591edb7): `check:craft` — 69 адресов дерева с
сессиями, три языка, обе темы, палец; `check:detect` — 69 × 375 и 1440 ×
обе темы; свип — 69 адресов × 41 ширина; `check:seo`, `check:urls`,
`check:open --built`; файловые проверки. Исполнителю досталось и сделано:
`target` 8 (подвал кассы, И380), имя по-венгерски (И381), коробка страницы
(И382), `.more` (И383), `plateGap` (И384), шрифт и тени (И385), закон craft
(И386), число с единицей и диапазон (И387). Здесь — что осталось.

- **«Спасибо» по-венгерски на 320 — заголовок столбиком** (свип:
  «Köszönjük, a / rendelését / rögzítettük», три строки по ≤ 12 знаков).
  Два длинных слова не встают в одну строку заголовка страницы на полу его
  кегля (30 px в колонке 296 px). Вёрсткой без смены слов или шкалы это не
  лечится: слова утверждает заказчик (`docs/words.md`), набор ритма
  «Тесный» — его выбор. Решает заказчик: короче слова («Köszönjük a
  rendelését» — две строки) или пол кегля заголовка страницы ниже на
  телефоне (затронет все заголовки страниц на самых узких экранах).
- **Главная — находки для сессии вариантов главной:** `flatRhythm` 1
  (`components/blocks/Categories.tsx:21`: между плитками `--gap-grid`,
  внутри плитки `--air-row` — группа не отделена); громкая кнопка-контур на
  палубе героя (раздел «найдено проверкой выбранного вида» выше).
- **`LangSwitch` и `LangMenu` — один и тот же расчёт адреса дважды** и оба
  теряют строку запроса (вариант, фильтры) — план 4, вместе с адресами
  языков от страницы.
