# Правила интерфейса

Файл переносимый: он не про этот магазин, а про любой. Правила пишутся так,
чтобы их можно было положить рядом с чужим проектом и применить, ничего не
переписывая. Частные случаи и числа этого проекта — в `docs/corrections.md`,
здесь только общее.

Каждое правило заведено потому, что его отсутствие уже стоило дефекта.
Дефект назван, иначе правило через полгода читается как вкусовщина и его
снимают.

---

## И1 · У шапки есть очередь на выживание

Элементы в строке шапки не равны. Когда ширины перестаёт хватать, они
уходят **строго по одному, начиная с самого дешёвого** — и никогда не бывает
так, что один элемент выдавил другой за край.

Очередь для магазина, сверху вниз — первым уходит верхний:

| Уходит | Элемент | Куда уходит |
| ---: | --- | --- |
| 1-м | пункты меню | по одному в бургер, начиная с последнего |
| 2-м | контакты, мессенджеры | в бургер |
| 3-м | поиск | в бургер, своим полем |
| 4-м | избранное | в бургер / нижнюю панель |
| 5-м | кабинет | в бургер / нижнюю панель |
| — | логотип | остаётся всегда |
| — | **корзина со счётчиком** | **не уходит ни на одной ширине** |

Порядок этой очереди — про то, кто уступает. Про то, что уступать приходится
слишком многим, — И5: перестановка очереди меню, которое не помещается, не
чинит.

Меню уступает первым, и это не мелочь порядка: одиннадцать имён категорий —
единственное в строке, что стоит тысячу пикселей, тогда как все знаки вместе
стоят полутора сотен. Отдав знаки первыми, платишь поиском и кабинетом, а
меню всё равно не помещается. Проверено на живой шапке: при порядке
«сначала знаки» на 1600px в магазине не оставалось ни поиска, ни кабинета,
ни избранного — и всё равно уходили три пункта меню.

Корзина — это не иконка, это касса. Магазин, у которого на какой-то ширине
нет корзины, на этой ширине не магазин. Никакой брейкпоинт, никакая нижняя
панель и никакое «зато оно есть в бургере» этого не отменяют: счётчик в
шапке — единственное место, где человек видит, что его выбор не потерялся.

**Дефект, из-за которого правило заведено.** Меню шапки росло вместе со
списком категорий (11 пунктов), стояло `flex:none`, и на широком экране
просто выдавливало поиск и все иконки за правый край — включая корзину.
Ниже 1280px всё меню исчезало целиком одним `display:none`, а ниже 820px
одним `display:none` исчезали **все** иконки разом, корзина в их числе.
Два `display:none` на группу — это и есть отсутствие очереди.

## И2 · Схлопывается по одному, а не группой

`display:none` на контейнере — не адаптив, а капитуляция. Меню теряет пункты
по одному, кластер иконок теряет иконки по одной. Пользователь, у которого
окно уже на 40px, должен потерять один пункт, а не всю навигацию.

Из этого следует: у выпавшего есть адрес. Пункт уходит в «Ещё» или в бургер,
знак — в бургер или в нижнюю панель. Элемент, который никуда не ушёл, а
просто пропал, считается потерянным, даже если на экране стало красиво.

Адрес — это работающая вещь, а не упоминание. Поиск, уступивший место в
строке, получает в бургере собственное поле: ссылка «искать», ведущая обратно
в ряд, из которого поле только что убрали, — это не адрес, а круг.

**Дефект, из-за которого правило заведено.** Тот же, что у И1, второй его
половиной: ниже 1280px всё меню исчезало целиком одним `display:none`, а
ниже 820px одним `display:none` исчезали ВСЕ иконки разом — корзина в их
числе. Два `display:none` на группу — это и есть схлопывание группой.

## И3 · Порог измеряется, а не назначается

Число в медиазапросе (`max-width:1280px`) — это не решение, а слепок одного
замера: одного шрифта, одного языка, одного набора названий категорий.
Переименовали «Oils» в «CBD oil», добавили десятую категорию, включили
болгарский вместо английского — и порог мимо, а строка переполнилась.

Строка меряет себя сама: `ResizeObserver` на контейнере или контейнерные
запросы. Медиазапрос остаётся там, где меняется **смысл** раскладки
(телефон против десктопа), а не там, где кончилось место.

Уступивший не прячется `display:none`, а выходит из потока:
`position:absolute; visibility:hidden`. Причина не в красоте — у `display:none`
ширины нет вовсе, и решить, влезет ли вещь обратно, было бы уже не по чему:
пришлось бы снова назначать порог числом, то есть вернуться к тому, из-за
чего всё и сломалось. Ширина, оставшаяся при вещи, и есть то, чем меряют.

**Дефект, из-за которого правило заведено.** Порог 1280 в шапке был назначен
числом — слепком одного замера с английскими названиями категорий. Стоило
включить болгарский, и строка переполнялась ВЫШЕ порога: число не знало, что
названия стали длиннее.

## И4 · Шапка не переполняется

Горизонтального переполнения в шапке не бывает ни на одной ширине от 320 до
2560. Если строка переполнилась — виновата не ширина окна, а элемент,
которому забыли разрешить сжиматься (`min-width:0`, `flex-shrink`) или
уступить очередь по И1.

Проверяется не глазами на трёх ширинах, а прогоном по всем с шагом: у
переполнения обычно есть узкий диапазон, в который взгляд не попадает.

**Дефект, из-за которого правило заведено.** Меню шапки стояло `flex:none`
и на широком экране выдавливало поиск и все иконки за правый край — включая
корзину. Переполнение жило в узкой полосе ширин, куда взгляд на трёх
контрольных ширинах не попадал; нашлось прогоном по всем ширинам с шагом.

## И5 · Очередь не чинит меню, которое не помещается

Очередь решает, **кто** уступит место. Она не решает, **хватит ли места**.
Если сумма имён больше строки, очередь честно снимает пункты — и меню всё
равно стоит неполным на всякой ширине. Это не отказ очереди, это отказ
принять, что имя раздела и подпись в строке — не обязательно одно слово.

Считается до того, как выбирается порядок: сложить ширины всех пунктов и
сравнить со строкой минус знак магазина, корзина и знаки. Не сошлось —
порядок очереди не поможет, сколько его ни переставляй.

**У строки своё имя раздела.** Полное имя — то, которым товар ищут, — стоит
там, где есть место: в заголовке страницы, в чекмедже, на полке, в фильтрах.
Строка шапки берёт короткое. Это не сокращение ради красоты: полное имя
никуда не девается и с той же страницы никуда не уходит, а строка перестаёт
терять разделы.

**Дефект, из-за которого правило заведено.** Одиннадцать имён вида
`Cannabis oil capsules` стоят 1378px, а строка шире 1416px не бывает — после
знака магазина и корзины меню остаётся 1130px. На 1440 в строке стояло 6
разделов из 11, на 1024 — три. Перестановка очереди («знаки уступают
первыми») была собрана и замерена: она возвращает один-два раздела и уносит
поиск, избранное и кабинет со всех десктопных ширин. Короткие имена вернули
все одиннадцать от 1380px и восемь на 1200 — при нетронутых знаках.

## И6 · Пропорцию кадра задаёт кадр, а не окно

Снимок, который на десктопе стоит в колонке, а на телефоне — во всю ширину,
на этих двух ширинах разный. Одна пропорция на оба случая означает, что на
широком её придётся резать потолком высоты, а потолок — это не пропорция:
коробка сохраняет ширину, теряет заявленное отношение и кадрирует сильнее,
чем кто-либо решал.

Поэтому пропорция читается с ширины **самого кадра** (`@container`), а не с
ширины окна: одно и то же окно даёт разную колонку в разных раскладках.
Потолок остаётся — но как потолок, для низкого окна, а не как способ
переопределить пропорцию.

**Дефект, из-за которого правило заведено.** 4/3 на полной ширине 820px —
это 591px высоты; потолок в 560px резал их до отношения 1.41 и до 35%
отброшенного снимка. На экране это половина первого экрана под фотографией:
подпись слайда уходила под нижнюю панель, а пилюли разделов — за первый
экран целиком.

## И7 · У строки шапки два якоря: знак слева, касса справа

Знак магазина стоит у левого поля строки, корзина — у правого. Это не
выравнивание, а адрес: человек не ищет корзину глазами, он тянется в угол,
где она была в прошлый раз. Угол обязан быть один и тот же на всякой
ширине и в **любом состоянии строки** — в покое, с открытым полем поиска,
с ушедшими в очередь знаками, с раскрытым чекмедже.

Проверяется не глазами, а координатой: правый край корзины совпадает с
правым полем строки во всех состояниях, которые строка умеет принимать.
Состояний обычно больше, чем помнит тот, кто их писал, — поэтому список
состояний пишется раньше проверки.

**Дефект, из-за которого правило заведено.** На телефоне открытое поле
поиска забирает себе целый ряд, и `margin-left:auto`, которым корзина
отжималась вправо, уезжает вместе с полем: строка становится «бургер, знак,
корзина», корзина липнет к знаку, а справа остаётся пустое место в треть
ширины. Раскладка держалась на промежутке между вещами, а не на якоре у
края — и первое же новое состояние строки её развалило.

## И8 · У открытого есть выход, и он виден

Всё, что раскрылось поверх страницы — поле поиска, чекмедже, выпадашка,
панель, — закрывается тремя путями, и все три обязательны:

| Путь | Кому | Почему обязателен |
| --- | --- | --- |
| крестик | пальцу | единственный **видимый** выход; цель не меньше 44px. Может стоять на месте кнопки, которая открывала, — если знак меняется |
| Escape | клавиатуре | на телефоне его нет вовсе — значит, один он не считается |
| уход | и тому и другому | прокрутка или свайп: человек ушёл — открытое закрылось |

Крестик может быть **той же кнопкой, что открывала** — при одном условии:
знак на ней меняется. Лупа открывает, крестик закрывает; кнопка одна, место
одно, палец не ищет новую цель, а состояние читается с рисунка. Запрещено
не «то же место», а «тот же вид»: кнопка, которая в обоих состояниях
выглядит лупой, требует догадаться, что она умеет и то и другое, — и
догадываться никто не станет.

Тап мимо — выход, но только если он работает всегда; выход, который
перестаёт работать, когда в поле что-то набрано, — это не выход, а ловушка
ровно в тот момент, когда из неё и хотят выйти.

**Свайп — это половина третьего пути, и своя у каждой панели.** Панель,
выехавшая сбоку, закрывается тем же движением, каким пришла; жест решается не
сразу (пока палец не прошёл 8px, он ничей), иначе он отнимает у панели её
собственную вертикальную прокрутку. Браузерная половина — `touch-action:
pan-y` на панели, вторая половина — общий обработчик на все панели сайта.

И отдельно — про переезд в верхний слой (правило И14). `<dialog>` приносит с
собой Escape, возврат фокуса и затемнение, но **свайпа не приносит**: движение
остаётся нашей работой и после переезда. Потерять его на переезде — значит
закрыть один из трёх выходов, оставив ощущение, что браузер сделал всё сам.

**Дефект, из-за которого правило заведено.** У открытого поля поиска не было
крестика вовсе. Escape закрывал — но на телефоне клавиши Escape нет. Тап
мимо закрывал — но только при пустом поле, то есть не тогда, когда нужно.
Прокрутка не закрывала ничего: страница ехала, а поле ехало вместе с ней,
занятое и открытое. Из трёх путей не работал ни один, и все трое считались
сделанными, потому что каждый по отдельности существовал в коде.

## И9 · Размер шрифта течёт, и на телефоне шкала плотнее

Кегль не переключается ступенями на брейкпоинтах, а течёт: каждая ступень —
`clamp(мин, формула от ширины, макс)`. Ступенька неверна между
брейкпоинтами — на 700px размер либо десктопный, либо мобильный, и оба не
те; каждый новый размер тянет новый медиазапрос; а при повороте телефона
скачок кегля видно глазом.

**Мин и макс — два разных дизайна, а не одно число, поделённое пополам.**
На узком экране шкала должна быть не только мельче, но и **плотнее**:
контраст между заголовком и телом на десктопе около 4:1, на телефоне
2–2.3:1. Десктопная шкала, ужатая линейно, оставляет заголовки слишком
крупными относительно текста — иерархия схлопывается, и главный заголовок
страницы перестаёт отличаться от заголовка блока.

Проверяется отношением, а не размером: посчитать кегль главного заголовка,
делённый на кегль тела, на самой узкой и на самой широкой ширине. Если
отношения совпали — шкала не адаптирована, её просто масштабировали.

С кеглем на той же ступени едут трекинг и интерлиньяж: крупный требует
минусового трекинга и плотных строк, мелкий — обратного. Разведённые по
разным местам, они расходятся при первой же правке размера.

**Дефект, из-за которого правило заведено.** Текли ровно две вещи — H1 героя
(36→64) и его подзаголовок (16→18). Заголовок слайда, заголовок блока и имя
товара стояли одним числом на 360 и на 1920: 30, 30 и 18. На десктопе
отношение H1 к заголовку блока 64/30 ≈ 2.1, на телефоне 36/30 = 1.2 — на
телефоне заголовок блока почти сравнялся с заголовком страницы.

## И10 · Настройка пишет число только тогда, когда его выбрал человек

Панель настроек, инструмент разработки, тема — всё, что пишет CSS-переменные
поверх системы, обязано писать **только выбранное**. Умолчание уже лежит в
токенах, и лежит там правильно: часто не числом, а кривой (`clamp`,
`@container`, `light-dark`). Настройка, которая на старте записывает своё
умолчание, накрывает кривую числом — и вся работа, вложенная в текучесть,
перестаёт существовать, не оставив следа в диффе.

Инлайн-стиль на корне сильнее любого правила `:root`. Поэтому «панель просто
проставила значения по умолчанию» — это не нейтральное действие, а
переопределение всей системы, сделанное молча.

Из этого два следствия:

**«Авто» не пишет ничего.** Положение «авто» означает «решает система» —
значит, переменная не трогается вовсе, и работает то, что объявлено в
токенах. Число, вычисленное для показа в панели, показывается, но не
записывается.

**Инструмент не едет в прод — ни панелью, ни её следом.** Сборка без панели
обязана быть сборкой и без её значений: если самой панели в странице нет, а
скрипт с её числами есть, инструмент всё-таки приехал.

**Дефект, из-за которого правило заведено.** `tokens.css` объявляет три
кривые: `--h2-size` (22→30), `--lede-size` (13→16), `--hero-size` (36→64).
На живом сайте текла одна — `--hero-size`, и только потому, что к ней не
привязано ни одного поля панели. Две другие панель писала числом на
`<html>`, и заголовок блока стоял 30px и на 360, и на 1920. В сборке без
панели — тоже.

## И11 · «Нажали мимо» читается по пути события, а не по его цели

Обработчик, закрывающий открытое по нажатию снаружи, спрашивает не
`event.target.closest(...)`, а `event.composedPath()`. Путь снят в момент
отправки события и держит всех предков; цель к моменту, когда до обработчика
дойдёт очередь, может быть уже вынута из документа. `closest` на вынутом узле
возвращает null, и «снаружи» срабатывает на том, что было внутри.

Вынимает узел обычно сама реакция на это же нажатие: кнопка, меняющая свой
знак, заменяет иконку — старая уходит из документа между реакцией и
обработчиком.

**Дефект, из-за которого правило заведено.** Кнопка поиска закрывала сама
себя. Нажатие открывало поле и меняло лупу на крестик; старая лупа
оказывалась вне документа; обработчик «нажали мимо» получал её как цель,
`closest` возвращал null, и поле закрывалось тем же нажатием, которым его
открыли. В диффе это выглядело безупречно.

## И12 · Мерить после того, как доехало

Раскладка, которая считает себя по ширинам, не должна мерить их в середине
перехода. Анимация — это временная неправда о размере: поле шириной во всю
строку, которое едет к 44px, полсекунды сообщает первое.

Замер, сделанный в начале анимации, принимает временное за окончательное. И
чинится это не задержкой на глаз, а подпиской на `transitionend` того
свойства, которое едет: система сама скажет, когда её можно спрашивать.

Отдельно опасно то, что такой замер **залипает**: `ResizeObserver` на
контейнере больше не сработает, если сам контейнер размера не менял. Один
неверный замер остаётся решением до следующего поворота экрана.

**Дефект, из-за которого правило заведено.** Очередь на выживание мерила
строку сразу после закрытия поиска, видела поле во всю ширину, решала, что не
помещается ничего, и прятала сам поиск. Кнопка поиска исчезала из шапки — и
не возвращалась, потому что перемерять было не по чему.

## И13 · У движущегося есть чем его остановить, и это не наведение

Всё, что идёт само дольше пяти секунд, обязано иметь на странице орган
остановки. Это SC 2.2.2, уровень A — самый низкий порог доступности, ниже
которого не бывает.

Тремя способами это правило пытаются закрыть, и ни один не считается.
Наведение мышью — пальцем не работает и словом не называется. Настройка
системы «меньше движения» — не орган на странице, и большинство тех, кому
движение мешает, в системе ничего не меняли. Остановка по касанию — это
исчезнувшая возможность, а не орган: убрал палец, и снова поехало.

Из этого следует разделение, без которого кнопка ломается. «Стоп» бывает
трёх сортов, и путать их нельзя:

* **выбор** — что человек нажал. Живёт до следующего нажатия;
* **удержание** — наведение или фокус внутри. Временное, само отпускает;
* **среда** — настройка системы, отсутствие мыши, узкий экран.

Подпись кнопки говорит о **выборе** и только о нём. Стоит завязать её на то,
что происходит прямо сейчас, — и кнопка начинает врать: показ стоит от
наведения, кнопка подписана «Пусни», человек нажимает и получает то, что шёл
выключить.

Сама кнопка из удержания исключается. Иначе она приезжает под фокус уже
переподписанной, и первое нажатие делает обратное задуманному.

Там, где движения нет — узкий экран, где показ и не запускался, включённая
настройка «меньше движения», — кнопки нет тоже. Орган остановки того, что
стоит, — это лишний предмет в углу картинки.

**Дефект, из-за которого правило заведено.** Слайдер героя менял кадр раз в
5.2 секунды и останавливался только под курсором. Пальцем — никак,
клавиатурой — никак. А когда кнопка появилась, фокус на ней самой выключал
показ раньше нажатия: подпись успевала смениться, и Enter включал обратно.

**Ещё одно, попутно.** Метка, которая заливается за время кадра, обязана
вставать вместе с показом: часы, идущие при неподвижной картинке, врут о том,
сколько осталось. И правило паузы пишется **ниже** правил, назначающих
`animation` сокращённой записью: сокращённая запись сбрасывает
`animation-play-state` в `running`, даже когда пауза объявлена в правиле той
же силы.

---

## И14 · Открывающееся уходит в верхний слой, а не спорит числами

Шторки, окна и меню номера не получают: `<dialog>` с `showModal()` для окон и
шторок, атрибут `popover` для меню. Что открыто последним, то и сверху; это
правило браузера, и перебить его чужим числом из чужого файла нельзя.

Номер остаётся только у того, что висит на экране всегда: шапка, нижняя
полоса, помощник, всплывающее сообщение, ссылка «к содержимому». Их немного,
они не открываются, и у каждого есть **имя** (`--layer-header`,
`--layer-tabbar`), а не число. Однозначное число внутри своего блока
(подпись поверх снимка) разрешено: оно ни на что чужое не претендует.

Отдача больше порядка слоёв. Escape, возврат фокуса и затемнение
(`::backdrop`) в верхнем слое приходят от браузера — то есть три правила
отсюда (И8, И11, И12) перестают быть работой рук. Крестик всё равно нужен:
пальцу Escape недоступен.

**Дефект, из-за которого правило заведено.** Пилюля сортировки носила
`z-index: 71`, чтобы её шторка перекрыла затемнение, — и закрытая пилюля
лезла поверх шторки фильтров. Число, поставленное элементу ради его
СОДЕРЖИМОГО, ломает страницу всегда: спорить ему приходится с числами,
которых автор не видел.

И сброс пишется один раз на все окна: `<dialog>` приносит с собой
`inset-inline: 0`, размер по содержимому, `margin: auto`, рамку и поля. Пока
это снимало каждое окно само, восемь сняли по-своему, а девятое не сняло
левый край и уехало за правую границу экрана.

---

## И15 · Ширина решает раскладку, указатель решает размер цели

Правило «44 пикселя под палец» пишется в `@media (pointer: coarse)`, ровно
как `:hover` пишется в `@media (hover: hover)`. Задавать его через
`max-width` нельзя.

**Дефект.** Планшет в альбоме отдаёт 1024 CSS-пикселя. По ширине это
десктоп — и он получал курсорную разметку, хотя палец у него остался пальцем:
на входе палец, в разметке курсор. Обратное так же верно: телефон в альбоме
(800 пикселей) уходил в широкую раскладку и получал плитку во всю ширину.

Проверяется вторым прогоном отрисованной проверки: те же страницы на 768 и
1024, но с `hasTouch` — и семья цели нажатия там требует 44, хотя ширина
десктопная.

---

## И16 · Цвет текста берётся оттуда же, откуда цвет фона под ним

Фон из палитры (взятый из фотографии, из макета, одинаковый в обеих темах) —
и текст из палитры. Фон из роли темы — и текст из роли. Смешивать половины
нельзя: медиазапрос узкой раскладки меняет кегль, число строк, отступы — но
не ИСТОЧНИК цвета.

**Дефект.** Подписи плиток на телефоне: фон взят из палитры, текст из токена
темы. В светлой всё правильно. В тёмной — белым по светло-зелёному, 1.12 при
норме 4.5, то есть подписей не было видно совсем. Пара, собранная из
половин, выглядит верной в той теме, в которой её написали, и пропадает в
соседней.

Проверяется так же, как И15, — вторым прогоном, но с `colorScheme: 'dark'`:
пока темы две, а проход один, половина цвета не проверена ничем.

---

## И17 · Обрезанный текст — та же беда, что вылезший за экран

Разница лишь в том, кто его прячет. Съёмка по ширинам ловит переполнение
страницы; текст, обрезанный **своим же блоком**, не видит никто: страница
выглядит опрятной, слово просто короче, чем должно быть.

**Дефекты.** `nowrap` на заголовке подвала значит «эта строка не переносится
НИКОГДА», а не «на широкой она в одну строку»: на 375 он стоил половины
слова. И «20% CBD+CBN» браузер не рвёт — плюс держит то, что за ним, и
карточка обрезает последнюю букву. Там, где в названии есть `+` или `/`,
ставится `<wbr>`.

Признак: `scrollWidth` больше `clientWidth` у текстового узла, чей блок
обрезает. Полоса с прокруткой не в счёт — она для того и сделана.

---

## И18 · Условие пишется одной функцией на все места сразу

Условие, написанное дважды, разойдётся третьим. Признак болезни виден не в
том месте, где условие написано, а в том, где его забыли написать: три
страницы одного рода, у одной правило есть, у двух нет, и **выглядят все три
одинаково**.

**Дефект.** Корзина и оформление не стояли в карте сайта — намеренно, это не
страницы, а состояние покупателя. Намерение было записано комментарием в
карте сайта и до самих страниц не доходило: ни одна не говорила о себе
`noindex`, а «нет в карте» из выдачи не убирает. У страницы «не найдено»
`noindex` был — написанный руками.

Починка: одна функция (`isPrivate(path)`), и читают её все, кому нужно.

---

## И19 · Обещанный адрес открывается, открытый — обещан ровно один раз

Обе половины обязательны, и вторая ловит то, чего первая не видит.

* Каждый адрес из карты сайта, каждый `canonical` и каждый `hreflang`
  существует. Указать на адрес, которого нет, хуже, чем не указать ни на
  что: поисковик сходит по нему один раз и перестанет верить остальным.
* Каждая существующая страница либо обещана картой, либо сама сказала о себе
  `noindex`.

**Дефект.** У товара переводился адрес, а карта сайта продолжала клеить
`/en/` + путь родного языка. Восемьдесят четыре адреса из карты отвечали «не
найдено», ровно столько же настоящих английских страниц в карту не попадало
вовсе, а `hreflang` называл несуществующего двойника — то есть сообщал
поисковику, что английской версии нет. Глазами это не видно: обе половины
выглядят правдоподобно, страницы открываются, карта отдаётся.

---

## И20 · Нажатие обязано отвечать, и отвечать нашим

На телефоне у контрола один отклик — нажатое состояние. Наведения там нет
вовсе (`@media (hover:hover)` его туда и не пускает, и это правильно), фокус
приходит только с клавиатуры (`:focus-visible`).

Своего отклика по умолчанию нет, а чужой есть: Android рисует поверх нажатой
ссылки серую рамку и держит её ещё долю секунды после того, как палец ушёл.
Она не знает ни формы предмета — ложится прямоугольником на скруглённую
пилюлю и на круглую кнопку, — ни его цвета, и появляется поверх собственного
нажатого состояния, если оно есть.

Снимается она одной строкой на весь сайт:

```css
html{ -webkit-tap-highlight-color: transparent }
```

**Но снимается только ПАРОЙ.** Сняли чужой отклик — обязаны дать свой:
`:active` у каждого контрола, у которого объявлен `:hover`. Контрол, у
которого нет ни того ни другого, на телефоне после этой строки не отвечает
ВООБЩЕ — а это хуже некрасивой рамки: человек не понимает, нажалось ли, и
нажимает второй раз.

Отклик засчитывается трёх видов, и все три — видимая перемена в момент
нажатия: `:active`, состояние из `aria-` (переключатель показывает своё новое
положение) и состояние из `data-`.

**Дефект, из-за которого правило заведено:** его назвал заказчик, увидев
чужую рамку на телефоне соседнего магазина. Здесь она была на всём сайте, при
том что своё `:active` объявлено у двадцати четырёх файлов из двадцати
четырёх, — то есть чужой отклик рисовался поверх готового своего.

---

## И21 · Одна вёрстка ставится на разный движок

Требование заказчика, сентябрь: то, что делается, должно становиться на
WordPress, Shopify, Medusa — на любой бекенд и любой движок. Правило про
сегодняшнюю правку, а не про день переноса: перенести готовый сайт значит
переписать его, а не накопить непереносимого — значит не переписывать вовсе.

Три слоя, и договор между ними — имя класса и форма разметки, а не файл и не
импорт:

* **общее** — простой CSS на именах классов (`cb-card`, `cb-card__foot`),
  токены, значки. Не знает ни про React, ни про сборщик, ни про PHP;
* **переходник** — Liquid, PHP, компонент: превращает данные СВОЕГО движка в
  ту же форму разметки и ни одного решения о виде в себе не держит;
* **данные** — источник. За ними ходит страница, компонент их получает.

Отсюда четыре следствия: переносимый слой объявляет переменные, которые сам
просит; один токен — одно значение; список берёт страница, а не компонент;
валюта, порог и ставка живут в данных, а не в разметке.

**Дефект, из-за которого правило заведено:** заказчик спросил, доведена ли
типографика. По сайту — доведена, проверка показывала ноль и была права. А в
переносимом пакете четырнадцать переменных, которых он сам не объявляет,
среди них вся шкала размера. По правилам CSS объявление с неизвестной
переменной недействительно ЦЕЛИКОМ, и браузер выбрасывает его молча: на
Shopify карточка товара набирается одной величиной. Общая форма болезни —
пока исполнение одно, расхождения не видно; оно копится молча и вылезает
всё сразу в день, когда исполнений становится два.

Мерится `check:port`, шесть семей, храповик.

---

*Новые правила дописываются сюда же, по мере того как правки их выявляют.*

---

## И22 · Фильтр живёт в адресе, а в поиск полка просится покрытием

**Дефект, которым заведено.** У каталога фильтры не меняли адрес: выбрал
категорию, крепость, спектр — адрес остался `/catalog`. Три следствия разом:
ссылкой поделиться нельзя (покупатель нашёл нужное и не может послать это
себе), кнопка «назад» выбрасывает из каталога целиком, а выбор теряется при
обновлении страницы. Ни одно из трёх не про поиск — все три про руки.

**Правило, часть первая: фильтр живёт в адресе всегда.** Состояние, которое
покупатель собрал руками, обязано быть в адресе — иначе оно не существует
для браузера, для ссылки и для истории. Это ремесло, и решения заказчика оно
не требует.

**Правило, часть вторая: в поиск отфильтрованная страница просится сама, и
просится ПОКРЫТИЕМ.** По умолчанию любая комбинация фильтров — `noindex`, а
`canonical` ведёт на родительскую полку. Открытой она становится не по
списку в коде, а по тому, что покрывает:

* покрывает товары **нескольких марок** — полка нужна, потому что страница
  товара структурно не способна ответить: она про одну марку, а спросили
  тип;
* сворачивается к **одному-двум товарам одной марки** — отвечает товар, а
  полка была бы пустой страницей собой.

**Почему покрытие, а не список.** Какой это случай, знает только наполнение:
сегодня тип покрывает пять марок, после завоза десять, а на другом рынке
одну. Список, записанный в коде, — это архитектура, привязанная к
сегодняшнему складу; он разойдётся с ним в первый же завоз. Полка,
считающая своё покрытие сама, переживает и завоз, и распродажу, и переезд на
другой рынок.

**Порог — одно число в данных.** Сколько товаров и сколько марок считать
достаточным — решение владельца магазина, не вёрстки. В вёрстке живёт
механизм, в данных — число.

**Чем это грозит, если сделать наоборот.** Открыть поиску все комбинации —
категория × крепость × спектр × объём — значит завести сотни почти
одинаковых страниц. Вред известен и троен: свои страницы спорят друг с
другом за один запрос; почти повторяющиеся полки читаются пустыми; бюджет
обхода тратится на то, чего никто не просил, и настоящие страницы
переобходятся реже. Это не «меньше пользы», а хуже, чем было.

**Открытая комбинация получает свой заголовок, описание и текст.** Полка,
отличающаяся от родителя только набором товаров, и есть пустая страница.

**Чего этим правилом не поймать.** Объёмы запросов живут вне репозитория:
их меряют Search Console (по живому сайту) и Keyword Planner (по стране и
языку), и ни модель, ни проверка их не знают. Покрытие проверяется машиной,
спрос — нет. Правило говорит, чего не делать без замера, и не говорит, что
именно открыть.

**Порядок работ.** Фильтры в адресе делаются сразу — их польза от спроса не
зависит. Список открытых поиску комбинаций собирается после запуска, по
накопленным данным. Заведённый заранее, он гадание.

**Чему не верить при проверке этого правила.** Одномарочный магазин
отвечает на запрос-тип страницей товара и полок под тип не держит вовсе —
у него тип и есть товар. Переносить его устройство на многомарочный нельзя,
и наоборот: сравнивать можно только с магазином того же устройства. Я эту
ошибку сделал, сравнив с одномарочным конкурентом, и вывод получился
обратный верному.

---

## И23 · Роль набирается своей ступенью, а не размером соседней роли

Лид, подпись, основной текст — разные роли, и у каждой своя ступень шкалы.
Роль, набранная ступенью соседней роли, на странице не существует: глаз
видит не «вводный абзац», а «ещё один абзац того же кегля».

Признак виден без линейки: у двух ролей одно число в стилях, и тогда одна из
них выбрана не по своему делу, а по тому, что «подошло». Проверяется же
глазом на странице — в одной колонке, а не в файле: соседство решает всё.
Абзац в 18 пикселей крупен рядом с подписью и мелок под именем в 21.

**Дефект, из-за которого правило заведено.** Описание товара стояло ровно в
основной текст (18px на десктопе). Над ним в той же колонке имя марки шло
21px, под ним панель фактов — те же 18px: вводный абзац, который читают
первым, оказался самым тихим местом колонки. Заказчик нашёл это глазом и
спросил прямо: «текст описания товара… мелкий текст? подписи, пояснения? уж
мелковат он сейчас». Правка — в ступени, а не на странице: лид одна роль на
весь сайт, и своя величина на одной странице означала бы, что на вопрос
«каким кеглем набран лид» ответов стало два.

**Чем мерить.** Ступень объявлена в токенах, а не в блоке; ниже основного
текста лид не опускается ни на одном конце шкалы (на телефоне он стоял 16
при основном 16.5 — то есть лид был мельче текста, который предваряет).

---

## И24 · Признак, по которому обещан выбор, — это адрес, а не галочка

Столбик «по марке», плитка «по производителю», строка «по эффекту» обещают
страницу: человек нажимает имя и ждёт всё, что под этим именем лежит. Если
за признаком стоит только галочка фильтра, нажатие ведёт либо на общую
полку, либо на якорь к списку чекбоксов — то есть отвечает не на тот вопрос,
который в надписи задан.

Поэтому: **признак, вынесенный в навигацию, получает свой адрес** — с
заголовком, описанием, местом в карте сайта и парой языков. Признак, который
остаётся гранью фильтра, в навигацию не выносится вовсе.

Адрес при этом хранится **полем** (правило про ключ, см. `.claude/skills/shop/references/facts.md`):
переименование марки не должно уносить страницу вместе с накопленным поиском.

**Дефект, из-за которого правило заведено.** Имя марки над заголовком товара
было простым текстом: оно называло производителя и никуда не вело, хотя это
первое, что нажимают, выбрав марку. Рядом, в меню масел, столбик «По марка»
вёл все пять строк в одно место — на полку масел: нажавший «Hemptura»
получал всё масло магазина. Плитка «Марки» на главной обещала «указатель, по
одной марке наведнъж» и открывала якорь к галочкам фильтра. Три места
обещали страницу марки, которой не существовало.

---

## И25 · На телефоне у страницы одна вертикаль, и её держит одно число

У широкой страницы вертикалей две, и это по делу: край плавающего предмета
(шапка, полоса, карточка) — одна, строка текста внутри него — другая. Текст
страницы равняется на вторую, и тогда он встаёт под слово шапки, а не под её
угол.

На телефоне второй вертикали нет места. Правило: **ниже шва телефона линия
одна**, и число у неё одно — то, на котором стоит нижняя полоса. На него
встают край шапки, текст, карточки товара и сама полоса. Брейкпоинт по
смыслу, а не по величине: меняется не размер поля, а сколько у страницы
линий.

Следствие, которое надо мерить отдельно: ширина строки от этой правки
растёт, и вместе с ней растут карточки. То, что в них стоит, надо
перемерить — надпись, не помещавшаяся раньше, может поместиться, а может и
нет.

**Дефект, из-за которого правило заведено.** Заказчик на телефоне: «ширина
поля сайта и магазина должна быть как нижнее меню, почему карточки товаров
уже ширины меню, это неправильно». Замер на экране 360 нашёл четыре линии,
вложенные одна в другую: 12 у нижней полосы (своё число, мимо всех ручек),
16 у белой карточки шапки, 24 у тёмной строки внутри неё, 32 у текста и
карточек. Шестьдесят четыре пикселя из трёхсот шестидесяти — восемнадцатая
доля экрана — уходили на вложенность, которой на телефоне не видно вовсе.

---

## И26 · Чужой контрол настраивают ручкой, а не своим свойством

Блок, взявший общий контрол, не спорит с ним свойствами. Сила селекторов у
них равна, и спор решает порядок файлов в собранном листе — а он блоку не
принадлежит: он следствие графа импортов и меняется от правки в третьем
месте. Особенно молча ломается пара «сокращённая запись против длинной»
(`padding` против `padding-inline`).

Правило: у контрола есть ручки (`--btn-pad`, `--btn-h`, `--btn-fs`), и
настраивают его ими. Нужной ручки нет — её заводят в файле контрола с
прежним умолчанием: там, где её не трогают, не меняется ничего.

**Дефект, из-за которого правило заведено.** Карточка полки сжимала поле
кнопки в тесной раскладке правилом `@container (max-width:212px){
.add{padding:0 var(--sp-3)} }`. Правило стояло, читалось верным и не
применялось ни разу: общий контрол писал длинную `padding-inline`, и его
файл ложился ниже. Признак дорогой тем, что его не видно ни в диффе, ни в
проверке по файлам — только замером на странице. Наружу это вышло надписью
«Добавено 2», не помещавшейся в кнопку: заказчик увидел её на телефоне.

---

## И27 · Группа органов переносит своих, а не вылезает за край

Лоток — группа органов в одном поле — собирается ради того, чтобы несколько
предметов об одном не занимали несколько строк. У этой сборки есть предел:
слова в другом языке длиннее, и группа перестаёт помещаться в строку.

Правило: **группа переносит своих внутри себя.** Высота держится полом, а не
назначается: пока места хватает, одна строка остаётся одной строкой. Подгонять
поля органов под сегодняшние слова нельзя — сэкономленные пиксели кончатся на
первом же языке с более длинными словами.

**Дефект, из-за которого правило заведено.** Полоса над полкой каталога:
«Филтър 1», «Изчисти», «13 продукта» просят по-болгарски 312 пикселей, а
строка на экране 320 даёт 296. Группа вылезала на четыре пикселя за правый
край — страницу можно было утащить вбок. Нашла это съёмка по ширинам, а не
глаз.

Вторая половина находки важнее первой: перелив был и раньше, и был он больше
— двадцать девять пикселей, — но группа стояла левее, и перелив прятался
внутри экрана. Стоило полю страницы сравняться с нижней полосой, и тот же
дефект достал до края. **Спрятанный перелив — это не отсутствие дефекта, а
дефект, которому не хватило нескольких пикселей, чтобы стать видимым.**

---

## И28 · Блок красит свой текст, а не всё, что внутри

Правило про селектор, а следствие — про контрол. Блок страницы, который
красит подписи **по тегу** (`.voice span{color:…}`), достаёт до всего, что
внутри, — включая взятую кнопку: коробка её знака тоже `span`. Слово в кнопке
остаётся своим белым, знак становится серо-зелёным, и кнопка читается
полувыключенной.

**Как писать.** Свой текст красится через `>` или через свой класс. Краска и
кегль по голому тегу в глубину запрещены всюду, где внутрь блока может встать
готовый контрол, — а встать он может почти везде.

**Дефект.** Заказчик посмотрел лист набора: «иконка корзины не цвета текста».
Первый раз я починил вес и размер знака, а причина была снаружи кнопки — в
подписи соседнего абзаца. У знака в его собственном файле написано
`stroke:currentColor`, то есть «иду за краской слова»: в диффе дефекта не
видно вовсе, обмануть это можно только чужим правилом.

**Чем меряется.** Семья `markInk` в отрисованной проверке: у каждого контрола
на каждой странице дерева краска знака сравнивается с краской слова. Законных
исключений три — чужая марка со своим цветом в атрибуте (Visa, Mastercard),
знак-плашка со своим полом и знак, которому цвет назначен нарочно.

---

## И29 · Поле — предмету, воздух — странице

Два разных факта, которые в вёрстке выглядят одинаково — оба числа в
пикселях, оба через `padding`/`margin`/`gap`, — и потому сливаются в один.

**ПОЛЕ** — от края предмета до его содержимого. Принадлежит ПРЕДМЕТУ. Нулём
не бывает: предмет, содержимое которого лежит на его собственном краю, — не
предмет, а обрыв.

**ВОЗДУХ** — между двумя предметами. Принадлежит СТРАНИЦЕ и ставится МЕЖДУ
соседями просветом РОДИТЕЛЯ, а не полем каждого ребёнка.

**Как писать.** Поверх шкалы `--sp-*` стоят имена ролей — так же, как у
цвета: не «серый-3», а «поверхность». Поле: `--pad-sheet`, `--pad-card`,
`--pad-inner`. Воздух: `--air-page`, `--air-band`, `--air-block`,
`--air-head`, `--air-group`, `--air-row`. Предмет со своим полом берёт
примитив `sheet`, забыть поле тогда нельзя.

**Дефект.** Заказчик показал тремя снимками: на телефоне заголовок героя,
список вопросов и лента Instagram стояли вплотную к краю собственной
подложки — «что-то ты поломал, отступы по бокам». В файлах при этом было
безупречно, ни одного числа:

    .wrap{ padding-inline: calc(var(--page-gut) - var(--gut)) }

Поле листа было РАЗНОСТЬЮ двух линий страницы. Ниже 820 линия у страницы
одна (решение заказчика, И24: карточки обязаны стоять на линии нижней
панели) — разность обнулилась, и поле исчезло у каждого листа разом.

**Чем меряется.** Отрисованная семья `field`: у каждого предмета с полом
ищется текст, чей ближайший пол — этот предмет, и меряется расстояние до
края. По файлам такое не ловится никогда: там имя, а не число.

---

## И30 · Линия страницы кончается на первом полу

Продолжение И29 и ответ на вопрос заказчика «есть ситуации, когда блок в
блоке, там где квиз».

У коробки страницы два края: внешний — где кончается страница как предмет,
внутренний — линия типографики (И21). **Внутренний край действует ровно до
первого пола.** У листа со своим полом линию внутри задаёт он сам — своим
полем; прикладывать к ней ещё и линию страницы значит считать одно и то же
дважды.

**Как писать.** Лист снимает линию у себя и у всего, что внутри
(`--page-line:0` в примитиве `sheet`). Коробка, в которой лист ЛЕЖИТ, снимает
свою (`flush`). Лист внутри листа берёт поле ступенью ниже — так же, как
внутренний угол берётся из внешнего минус поле.

**Дефект.** Две линии подряд давали предмету с полом двойное поле на
мониторе и никакого на телефоне: подвал стоял на двадцать пикселей правее
полос главной — два одинаковых по роду листа на разных вертикалях.

---

## И31 · Запас под палец не меняет размер органа

Сорок четыре пикселя приходят НЕВИДИМЫМ прямоугольником (примитив `tap`),
вынутым из потока. Орган, выросший ради пальца по-настоящему, двигает всё,
что стоит с ним в одной строке.

**Как писать.** Органу с НАРИСОВАННЫМ размером палец рисунка не меняет:
запас вместо роста. Рост законен там, где органы стоят вплотную колонкой и
невидимые запасы соседей наложились бы друг на друга (строки списка в
подвале) — и тогда он объясняется комментарием на месте.

**Дефект.** Заказчик дважды сказал одно и то же: «белая полоса в верхнем
меню, её уже уменьшали, почему-то она снова выше стала». Полосу действительно
уменьшали — и оба раза она возвращалась, потому что причина была не в ней:
кнопка темы внутри неё носила `min-height:44px` под `pointer:coarse`, росла с
28 до 44 и поднимала полосу целиком. Тем же ростом двигался и ряд знаков
соцсетей в подвале.

**Чем меряется.** Семья `tapGrows` в проверке вёрстки: под
`@media (pointer:coarse)` назначен размер, который БОЛЬШЕ нарисованного тому
же органу вне медиазапроса.

---

## И32 · Воздух вокруг волоска равный

Разделитель делит две части, и доли у них одинаковые. Тридцать шесть сверху
при двадцати двух снизу читаются не как «разделитель», а как «разделитель
сполз».

То же правило и для полосы, лежащей в углу листа: белого над её содержимым
столько же, сколько под ним.

**Дефект.** Пока верхнее поле принадлежало КАРТОЧКЕ, а нижнего не было вовсе
(под полосой сразу начинается тёмная строка шапки), содержимое стояло на
половину поля ниже середины белого — и заказчик нашёл это глазом на кнопке
темы: «не симметрична по вертикали, а текст симметричен». Кнопка была ни при
чём: так же стояла и надпись рядом. В подвале тот же перекос был у волоска
над правовой полосой: 22 сверху, 18 снизу.

---

## И33 · Чужие марки ровняются по самому крупному пятну

Ряд платёжных марок — это чужие знаки, нарисованные в разных языках, и
ровнять их надо по чему-то одному. Число `CAP` одно, а вот что им меряется —
менялось дважды, и оба раза по находке заказчика.

**Дефект.** Сперва ровняли КОРОБКИ: заказчик нашёл это глазом — «apple pay и
google pay выглядят сильно мельче иконок виза и мастеркард», — в коробку
входил воздух начертания. Потом стали
ровнять ПРОПИСНУЮ, букву в букву: слова сошлись, а знаки разъехались в
другую сторону — яблоко и «G» ВЫШЕ прописной, ровно на столько они и стали
выпирать. «Сделай иконку apple яблоко и Google Pay иконку высотой как visa
текст».

**Как писать.** Одной высоты — САМОЕ КРУПНОЕ ПЯТНО каждой марки: у Visa это
буквы, у Apple Pay яблоко, у Google Pay «G». В единицах начертания это все
чернила НАД БАЗОВОЙ ЛИНИЕЙ. У марки без выносных правило сходится со старым
тождественно. Круглому знаку без слова (Mastercard) положена оптическая
надбавка — но именно надбавка: 17 при `CAP` 11 это уже другой размер.

---

## И34 · В паре «знак и слово» ровняется СЛОВО

Дополнение к И33 и третий замер того же ряда платёжных марок.

Ровнять по самому крупному пятну — по яблоку и по «G» — сошлось до пикселя:
яблоко 11.1 при буквах Visa 11.0, «G» 11.8. А на экране марка стала мельче.

**Дефект.** Заказчик сказал это сразу: «разве не видно, что сейчас мелко по
сравнению с Visa». Причина в том, что в паре «знак + слово» глаз читает
СЛОВО: оно длиннее знака и стоит с ним в одной строке. Ровняя знак, слово
уводили вниз — «Pay» выходило 10.2 против 11.0 у «VISA».

**Как писать.** Одной высоты — прописные СЛОВ. Знак при этом получает свою
законную оптическую надбавку: яблоко 11.9, «G» 12.6 при буквах в 11.0 —
круглая и органическая форма обязана переливаться за полосу прописных, иначе
читается мельче неё. Надбавка не назначается числом, а следует из самого
начертания — в отличие от марки БЕЗ слова (круги Mastercard), где её пишут
руками.

---

## И35 · Замер идёт по НЕПОДВИЖНОЙ странице

Ожидания конца анимаций для этого мало: показ слайдов заводит следующую по
таймеру, и страница не бывает неподвижной никогда.

**Дефект.** Полный прогон отрисованной проверки показал контраст 3.35 у
подписи героя на ширине 700; узкий прогон по той же странице и той же семье
дал ноль. Причина была не в странице: кадры героя сменяются сами, и замер
попадал В СЕРЕДИНУ ПЕРЕХОДА — на экране два снимка сразу, подпись лежит на
их смеси. Все четыре снимка замерены по отдельности и держат 5.76:1.
Мерилось не то, что видит покупатель.

**Как писать.** Перед замером страница останавливается ТЕМ ЖЕ ОРГАНОМ,
которым её останавливает человек, — кнопкой паузы. Не подменой медиазапроса
«поменьше движения»: он меняет то, что меряется. Проверка проходит путь,
который проверяет (четвёртое свойство сторожа).

**Правило шире слайдера.** Находка, не повторившаяся на узком прогоне, — это
сперва подозрение на мерку, а не на страницу. Чинить показанное место, не
проверив мерку, значит подкрутить вёрстку под ошибку измерения.

---

## И36 · Лестница колонок кончается на двух, а не на одной

Полка, у которой в ряду одна карточка, — уже не полка. То же у полосы, в
которой видно меньше двух ячеек.

**Дефект.** Заказчик открыл опрос на телефоне: «квиз показывает карточку на
всю ширину. да это блять запрещено. везде две карточки на ширину может
поместиться, одну не нужно — удаляй такой вариант вообще с сайта, чтоб более
нигде такого не было». Столбик стоял в трёх местах сразу и все три выглядели
безупречно: у опроса — медиазапрос, переписывавший дорожку примитива на одну
колонку; у поля плиток — ширина ячейки в полосе, назначенная как 84% экрана;
у полки товаров — 72%. Ни одна строка не говорила «одна колонка»: её давали
пол ячейки и ширина ряда, встретившиеся на узком окне.

**Как писать.** Пол ряда — часть формулы дорожки, а не правка по месту:

    min( max(--cell-min, доля ряда на --n), доля ряда на --n-min )

`--n-min` по умолчанию 2. Внешний `min()` и есть запрет: как только пол
ячейки перерастает половину ряда, побеждает половина — и ячеек остаётся
ровно две.

**Полос это НЕ касается, и границу провёл заказчик.** Сперва запрет растянули
и на полки с прокруткой — там стало «две целиком и початая третья». Через час
он вернул как было: «верни размеры карточек и плиток, что было до последнего
исправления». Он прав по сути: в РЯДУ одна ячейка — тупик, соседа нет вовсе
и сравнивать не с чем; в ПОЛОСЕ соседняя выглядывает из-за края и доезжает
одним движением пальца, а карточка товара в 160 пикселей на телефоне
перестаёт читаться — в ней снимок, имя, спектр, миллиграммы, цена и кнопка.
Полоса на телефоне: ячейка 72–84% экрана, початая соседняя у края.

**Опт-аут объявляется вслух.** Блоку, у которого в ячейке длинный текст
(вопрос опроса с пилюлями ответов — пилюля шире половины телефона), пишется
`--n-min:1` у себя. Это решение, названное в файле, а не медиазапрос,
переписывающий примитив за его спиной.

**Чем мерится.** Семья `alone` в отрисованной проверке: две карточки подряд
одна под другой, то есть ряд, держащий одну. Полосы она не трогает. В файлах
этот дефект не виден, поэтому проверка только отрисованная.

---

## И37 · Орган, открывающий панель вместо перехода, остаётся ссылкой

Корзина, избранное, учётка — у каждого есть СВОЙ адрес, и нажатие на знак
теперь открывает шторку, а не уводит страницу. Соблазн при этом один:
сделать знак кнопкой, раз он больше никуда не ведёт.

**Дефект, которого этим избегают.** Кнопка не открывается в новой вкладке,
не копируется правым нажатием, не открывается средним, не показывает адрес в
строке состояния и не достаётся поиску. Покупатель, привыкший открывать
корзину во второй вкладке, теряет способ, а поиск теряет страницу — ради
удобства, которое можно получить, ничего не ломая.

**Как писать.** Тег остаётся ссылкой на настоящий адрес, а перехватывается
ТОЛЬКО простое нажатие:

```jsx
<Link href="/cart" onClick={(e) => {
  if (e.metaKey || e.ctrlKey || e.shiftKey || e.altKey || e.button !== 0) return
  e.preventDefault()
  panel.show()
}}>
```

Ctrl, Cmd, Shift, Alt и среднее нажатие проходят насквозь — то есть все
способы «открой это иначе» работают, как работали. Страница при этом обязана
существовать по-настоящему: шторка показывает то же самое, но она не адрес.

**Состояние панели — состояние САЙТА, а не блока.** Открывают шторку корзины
из двух мест (шапка и нижняя панель), а нарисована она одна на страницу. Пока
«открыта или нет» живёт внутри шапки, второму месту приходится заводить свою
вторую шторку — и они расходятся.

---

## И38 · Затемнение — тёмное в обеих темах

`::backdrop` под окном и шторкой — это ЗАТЕМНЕНИЕ, а не «краска чернил с
прозрачностью».

**Дефект.** Во всех трёх окнах проекта стояло одно и то же выражение:
`color-mix(in srgb, var(--sage-12) 55%, transparent)`. В светлой теме чернила
— тёмная краска, и подмены не видно. В тёмной чернила БЕЛЫЕ: поверх тёмной
страницы ложилась белая вуаль, и страница под открытой шторкой выбеливалась
вместо того, чтобы уйти в тень. Нашлось снимком открытой шторки корзины в
тёмной теме — отрисованная проверка этого не видит, потому что окна на ней
закрыты.

**Как писать.** Затемнение — свой токен `--scrim`, названный ЧИСЛОМ, а не
ролью: роль `--sage-12` переопределяют полы (`data-ground`), и затемнение,
собранное из неё в `:root`, подставило бы туда чужое значение (var() внутри
переменной раскрывается там, где переменная объявлена). В тёмной теме оно
вдобавок гуще: у тёмной страницы меньше собственного контраста, и 55% её
почти не приглушают.

**Правило шире затемнения.** Всё, что должно остаться ТЁМНЫМ (или светлым) в
обеих темах, нельзя собирать из роли, которая тему переворачивает. Признак:
величина, названная по смыслу «тень», «вуаль», «подложка снимка», — и роль,
названная по месту в лестнице.

---

## И39 · Полоса едет только вбок

`overflow-x:auto` НЕ оставляет вторую ось «видимой»: по правилу CSS она тоже
становится `auto`. Полоса, у которой содержимое хоть на пиксель выше ячейки,
начинает прокручиваться ещё и вверх-вниз.

**Дефект.** Заказчик увидел это на поле плиток: «в плитках какой-то
скроллинг, и внутри плиток, и плитки скролятся в блоке — откуда эта хуета
взялась, отменяй это; плитки только с текстом и только влево-вправо
скролятся в блоке». В файле не видно ничем: там написана ОДНА ось. Стояло
так в шести местах — поле плиток, полка товаров, примитив `rail`, кадр
товара, таблица документа, — то есть правила не было вовсе.

**Как писать.** У всякой горизонтальной полосы вторая ось закрыта явно:
`overflow-x:auto; overflow-y:hidden`. Мерится семьёй `railY` в проверке
вёрстки по файлам.

**И вторая половина того же дефекта — почему содержимое вообще переросло
ячейку.** Высоту ячейки считала ПРОПОРЦИЯ (`aspect-ratio`), а длину подписи
она не знает: болгарский перевод длиннее английского, и хвост уезжал за край.
Текстовой плитке высоту даёт ТЕКСТ: пропорции у неё нет, есть пол (чтобы не
выродилась в строку) и общий потолок. Пропорция остаётся там, где содержимое
— снимок: снимок кадрируется, текст нет.

---

---

## И40 · Вопрос заказчику задаётся так, чтобы он мог ответить со своего устройства

Выбор, который показывают человеку на живой странице, обязан быть виден **на
том устройстве, с которого он смотрит**. Иначе это не вопрос, а неработающая
кнопка.

**Дефект.** Заказчику предложили выбрать, чем отвечает под рукой выключенная
половина переключателя: три слова — «молчит · вуаль · чернила» — и просьба
«наведите мышь». Он смотрел с телефона. Наведения на телефоне нет вовсе
(правило живёт в `@media (hover:hover)`, и это верно), поэтому все три
варианта выглядели одинаково. Его слова: «эти кнопки ни на что не влияют, не
работают». Кнопки работали — ответить с телефона было нельзя.

**Как правильно.** Вариант показывает свой ответ **образцом на себе**, а не
словом: половина, одетая так, как она оденется под рукой. Три образца стоят
рядом и видны разом, без наведения и без нажатия.

**Чем куплено сверх этого.** Правило шире переключателей: любой вопрос «как
вам больше нравится» на витрине обязан показывать ответ статично. Наведение,
фокус клавиатуры, длинный текст в чужом языке — то, чего на устройстве
заказчика может не быть вовсе.

**Одна правда о цвете при этом сохраняется.** Образец берёт те же ручки
(`--tray-off-*`), что и живой ответ, а не вторую их копию: отметка режима
читается с любого предка, а не только с `<html>`.

---

## И41 · Родной список одеть нельзя: его перечень рисует система

Выбор одного из многих либо выглядит чужим, либо рисуется своим листом.
Третьего нет, и это не вопрос старательности: у `<select>` можно покрасить
коробку, а САМ ПЕРЕЧЕНЬ рисует операционная система, и никаким CSS до него не
дотянуться.

**Дефект.** Заказчик прислал снимок полосы над полкой: «Price per mg нужно ж
в таком же дизайне сделать как и другие кнопки». На снимке — синяя системная
полоса выделения посреди тихого магазина, рядом с пилюлями «Cards / Table».
Рядом стоял и второй орган: кнопка, переворачивающая порядок. Она показывала
НЫНЕШНЕЕ направление, а подпись обещала то, что случится по нажатию, — пару
эту нельзя объяснить, не написав абзац.

**Как правильно.** Список, который раскрывается, — это кнопка и свой лист в
ВЕРХНЕМ СЛОЕ браузера (`popover`, правило 8). Оттуда же приходит бесплатно
всё, что иначе пишут руками и забывают: Escape, закрытие нажатием мимо,
возврат фокуса на кнопку, перекрытие всего, что ниже. Место листа считается
на открытии — верхний слой стоит над страницей, и «под кнопкой» там значит
координаты окна; не поместился снизу — встаёт сверху.

**Заодно исчезает второй орган.** Ключ и направление — два вопроса одного
органа, и оба живут в одном перечне двумя ИМЕНОВАННЫМИ группами: «Подредба
по» и «Посока». Без имён пять строк читаются одним списком. Имя группы —
слово заказчика: «назови пункт типа сортировка, это же сортировка».

**Знак после этого симметричен.** Две встречные стрелки говорят «здесь
решается порядок», а какой выбран — говорит строка с галочкой. Прежний знак
показывал направление поворотом, потому что направление переключала кнопка;
кнопки нет — показывать нечем. Одна стрелка вверх сюда не годится и сейчас:
на сайте она уже значит «наверх страницы».

---

## И42 · Орган, который берёт только лист набора, — не орган

Рисунок, который никто не выводит на витрину, живёт ровно в одном месте — на
листе набора. Лист при этом показывает то, чего покупатель не видит нигде, то
есть врёт молча.

**Дефект.** За одну сессию таких нашлось трое. Знаки `Whatsapp` и `Viber`
остались от прежнего набора соцсетей — заказчик нашёл их глазом на листе
(«посмотри иконку вайбера, с ней явно что-то не то»), и оказалось, что в
подвале рисует другая марка. Знак `Mark` — две капли и черта — не выводился
никуда, кроме листа: «это удали, не используем». Кнопка-перевёртыш и её знак
`SortBars` стали такими в тот час, когда подредба съехала в один орган.

**Как правильно.** Орган уходит вместе с вопросом, на который отвечал, — в
ту же правку, а не «потом». Признак виден в файле и потому проверяем: рисунок
или класс, который не импортирован никуда, кроме страницы набора.

**Чего правило НЕ говорит.** Одежда, показанная на листе рядом с
сегодняшней, — не мёртвый орган: это варианты одного и того же контрола,
между которыми заказчик выбирает. Мёртв тот, у кого нет ни одного места на
витрине — ни выбранным, ни возможным.

---

## И43 · Надпись на кнопке центрируется по ЧЕРНИЛАМ, а не по слову

Пустота, зарезервированная под то, чего сейчас нет, — для глаза просто дыра
сбоку. Центрировать надо то, что ВИДНО: знак, слово и число, если оно есть.

**Дефект.** Кнопка «в корзину» со второй штуки показывает число. Чтобы слово
не прыгало в момент, когда число появляется, под число заранее держали место
и зеркалили его пустотой с другой стороны. Замер отрисованной кнопки показал,
что вышло: слово и правда почти не двигалось, зато видимая надпись уезжала —
на 17 пикселей влево, когда числа нет, и на 21 вправо, когда оно есть. Оба
раза заказчик находил это глазом, и второй раз — уже по «исправленному»: «она
смещается влево и не симметрична относительно центра».

**Как правильно.** Числа нет — оно и места не занимает (`display:none`, а не
невидимость), и пара «знак и слово» стоит посередине. Число появилось — оно
часть надписи, и посередине стоит вся тройка. Никаких зеркал: их ширины
подгоняются числом, а подгонка расходится при первом же чужом языке.

**Цена решения названа вслух.** На второй штуке кнопка становится шире на
ширину числа. В тесной карточке это не двигает ничего — там кнопка и так во
всю ширину; в широкой рядом с ценой место есть.

**Мерится отрисованным.** Сдвиг чернил от середины кнопки — величина, которой
в файле не видно: в файле обе попытки выглядят безупречно.

---

## И44 · Лист набора показывает предмет в тех размерах, в которых он бывает

Образец, растянутый на ширину листа, — это не тот предмет, что стоит на
витрине. Лист, показывающий несуществующий размер, врёт так же, как лист,
рисующий свой рисунок вместо общего.

**Дефект.** Заказчик открыл лист набора, открыл магазин и сказал прямо: «эти
карточки товара совсем не одинаковы, не синхронизированы». Карточка была ТА
ЖЕ САМАЯ — тот же компонент, — но лист ставил её в сетку на треть ширины
экрана: 470 пикселей, каких на витрине не бывает нигде. На витрине она бывает
179, 210, 240 и 265.

**Вторая половина того же дефекта.** «В моб. версии кнопка добавлено
показывается без значка корзины. А где в дизайн системе отображение
мобильного варианта, его просто нет?» Не было. Узких ширин лист не показывал
вовсе, поэтому правило «в тесноте знак уходит» нельзя было ни увидеть, ни
обсудить — только наткнуться на него на телефоне.

**Как правильно.** Образец стоит в РАМКЕ замеренной ширины, и рядом сказано,
где эта ширина встречается. Числа снимаются с собранного сайта, а не берутся
на глаз.

**Почему это работает.** Компонент меряет свой контейнер, а не окно (запрет
6). Значит рамка в 179 пикселей на столе показывает ровно то же, что телефон.
Рамка при этом обязана быть объявлена контейнером (`container-type`), иначе
правила внутри образца считаются от чужого предка, и лист снова показывает не
то.

**Чего рамка не покажет.** Прибавку под палец: она приходит не от ширины, а
от того, чем нажимают (`pointer:coarse`). Это говорится словами рядом.

---

## И45 · Верх листа — один на все листы

Панель, шторка, меню и окно — это ЛИСТЫ, и верх у них устроен одинаково:
имя, метка, действие справа. Рисуется он один раз.

**Дефект.** Заказчик прислал три снимка — корзину, фильтры и меню масел — и
сказал: «это какие-то панели, окна и т.п., у них должен быть единый дизайн,
они ж сейчас разные». Разными они были потому, что верх листа был нарисован
ПЯТЬ раз в пяти файлах на семь листов, и два из пяти — посимвольная копия
друг друга. Имя набиралось тремя ступенями (21, 18 и 22.5) и двумя
жирностями, метка бывала 22 и 20 пикселей, а справа стоял то круглый крестик,
то ничего, то ссылка.

**Как правильно.** Мест в шапке три, и занимает их ЛИСТ, а не шапка: имя,
метка, действие. Чего у листа нет — того в шапке и не видно. Одежда одна на
все («волосок» — тонкая линия под шапкой, выбор заказчика по вариантам в
артефакте), и там, где под шапкой нет прокрутки, линия снимается ручкой, а не
вторым рисунком.

**Поле по бокам даёт лист, а не шапка.** Поле принадлежит предмету, и у
шторки, панели и меню оно своё. Шапка отвечает за строку и за воздух под ней.

**Пустая шапка волоска не получает.** У панели фильтров имя скрыто от глаза
(его говорит кнопка над ней), метки нет — значит видна была бы одна линия, а
линия ниоткуда читается как ошибка. Коробка при этом остаётся: у панели она и
есть верхнее поле.

**Имя, скрытое от глаза, остаётся для тех, кто слушает.** На нём держится
лестница заголовков — h1 страницы, h2 панели, h3 групп; убрать его совсем
значило бы прыгнуть через ступень.

**Выход тоже один.** Крестик был нарисован четырежды — в шторке корзины, в
быстром заказе, в «напишите нам» и в выдвижном меню, — и каждый раз чуть
иначе: 36 и 32 пикселя, разные краски под рукой. Теперь его рисует шапка,
и лист только говорит, что закрывать (`onClose`).

**Шесть листов из семи переведены.** Седьмой — выдвижное меню телефона — стоит
особняком осознанно: выход у него слева, под большой палец, а не справа, и
это не разнобой, а другой вопрос. Переводить его значило бы двигать крестик
туда, где до него не дотянуться одной рукой.

---

## И46 · Метка показа — одна, и шириной её владеет кто-то один

Слайдеров на сайте два — герой и карусель в меню, — а метка под ними одна:
«какой кадр сейчас и сколько ему осталось». Значит и нарисована она один раз.

**Дефект.** Заказчик открыл мега-меню: «а тут блять слайдер другой, т.е. у
нас два разных слайдера». Слайдер был тот же по смыслу, но метка нарисована
своей копией: в герое точка растягивалась в полоску и заливалась, в меню —
в пилюлю другого роста и другого цвета.

**Второй дефект, в той же метке.** «После заполнения и перехода к новой точке
происходит резкий скачок, прыжок — это очень некрасиво.» Метка делала два
движения разом: растягивалась И заливалась, и на смене оба обрывались —
заливка гасла на середине, старая полоска схлопывалась, новая начинала расти
и заливаться одновременно.

**И третий, про краску.** «Полоска белая, ещё и поэтому оно как бы прыгает и
в глаза бросается.» Подложка была набрана ступенью серого (`--sage-5`), а на
тёмной палубе эта ступень читается почти белым.

**Как правильно (выбор заказчика по вариантам в артефакте, со второй
попытки).** Сперва была сделана сквозная дорожка: точки на одной линии,
заливка едет по ней насквозь. Заказчик её отверг — «верни предыдущий, просто
белый фон для синей полоски нужно было убрать». Стало: точка вырастает в
полоску и наливается, а ПОДЛОЖКИ У НЕЁ НЕТ ВОВСЕ — растёт сама метка, из
точки такого же размера, как соседние. Ни белого скачка, ни пустоты в ряду.
Краски две: покой — вуаль из чернил строки (22% `currentColor`), которая на
палубе темнеет, а на бумаге светлеет и белой не бывает нигде; текущая —
ступень живого (`--cyan-11`), та же, которой набраны ссылка и ответ под рукой.

**И четвёртый, купивший главное правило файла: ШИРИНОЙ ВЛАДЕЕТ КТО-ТО ОДИН.**
«После того как полоска дошла до конца, какое-то дёрганье происходит.»
Ширину метки вели двое: переход — на нажатие, показ — на ход кадра. Так
делать нельзя, и оба конца этого проверены замером, а не рассуждением.
Переход в каскаде стоит ВЫШЕ показа: объявленная ширина уводит метку в
полоску за свои двести миллисекунд, после чего показ возвращает её к точке.
А когда кадр кончился и показ снят, ширина схлопывается в точку ОДНИМ
КАДРОМ — перехода от значения, которое вело показ, браузер не заводит. Замер
на поднятом сайте: 22.0 → 6.0 за один кадр.

Лечится разведением по ДВУМ ПРЕДМЕТАМ: коробка — место, где метка помещается,
её ширину ведёт рука; метка внутри — то, что видно, её ширину ведут часы.
Метка стоит во всю коробку и потому на уходе сжимается вместе с ней, плавно и
без своего правила. Замер после правки: 22.0 → 18.5 → 13.6 → 9.5 → 6.0 за
170 мс.

**Общее правило, переносимое.** Одно свойство — один хозяин. Переход и
показ, посаженные на одно и то же свойство одного и того же предмета, дерутся
всегда: в каскаде они на разных этажах, и на снятии показа переход не
подхватывает то, что показ вёл. Нужны оба — нужны два предмета.

---

## И47 · Строка подписи одна на все виды предмета

Виды предмета — это одежда, а не разная раскладка. Если у четырёх видов
плитки знак «куда ведёт» стоит на четырёх разных высотах, значит раскладка
написана четыре раза, и система существует только на словах.

**Дефект.** Заказчик открыл лист набора: «эти стрелочки вообще хаотично по
высоте расставлены, ну у нас же система какая-то должна быть? Архитектура,
системность. Или ты всё по несколько раз независимо в коде рисуешь?» Он был
прав буквально: строка подписи была описана у трёх видов порознь одинаковыми
двадцатью строками, а четвёртый («full») вешал знак в УГОЛ плитки отдельным
правилом — и ему же приходилось отодвигать строки подписи, чтобы они не
уезжали под знак.

**Как правильно.** Слова столбиком слева, знак справа, по середине ВСЕГО
столбика — одно правило на `.body`, и виды к нему больше не возвращаются.
Замер после правки: у всех четырёх видов знак стоит ровно на середине слов
(ноль пикселей отклонения).

**Столбик слов — свой предмет, а не ячейки сетки.** Сетке пришлось бы
называть число строк (`grid-row: 1 / span 2`), и с третьей строкой знак
съезжал бы вверх. Столбик центрируется целиком, сколько бы строк в нём ни
было.

**Проверять это надо отрисованным.** В файлах четыре копии выглядели
безупречно: каждая по отдельности правильная. Видно только на листе набора,
где четыре вида стоят рядом, — и заказчик увидел это раньше проверки.

---

## И48 · Потолок меры — про возврат, и спрашивается с того, что переносится

Мера набора — 45…75 знаков в строке. Верхняя граница бережёт ВОЗВРАТ: путь
глаза от конца строки к началу следующей. Чем строка длиннее, тем чаще глаз
промахивается этажом.

**Дефект.** Подпись под заголовком блока ломалась надвое там, где места было
на полтора экрана: колонке стояла мера лида, посчитанная для бегущего текста.
Заказчик увидел это глазом и сказал прямо: «тексты под заголовком пусть идут
в одну строку, переносить в две не нужно». Он прав по устройству: подпись
читают ОДНИМ взглядом вместе с заголовком, и перенос рвёт этот взгляд надвое.

**Как правильно.** У текста в одну строку возврата нет вовсе — значит и
потолок с него не спрашивается. Проверка сузилась до текста, который
ПЕРЕНОСИТСЯ (`check:craft`, семья `measure`), а подпись блока получила свою
меру (`--measure-note`), посчитанную замером: 64ch по-латински и 71ch по
кириллице — в них встают все подписи сайта, а та, что не встала, длинна
по-настоящему и переносится. Перенёсшись, она снова спрашивается потолком.

Это второе сужение той же проверки по тому же основанию: первым была
правовая мелочь подвала (`data-fine`), которую не читают подряд. Оба раза
граница проведена в проверке по смыслу, а не подкручена в базе нарушений.

**Отменено заказчиком, сентябрь.** Увидев страницу «О нас» с текстом в
половину окна: «узкая колонка текста — это правило проекта? хуйня это, удали
это правило». Потолок меры снят из проверки, из закона и из стилей —
документы, ответы, проза под полкой, описание товара занимают свою колонку
целиком; `data-fine` ушёл вместе с потолком. Нижняя граница осталась: текст,
не берущий своей колонки, — это ровно то, что заказчик и увидел.

---

## И49 · Шторка корзины показывает товар, а не сводку

Шторка отвечает на «что у меня в корзине». Всё, что занимает в ней место, но
на этот вопрос не отвечает, отнимает высоту у того, что отвечает.

**Дефект.** Заказчик прислал снимок: «проблема корзины в том, что только одна
единица товара видна в боковой панели». Считать было что: сводка держала
четыре строки чисел — промежуточная сумма, доставка, НДС, итог, — полоса
бесплатной доставки стояла в подвале, два выхода стояли столбиком во всю
ширину, а строка товара выносила счётчик и сумму третьим этажом под снимком.
Вместе это триста с лишним пикселей, и списку оставалось на одну карточку.

**Как правильно, и каждое — по своему основанию:**

* **Доставка и НДС ушли на оформление.** Они не известны до адреса: до него
  обе цифры — обещание, а не факт. Слова заказчика: «delivery, vat убирай,
  это на этапе чекаута будет». Промежуточная сумма ушла с ними — без них она
  до копейки равна итогу, а две одинаковые цифры подряд читаются как ошибка
  счёта. Осталось одно число и тихая строка о том, где считают остальное.
* **Полоса бесплатной доставки — под шапкой.** Она про то, что происходит
  ПОКА кладут в корзину, а внизу, под итогом, читалась строкой чека. Полоса
  стала своим предметом (`CartFreeBar`): место ей назначает тот, кто ставит,
  — в шторке верх, в сводке заказа середина.
* **Два выхода — в одну строку, поровну.** Столбиком они забирали у списка
  сто с лишним пикселей, а говорят об одном: куда идти дальше.
* **Строка товара держит два этажа, а не три.** Снимок держит высоту обоих:
  имя сверху, органы под ним. «Убрать» переехало из текста к счётчику — это
  органы одной строки, и разносить их по этажам незачем.

Замер после правки: в шторке видно три карточки вместо одной.

---

## И50 · Плитка не рисует знак и не описывает его ответ

Знак «куда ведёт» — один на весь сайт (`components/Go.tsx`). Всё, что его
ставит, выбирает не «одежду из списка», а три независимые вещи: что
нарисовано вокруг, куда ведёт, как отвечает на руку.

**Дефект.** Заказчик спросил, глядя на лист набора: «а откуда ты взял эту
стрелку, она не движется — разве она не из нашей дизайн-системы?» Не была.
Плитка держала свой рисунок: три размера (14, 16 и 22), четыре своих одежды
с собственными заливками и обводками, и движение — только у одной из
четырёх. То есть три вида плитки из четырёх на руку не отвечали вовсе, а
`bare` и `slide` были не двумя одеждами, а одной, у которой во втором случае
не забыли ответ.

**Как правильно.** Плитка говорит, какая у знака одежда, и подаёт своё
наведение ЧИСЛОМ (`--go-on`, `--go-press`): наводят и нажимают плитку
целиком, знак внутри неё — рисунок, а не вторая мишень. Для этого заведён
`Sign` — тот же знак без мишени и без имени, потому что имя носит то, что
его окружает. Четыре одежды плитки остались именами, но развернулись в
сочетания осей общего знака (`TILE_SIGN`): имена лежат в браузере заказчика,
и менять их значило бы молча переодеть его плитки.

Мерится семьёй `twiceDrawn` в `check:css`: файл, который сам задаёт рисунок
знака или его движение. После правки 4 → 2.

**Заодно ушёл четвёртый вид плитки.** Заказчик: «Plain удаляй вовсе» — этим
он отменил своё же решение месяцем раньше. Вместе с видом ушёл и его отклик:
он подменял тёмную поверхность плитки десятипроцентной вуалью, то есть на
наведении тёмная плитка СВЕТЛЕЛА целиком. Та же семья `halfRole`, что и
белые пилюли на палубе: пол сменили, состояние осталось от прежнего пола.

**И третье, про лист набора.** Рост образцу теперь даёт РЯД, а не его
собственное содержимое. Содержимым его брала каждая плитка по-своему: у
полосы нет ничего, кроме подписи, — и на листе она схлопывалась в белую
плашку без снимка вовсе, тогда как в магазине рост ей даёт ряд. Три вида
разного роста сравнить нельзя: лист показывал не виды, а их вырождение.
Заказчик увидел и это: «у нас же было три варианта — сделай их нормально,
полноценно».

---

## И51 · Подвал на телефоне: полоса знаков не колонка, ритм — ступень тише

Подвал складывается в столбик длиной почти в две тысячи пикселей, и на этой
длине всё решают две вещи: что чему принадлежит и что куда помещается.

**Дефект, найденный заказчиком глазом.** «Иконки — зачем перенос сделан? Они
вполне помещаются по ширине.» Помещались: в подвале 292 пикселя, ряду
соцсетей нужно 168, ряду платёжных марок — 232. Полоса знаков стояла ЯЧЕЙКОЙ
двухколонной сетки телефона, то есть 137 пикселей, и переносилась после
третьего знака.

**Как правильно.** Полоса знаков — не колонка справки: справа от неё нечему
стоять, и ряд идёт во всю ширину. Признак общий: если у части блока нет
соседа сбоку, она не колонка, какой бы сеткой её ни складывали.

**Второй дефект, там же.** «Посмотри расстояния, ритм, поле, воздух — тут
явно есть что улучшить.» Замер: все восемь швов подвала — 28 пикселей, до
одного. Одно расстояние на всё читается не ритмом, а списком: знак, строка о
магазине, реквизиты, контакты, два столбца ссылок, оговорка и знаки стояли
одинаково далеко друг от друга, и ничто ни к чему не относилось. Ступень
внутри блока обязана быть ТИШЕ ступени между блоками — иначе блока нет.

**И три мелких из того же замера:** поле для письма ужималось до 144
пикселей и обрезало адрес — теперь у поля есть основание, и пара переносится,
когда ряду тесно; у колонки реквизитов не было имени, а у соседней было —
два ярлыка одного кегля в одной строке глаз разбирает не сразу; правая
группа правовой полосы уходила ПОД плавающие кружки в углу — «текст нужно
подвинуть левее, чтоб не упирался в кнопки». Полосу под кружки называет
теперь токен (`--dock-side`), и читают его двое: сам помощник (это его рост)
и тот, кто под него отступает.

---

## И52 · Полоса, сменившая пол, переопределяет ЛЕСТНИЦУ ЦЕЛИКОМ

Тёмная палуба под блоком — это смена пола. Пол сменили — обязаны смениться
все краски, которые от пола зависят. Не часть, а все: половина роли всегда
ломается на чужом полу.

**Дефект, и это третий случай той же породы за проект.** Заказчик поставил
полку товаров на палубу и увидел: «название бренда — синий на синем, это ты
проебал». Он прав буквально. Марка на карточке набрана ЖИВЫМИ чернилами
(`--cyan-11`), а живые чернила на светлом полу темны — палуба же и есть
фирменный цвет. Замер: 1.54:1 при норме 4.5, то есть практически не видно.

Палуба переопределяла только СЕРУЮ лестницу — чернила строки, тихую краску,
поверхность. Живая осталась от светлого пола, и с ней утонуло всё, что ею
набрано: имя марки, кнопка «ко всему», кружок под её стрелкой, ответ под
рукой.

**Как правильно.** Ступени берутся не новые: у живой лестницы СВОИ тёмные
рунги уже объявлены — те, которыми она встаёт в тёмной теме. Палуба и есть
тёмный пол, поэтому на ней ровно они. В тёмной теме объявление ничего не
меняет: там лестница уже стоит на них. Замер после правки: 4.98:1.

**И роли, составленные из чернил, переопределяются вместе с ними** —
`--hover-ink` здесь. `var()` внутри объявления своей переменной раскрывается
там, где переменная объявлена, — на корне, — и вниз наследуется уже готовая
краска. Ровно эта ловушка однажды оставила на палубе волосок и вуаль,
посчитанные от светлых чернил.

**Почему проверка молчала.** Пол под блоком — НАСТРОЙКА, а проверки ходят по
собранному сайту с умолчаниями. Полка масел на палубе не стоит ни на одной
странице дерева, поэтому отрисованный контраст этого не видел и не мог
увидеть. Отсюда следствие, которое дороже самого дефекта: правка ради
настройки делается в СЛОЕ ЦВЕТА, а не в блоке. Слой держится сам, какой бы
пол блоку ни выбрали; блок, починенный по месту, держится только для того
пола, на котором его чинили.

## И53 · Поле предмета не одалживают у соседа

ПОЛЕ принадлежит предмету и нулём не бывает (правило 2). Отсюда следствие,
которое в файле не видно: поле, которое за предмет платит СОСЕД, исчезает
вместе с соседом — а сосед может схлопнуться, уехать в прокрутку или вовсе
не появиться на этой ширине.

**Дефект.** Белый лист шапки держал своё верхнее поле не сам: сверху в нём
стояла полоса настроек, и белое над её содержимым и было тем полем. При
прокрутке полоса схлопывается — и рамка вокруг тёмной строки становилась 0
сверху при 8 снизу и по бокам, а угол листа (28) вылезал из-под угла строки
(20) белым клином в верхних углах. Заказчик увидел это глазом: «верхнее и
нижнее белое поле вокруг синего куска — что с ним».

**Как правильно.** У листа своё поле со всех четырёх сторон. Соседу
остаётся только ВОЗДУХ до следующего предмета — тот самый, который обязан
исчезать вместе с ним.

Та же болезнь была во второй сборке шапки, и починена там же: строка,
ставшая всем предметом, берёт и его угол.

## И54 · Один шов — один хозяин

ВОЗДУХ между двумя блоками ставит ОДИН из них. На этом сайте это тот, кто
стоит НИЖЕ: `.section` объявляет просвет над собой. Когда верхний блок
вдобавок платит своё нижнее поле, просвет складывается из двух чисел — и
выходит вдвое больше задуманного, причём ни одно из двух чисел не выглядит
в файле неверным.

**Дефект.** На странице товара между коробкой покупки и вкладками стояло
124px, а между вкладками и полкой «сравните с» — 48 при том же смысле шва.
Платили двое: ряд товара своим полем в 80 и вкладки своим воздухом в 48.
Заказчик увидел ритм: «тут между блоками — воздух, поле, ритм же должен
быть, проверь карточку всю».

**Мерится** семьёй `twoAir` в отрисованной проверке: у двух соседей-блоков
во всю ширину верхний платит полем, а нижний — полем или просветом. Пара
«просвет + просвет» дефектом не бывает: просветы схлопываются сами.

**И половина, без которой правило ловит невиновных.** Блок, который КРАСИТ
свой пол — лист, карточка, полоса со своей заливкой, — полем за шов не
платит: оно лежит уже ВНУТРИ его поверхности, и между соседями видно край
листа, а не воздух. Считать его вторым хозяином шва значит объявить дефектом
обычную пару карточек в столбик: два белых листа с просветом 48 читались как
шов в 96. Ровно эта же половина записана у соседней семьи «слипшиеся
блоки».

## И55 · Образец на витрине можно, в разметке — нельзя

Оценка, цена и наличие на образцовом каталоге — часть образцовой витрины, и
заказчик про них знает. Те же числа, отданные машине разметкой, — это
утверждение поиску, и за фальшивую разметку отзывов меру получает домен, а
домен один.

Значит, вопросов ДВА, и одной функцией они не отвечаются. `ratingOf` —
что показывает страница; `realRating` — что можно отдать машинам. Один
выключатель (`REVIEWS_ARE_REAL`) остаётся один, но стоит он теперь во
втором, а не в обоих сразу.

**Дефект.** Общий выключатель гасил и звёзды на витрине: страница товара
молчала об оценке вовсе, хотя показать её было можно. Заказчик попросил
вернуть: «SKU и оценку же нужно ещё добавить в карточку».

## И56 · Артикул — не адрес

**Дефект.** Артикула у товара не было вовсе: на странице его негде было
напечатать, а в разметке товара не стояло поле `sku` — то самое, которым
поиск связывает один и тот же товар у нас и у соседа. Заказчик заметил
пропажу глазом: «SKU и оценку же нужно ещё добавить в карточку».

Соблазн был взять под артикул адрес товара — он уже уникален. Нельзя:
адрес страницы придумываем мы и меняем когда угодно; артикул придумывает
магазин и не меняет никогда. Склеить их значит запретить себе
переименование адреса — или переучить склад. Поэтому `sku` у товара своё
поле, и оно же уходит в разметку: `sku` — то, чем поиск связывает один и
тот же товар в разных магазинах.

## И57 · Решённый выбор уходит из панели в код

У панели одна работа — показать варианты, между которыми ещё не выбрали. Как
только выбрали, вариант уходит из панели: выбор, который уже сделан, — это
лишний вопрос при каждом открытии и лишняя ветка в коде витрины, которая
молча ломается при правке соседа. В наборе вёрстки он при этом ОСТАЁТСЯ
образцом, на вкладке «На потом».

**Дефект.** Двенадцать ручек меню «По ефект» и восемь ручек меню «Oils»
стояли в панели после того, как заказчик назвал по ним все числа. Одна из
двенадцати — «Per-drop figure» — не была привязана ни к одной строке CSS
вовсе: настройка, которая ничего не меняла, прожила месяцы, потому что никто
не спрашивал, работает ли она.

Разбор и вторая половина правила — чем настройка ВИДА отличается от настройки
СОДЕРЖИМОГО и почему вторая не удаляется никогда — в скилле,
`references/controls.md`.

## И58 · Выпадашка на два пункта — лишнее нажатие

Список окупается, когда в нём есть что искать. На двух пунктах он берёт
нажатие за то, чтобы показать один невыбранный вариант, и тянет за собой всё
своё устройство: открыто-закрыто, Escape, нажатие мимо, жест закрытия,
одежда списка. Пара показывает оба сразу и меняет выбор одним нажатием.

Порог — три-четыре пункта; появится третий язык, список вернётся.

**И вторая половина, которая дороже первой.** Если выбор МЕНЯЕТ АДРЕС, он
делается ССЫЛКОЙ, а не кнопкой. Язык на этом сайте — адрес (`/bg`, `/en`), и
у каждой страницы есть близнец: ссылку можно открыть в новой вкладке,
скопировать и послать, а поиск по ней связывает две версии страницы — ровно
то, что обещает `hreflang` в разметке. Кнопка обещала бы действие с
неизвестным исходом и не сказала бы поиску ничего.

**Дефект.** Выбор языка стоял двумя копиями — выпадающее меню в шапке и пара
кнопок в чекмедже, — и в меню вдобавок жил ряд кнопок «какой одеждой его
рисовать»: настройка панели прямо на витрине магазина.

## И59 · Настройка, которая ничего не меняет, — хуже отсутствующей

У поля панели есть ПРОВОД: он ставит атрибут на корень документа или
переменную. Провод, у которого на другом конце ничего нет, — это ручка,
которую можно крутить без всякого следствия. И она не «про запас»: её никто
не открывает, а значит никто и не замечает, что она мертва.

Хуже отсутствующей она потому, что заказчик по ней ВЫБИРАЕТ. Он крутит,
смотрит, не видит разницы — и относит это на свой глаз, а не на сломанный
провод.

**Дефект.** «Per-drop figure» в разделе выпадающих меню — выбор из двух
положений, не привязанный ни к одной строке CSS. Прожил месяцы и нашёлся
только когда раздел снимали целиком и я спросил, к чему были привязаны его
ручки.

**Мерится** семьёй `deadSetting` в проверке кода: для каждого поля панели
ищется след его провода в остальном коде, а у поля без провода — чтение его
ключа по имени.

## И60 · Страница товара растёт до потолка и дальше стоит

Вопрос заказчика: «тоже в зависимости от размера монитора должна ли карточка
меняться, подстраиваться — как принято в ecommerce, подумай».

**Дефект.** Мера страницы товара стояла числом — 1248, — и кадр числом: 420.
На мониторе в 1920 это давало снимок ноутбучного размера и семьсот пикселей
пустоты по бокам; на 2560 — тысячу четыреста. Страница товара, у которой
главное содержимое это снимок, показывала его одинаково мелким на любом
экране.

Принято так: страница РАСТЁТ до потолка и дальше стоит по центру. Не
растянуться вовсе — значит на широком мониторе оставить снимок размером с
ноутбучный, когда места вдвое больше. Растянуться без потолка — значит
получить строку в двести знаков и путь от снимка у левой кромки до кнопки у
правой.

**Растёт при этом только КАДР.** Колонка покупки стоит на своей мере набора
(600px ≈ 55 знаков): текст шире этого читается хуже, сколько бы места ни
было. На 27 дюймах покупатель получает снимок крупнее, а не строку длиннее —
ровно то, ради чего он открыл страницу товара.

Замерено: кадр 420 → 461 → 538 → 560 на 1280 → 1440 → 1680 → 1920 и дальше
не растёт; колонка покупки 600 на всех, 67 знаков в строке. На ноутбуке с
окном в 657 по высоте кадр сам садится до 410 — потолок от окна (запрет 9)
стоит выше потолка от ширины.

## И61 · Выход из блока теряет слово ступенями — ВСЯКИЙ, а не только общий

У кнопки «Виж всички» три ступени: полная фраза, короткое слово, один знак.
У выхода, который блок нарисовал своей кнопкой, ступеней не было ни одной.

**Дефект.** «Последвай @cbdinbg» в шапке блока Instagram не переносится —
это одно слово с собакой, — и на 320 оставляло заголовку двенадцать пикселей.
Шапка становилась шире экрана, и всю страницу можно было утащить вбок на 74
пикселя. Нашёл свип; в файлах не видно ничего.

Правило шире случая: всякая надпись в шапке блока обязана иметь ступень для
телефона, и ставится она там же, где стоит сама надпись.

## И62 · Прокручивается коробка ВНУТРИ листа, а не сам лист

Полосу прокрутки браузер рисует по краю той коробки, которая прокручивается.
У листа этот край скруглён — и бегунок ложится прямо на дугу угла: угол
читается срезанным, а полоса выглядит приклеенной снаружи.

Поэтому лист держит краску, угол и поле, а прокручивается коробка внутри
поля. Тогда между бегунком и углом остаётся поле листа, и встретиться им
негде.

**Дефект.** Панель фильтров каталога прокручивалась сама. Заказчик прислал
снимок угла и спросил, давал ли он это в правку, — то есть увидел глазом
раньше, чем понял, что именно не так.

Исключение — лист, у которого край прокрутки НЕ скруглён: у шторки корзины
скругление только слева, а прокрутка идёт у правого, прямого края.

## И63 · Цена за штуку в строке корзины

**Дефект.** В строке стояли количество и сумма — «3» и «237,00 €», — а из чего сумма
сложилась, покупателю приходилось делить в уме. Заказчик увидел это прямо:
«количество и сумма есть, а цены-то нет».

Скидка печатается ТОЛЬКО из пары цен: значок «−13%» там, где цены равны, —
обман на витрине. Старая цена стоит перед новой: обратный порядок читается
как подорожание.

Цены за миллиграм в корзине при этом нет и не должно быть: мера «за мг»
существует, чтобы СРАВНИВАТЬ до выбора, а в корзине уже выбрали. Заказчик
снял её тем же сообщением.

## И64 · Витрина — на языке рынка, разговор — на языке заказчика

**Дефект.** Артефакт с четырьмя вариантами поля кода скидки был набран
по-болгарски целиком, включая пояснения — при том, что заказчик болгарского
не читает. Он согласился с рекомендацией вслепую: выбор ему показали, а
прочитать не дали.

На витрине язык рынка остаётся — это правило проекта и правило поиска. Но
всё, что сделано ДЛЯ ЗАКАЗЧИКА — артефакт, подпись под снимком, объяснение
решения, — на его языке. И слова витрины на снимке в ответе переводятся.

## И65 · Админка предлагает ВЫБОР, а не числа

Число живёт в дизайн-системе: там оно объявлено кривой (`clamp`), показано на
листе набора и правится один раз для всего сайта. Ручка в админке, задающая
то же число, — второй способ задать одно и то же, и однажды они расходятся:
инлайн-стиль на `<html>` сильнее правила `:root`, поэтому побеждает ручка, а
правка токена молча не доезжает.

**Дефект.** Тридцать ползунков задавали величины, уже объявленные в
`styles/tokens.css`: лестницу размеров, кегли заголовка и карточки, геометрию
органов, ширину страницы, скругление, длительность отклика. Заказчик крутил
их, а потом называл числа словами — и числа всё равно приходилось класть в
набор руками, потому что ползунок живёт в браузере одного человека.

Граница проходит по вопросу, на который отвечает ручка:

* **вид и содержимое** — какая одежда, какой пол, какие мессенджеры, порядок
  блоков. Это вопрос к владельцу магазина, он остаётся;
* **число** — кегль, просвет, радиус, длительность. Это вопрос к
  дизайн-системе, и в админке его быть не должно.

## И66 · Одежда переезжает вместе с ролью

Прокрутка, приклейка, поле, обрезка — это **роли**, и правила, которые их
одевают, принадлежат не имени класса, а тому, кто роль играет. Переехала
роль на соседа — с ней переезжает и одежда. Оставленная на прежнем хозяине,
она не падает и не краснеет: она просто перестаёт работать, а браузер
рисует своё умолчание.

Признак виден прямо в файле и потому стал проверкой (`barNoScroll` в
`check:css`): **одевает ползунок — значит крутится.** Селектор, у которого
есть `scrollbar-width`, `scrollbar-color` или `::-webkit-scrollbar`, обязан
сам объявлять `overflow`. Страница (`html`, `body`, `:root`) не в счёт — она
прокручивается всегда.

**Дефект.** Заказчик нашёл его дважды, с промежутком в месяц, в одном и том
же месте. Сперва сам ползунок панели фильтров: «указатель скрола имеет дефект
на углах, он вылазит, может просто удалить стрелку на концах» — ползунок
одели: тонкий, без стрелок, вдвинут от края прозрачной каймой. Потом
прокрутку перенесли ВНУТРЬ панели (чтобы бегунок не ложился на дугу
скруглённого угла), а одежда осталась на листе. Лист больше не крутится —
правило висит мёртвым, и на снимке заказчика снова родной ползунок со
стрелкой, во всю высоту и прижатый к углу.

Правило было. Проверки не было — и починенный дефект вернулся целиком.

## И67 · Меню под шапкой — одна вещь

Где меню висит, из чего сделано и каким углом — сказано ОДИН раз, и все меню
шапки это берут. Не «такое же, как у соседа», а то же самое: линия под
шапкой одна, поверхность меню одна, угол один.

Признак нарушения виден в диффе без проверки: после правки на вопрос «откуда
у меню его поверхность?» ответов стало два.

**Дефект.** Заказчик прислал два снимка и назвал разницу точнее, чем она
записана в файлах: «поле под меню появляется — так правильно; а тут и поле
другого цвета, и не снизу меню оно привязано, т.е. оно несистемное, берётся
не из дизайн-системы — снова заплатка». Меню мессенджеров было белым
(`--surface`) при тёплой бумаге меню (`--menu-bg`), висело на девятнадцать
пикселей выше двух других — потому что считало от СВОЕЙ КНОПКИ, а не от ряда
шапки, — и имело свой угол: 18 против 24.

Причина не в трёх числах, а в том, что каждое из трёх меню одевалось само.
Одежда переехала в одно место (`.underHead`), а список мессенджеров — в ту же
точку разметки, что и полки: он попадает туда порталом, кнопка остаётся в
строке знаков. Замерено после правки: обе полки и список висят от 140,
поверхность у всех `#FDFCF8`, угол 24.

## И68 · Надпись контрола — слой управления, а не ритм страницы

Слово внутри кнопки, вкладки, пилюли — часть УСТРОЙСТВА контрола, и берётся
оно из `--fs-ui-*`, которая не течёт. Шкала абзаца (`--fs-*`) описывает
набор, а не мишени для пальца: кнопка на широком мониторе не становится
крупнее оттого, что окно шире.

Признак виден в файле: `font-size` кнопки, вкладки или пилюли, взятый из
`--fs-base` и вообще из текучей шкалы.

**Дефект.** Заказчик посмотрел на низ шторки корзины и сказал: «посмотри, в
целом какая большая разница в размерах шрифтов… размеры шрифтов должны быть
поменьше». Замер: в панели шириной 440 текст набирался шестнадцатью, а
надписи кнопок — восемнадцатью, то есть подпись была громче того, что она
подписывает. Причина одна на три места: общая кнопка, кнопка покупки и
крупная ступень все брали размер из шкалы абзаца.

## И69 · Итог сводки — самое крупное число, но не заголовок

Число, ради которого блок существует, берёт ступень ИМЕНИ этого блока, а не
ступень заголовка страницы, и отличается от соседей весом, а не ростом.
Подпись к числу («Общо», «Итого») уходит ступенью ниже: подпись не спорит с
тем, что подписывает.

**Дефект.** Тот же замер шторки: итог стоял на `--fs-h3` и набирался
двадцатью девятью при шестнадцати у всего вокруг — в панели шириной 440 это
не «главное число», а крик. Стал `--fs-xl` — ровно ростом имени шторки
(«Количка»), весом тяжелее.

## И70 · Ряд органов прижимается к краю, а не растягивается по ширине

Несколько вещей, стоящих в строке одной группой, прижимаются к своему краю с
ровным просветом между ними. Растянутый враспор ряд читается не группой, а
тремя одиночками: просвет между ними становится случайным числом, которое
меняется от длины соседа.

Ряд, которому строки не хватило, ПЕРЕНОСИТ своих, а не вылезает за край, — и
переносит по тому же краю, к которому прижат.

**Дефект.** Цена, счётчик и сумма в строке корзины были показаны заказчику
враспор по всей ширине карточки, и он снял это сразу: «не нужно нижнюю строку
растягивать по ширине, нужно прижать к правому краю». Замер объяснил, зачем
перенос: в шторке на 320 строке дано 264 пикселя, а пара цен со скидкой,
счётчик и четырёхзначная сумма с просветами просят 291.

## И71 · Заливка берёт краску заливки, а не ступень кольца фокуса

В лестнице цвета есть ступени, нарочно ЗВОНКИЕ: ими рисуют кольцо фокуса,
волосок вокруг выбранного, подчёркивание ссылки. Они рассчитаны на линию в
один-два пикселя и на большой площади звенят.

Сплошная заливка берёт основную краску сайта (`--pop`), а пустой остаток —
тинт той же краски. Смешивать семьи нельзя: холодная полоса на тёплом сером
читается как две разные вещи, случайно оказавшиеся рядом.

Признак виден в файле: `background` крупного предмета, взятый ступенью,
которая в остальном проекте стоит только в `outline` и `box-shadow`.

**Дефект.** Полоса «до бесплатной доставки» была залита `--cyan-9` — той же
ступенью, которой на сайте рисуется кольцо фокуса, — и на четырёхстах
пикселях звенела так, что заказчик усомнился в её происхождении: «этот синий
цвет разве у нас есть в дизайн-системе? нет же, это какой-то стандартный
ИИ-цвет, отвратительный». Краска была СВОЯ, выведенная из основной, — но
ступень чужая делу. Полоса стала `--pop`, остаток — `--cyan-4` вместо тёплого
`--sage-4`.

## И72 · Всплывающий лист — одна бумага на все

Всё, что ОТКРЫВАЕТСЯ поверх страницы — полка шапки, список мессенджеров,
карточка помощника, — одето ОДНОЙ бумагой: краска, угол и тень названы один
раз, а места их берут. Где лист висит, решает место; из чего он сделан — не
решает никто, кроме одного правила.

Это шире И67: тот говорил про меню ШАПКИ, и потому починил три меню из
четырёх. Четвёртое висит у своего угла экрана, под шапку не лезет — и
осталось белым.

**Дефект.** Заказчик прислал снимок карточки помощника и спросил одним
словом: «этот фон откуда?». Тот же список мессенджеров, что в шапке, стоял
на белом листе (`--surface`) при тёплой бумаге меню — и это было не
единственное расхождение. Замер обоих списков: бумага `#FFFFFF` против
`#FDFCF8`, ширина 266 против 268, угол строки 14 против 10, подпись 18
против 16. Четыре числа на один список — потому что список был описан дважды,
а комментарий «та же строка канала, что в шапке» держался честным словом.

## И73 · Просвет между соседями держит одна вещь

Расстояние между двумя соседями в строке задаётся ОДНИМ просветом. Коробка
фиксированной ширины, внутри которой содержимое прижато к дальнему краю,
добавляет к этому просвету свой остаток — и остаток разный у каждой строки,
потому что содержимое разной длины.

Признак виден в файле: у соседа в ряду стоит `width` числом вместе с
`text-align`, противоположным направлению ряда.

**Дефект.** В корзине сумма стояла в коробке шириной 88 и равнялась в ней по
правому краю. Просвет до счётчика получался 47 пикселей у «42,00 €» и 38 у
«158,00 €» — глазом это читается как сбитая вёрстка. Заказчик увидел на трёх
строках сразу: «ну почему тут разные зазоры… почему не применить одинаковое
поле-воздух там, где требуется симметрия». Коробка снята, просвет остался
один — 22 на всех строках.

Выбор тут есть, и он честный: ровный воздух или столбик счётчиков, обе
ровности сразу недостижимы, пока числа разной длины. Ровный воздух выбрал
заказчик.

## И74 · Долгое движение идёт сдвигом, а не шириной

Всё, что едет на глазах дольше секунды, двигается `transform` или
`translate` — тем, что живёт на композиторе. Ширина, высота и отступ — это
раскладка: браузер пересчитывает их каждый кадр и перерисовывает целыми
пикселями экрана.

Короткий переход этого не показывает: за двести миллисекунд ступеньки
сливаются. Показывает медленный — там шаг меньше пикселя за кадр, и предмет
ползёт рывками.

**Дефект.** Метка кадра у героя росла шириной: шестнадцать пикселей за шесть
секунд, то есть сорок тысячных пикселя за кадр. Заказчик увидел глазом:
«полоса слайдера движется не плавно, а мелкими рывками». Замер снял
подозрение с часов — сама величина шла гладко, 120 разных значений на 120
кадров. Рвал не счёт, а отрисовка. Полоска легла во всю коробку и стала
выезжать из-под её края сдвигом; края у коробки и у полоски скруглены одним
радиусом, поэтому в начале видна ровная точка, а не обрубок.

## И75 · Отпечаток на `<html>` — не источник правды

Настройка, сохранённая в браузере, живёт в СКЛАДЕ, а на странице она видна
атрибутом. Атрибут — отпечаток, и читать его как источник нельзя: `<html>`
принадлежит фреймворку, и когда меняется то, что фреймворк о нём знает, он
перерисовывает элемент и сносит всё, чего нет в его свойствах.

Отсюда две половины одного правила: читать — из склада; ставить отпечаток
заново — после каждого перехода, а не только до первой отрисовки.

**Дефект.** Заказчик: «переключение языка кнопками языка ломает шапку». Язык
на этом сайте — часть адреса, и при его смене у `<html>` меняется `lang`;
React перерисовывает элемент, и пять атрибутов оформления исчезают разом.
Скрипт из `<head>`, который их ставит, выполняется один раз за загрузку
документа, а переход между языками — мягкий. Замер: на `/bg` пять атрибутов,
после нажатия «EN» ни одного, шапка с 120 пикселей падала до 96. Склад при
этом отвечал «оформление по умолчанию» — потому что читал тот самый стёртый
отпечаток.

## И76 · Покой — тоже состояние

У органа, который что-то переключает, состояний не одно, а два, и второе —
ПОКОЙ. Нарисовать только «нажато» значит оставить кнопку без вида: отжатая
она перестаёт быть кнопкой и читается подписью, особенно рядом с соседом,
который стоит плашкой всегда.

Состояние говорит КРАСКОЙ, а не шириной набора: вес букв в обоих состояниях
один, иначе слово при переключении меняет ширину и двигает соседей.

Ступени берутся от ОДНОЙ основы и идут в ряд без разрывов, чтобы ответ на
руку нельзя было спутать с состоянием: покой 8% чернил в жёлобе, рука 12,
открыто 14, открыто под рукой 20.

**Дефект.** Заказчик: «у кнопки фильтр есть нажатое состояние, а отжатого
нет». Замер: закрытая створка — фон прозрачный, тени нет, вес 400; открытая —
темнее жёлоба, тень внутрь, вес 600. Рядом «Изчисти» стоит плашкой в обоих
случаях, и закрытый «Филтър» рядом с ней читался подписью, а не кнопкой.

## И77 · Белый лист берёт ЛИСТ, а не поверхность

`--surface` — это РОЛЬ: «то, на чём стоит плашка ЗДЕСЬ». На тёмной палубе
роль обязана стать тёмной, иначе пилюля выйдет белым по белому. `--plate` —
это ВЕЩЬ: белое поле, которое остаётся белым, на чём бы ни лежало.

Предмет, у которого белизна — часть вида (карточка, полоса подписи под
снимком, паспарту), берёт ВЕЩЬ. Предмет, который лишь стоит на текущем полу,
берёт роль. И краски идут парой с тем, что выбрано: у листа свои — `--on-plate`
и `--on-plate-2`, у пола — `--sage-12` и `--sage-11`.

**Дефект.** Вид плитки «Bar» — белая полоса подписи под снимком — на тёмной
палубе превращался в десятипроцентную плёнку поверх снимка и переставал
отличаться от вида «Full». Заказчик нашёл это глазом и назвал точно: «тут Bar
был выбран, и вот он поломано отображается — не так, как в дизайн-системе».
Замер на палубе: фон полосы `rgba(255,255,255,.1)` вместо белого, краска
слов — белая. Тем же самым была сломана и белая плитка «Mount»: её карточка
на палубе тоже становилась плёнкой. Один токен, два вида, одна причина.

## И78 · Черта и воздух под ней — пара

Линия, отделяющая шапку от содержимого, работает только вместе с воздухом
под собой. Содержимое, прижатое к черте вплотную, не выглядит отделённым:
черта читается подчёркиванием последней строки шапки, а не границей.

Воздух объявляется ТАМ ЖЕ, где черта, и одной ручкой — чтобы место, которому
он не нужен, объявляло исключение у себя, а не чтобы каждое место объявляло
правило заново.

**Дефект.** Заказчик спросил ровно то, чего не было: «вот эта полоска под
заголовком — под ней должен быть воздух? эта форма же из дизайн-системы? там
под полосой прописан воздух? покажи». Замер на собранном сайте: в обоих меню
шапки между чертой и первой строкой содержимого 0 пикселей. Форма была из
набора, а воздух под чертой не был прописан нигде — ни у шапки, ни у меню.
Исключение нашлось одно: в шторке корзины под чертой стоит полоса доставки —
цветной лист, который к черте и примыкает.

## И79 · Один выход из блока — одно слово

Кнопка, выводящая из блока ко всему, что в нём, — один контрол, и надпись у
неё одна. Пять формулировок одного обещания читаются как пять разных
действий, а на листе набора не находится ни одна из них.

**Дефект.** Заказчик искал кнопку на листе набора и не нашёл: «„see
everything", „see all oils" — это из дизайн-системы берутся кнопки? там есть
вроде „see all"». Он прав: контрол был один, а слов на нём пять — «See all» в
шести местах, «See everything» в меню пользы, «See all oils» в меню масел,
«The whole shelf» на странице марки и «All products» на самом листе набора,
то есть образец не совпадал ни с чем.

## И80 · Под одной рукой поднимается один предмет

Подъём под указателем берёт САМЫЙ ВНЕШНИЙ из тех, на кого легла рука.
Контрол, стоящий на карточке, которая плавает сама, своего подъёма не
добавляет: он едет на карточке. Двое, поднявшиеся вместе, дают не двойной
ответ, а ползание одного по другому — и глаз читает это рывком.

Выключается ручкой у того, кто едет, а не отменой подъёма у всех: кнопка,
стоящая сама по себе, поднимается по-прежнему — это её «сюда можно».

В файлах признака нет: оба подъёма написаны верно и каждый в своём месте —
один у карточки, другой общий у кнопки. Складываются они только на странице.
**Мерится** семьёй `twiceLift` в отрисованной проверке: указатель наводится
по-настоящему, и сравниваются сдвиги органа и всех его предков.

**Дефект.** Заказчик увидел это на сердце «в избранное»: «она поднимается при
наведении — такого поведения конечно не нужно, нахуя она прыгает». У сердца
при этом в его же файле написано, что отвечает оно краской, а не движением:
подъём приезжал вместе с общей кнопкой и про это не знал. Замер на полке:
карточка уходила вверх на 2 пикселя, сердце на 4 — то есть сердце ползло по
фотографии под собой. Той же болезнью была больна и кнопка «в корзину» в той
же карточке. Вторым дефектом оказалось то, что самого сердца не было на листе
набора вовсе — стояла строчка «сердце это та же кнопка без слова», и сверить
его поведение было не с чем.

## И81 · Рама стоит, движется снимок

Плавающее со снимком — карточка товара, белая плитка, публикация — отвечает
руке НАПЛЫВОМ снимка и шагом ТЕНИ. Сама рама не двигается: ни вверх, ни
вглубь, ни сжатием.

На столе ответ приходит по наведению, на телефоне — по нажатию, потому что
наведения там нет вовсе. Нажатие вдобавок берёт следующую ступень тени: на
столе именно она отличает «сюда можно» от «принял», и отличает НЕ движением.

Манера одна. Их было три — подъём, взлёт, наплыв — и выбирала их панель;
выбор сделан, две другие сняты вместе с атрибутом, который их переключал.

**Дефект.** Заказчик назвал причину точнее любого правила: «в карточках
товара при наведении и нажатии делаем наплыв и тень — только чтоб два наплыва
не было». Прежняя манера двигала обоих сразу: карточка уходила вверх на 2
пикселя, снимок в ней наплывал, а на нажатии добавлялось третье движение —
сжатие карточки до 0.985. Три движения на одну руку читаются не как ответ, а
как дрожь. Той же болезнью болела плитка: `scale(.995)` на нажатии поверх
наплыва её же снимка. Второй половиной правила стало то, что на телефоне
ответа не было почти никакого: наведения там нет, а нажатие только сжимало
раму — «мне на мобилке при нажатии нужен наплыв и тень».

## И82 · Краска на насыщенной заливке — белая

Пара «заливка и краска на ней» считается отдельно от пары «пол и краска на
нём». Тёплые чернила страницы подобраны к тёплому полу и на нём верны; на
фирменном цвете тёплая примесь съедает как раз недостающее.

**Мерится** `check:theme` (пара «знак на фирменном») и отрисованной проверкой
на живых органах.

**Дефект.** Сменили основной цвет тёмной темы — и выбранная пилюля
концентрации на странице товара дала 4.46 при норме 4.5. Причина не в новом
цвете: на заливке стояли чернила страницы, `#EFECE7`. На бирюзе `#2E7C8F` они
дают 4.05, чистое белое — 4.78. Смена акцента тянет за собой всю пару, и
проверять надо обе её половины.

## И83 · Белое на снимке держит вуаль, а не тень

У всякого текста поверх фотографии есть своя вуаль. Тень под буквами фона не
делает: она обводит букву и на светлом кадре не спасает ничего, а снимок
приходит из админки и бывает любым.

Плотность вуали выводится замером по самому светлому кадру, а не подбирается
глазом. Ставится она тем же градиентом и той же краской, которые у блока уже
есть, — иначе у одного кадра заводится два вида вуали.

**Дефект.** Число лайков на публикации Instagram — белое, на снимке, с одной
тенью: 1.45:1 при норме 4.5 на самом светлом кадре стены. Стояло так всё
время, и молчала не рука, а проверка — она мерила чужое место страницей ниже
из-за плавной прокрутки. Первая починка с плотностью 0.55 дала 3.84 — лучше и
всё ещё ниже нормы; сошлось на 0.9 у края с гашением к четверти кадра.

## И84 · То, на что ссылаются снаружи, — страница, а не якорь

Ответ, которого ждут по имени — контакты, доставка, возврат, о нас, —
получает **свой адрес**: со своим заголовком, описанием, местом в карте
сайта и разметкой, которая говорит машине, чем эта страница является. Якорь
остаётся тем, чем он и является: местом на странице, которая и без него
есть.

Разница не в удобстве, а в том, чего у якоря нет по построению:

* сослаться на него снаружи нечем — карточка компании в поиске, подпись в
  письме о заказе, профиль в соцсети просят адрес, а не якорь;
* отдельным результатом в выдаче он не показывается, и `title` с описанием
  у него чужие;
* разметки о себе он не даёт: `ContactPage` описывает страницу.

**Половина, без которой страница врёт.** Канал связи, у которого адреса ещё
нет, на такой странице остаётся **надписью, а не ссылкой**. `tel:[PHONE]` —
не «почти телефон»: палец по нему набирает мусор, чтец объявляет его
ссылкой, таб на нём останавливается. Условие спрашивается у данных, и в
день, когда адрес впишут, строка станет ссылкой сама — везде, где это
условие спрошено. Спросить его обязано каждое место, которое канал
печатает: место, которое не спросило, остаётся со ссылкой в никуда.

**Дефект, которым правило куплено.** Заказчик попросил страницу контактов и
прислал чужую как образец. Страницы не было — были три пункта меню («шапка,
чекмедже, подвал»), и все три вели на `#contact`: колонку в подвале той же
страницы, на которой стоял покупатель.

---

## И85 · Поле предмета течёт от предмета, а не от окна

Общее поле листа задаётся `clamp()` по ширине окна: чем шире монитор, тем
щедрее край страницы. Листу во всю ширину это верно. Предмету, который
держит **свою** ширину — узкой колонке пары, форме, сводке заказа, — нет: он
про окно ничего не знает, а поле у него растёт вместе с окном.

Мерится долей: боковые поля больше четверти собственной ширины предмета —
дефект. Замер, которым правило куплено: колонка формы 381 пиксель, поле 48
с каждой стороны, 96 из 381. Полоса под содержимое оказалась уже, чем поля
вокруг неё, — и только на широком мониторе: на телефоне то же `clamp()`
даёт 22, и дефекта нет.

**Что вместо.** Предмет фиксированной ширины берёт поле ступенью шкалы, а не
величиной, текущей по окну. В файле признака нет никакого — и там и там
назван токен; видно только замером отрисованной страницы.


## И86 · Незаполненный факт видно, прячут не его, а нажатие

Факта ещё нет — на витрине стоит заглушка, и она остаётся стоять. Заглушка
это дефект **наполнения**: о нём сообщают, его не выносят со страницы и не
«прячут до появления настоящего». Закон записан в `CLAUDE.md` («Наполнение
— не моя работа») и куплен снятым с главной блоком отзывов.

Прячется другое — **орган**. Ссылка на `tel:[PHONE]` набирает мусор,
`https://t.me/` без имени открывает мессенджер вместо магазина, кнопка
«заказать в вайбере» уводит человека, уверенного, что заказ уехал. Поэтому
значение печатается всегда, а ссылкой строка становится ровно тогда, когда
адрес настоящий (`hrefOf` в `lib/contacts.ts` возвращает `undefined`).
Правило И52 про ссылку остаётся целиком: оно про нажатие, а не про текст.

**Дефект, которым куплено.** На странице «Контакти» реквизиты фирмы печатали
`[COMPANY] Ltd`, `[XXXXXXXXX]`, `[ADDRESS]`, а карточки каналов рядом
показывали одни имена: «Телефон», «Имейл», «Telegram». Два закона об одном
и том же «данных пока нет» в одном файле. Заказчик увидел это глазом —
«а данные компании мы как показываем, не имея реальных данных?».

Отдача больше ровности: механизм «адрес текстом и по нажатию» был показан
заказчику на подставленных для снимка номерах, а на настоящих данных
выглядел откатом — работа, которой не видно, читается как работа, которой
нет. И семья `placeholder` в отрисованной проверке — это список того, что
заказчику осталось заполнить; спрятанная заглушка прячет и строку списка.

## И87 · Вложенная стопка перебивает ритм внешней

Ручка примитива читается НА ТОМ, кому достаётся объявление. У стопки отбивку
получает ребёнок (`.stack > * + *{margin-block-start:var(--stack)}`), значит
и `--stack` берётся с ребёнка. Ребёнок, который сам стопка, объявляет эту
ручку для СВОИХ строк — и тем же объявлением перебивает ритм, заданный
родителем.

**Дефект.** На странице контактов два листа стоят в столбик с просветом
`--air-band` (48), а внутри каждого строки идут ритмом `--air-row` (16).
Между листами встало 16: значение внутреннего ритма победило внешний. В
файле обе строки безупречны — родитель просит 48, лист просит 16, и оба
правы про себя.

**Что вместо.** Внешний просвет называется на детях явно
(`.main > * + *{margin-block-start:var(--air-band)}`), а ручка остаётся тому
ритму, который живёт внутри листа. Настоящее лекарство — у самого примитива:
просвет, посчитанный на РОДИТЕЛЕ (`gap`), вложенностью не перебивается. Пока
это открытый вопрос (`docs/open.md`): смена `flow-root` на колонку с
просветом меняет схлопывание полей по всему сайту и требует полного
отрисованного прогона.
## И88 · Факт стоит в документе по имени, а не копией

Договор, политика, страница «О нас» и подвал называют одни и те же факты:
фирму, ЕИК, адрес, телефон, имейл, порог доставки, кто отвечает. В тексте
факт стоит именем — `{uic}`, `{email}` — и подставляется одной функцией из
данных. Имя, которого подстановка не знает, ловит тест документов; факт,
которого ещё нет, приходит заглушкой, и её считает отрисованная проверка.
Канал связи без адреса приходит той же заглушкой, что и в подвале, и на
странице контактов (И86): факт видно, прячут только нажатие.

**Дефект.** `{email}` стоял в договоре и политике восемнадцать раз и уходил
на витрину буквально — подстановка знала один-единственный `{free}`. ЕИК был
записан заглушкой `[COMPANY]`, той же, что имя фирмы, а в подвале —
`[XXXXXXXXX]`: два написания одного незаполненного факта. Телефон стоял
выдуманным «+359 00 000 0000» — хуже заглушки, потому что похож на настоящий.
Нашлось при переписывании «О нас»: страница, которая обязана называть, кто
за сайт отвечает, называла его тремя разными способами.

Что на странице «О нас» обязано быть и почему — `.claude/skills/craft/
.claude/skills/shop/references/pages.md`.

## И89 · Лист набора называет файл, а не только кадр

Число на листе набора читается как размер снимка, если рядом не сказано,
что это. «420 → 500» у кадра героя — высота кадра на экране в точках; файл
для него — 1600 × 1200 пикселей, и одно из другого глазом не выводится:
между ними плотность экрана (×2, на телефоне ×3), ширина колонки, которой
на листе кадра не написано, и обрезка под форму кадра, которая меняется с
шириной окна.

**Дефект.** Заказчик собрался генерировать снимки героя, прочёл на листе
набора «420 → 500» и спросил, это ли размер файла и учтена ли ретина.
Размера файла не было нигде: он жил в голове модели и пересчитывался
каждый раз заново — а следующей сессии не доставался.

**Что вместо.** У каждой семьи снимков на листе набора свой лист
(`app/[lang]/design/DesignShots.tsx`): размер файла в пикселях — самое
широкое место кадра, умноженное на плотность, — форма, папка и имена, где
стоит и зона, видимая при любой форме кадра. Число на экране подписано
словами «высота на экране». Считается от кадра, а не от снимка: снимок
заменят, кадр останется.

## И90 · Снимая настройку, в код уходит ОТВЕТ, а не умолчание репозитория

Решённая настройка удаляется из панели вместе с кодом — но в код записывается
то, что ВЫБРАЛ заказчик, а не то, что стояло в поле `fallback`. Умолчание
репозитория — это состояние «никто ещё не выбрал»; ответ живёт у заказчика в
браузере и в панели не виден никому, кроме него.

Поэтому перед удалением поля его ответ спрашивают или смотрят на витрине
глазом. Признака в файлах нет вовсе: и старое умолчание, и новая строка
выглядят одинаково правильными.

**Дефект.** Снял настройки подвала и зашил `footMarks: 'mono'` — умолчание
репозитория, белые силуэты на кружках. У заказчика был выбран `plain`, голый
силуэт без кружка, и витрина у него именно так и выглядела. Его слова:
«не было тут фона у иконок, теперь какие-то кружки появились… снова что-то
чиним, а ты что-то ломаешь». Правка выглядела чистой уборкой и молча отменила
его выбор.

## И91 · Цветом отмечается событие, а не место

Цветная плашка появляется тогда, когда есть что сказать, и гаснет, когда
сказать нечего. Залитая всегда, она обещает одно и то же обоими состояниями
и перестаёт значить что-либо.

И вторая половина: хорошую новость говорят СВОЕЙ краской, а не фирменной.
Фирменный цвет на витрине значит «сюда нажать»; залитая им полоса читается
вторым главным действием экрана. У «нельзя» краска есть давно — у «можно»
должна быть своя, и берётся она той же дорогой: сдвигом по кругу от
фирменного, а не подбирается отдельно.

**Дефект.** Полоса бесплатной доставки в корзине была залита всегда и
голубым. Заказчик увидел это дважды: сперва «этот синий цвет разве у нас есть
в дизайн-системе? нет же, это какой-то стандартный ИИ-цвет» — и краска
переехала с звонкой ступени кольца фокуса на фирменную; потом «ну хуёвый этот
синий, он аишный, нужен другой цвет, разрешающий… если не набрана сумма, цвет
нужно убрать и написать, сколько ещё нужно добрать». Теперь плашки нет, пока
идёт набор, а дотянув до порога полоса берёт роль «разрешено» — зелёный лист,
пару к кирпичной краске ошибки.

## И92 · Невыбранный вид не едет покупателю

Вид предмета, пока его ВЫБИРАЮТ, приходит атрибутом на корень: одна разметка,
несколько одежд, переключаются стилем. Как только выбрали — атрибут перестают
ставить, а правила и разметка остаются. Дальше это не «запас», а груз:
невидимые копии едут покупателю, их грузит браузер, их читает поиск, и не
видит их никто.

Убирается всё: правила отвергнутых видов, разметка под них и сам атрибут.
Память об отвергнутом живёт на листе набора образцом, а не веткой кода.

**Правило начинается с выбора, а не с умолчания.** Пока вид ВЫБИРАЮТ, все
виды обязаны остаться — и в коде, и в панели, и на листе: переключатель и
есть то, чем выбирают. Умолчание в файлах («сегодня стоит `mount`») выбора не
означает. Спрашивать при этом не о том, «удалять ли лишнее», а о том,
«выбрано ли»: заказчик уже отвечал на первый вопрос «я написал НЕ подключать,
а не удалять» — и второй раз, про виды плитки: «НЕТ, не убирать, я ещё буду
выбирать, какую применить на проде».

**Мерится** семьёй `deadDress` в проверке вёрстки: селектор на
`html[data-X…]`, где `X` не ставит ни один файл кода. Комментарии при поиске
не считаются — первый заход их считал, и проверка молчала о счётчике из-за
строки, объяснявшей, что атрибут снят.

**Дефект.** Карточка товара несла в разметке ВСЕ ЧЕТЫРЕ вида фактов и прятала
три стилем: сорок карточек полки — сто шестьдесят наборов фактов, из которых
видно сорок. Счётчик болел тем же: в каждой кнопке лежали два знака, штрих и
уголок, и один прятался. Заказчик увидел это на схеме и сказал: «пиздец, я и
не знал, что одновременно подгружались все четыре вида карточек, это было
конечно плохо».

## И93 · Образец на листе набора стоит в настоящем размере

Лист набора существует затем, чтобы по нему ПРИНИМАЛИ РЕШЕНИЯ. Предмет,
нарисованный на листе не в том размере, в котором он стоит на витрине, —
такая же неправда, как чужой рисунок: решают по одному, покупатель видит
другое.

Отсюда два следствия, и оба исполняемые.

Первое: число, от которого зависит размер предмета, живёт в НАБОРЕ, а не в
модуле блока. Пропорция плитки стояла в модуле блока поводов — лист до неё не
доставал и давал плитке свой рост. Пункт меню описывался правилом по потомку
(`.nav a`) — `composes` до такого правила не достаёт, и лист рисовал пункт
заново, своими числами. Правило общее: то, что показывает и витрина, и лист,
берётся классом или токеном, а не переписывается.

Второе: «один в ряд» — это заявка, а не недосмотр, и у неё есть своя строка.
Поле на три колонки, которому не сказали иного, делит рамку натрое, даже если
в рамке один предмет.

**Мерится** семьёй `sheetSize` в отрисованной проверке: пары «образец на листе
— тот же предмет в магазине» описаны в `tools/sheet-samples.mjs` вместе с
окном замера и тем, что обязано совпасть — ширина, рост, кегль, а там, где
колонка на листе и в магазине разная по делу, складка: рост против ширины. В файлах признака нет никакого:
и лист, и витрина безупречны по отдельности, расходятся они только на
странице.

**Дефект.** Заказчик открыл лист рядом с магазином и нашёл три расхождения
подряд: публикация и ролик — 230 и 238 на витрине против 107 и 111 на листе;
плитка — 421×301 против 421×358; пункт меню — 107×41 против 101×38. Его
слова: «ты нарисовал не в реальном размере, хотя другие элементы нарисованы в
реальном размере на странице; проверь, посмотри, может, где-то ещё есть такая
ошибка, сделай, чтоб ошибок больше не было».

## И94 · Адрес страницы знает язык, ссылка — нет

Ссылки внутри сайта пишутся без языка (`/catalog/oils`), язык добавляет один
компонент на весь сайт. А адрес текущей страницы язык содержит
(`/bg/catalog/oils`). Сравнивать их «в лоб» нельзя: совпадения не будет
никогда, и признак «вы здесь» просто не сработает — молча, без ошибки в
сборке и без красного в проверке.

Сравнение идёт по адресу БЕЗ языка (`stripLocale`), и это касается всякого
места, где страница узнаёт себя: пункт шапки, пункт чекмедже, нижняя полоса.

**Дефект.** Метка раскрытого и текущего раздела — сплошная пилюля, выбранная
заказчиком и подтверждённая дважды, — не показывалась в магазине НИ НА ОДНОЙ
странице: три места сравнивали адрес с языком и ссылку без него. Нашлось при
сверке листа набора с витриной: на листе метка была, в шапке её не было.
## И95 · Факт о доставке живёт одним списком, и его читают все

Способы доставки с ценой, срок, перевозчики и способы оплаты — один
список в данных (`lib/delivery.ts`). Страница «Плащане и доставка»
получает таблицу именем (`"table": "delivery"`), а не строками; договор
и политика называют перевозчиков и срок именем факта (`{couriers}`,
`{days}`); касса рисует кнопки из того же списка и считает сводку той же
функцией, что корзина (`totalsOf`); разметка магазина для машин отдаёт
условия доставки и возврата из него же. Надпись способа — ключ словаря,
как имя товара, и тест доставки требует перевода у каждой.

**Дефект.** Курьер стоял заглушкой `[COURIER]` в трёх документах и двух
кнопках кассы; цена доставки — `4.9` в данных и `0` в сводке кассы; срок —
«1–2 дни» на странице доставки, «1–3» в договоре, «утре до 15:00» в кассе;
карта стояла в кассе первой и отмеченной, при том что принимать её
магазину нечем. Один факт в пяти написаниях — и заполнять его в день,
когда курьер выбран, пришлось бы пять раз. Нашлось при написании страницы
доставки по слову заказчика.

Что на странице доставки обязано быть и почему —
`.claude/skills/shop/references/pages.md`, раздел «Плащане и доставка».

## И96 · Лист — это тоже пол, и он объявляет себя

Полоса объявляет пол атрибутом `data-ground`, и пол ПЕРЕНАЗНАЧАЕТ РОЛИ:
чернила, поверхность, плитку, волосок, пару состояния. Роли наследуются
вниз — сквозь всё, что внутри, включая белую карточку. А краска самой
карточки (`--plate`) за полом не идёт: это ВЕЩЬ, белый лист остаётся белым,
чем бы ни был пол под ним.

Отсюда правило: **предмет, который красится `--plate`, ставит `data-plate`**
— и роли внутри него возвращаются к значениям темы (`styles/base.css`).
Список возврата равен списку палубы строка в строку: палуба переназначила
роль — лист обязан её вернуть. Обратное тоже правило: слова, лежащие прямо
на полу полосы, красятся РОЛЬЮ (`--sage-*`), а не краской листа
(`--on-plate`), иначе на тёмной палубе они выйдут тёмным по тёмному.

**Дефект.** Заказчик поставил блок вопросов на палубу и выбрал одежду
«Карточки»: белые карточки на тёмном полу, вопрос виден, ответа не видно
вовсе. Замер: контраст 1.00 при норме 4.5 — то есть букв на экране не было.
Той же породы оказались ещё два места, которых он не видел: марка на
карточке товара на палубе (1.82) и слова отзыва, набранные краской листа на
полу полосы (1.33). Это четвёртый случай породы «роль переопределена
наполовину» — первые три чинились по месту, и потому был четвёртый.

Сторожат две проверки. `plateGap` в `check:css` сверяет списки палубы и
листа и валится на первой невозвращённой роли, а заодно ловит лист,
который красится `--plate` и не объявил себя. `check:theme` ставит
измеритель ВНУТРЬ листа, лежащего на палубе, и меряет там те же пары
краски: список может сойтись и при неверном значении.

Разбор — `.claude/skills/craft/references/color.md`, раздел «Лист — это
тоже пол».

## И97 · Рисующая одежда ползунка живёт под `@supports`

Ползунок одевается двумя разными записями, и они не складываются. Общая
(`scrollbar-width`, `scrollbar-color`) умеет только «тоньше» и «такого
цвета». Вдвинуть ползунок от скруглённого края, скруглить его и убрать
стрелки умеют только `::-webkit-scrollbar` — и движок Chrome выбрасывает их
ЦЕЛИКОМ, если на том же предмете стоит общая запись, хоть унаследованная.

Поэтому рисующие правила пишутся внутри
`@supports selector(::-webkit-scrollbar)`, где общая запись первым делом
сбрасывается к умолчанию. Снаружи остаётся общая — для Firefox. Прячущие
правила (`display:none`) охраны не требуют.

**Дефект.** Восемь строк одежды ползунка панели фильтров были написаны,
прочитаны браузером и отброшены: строкой выше стояло `scrollbar-width:thin`.
Браузер рисовал свой — на краю скруглённой дуги и со стрелкой наверху, то
есть ровно то, что заказчик показывал в первый раз. Третий заход одного
дефекта, и все три он нашёл глазом. Семья `barTwice` в `check:css`.

Разбор — `.claude/skills/craft/references/controls.md`.

## И98 · Настройка ведёт себя одинаково при всех своих значениях

Пол блока (`Page`, `Paper`, `Deck`) меняет краску, и только её. Правило,
написанное на пару соседей с одинаковым полом и гасящее между ними шов —
угол или просвет, — делает из одной настройки две: при одном значении блоки
сливаются, при другом остаются двумя.

**Дефект.** Заказчик поставил двум соседним полкам «Paper»: они слились в
один бесконечный блок. Те же две на «Deck» разделились. Показал двумя
снимками. Правило жило в файле главной, а настройка общая для всего сайта.

**И вторая половина того же дефекта**, найденная его же вопросом — «почему у
Paper сделал, а у Deck нет». У палубы было СВОЁ поле снизу (16), и снималось
оно только перед соседом с полом. Перед обычной полосой оно оставалось:
палуба уходила от соседа на 80, бумага на 64. Убрано; теперь все четыре пары
(бумага-бумага, палуба-палуба и обе крест-накрест) дают 64, и столько же до
подвала, если лист последний. Своего воздуха снизу у листа нет вовсе —
расстояние до соседа объявляет сосед.

**Чего на самом деле не хватало**, сказано им же следом: «иногда объединение
нужно — если товары из одной категории, а иногда нет — если товар и вопросы».
То есть склейка нужна, но решает её не цвет пола, а смысл соседства. Она
стала своим выключателем в панели («Join with the block above») у НИЖНЕГО из
пары: он смотрит вверх. Правило склейки не знает ни одной краски — работает
на любом поле и на любой паре, включая блоки без пола, где гасится воздух над
нижним.

Семья `groundGlue` в `check:css`. Разбор —
`.claude/skills/craft/references/layout.md`.

## И99 · Лист не берёт фирменный цвет пола, на котором стоит

И77 закрыл фон и чернила листа: карточка товара белая на любом полу,
`--plate`/`--on-plate` пол не слушают. Но фирменная роль — `--pop` /
`--on-pop` / `--pop-hover` и живая лестница `--cyan-11` / `--cyan-12` —
осталась слушать пол (И52 её такой и сделала, для контролов, что стоят
ПРЯМО на полу). Карточка эти токены не переобъявляла, и они текли в неё
сквозь ограду листа: кнопка «Добавлено» и марка бренда читали пол блока,
которого их лист не видит.

**Дефект.** Заказчик поставил полку масел на Ground: Deck и увидел, что
кнопка «Added» стала бледно-бежевой на белой карточке — почти не видна.
На Ground: Page и Paper та же кнопка тёмно-синяя, чёткая. То самое, от чего
заводили `--pop`: контрол тонет в своём фоне — только фон тут не палуба
напрямую, а белый лист карточки, который на палубе стоит.

**Как правильно.** Не по месту. Первая починка сделала это в самой карточке
— переписала формулы корня третьим экземпляром, — и работала бы ровно до
второго листа с контролом внутри. Причина у дыры общая, и она же названа
правилом И96: лист не объявлял себя полом. Теперь объявляет (`data-plate`), а
роли внутри него возвращает ОДИН блок в `styles/base.css` — именами вещи, а
не копиями чисел. Карточка при этом не знает о поле ничего и ничего у себя не
переобъявляет.

Два правила — не одно, и оба нужны: И96 говорит, ЧЕМ лист себя объявляет,
И99 — что фирменная роль и живая лестница входят в возврат наравне с
чернилами. Список возврата сторожит семья `plateGap`.

**Почему проверка молчала.** Пол блока — настройка панели, а отрисованная
проверка ходит по дереву с умолчаниями: полка на Ground: Deck не стоит ни на
одной странице. Теперь молчать ей нечем: `check:theme` ставит измеритель
ВНУТРЬ листа, лежащего на палубе, и меряет там пары краски, включая знак на
фирменной заливке; `plateGap` в `check:css` сверяет список возврата со
списком палубы строка в строку.

**Где искать то же самое.** Любой лист (`--plate`), внутри которого
стоит контрол на `--pop`/`--cyan-11`/`--cyan-12`, — кандидат. Проверены и
чисты: белая плитка (`tile.module.css`, `mount`/`bar`) — своего контрола
на `--pop` в разметке пока нет; «сердце» карточки — оно на снимке, не на
листе, и фон у него сам от пола (`--surface`), пара цельная.

## И100 · Тень цвета своего пола тени не отбрасывает

Пятая роль породы И96/И99: тень (`--sh-1`/`--sh-2`/`--sh-3`, собранные из
`--sh-ring`/`--sh-near`/`--sh-far-*`) осталась «составленной из чернил
корня» и за полом не шла, как и `--rule`/`--quiet`/`--hover-row` до правки
рядом с ней. Но её отсутствие не «тёмное по тёмному» — оно хуже, потому
что не читается на глаз по описанию, только на живой странице.

**Дефект.** Заказчик поставил герой на Ground: Deck и увидел: в тёмной теме
у пилюль ряда («CBD oil», «Cannabis cream and gel»…) есть тень, в светлой —
нет вовсе, хотя `box-shadow` один и тот же (`var(--sh-1)`, `home.module.css`
`.chips a`). Причина числом: `--sh-ring`/`--sh-near`/`--sh-far-1` в светлой
теме подмешаны из `--accent-solid` (5–20%), а `--accent-solid` светлой темы
— `#0C3A46`, то есть буквально то же число, что и `--chrome-bg`, из
которого сделана палуба (`--page-deck: var(--chrome-bg)`). Тень красится
цветом пола, на котором стоит, — и растворяется в нём тождественно, а не
приблизительно. В тёмной теме её спасает то, что тень там и так собрана из
чёрного, а не из акцента, — второй половине повезло случайно.

**Как правильно.** Не подбирать новый цвет, а вернуть готовый — ровно то,
чем правилась И99: палуба (`[data-ground='deck']` в `styles/base.css`)
переобъявляет `--sh-ring`/`--sh-near`/`--sh-far-1..3` тем, что несёт тёмная
половина `light-dark()` (белое кольцо, чёрные слои) — это тень, которая уже
видна на дне, потому что не зависит от того, какой краской покрашен пол.
Лист (`[data-plate]`) обязан вернуть те же пять переменных к своей
собственной формуле — иначе белая карточка на палубе унаследует тень
чужого пола вместо тени листа, и это была бы ровно та же половинчатая роль,
от которой чинили И99. Обе полосы сторожит `plateGap` в `check:css` — она и
нашла пропуск, когда переменные появились в списке палубы без пары в листе.

**Где искать то же самое.** Любой `box-shadow` на `--sh-1`/`--sh-2`/`--sh-3`
внутри блока, у которого есть `Ground: Deck`, — кандидат. На сегодня это
пилюли героя (`.chips a`); других мест, где тень стоит прямо на палубе,
а не на листе (`data-plate`), в коде не найдено.

## И101 · Прокрутку панель слушает наравне с Escape и нажатием мимо

Заказчик увидел, что меню мессенджеров в шапке и карточка помощника
остаются открытыми, пока страница едет у них под низом: «открытые панели …
при скроллинге сайта должны закрываться».

**Дефект.** Из панелей сайта, не занимающих экран целиком, прокрутку слушало
только поле поиска в шапке (`Header.tsx`) — и то потому, что задачей там
было не дать полю уехать вместе со страницей, а не общее правило про панели.
Меню мессенджеров (`HeaderChat.tsx`), карточка помощника (`Helper.tsx`) и
три мега-меню (`useMegaMenu.ts`) реагировали на Escape и нажатие мимо, но не
на прокрутку — открытая панель ехала вместе со страницей до отдельного клика.

**Как правильно.** Прокрутка страницы — третий способ уйти от панели,
наравне с Escape и нажатием мимо, у всего, что не modal-`<dialog>` (шторка
корзины, окно аккаунта, «напишите нам» сюда не входят — они сами есть экран).
Приём вынесен в `useCloseOnScroll(active, close)`
(`components/useCloseOnScroll.ts`), устроен по образцу того самого поля
поиска: задержка 400мс, прежде чем отсчёт вообще начинается (открытие само
иногда двигает страницу — клавиатура, переход по якорю), и порог 24px
(дрожание пальца прокруткой не считается). Подключён к `HeaderChat.tsx`,
`Helper.tsx`, `useMegaMenu.ts` и переписанному полю поиска в `Header.tsx` —
один хук на четыре панели, а не четвёртая копия слушателя.

**Найдено заодно — и, по слову заказчика, объединено следом.** У всех
четырёх панелей Escape и нажатие мимо были написаны по отдельности, разными
приёмами (`pointerdown` + `composedPath()` в одних, `click` + `.contains()`
в других) — та же болезнь, из которой уже родился `lib/panel.ts`
(`makePanel()`): два байт-в-байт одинаковых тела хука. Заказчик, увидев эту
находку, сказал прямо: «чини, обьединяй, перепроверь». Общее закрытие уехало
в `useOutsideClose(active, close, isInside)` (`components/useOutsideClose.ts`):
подписка на `keydown`/`pointerdown`, Escape и передача пути события — одни на
все панели, «внутри» — единственный параметр, которым панели отличаются.
Две панели проверяют «внутри» рефом (`HeaderChat.tsx` — два рефа, кнопка и
портал; `Helper.tsx` — один, кнопка и карточка в одном узле), две — классом
или атрибутом CSS-модуля (`useMegaMenu.ts`, поле поиска в `Header.tsx`), и
для этой пары `check:code` тут же нашёл ту же дублирующую находку уровнем
ниже — два одинаковых теста `n instanceof HTMLElement && …` с разным именем
класса. Вынесены фабрики `hasClass(cls)` и `hasAttrOrClass(attr, cls)` в тот
же файл, панели зовут их с разными аргументами, а не пишут тело заново.

По ходу разбора нашлась и мелкая мёртвая находка рядом: `Header.tsx` звал
`closeMega()` в своём общем обработчике Escape, хотя `useMegaMenu.ts` уже
закрывает себя по Escape сам (комментарий в файле так и говорил — «своё
закрытие, а не общее на пятерых») — вызов остался с тех пор, как мега-меню
получило свой обработчик, и решал то же самое во второй раз без всякого
эффекта. Убран; `openSearch(false)` в том же обработчике убран тоже — теперь
это решает `useOutsideClose` у поля поиска, а не два места одной строкой.

**Где искать то же самое.** Любая новая панель с закрытием по нажатию мимо
берёт `useOutsideClose`, а не пишет пару `keydown`/`pointerdown` заново;
предикат «внутри» — через `hasClass`/`hasAttrOrClass`, если он сводится к
проверке класса или атрибута, и рефом только когда своих узлов несколько
или разметка ещё не несёт устойчивого класса.

## И102 · Общая кнопка сменила `display` — своя кнопка потеряла сетку, о которой не просила

`.btn` (`styles/btn.module.css`) перешла с `display:inline-grid` на
`inline-flex`, чтобы знак и слово не расходились в широкой кнопке. Кнопка
«в корзину» (`BuyBtn.module.css`) держит «Добави»/«Добавено N» в ОДНОЙ
клетке через `grid-template-areas`, ничего не объявляя о `display`
сама, — весь фокус держался на том, что `.btn`, класс, который она себе
подмешивает, была сеткой.

**Дефект.** Разошедшийся текст в кнопке «в корзину» на живом сайте: слово
и число вместо стопки в одной клетке стали двумя соседними флекс-статьями
и разъехались в стороны, а сама кнопка стала шире, чем нужно, — карточки
товара шириной 260–420px получили кнопку, вылезающую за карточку. Ни
`BuyBtn.module.css`, ни его правка в тот день не менялись вовсе: причина
целиком в чужом файле.

**Как правильно.** Компонент, чей приём держится на CSS-СВОЙСТВЕ
родительского класса (здесь — на `display` у `.btn`), а не только на его
токенах-ручках, обязан объявить это свойство СЕБЕ САМ на более специфичном
селекторе, а не полагаться, что общая кнопка останется прежней вечно.
`.buy` теперь сама пишет `display:grid;grid-auto-flow:row` — тот же
результат, что и раньше, но не зависящий от решения соседнего файла.

**Где искать то же самое.** Любой класс, использующий `grid-template-areas`,
`grid-auto-flow`, `place-items` или подобные grid/flex-специфичные свойства
на элементе, чей БАЗОВЫЙ `display` объявляет ДРУГОЙ, более общий класс
(`composes` или сложенные классы в JSX), — кандидат: правка общего класса
однажды снимет чужую опору.

**Правка выше не сработала, и вот почему.** `.buy{display:grid}` — тоже
один класс, ТА ЖЕ специфичность (0-1-0), что у чужого `.btn{display:
inline-flex}`. При равной специфичности решает не намерение, а то, чьё
правило легло в собранный файл ПОЗЖЕ, — а это порядок импортов в JS,
который от разметки не виден вовсе. Здесь он оказался не в пользу `.buy`:
кнопка тихо считалась `flex` (видно только `getComputedStyle`, не диффом и
не глазом на большинстве состояний), скрытое `visibility:hidden` слово
рядом с видимым «Добавено N» тянуло видимую надпись вбок как второго
соседа по флекс-строке, и число вылезало за собственный край кнопки на
9px. Заказчик поймал это дважды — сперва как «на проде не починено», потом
на кнопке без числа вовсе, — по одной и той же причине, которую первая
попытка не устраняла, а лишь маскировала для части состояний.

**Как правильно (второй раз).** Специфичность нужна СВОЯ, а не только своя
запись. `.buy.buy{display:grid;...}` — тот же класс дважды, специфичность
0-2-0, которая бьёт чужой 0-1-0 независимо от порядка файлов в сборке.
Урок: класс, который переопределяет БАЗОВОЕ CSS-свойство (не своё, а
унаследованное от `composes`/сложенных классов) чужого класса ТОЙ ЖЕ
специфичности, обязан либо победить числом (двойной класс), либо
специфичность вообще не совпадает случайно — совпадение стоит считать
дефектом, а не удачей.

## И103 · `margin` одним значением на все стороны гасит и то поле, которое гасить не просили

Полоса бесплатной доставки в сводке заказа (`app/[lang]/cart/cart.module.css`,
`.free[data-free='yes']`) получила заливку и внутреннее поле (`padding`), а
следом — отрицательный `margin` тем же числом на все четыре стороны, чтобы
плашка «не выросла шире строки и не подвинула соседа».

**Дефект.** Заказчик увидел плашку, упёршуюся в заголовок «Order» над ней.
Причина: `margin` сокращённой записью ЗАМЕНЯЕТ, а не складывает, значение,
уже стоящее на этом свойстве, — а сверху воздух между заголовком и первым
соседом ставит примитив `stack` (`margin-block-start`). Одна и та же
логическая ось (верх) оказалась дважды назначена — один раз стеком, один
раз плашкой, — и выигрывает более специфичный селектор плашки, а не
намерение автора «погасить только своё».

**Как правильно.** Гасить нужно только те стороны, у которых собственное
поле плашки действительно раздвигает соседей: `margin-inline` (чтобы плашка
не казалась шире строки) и `margin-block-end` (чтобы не выросло расстояние
до следующей части). `margin-block-start` не трогается вовсе — воздух НАД
плашкой не её забота, это забота стека, который стоит выше по дереву
решений.

**Где искать то же самое.** Любой `margin` сокращённой записью с
ОТРИЦАТЕЛЬНЫМ числом внутри `stack`, `switcher` или другого примитива с
собственным ритмом между соседями, — кандидат: сокращение почти наверняка
гасит больше осей, чем нужно.

## И104 · Одна форма, названная двумя именами, — второе имя рано или поздно расходится

«Choice by need» и «Choice by category» отвечают на один и тот же вопрос
— выбор по одному признаку, три плитки в ряд, — но пропорция плитки жила
двумя именами: `--tile-look` (1.4) у первого блока, `--tile-cat` (0.87,
объяснённая более длинной подписью) у второго.

**Дефект.** Заказчик поставил блоки рядом и увидел разного роста плитки —
и напомнил, что уже просил сделать их одинаковыми. Подпись, которой
объяснялась разница, на деле занимает те же одну-две строки в обоих
блоках: рост держало число, объявленное отдельно, а не текст.

**Как правильно.** `--tile-cat` убран совсем, `Categories.module.css`
читает `--tile-look`/`--tile-look-fold` напрямую. Не два токена с
одинаковым (пока одинаковым) значением, а один: одинаковыми они были по
договорённости, а разошлись на первой же правке одного из двух файлов —
ровно то, о чём предупреждает раздел переносимости «Один токен — одно
значение», только здесь речь о токене внутри одного сайта, а не между
сайтом и пакетом.

**Где искать то же самое.** Два имени токена с одним и тем же числом,
объяснённые разными словами в комментарии, — кандидат: если оба места
отвечают на один вопрос дизайна, а не на два разных, второе имя лишнее.

## И105 · Лист, которому назначили остаться тёмным, — исключение из И96, а не отмена его

FAQ на тёмной палубе печатал вопрос белым листом (`--plate`,
`[data-plate]` возвращает роли к светлой стороне, И96) — и заказчик назвал
это «вырвиглазно», попросив плашку тёмной, светлее самой палубы, а не
белой.

**Дефект/решение.** Не дефект, а именованное исключение из правила И96:
там, где лист ДОЛЖЕН остаться частью тёмного мира палубы, а не стать белым
листом на ней, `--plate`/`--on-plate*` переопределяются точечно, на более
специфичном селекторе (`[data-ground='deck'] .item` в `Faq.module.css`), а
не через общий `[data-plate]` в `styles/base.css` — общий приём остаётся
цел, и все ОСТАЛЬНЫЕ листы на палубе (карточка товара, кадр героя, пост
инстаграма) по-прежнему белеют по И96.

**Как правильно.** Новый токен `--chrome-plate` в `styles/tokens.css` —
палуба, посветлевшая на 14% в сторону чернил (тот же приём, что у
`--chrome-hover`, другая доля и непрозрачный результат). Доля выбрана по
контрасту: белые чернила на ней в тёмной теме дают 4.98:1 — тёмная тема
сама светлее, и доля покрупнее либо впритык у порога 4.5:1, либо ниже.

**Где искать то же самое.** Любой блок, для которого заказчик явно
попросил остаться тёмным на тёмной палубе вместо обычного «лист есть тоже
пол», — кандидат на тот же приём: своя переменная в `tokens.css`, точечное
переопределение `--plate`/`--on-plate*` на своём селекторе, замер контраста
в обеих темах перед тем, как называть число окончательным.

## И106 · Сжатие на нажатии — тоже движение, которое нельзя дарить второй раз

Сердце «в избранное» на фотографии товара уже не поднималось на наведении
(предыдущая правка того же файла, `--btn-rise:0px`, — «одна рука, один
подъём»). Заказчик нашёл тот же дефект второй раз, теперь на нажатии:
«подпрыгивает… кружочк не должен».

**Дефект.** Общая кнопка (`styles/btn.module.css`, `.btn:active`) на
нажатии не только поднимается — она ещё и сжимается на фиксированные 3%
(`scale:calc(1 - var(--btn-press) * .03)`), и это число было вкопано прямо
в формулу, без ручки — в отличие от подъёма, у которого ручка (`--btn-rise`)
уже была. Сжатие с центром посредине для круга 32px на фотографии двигает
его верхний левый угол на те же доли пикселя, что раньше двигал подъём:
замерено — левый край уезжает на 0.48px, правый на столько же в другую
сторону, и на статичном снимке под кругом это читается тем же «прыжком»,
что и раньше читался подъём. Причина та же: одна рука не должна отвечать
на неё двумя разными движениями сразу, когда одно из них уже отключено.

**Как правильно.** Сжатие получило ту же ручку, что подъём: `--btn-dip`,
по умолчанию `.03`, ноль — у `.like`. Формула
`scale:calc(1 - var(--btn-press) * var(--btn-dip, .03))`, а не отдельный
селектор, отменяющий готовое значение: изменилось общее устройство кнопки,
а не список исключений на одну кнопку. Замерено `requestAnimationFrame`-циклом
поверх настоящего `page.mouse.down()`/`up()` (синтетический `PointerEvent`
через `dispatchEvent` не включает CSS `:active` вовсе и до правки показывал
бы «всё в порядке» там, где на самом деле ничего не проверено).

**Где искать то же самое.** Любая кнопка, стоящая НА фотографии или
поверх другого предмета, который сам уже двигается под той же рукой
(карточка поднимается, кнопка на ней тоже пробует), — кандидат на обе
ручки сразу: и `--btn-rise:0`, и `--btn-dip:0`. Проверять на глаз мышью
недостаточно — `:active` централизованно измеряется только настоящим
нажатием, не синтетическим событием.

## И107 · Переход, который нельзя проверить, — не переход

Заказчик показал одно и то же трижды: шторка корзины при открытии дёргается
— уезжает влево, потом двумя рывками прижимается к правому краю. Третий раз
пришёл с видео и фразой «дёрганье я проверял во всех 4х браузерах, дёргается
во всех».

**Дефект.** `.sheet` (`CartPanel.module.css`) приезжала слайдом: правило
`@starting-style{ .panel[open] .sheet{translate:100% 0} }` ставило старт
перехода на «за краем экрана», а `transition:translate .35s` тянуло его на
место. Само правило было написано верно — дело не в опечатке. На видео
заказчика момент открытия виден как настоящее задвоение картинки, то есть
браузер в этот миг действительно рисовал ДВЕ разные позиции листа почти
одновременно, а не одна съёмка показалась смазанной. Ни разу не
воспроизвелось здесь: ни в headless-браузере с покадровой съёмкой через
`requestAnimationFrame` (дважды, разными методами замера), ни с
принудительной классической полосой прокрутки, ни с выключенным
`OverlayScrollbar`. «Во всех четырёх браузерах заказчика» снимает версию
«особенность одного движка рендеринга» — это не баг Chromium или Gecko по
отдельности, похоже на то, как именно ЭТОТ показ (`showModal()` заводит
новый композитный слой поверх страницы) стыкуется с переходом на
конкретном железе или драйвере экрана, которого нет в тестовом окружении.

**Как правильно.** Причину, которую нельзя воспроизвести, нельзя и
починить прицельно — можно только убрать условие, при котором она вообще
возникает. `@starting-style` снят: лист появляется на месте сразу, без
слайда. Это не заплатка (устройство листа не изменилось, снято ровно одно
необязательное украшение) и не отступление от правила: сам файл уже нёс
комментарий-предупреждение с самого начала — «поддержки `@starting-style`
нет — лист появляется на месте: движение здесь украшение, а не смысл» —
про браузеры, где правило не работает. Теперь так везде.

`transition:translate` на `.sheet` осталось: оно нужно для другого —
плавного возврата листа, если жест закрытия (`useSwipeClose`) не дотянут до
порога. Без `@starting-style` это свойство просто не участвует в открытии:
переходить не от чего, лист и так на месте с первого кадра. Проверено
отдельно: жест до половины и отпускание всё ещё едет назад плавно, десятью
кадрами подряд.

Заказчик увидел резкое появление без всякой анимации и попросил вернуть
плавность — не тем же способом. Добавлено затухание: `opacity:0` в том же
`@starting-style`, но БЕЗ `translate` рядом. Разница по существу, а не по
вкусу — слайд двигал ПОЗИЦИЮ листа, и позиция путалась в момент, когда
браузер заводит новый композитный слой; непрозрачность ничего не двигает и
раскладку не пересчитывает, это самый дешёвый для браузера переход. Живым
замером подтверждено: `opacity` идёт от 0 до 1 плавно, `translate` всё это
время стоит на `0px` без единого промежуточного значения.

**Где искать то же самое.** `@starting-style` в проекте остался в одном
месте — `Menu.module.css` (маленький попап сортировки/фильтра, не во весь
экран, без жеста закрытия пальцем). Дефект на нём заказчик не показывал;
трогать по одному тому же паттерну класса без живого репорта — расширять
работу, а не чинить показанное. Если тот же почерк («дёргается, воспроизвести
не получается») найдётся у другого перехода `@starting-style` — снимать
его тем же способом, не разбирая заново композитор браузера, которого не
потрогать руками.

**Второй заход, тоже неудачный.** Заказчик увидел рядом боковое меню
(обычный `<div>` с `transform`, слайд у него ровный) и попросил вернуть
слайд корзине другим способом — раз старый спорил с новым композитным
слоем `showModal()` именно ТАЙМИНГОМ (старт перехода в том же кадре, что
и показ). Развели по кадрам: `showModal()` как раньше, а атрибут,
запускающий переход, ставился двойным `requestAnimationFrame` — кадром
позже появления слоя, не одновременно с ним. В headless покадровая
выборка `translate` показала ровную кривую без единого скачка. На
настоящем телефоне заказчика дёрганье вернулось — тем же почерком, что и
в первый раз. Откачено тем же `git revert`, которым и обещали.

Это меняет диагноз. Раз развод по кадрам не помог, дело не только в
ТАЙМИНГЕ старта перехода — вероятно, сам факт, что `.sheet` (child)
заводит СВОЙ композитный слой под активный `transform`-переход в тот
момент, когда родительский `<dialog>` только что получил слой от
`showModal()`, ломается независимо от того, сколько кадров между ними
прошло. Следующая попытка, если будет, — не тайминг, а другое свойство:
что-то не по `transform`/`translate` (например, `inset-inline-end` или
`margin-inline-end`), то есть без отдельного композитного слоя у самого
перехода, ценой более тяжёлой анимации (пересчёт раскладки на кадр вместо
работы одного компоситора). Пробовать можно только с реальным телефоном
заказчика под рукой — headless этот дефект не видит ни разу ни в одной
версии, значит и не докажет починку.

**Третий заход, тоже неудачный — точка.** Свойство сменили ровно так, как
предсказано абзацем выше: `inset-inline-end` вместо `translate`,
`data-enter` из второго захода оставлен рядом. Дёрганье вернулось тем же
почерком на настоящем телефоне заказчика. Три захода, два разных
подозреваемых (тайминг кадра, тип свойства) — оба проверены и оба не
помогли. Дальше пробовать нечего: остающийся путь (`clip-path`) не двигает
лист, а скрывает статичный — другой эффект, не слайд. Шторка корзины
окончательно остаётся с одним затуханием; новых попыток вернуть слайд
здесь не будет, пока не появится способ отладить это на самом устройстве
заказчика.

## И108 · Ручка называла две вещи одной, а меняла только одну

Инструмент подбора вуали (`/design`, вкладка «Блоки») даёт ползунок «Высота
вуали». Заказчик покрутил его и увидел: двигается только размытие, а
затемнение снизу снимка — своя, отдельная полоса — за ползунком не идёт
вовсе. Спросил, как называется вторая, и попросил объединить высоту обеих
под одной ручкой.

**Дефект.** `TileParams.edge` уже был документирован верно —
«how far up the tile the veil and the blur start, as a share of height» — но
код этому не соответствовал. `--edge` управлял только высотой `.haze`
(размытие). Затемнение под подписью (`.tile[data-type='full'] .body`,
`--veil`) не читало `--edge` вовсе: его высота считалась от СОДЕРЖИМОГО —
поле плюс строки заголовка и подписи, — и это не случайность, а более
ранняя починка: раньше та же полоса стояла долей высоты плитки НАПРЯМУЮ, и
длинная подпись пробивала её насквозь («21 строка подписей из 31 ниже
порога» — правило рядом, в `styles/tile.module.css`). Дока обещала общую
ручку, код нёс две независимые.

**Как правильно.** Не тронул содержимо-зависимую вуаль `.body` — она
единственная гарантирует, что текст всегда лежит на сплошной краске,
сколько бы строк в подписи ни было, и трогать эту гарантию ради ползунка
нельзя. Вместо этого рядом с `.haze` встал `.shade` — такой же
абсолютно спозиционированный слой высотой `var(--edge)`, залитый тем же
`--veil` с растушёвкой к прозрачному сверху. Он не заменяет вуаль под
текстом, а добавляет к ней тон дальше вверх, туда же, куда тянется
размытие — визуально одна зона, физически два слоя с одной переменной на
высоту.

**Где искать то же самое.** Любая пара «эффект A и эффект B должны идти
одной ручкой», где один эффект завязан на % высоты, а другой — на
содержимое (текст, число строк, счётчик), — кандидат на тот же приём:
не подчинять содержимо-зависимый эффект проценту (это возвращает старый
дефект нечитаемого текста), а положить рядом независимый слой, который
эту долю действительно меряет.

## И109 · Именованное исключение из И96 унаследовало не всё, от чего отказалось

Заказчик увидел на плашке вопроса (И105 — «темна плашка на палубе, а не
белый лист») сразу два следа одной причины: стрелка закрытого вопроса не
видна в своём кружке, а название вопроса при наведении «синее на синем».

**Дефект.** И105 переопределил на `[data-ground='deck'] .item` три роли —
`--plate`, `--on-plate`, `--on-plate-2` — и этого хватило для фона листа и
текста. Не хватило для двух ДРУГИХ ролей, которые `[data-plate]` считает
ОТ НИХ ЖЕ, но по формуле «лист всегда светлый» (сам комментарий над
`[data-plate]` в `styles/base.css` так и говорит): `--ctrl` (круг вокруг
знака) берёт `--plate-3` — не тронутую, ещё светлую ступень, — и живая
лестница (`--cyan-11`, отсюда `--hover-ink`) откатывается к `--live-11`,
посчитанной для светлого пола. Пара «переопределил чернила — переопредели
и то, что из них составлено» уже стоила трёх находок на самой палубе
(`[data-ground='deck']` в `styles/base.css`, там же список) — здесь та же
болезнь ещё раз, на именованном ИСКЛЮЧЕНИИ из общего правила. Замерено:
знак 1.15:1, наведение 1.37:1 — при пороге 4.5.

**Как правильно.** Не трогать `[data-plate]` (общее правило верно для
любого ДЕЙСТВИТЕЛЬНО светлого листа) и не трогать общий `[data-ground=
'deck']` (палуба ни при чём — плашка на ней своя, отдельная краска). На
ТОМ ЖЕ специфичном селекторе, что уже держит И105, досчитаны только те
роли, которые эта плашка САМА потребляет: `--ctrl:var(--chrome-hover)` —
тот же приём, что и у общей палубы; живая ступень — та же формула
(`oklch(from var(--accent-solid) L 0.11 h)`), но со своей светлотой: у
общей палубы 0.80 против её более тёмного фона, у этой плашки — 0.82,
потому что сама плашка светлее палубы (`--chrome-plate` мешает 14% чернил
в `--chrome-bg`) и та же ступень на ней недодаёт 0.1 до порога. Роли,
которые плашка не потребляет (`--rule`, `--quiet*`, `--hover-row`, тень,
`--pop`/`--on-pop`), не тронуты — версия по умолчанию для них либо уже
верна через `--plate`, либо не читается этим файлом вовсе, и трогать
непотребляемое — расширять работу, а не чинить показанное.

**Где искать то же самое.** Любое ИМЕНОВАННОЕ исключение из И96 (лист,
которому велено остаться тёмным) — кандидат: проверить не только цвет
листа и его текста, но каждую роль, которую `[data-plate]` считает по
формуле «лист светел» (`--ctrl`, живая лестница, тень, `--pop`-пара) и
которую этот конкретный лист действительно использует. Пройденный список
один — тот, что уже составлен в комментарии над `[data-ground='deck']`.

## И110 · Строка списка повторяет карточку глазами, а ссылку — нет

Заказчик увидел строки товара в шторке корзины и назвал дефект прямо: «это
же карточки товара, они должны открываться на товар, а они без ссылки».

**Дефект.** Строка корзины (`CartLine` в `CartBits.tsx`) рисует те же факты,
что и карточка полки, — снимок, марку, название, цену, — но не берёт саму
карточку (запрет 6: у строки другая ширина и другая раскладка, ей и положено
рисовать себя заново). Карточка полки уже отвечает на нажатие ЦЕЛОЙ областью
— ссылкой во всю карточку снизу (`ProductCard.module.css`, `.hit`), а
счётчик и сердце подняты над ней своим z-index. Строка скопировала вид, а
эту невидимую в разметке часть — только то, что бросается в глаза, —
унаследовать было неоткуда: `.hit` не то, что копируют вместе с текстом.

**Как правильно.** Тот же приём, что у карточки: ссылка во всю строку снизу
(`z-index:1`), а органы, которым нужно остаться нажимаемыми поверх неё, —
счётчик и «убрать» — подняты одним `z-index:2` на их общем родителе
(`.deal`), а не поштучно на каждой кнопке.

**Где искать то же самое.** Любой компонент, печатающий факты товара (имя,
снимок, цену) рядом с другими похожими товарами — полка «с этим берут»,
список избранного, история заказов, — кандидат: он выглядит карточкой, и
нажатие на него обязано отвечать так же, как у карточки. Разбор — в скилле,
`.claude/skills/shop/references/catalog.md`.

## И111 · Окно заказа спрятало факт там, где остальной сайт прячет только нажатие

И86 записал закон: незаполненный канал связи печатается всегда, прячется
только нажатие («Прячется не факт, а НАЖАТИЕ»). Окно быстрого заказа
(`components/QuickOrder.tsx`) этот закон не читало — оно жило по СВОЕЙ,
более строгой версии, написанной раньше и рядом: `routesFor` в
`lib/contacts.ts` выбрасывала канал без настоящего адреса из списка
целиком, а не печатала строкой без ссылки.

**Дефект.** Заказчик включил Viber, Telegram и WhatsApp в панели, номеров
пока не вписал — и в окне заказа увидел ОДИН Instagram вместо четырёх строк.
Не «не подключено», а «пропало». Дважды объяснённая причина («это
заглушки, впишете номер — появятся сами») читалась как отговорка именно
потому, что нигде на странице не было ВИДНО, что три строки вообще
существуют, — а искать объяснение тому, чего не видно, никто не станет.

**Как правильно.** `routesFor` печатает строку для каждого включённого
мессенджера всегда, а `href` у неё либо есть, либо нет — ровно то же
разделение, что и у остального сайта. Окна решают, что делать со строкой
без адреса: `QuickOrder` рисует её `<span>` вместо `<a>`, с `aria-disabled`
и приглушённой краской (`opacity:.45;pointer-events:none`, тот же приём, что
уже стоял в `WriteForm.module.css` под `data-ready='false'`); `WriteForm`
получил ту же проверку НА КАЖДУЮ строку отдельно (`ready && Boolean(c.href)`),
а не только общую на форму, — иначе ненабранный номер канала читался бы
включённым, пока не набрано письмо.

**Где искать то же самое.** Любое место, где список считается из ДАННЫХ
(«какие каналы включены», «какие способы оплаты подключены», «какие адреса
доставки настоящие») и фильтрует по готовности ДО того, как список
отрисован, — кандидат: фильтр решил за пользователя, что список пуст, хотя
на деле список есть, просто часть его не отвечает на нажатие. Закон один на
оба случая: И86 и этот — про одно и то же правило «факт виден, нажатие
прячется», просто на разных этажах кода (данные vs. вид).

**Продолжение.** Строка без адреса вернулась не только в окно заказа —
вместе с ней вскрылось, что «строка есть, нажатия нет» видно было ОДНИМ
глазом (`.chan small` уже набрана приглушённой краской), а другим — нет:
подпись строки (`<b>`) набрана тем же жирным чёрным, что у настоящего
контакта, и ряд в целом читался нажимаемым наравне с рабочим. Заказчик
увидел это сразу в двух местах — помощнике и меню мессенджеров в шапке
(оба берут общий примитив `.chan` в `styles/primitives.module.css`) — и
назвал прямо: в одном месте гасили, в другом нет, хотя правило одно.

Гасится ряд ЦЕЛИКОМ (`opacity`, а не только цвет одной строки), и один раз
— на общем примитиве `.chan[data-empty='true']`, а не в каждом файле,
который его берёт: `Helper.tsx` и `HeaderChat.tsx` получили один атрибут
(`data-empty={known(c.value) ? undefined : 'true'}`), а не свою копию
CSS. Подвал (`Footer.tsx`) общий примитив не берёт — своя раскладка, своя
строка того же правила рядом с ней в `Footer.module.css`. Пять мест на
весь сайт спрашивают `useChannels()`, и все пять теперь отвечают на
«показан ли канал без адреса» одним и тем же «да, приглушённо» — там, где
раньше было три разных ответа на один вопрос.

## И112 · Переопределили ингредиенты тени, а саму тень — нет

И100 (сентябрь) уже чинил тень на пилюлях героя: `--sh-ring`/`--sh-near`/
`--sh-far-*` на светлом полу были подмешаны из фирменного цвета, а
фирменный цвет и есть цвет палубы — тень сливалась с полом, на котором
лежит. Правка тогда переопределила эти четыре роли и на `[data-ground=
'deck']`, и на `[data-plate]`, и была принята как починенная.

**Дефект.** Заказчик спросил про пилюли героя снова: «а тут нет тени?» —
и в этот раз, вместо того чтобы ответить по чтению файла (как в первый
раз этого дня, ошибочно), потребовал замера. `getComputedStyle` на самой
пилюле показал: `--sh-ring` — уже подправленный, нейтральный; а
`box-shadow` (тот, что фактически рисуется) — всё ещё старый, замешанный
на фирменном цвете. Правка не сработала НИКОГДА — с самого И100, — а
казалась рабочей, потому что читать про неё в файле означало проверять её
же собственную логику, а не браузер.

Причина — в устройстве переменных, а не в числах. `--sh-1` (то, что
`box-shadow` в разметке реально просит) объявлена ОДИН раз, в
`styles/tokens.css`, на корне: `0 0 0 1px var(--sh-ring), …`. `var()`
внутри объявления переменной раскрывается ТАМ, ГДЕ ЭТА ПЕРЕМЕННАЯ
ОБЪЯВЛЕНА, — тот же закон, что уже стоил роли `--rule`/`--quiet`/
`--hover-row` на палубе (комментарий над И100 в `styles/base.css` называет
его прямо), — и `--sh-1` объявлена на корне. Переопределить `--sh-ring`
глубже в дереве значит переопределить ИНГРЕДИЕНТ; готовое блюдо (`--sh-1`),
однажды замешанное на корневых ингредиентах, наследуется вниз уже
приготовленным и никогда не пересчитывается заново — сколько бы ролей под
ним ни сменилось. И100 переопределил всё, ЧТО САМ ВИДЕЛ составленным
(`--rule`, `--quiet`, `--quiet-on`, `--hover-row`), но не заметил, что
`--sh-1`/`--sh-2`/`--sh-3` той же природы: тоже составлены, тоже требуют
пересчёта на месте, а не только своих ингредиентов.

**Как правильно.** `[data-ground='deck']` и `[data-plate]` теперь
переобъявляют не только `--sh-ring`/`--sh-near`/`--sh-far-*`, но и сами
`--sh-1`/`--sh-2`/`--sh-3` — той же формулой, что в `tokens.css`, набранной
заново на месте, чтобы `var()` внутри неё раскрылся уже от МЕСТНЫХ
ингредиентов, а не от корневых.

**Где искать то же самое.** Любая переменная, СОСТАВЛЕННАЯ из других через
`var()` (не только тень — любой составной токен), которая при этом
переопределяется НЕ целиком, а только своими исходными ингредиентами на
каком-то `[data-*]`, — кандидат: составное наследуется с того места, где
объявлено последний раз ЦЕЛИКОМ, а не пересчитывается на лету из
переопределённых частей. Проверить нельзя чтением файла — только
`getComputedStyle` на реальном элементе в реальном контексте, ровно тем
способом, каким заказчик наконец это и поймал.

## И113 · Гашение «не настроено» верно только там, где рядом есть настоящее

И86/И111 решили: канал без адреса печатается всегда, глушится только цвет
ряда (`opacity`), чтобы отличить его от настоящего контакта рядом. Правило
верно, когда рядом ЕСТЬ с чем сравнивать — часть каналов настоящая, часть
нет, и разница что-то говорит.

**Дефект.** На витрине этого дня настоящих каналов нет ни одного — все пять
(`lib/contacts.ts`) стоят заглушкой `[PHONE]`/`[EMAIL]`/`[@HANDLE]`, пока
заказчик не впишет реальные номера. И111 гасил их все разом, и «остальные
четыре ярче этого» превратилось в «весь список одного цвета, и цвет тусклый»
— не сигнал о недонастройке, а вид сломанной страницы. Заказчик увидел это
сразу в подвале и на других контактных списках, идущих тем же приёмом
(«снова все контакты серые… снова что-то чиним, а ты что-то ломаешь»), и
велел прямо: контакты всегда показываются в полную силу, иконка и надпись —
это шаблон-заготовка сайта, и она обязана выглядеть готовой.

**Как правильно.** `opacity` снят с `[data-empty='true']` во всех трёх
местах, которые его несли (`styles/primitives.module.css` `.chan` — общий
для помощника и меню мессенджеров шапки; `components/Footer.module.css`
`.contacts`; `components/QuickOrder.module.css` `.row`). Атрибут и
`pointer-events:none` остались — нажатие всё ещё никуда не ведёт (`hrefOf`
не выдаёт адрес заглушке), просто без визуального гашения. `WriteForm`
(`data-ready='false'`) не трогать: там гаснет не заготовка данных, а форма
без набранного текста — другая причина, тот же побочный признак.

Подвал заодно попробовал вернуть цвет знакам колонки «Контакты»
(`ChannelMark` без `colour={false}`), приняв «полноценно» за «в цвете» —
и заказчик поправил в тот же день: «в футере цветные иконки не нужны,
верни монохромные, как было». Моно вернулось (`colour={false}` на
месте), опаловая часть правки (снятое гашение) не тронута — это были
два разных вопроса, серость и цвет, и решение по каждому своё. Ряд
соцсетей (Instagram/Facebook/YouTube) моно был и остаётся — отдельный,
раньше сделанный выбор заказчика («голый силуэт»), которого оба
разговора не касались.

**Где искать то же самое.** Любое место, где «показать заглушку тусклой»
предполагает соседство с настоящим значением. Когда ВСЕ значения списка —
заготовка (ранний этап производства, до наполнения из админки), тусклый
список читается не как метка, а как дефект страницы. Проверять глазом на
живой витрине, не только на файле: чтение кода не отличает «один из пяти
приглушён» от «все пять приглушены».

## И114 · Кнопка отвечает цветом; тень и подъём — это язык плашки, не кнопки

**Дефект.** Заказчик нашёл глазом: тени под кнопками на всём сайте, и сказал
прямо: «тени на кнопках — это плохо, убирай тени со всех кнопок сайта. Также
со всех кнопок выключай подъём, и с других компонентов тоже выключай подъём.
Все эффекты наведения и нажатия на кнопки будут отображаться только цветом».

**До этого дня.** Кнопка (`styles/btn.module.css`) отвечала на руку тремя
вещами разом: заливкой (`--btn-on`, шаг к своей же краске), тенью громкой
кнопки (`--btn-lift`/`--btn-lift-on`, `--sh-accent`/`--sh-accent-2`) и
подъёмом-проседанием (`translate`/`scale`, «наведено минус нажато»). Тот же
рецепт — тень плюс подъём — самостоятельно повторили ещё шесть мест, каждое
свой копией: «ещё» (`primitives.module.css` `.more`), вариант крепости
(`product.module.css` `.opt`), пилюли повода на главной (`home.module.css`
`.chips a`), пузырь помощника и «наверх» (`Helper.module.css` `.knob`/`.top`),
кнопка настроек сайта (`studio.module.css` `.launch`).

**Как правильно.** Кнопка (и всё, что нажимают, чтобы что-то сделать, —
ссылка-пилюля в том числе) отвечает ТОЛЬКО краской: заливкой у громкой,
вуалью в 16% чернил у тихой и у самодельных пилюль вроде `.opt`/`.more`/
`.launch` (та же доля, что уже была выверена у тихой кнопки — измерена и
держит порог видимости на обеих темах). Ни `box-shadow`, ни `translate`, ни
`scale` кнопка на наведение и нажатие больше не берёт. `--btn-lift`/
`--btn-lift-on`/`--btn-rise`/`--btn-dip`/`--btn-press` — вся эта проводка
снята вместе с эффектом: ручка без дела не остаётся лежать в файле.

Тень и подъём не исчезли из языка сайта — они остались языком ПЛАШКИ
(карточка товара, плитка, пост, лист): у плашки тень при наведении
переходит на ступень глубже (`--sh-1` → `--sh-2`), но сама плашка не
двигается — то же решение, что уже стояло у карточки товара («только чтоб
два наплыва не было»). У кнопки этого языка больше нет вовсе — если кнопка
стоит на плашке, значит про подъём и тень уже сказала плашка, а не она.

**Два места ответили без слова «тень» в привычном смысле.** Пузырь
помощника и «наверх» (`Helper.module.css`) — плавающие круглые кнопки без
соседа, который бы сказал за них «здесь край»: внешняя тень с них снята, а
кромка (`inset`-кольцо, светлеющее под рукой) осталась — это не тень-возвышение,
а обводка, тем же приёмом, каким контур отличает недоступную кнопку от фона.
Сердце «в избранное» (`.like`, `styles/btn.module.css`) стоит на фотографии
и держало тень именно как подложку, а не как ответ на руку, — снято по той
же букве правила, и это единственное место, где заказчику стоит проверить
глазом: если кружок потеряется на светлом снимке, вместо тени понадобится
другой приём.

**Тень плашки заодно чинится в отдельном месте.** Заказчик увидел плашки
FAQ рядом друг с другом в обеих темах: в тёмной — с ясной тенью, в светлой —
без всякой. Причина не в разметке (`box-shadow:var(--sh-1)` стоит в обеих
темах одинаково), а в самих числах: светлая половина `--sh-ring`/`--sh-near`/
`--sh-far-*` (`styles/tokens.css`) была в 4–5 раз тише тёмной (5/5/14/20/30%
против 5/50/60/68/75%) — на тёплой странице такая тень тонет почти без следа.
Замер — на самой странице, несколько степеней рядом, а не число, взятое на
глаз: новые проценты (18/18/40/55/70) читаются наравне с тёмной темой, не
темнее. Три копии формулы («там, где объявлена переменная» — И100/И112) —
корень (`styles/tokens.css`), лист-как-пол (`styles/base.css`
`[data-plate]`) и переносимый пакет (`packages/ui/tokens.css`) — обновлены
все три, иначе сайт и переносимый образец показали бы тень по-разному
(семья `varDrift` в `check:port`).

**Где искать то же самое.** Любая самодельная «пилюля», которая нажимается
подобно кнопке, но собрана не через общий `.btn`, — кандидат: у неё своя
копия того же рецепта (тень + подъём), и правило приходится приносить
руками, а не одной правкой в общем файле. Прежде чем красить такую пилюль
только цветом, проверить, что смена фона на наведении вообще ЕСТЬ и её
видно без лупы, — иначе кнопка, лишённая тени, окажется без ответа вовсе.

## И115 · `position:relative` ради чужого z-index тайно меняет чужой угол

**Дефект.** В этой же сессии строка корзины получила ссылку во всю строку
(`.hit`), а группа чисел (`.deal`) — `position:relative;z-index:2`, чтобы
счётчик и «убрать» остались нажимаемыми поверх неё. Знак «убрать» (`.drop`)
стоял внутри этой самой группы и был `position:absolute` — до правки
чувствовал углом стро́ки целиком (`.line`, единственный позиционированный
предок), а с ней ощутил ближайшим позиционированным предком `.deal` вместо
неё. `.deal` шириной по своему содержимому (цена, счётчик, сумма), без
знака «убрать» — он у неё в потоке не участвует, — и угол знака съехал к
правому краю ЧИСЕЛ, а не карточки: заказчик увидел ряд, где мусорная
корзина стоит вплотную к сумме, и назвал место словами — «должна быть в
правом верхнем углу плашки, как и было ранее».

Правка, добавившая `.hit`, тронула ОДНУ строку файла — `position:relative`
на `.deal` — ради причины, не имеющей отношения к «убрать» вообще. Второй
знак, чей угол считается от того же предка, не был показан заказчику и не
был замечен диффом: `position` — это не отдельное свойство одного знака, а
общий предок для ВСЕХ его позиционированных потомков сразу.

**Как правильно.** Знак «убрать» перенесён из разметки группы чисел
(`.deal`) в саму строку (`.line`), сиблингом к группе, а не её потомком:
ближайший позиционированный предок для него снова строка целиком, и её
угол свободен независимо от того, что делает `.deal` ради своего z-index.
Своё место в стопке над ссылкой (`.hit`, z-index:1) он держит явным
`z-index:2` на себе, а не одалживает его у соседа.

**Где искать то же самое.** Любой абсолютно спозиционированный знак,
чей предок получил `position:relative` НЕ ради этого знака, а ради своей
собственной причины (свой z-index, свой transform), — кандидат: знак
незаметно сменил систему отсчёта, и найти это чтением одной правки нельзя,
потому что причина и следствие лежат в разных предложениях диффа. Мерить —
`getBoundingClientRect()` знака и его ФАКТИЧЕСКОГО (не подразумеваемого)
позиционированного предка, а не верить комментарию «угол берёт стилем».

## И116 · Независимый слой можно вернуть под общую ручку полом, не заменой

И108 нарочно оставил вуаль под подписью (`.tile[data-type='full'] .body`)
не слушающей ползунок «Высота вуали» вовсе: она меряется от содержимого
(поле плюс строки подписи), и это гарантия — длинная подпись получает
вуаль полностью, короткой хватает того же. Ползунок двигал только соседний
слой, `.shade`/`.haze`.

**Дефект.** Заказчик открыл тот же инструмент подбора и увидел ту же
границу с другой стороны: подвинул «Высота вуали» — размытие и заливка под
ним выросли, а само тёмное поле под словами осталось прежним. Назвал это
прямо: «мне нужна регулировка высоты тёмного поля, сделай её регулировку
вместе с высотой вуали». И108 решило «эти два эффекта не могут делить одну
ручку», не заметив третьего варианта — один может стать НИЖНЕЙ ГРАНИЦЕЙ
другого, не подчиняясь ему целиком.

**Как правильно.** `.body` получил `min-block-size:var(--edge, 35%)` — не
взамен размера от содержимого, а РЯДОМ с ним: CSS уже берёт большее из
двух сам. Короткая подпись, чьё содержимое меньше выбранного процента,
теперь растёт вместе с ползунком; длинная, чьё содержимое больше, по-
прежнему получает своё и в вуаль не проваливается. Гарантия читаемости
из И108 не снята — она стала полом вместо потолка.

**Где искать то же самое.** Любая пара «эффект от содержимого» и «эффект
от процента», разведённая раньше по слову «эти двое не могут стоять на
одной ручке», — кандидат на повторную проверку: если предыдущий эффект
никогда не должен УМЕНЬШАТЬСЯ ниже содержимого, `min-block-size`
(или `max-block-size` для обратного случая — процент не должен расти выше
содержимого) мирит оба без потери гарантии, ради которой их когда-то
развели.

## И117 · Фото само себе край; тень нужна не каждой плашке со снимком

И114 сняло тень и подъём с кнопок, оставив тень языком плашки — карточки,
плитки, поста. В тот же день заказчик попросил тень туда, где её не было:
«Choice by need», «Choice by category», клип отзыва. Через день, посмотрев
на витрину целиком, он отменил ровно эту часть решения.

**Дефект.** «Тень — язык плашки» читалось как «у каждой плашки со снимком
должна быть тень», не разбирая, ЗАЧЕМ тень нужна там, где она уже стоит.
У карточки товара и плашки FAQ снимка нет вовсе или он занимает только
часть плашки — тень отделяет лист от пола, потому что больше отделить его
нечем. У плитки повода, меню «By benefit», поста стены Instagram и клипа
отзыва снимок занимает плашку целиком: край снимка (не белого, не
однотонного) и есть её край, и тень поверх него — вторая, лишняя граница.

**Как правильно.** Тень снята полностью — в покое, при наведении и при
нажатии — у пяти блоков: «Choice by need», «Choice by category», меню «By
benefit» (все три — один компонент, `styles/tile.module.css`, все три вида
`full`/`bar`/`mount`, других потребителей у файла нет), стена Instagram
(`components/Ugc.module.css`) и клип отзыва (`components/Says.module.css`).
Ответ на руку у всех пяти остаётся — наплыв снимка, тем же приёмом, что и
раньше. Карточка товара и плашка FAQ тень сохраняют, и FAQ вдобавок
получила ту же оговорку, что раньше досталась только карточке товара:
тень в покое снята (список показывает несколько плашек подряд, и тень на
каждой читается серым полем), тень под рукой осталась.

**Где искать то же самое.** Правило вида «эффект X нужен любой плашке
семейства Y» — кандидат на повторную проверку, если у плашек семейства
разное СОДЕРЖИМОЕ: то, что верно для плашки без снимка или с частичным
снимком, не обязано быть верным для плашки, где снимок — это она сама.
Спрашивать не «плашка ли это», а «есть ли у неё край без тени».

## И118 · Общий контрол на разных полотнах — разного роста, не одного числа

Сердце «в избранное» (`.like`, `styles/btn.module.css`) — один контрол,
composes'ится и в карточку полки, и в снимок страницы товара (правило 10).
Рос он одним числом, 32px, в обоих местах.

**Дефект.** Заказчик увидел сердце на большом снимке страницы товара и
спросил: «значок в избранное может больше по размеру должен быть?». 32px
верно на карточке полки — там кадр 200–280px, и знак в углу соразмерен; на
странице товара кадр вчетверо больше, и тот же контрол читается мелким
рядом со своим соседом по тому же снимку — стрелками галереи (`.flip`,
40px). У одного контрола в разных местах должна быть СВОЯ мера, а не одна
на всех, когда сами полотна разного роста.

**Как правильно.** `product.module.css` получил `.like.like{--btn-h:var(
--ctrl-h, 40px)}` — тот же рост, что и у соседних стрелок галереи, той же
ручкой (`--ctrl-h`). Двойной класс — тот же приём, что `.buy.buy` в
BuyBtn.module.css: своя запись и запись общего контрола обе бьют
специфичностью 0-1-0, и без удвоения побеждает та, чей файл лёг в сборку
позже. Карточка полки контрол не трогает — там 32px остаётся верным.

**Где искать то же самое.** Общий контрол, встроенный в разные ПО РОСТУ
полотна (маленькая карточка и большая фотография), — кандидат: одно число
для обоих мест либо мелкое там, где могло быть крупнее, либо крупное там,
где мельче было бы уместнее.

## И119 · Перенос решает по остатку места, а не по замыслу ряда

Строка действия на странице товара (счётчик — в корзину — быстрый заказ)
переносилась `flex-wrap`'ом, когда не хватало места.

**Дефект.** Заказчик увидел ряд на разных ширинах окна и назвал прямо:
«кнопки скачут… хаос». `flex-wrap` переносит по ПЕРВОЙ нехватке места в
моменте, и на соседних ширинах рвёт строку в разных местах — то счётчик
с корзиной наверху, а заказ снизу, то иначе. Причина глубже: колонка
покупки на столе — ФИКСИРОВАННАЯ мера набора (600px), и на болгарском
«Добави в количката» + «Бърза поръчка» вместе со счётчиком шире этой меры
ВСЕГДА — перенос был не аварией на некоторых ширинах, а нормой на всех.

**Как правильно.** Строка стала сеткой с именованными местами (`grid-
template-areas:'qty quick' 'add add'`) вместо переноса по остатку: счётчик
и «быстрый заказ» — верхняя строка, «в корзину» — нижняя, во всю ширину,
на любой ширине колонки. Заказчик расставил эту раскладку словами сам.

**Где искать то же самое.** Ряд из нескольких органов с `flex-wrap` —
кандидат, если хотя бы один из органов держит ТЕКСТ ПЕРЕВОДА (длина
которого не в нашей власти): перенос по остатку места читается предсказуемо
только на одной конкретной ширине, а на диапазоне ширин — как хаос. Сетка
с именованными местами не переносит непредсказуемо никогда.

## И120 · Заливка кнопки должна отвечать цветом, а не только кромка

«Пузырь» помощника (`.knob`, `components/Helper.module.css`) отвечал на
наведение и нажатие только сменой прозрачности внутренней кромки (26%→42%).

**Дефект.** И114 решило: кнопка отвечает ЦВЕТОМ. У большинства кнопок это
заливка; у пузыря — из-за снятой тени — осталась только кромка, и сама
заливка (`--accent-solid`) не менялась вовсе. Заказчик увидел это глазом:
«на наведение/нажатие никак не реагирует цветом». Кромка технически тоже
краска, но настолько тонкая и тихая, что не читается ответом.

**Как правильно.** Заливка пузыря добавлена в ответ: `--hover-solid` —
та же смесь, что уже красит громкую кнопку под рукой (`--pop-hover`),
потому что заливка пузыря — тот же `--accent-solid`, что и у громкой
кнопки в покое. Кромка осталась — теперь оба слоя отвечают вместе.

**Где искать то же самое.** Контрол, у которого тень заменили кромкой
(по И114), — кандидат: если при этом никто не добавил ответ ЗАЛИВКОЙ
(только кромка), кнопка формально следует букве правила и не следует его
смыслу — «отвечает цветом» означает цветом, который видно.

## И121 · Поле вокруг подписи стоило дороже самой подписи

Тёмное поле под подписью плитки (`.tile[data-type='full'] .body`) держало
поле 22px и растушёвку 34px — числа, не менявшиеся с самого начала.

**Дефект.** После И116 (поле растёт от `--edge`, не только от содержимого)
и на длинных двухстрочных заголовках поле стало отъедать у снимка почти
половину плитки. Заказчик увидел ряд плиток и сказал прямо: «выглядит это
отвратительно… может скрыть тёмным всё изображение». Считая по факту: у
однострочного заголовка поле+растушёвка (22+34=56px с каждой стороны)
стоили БОЛЬШЕ, чем сама строка текста над ними.

**Как правильно.** Оба числа уменьшены — 14px поля, 18px растушёвки.
Гарантия читаемости не снята: заливка под текстом непрозрачна на всю
высоту коробки независимо от размера поля, а растушёвка короче ТОЛЬКО
безопаснее (она и заведена была затем, чтобы не залезать на строку
заголовка — короче она отступает от неё дальше, а не ближе).

**Где искать то же самое.** Отступ или растушёвка, взятые числом до того,
как рядом появилась ручка, меняющая РОСТ той же коробки (здесь — И116), —
кандидат на пересчёт: числа, безобидные при фиксированной небольшой
коробке, складываются в заметную долю при выросшей.

## И122 · Второй ряд копится из нескольких органов, каждый по отдельности мелкий

Полоса каталога на телефоне держала два ряда: «Filter / Reset / N products»
и «Cards / Table / Price per mg».

**Дефект.** Заказчик увидел два ряда кнопок и назвал это прямо: «на тлф это
два ряда кнопок, нужно сократить до одного». Ни один орган по отдельности
не был лишним — виноват был не один орган, а сумма пяти. Показал решение
сам: вид и сортировка не обязаны носить слово на кнопке, если то же слово
остаётся в открытом меню и на `aria-label`.

**Как правильно.** Вид — пара пилюль («Cards»/«Table») стала одним знаком,
который меняет сам себя по нажатию (та же роль лотка, что и «Reset», —
`data-act`, «делает», а не выбор из половинок). Подредба (`Menu.tsx`)
получила `compact`: слово прячется на узком экране, знак и стрелка
остаются, имя — на `aria-label` кнопки и в открытом листе. Reset потерял
слово тем же приёмом — заказчик спросил прямо: «может слово reset не
писать, а просто крестика будет достаточно?» — крестик стоит сразу после
«Filter» с числом, и по месту понятно, что он про фильтр.

Даже так на болгарском при типичной ширине телефона (390px) ряд не сходился
— не хватало около 114px, и большую их часть держало слово «продукта» в
счётчике найденного. Заказчик сам назвал счётчик «важным», поэтому решение
снять с него слово спросили прямо, а не сняли молча — ответ пришёл «да», и
слово ушло тем же приёмом, что и у остальных: число остаётся, имя целиком —
на `aria-label` строки.

Даже после этого замер по нулевому зазору показал перевес всего на 10px —
и нашёлся он не в тексте, а в устройстве самого знака вида: он не был
принудительно круглым, как «Reset», и нёс поле пилюли, которое ему, орган
без слова, не нужно. Досжатый до той же меры (`inline-size:var(--tray-
cell)`, без поля) и с более тесным зазором между двумя группами (`--sp-2`
вместо `--sp-7` — читать нечего разделять воздухом ряда, если обе группы
уже сжаты) ряд наконец сошёлся в одну строку с запасом.

**Где искать то же самое.** Ряд из нескольких органов, который не сходится
в одну строку, — считать НЕ по самому широкому органу, а по СУММЕ всех: узкий
орган, сжатый до знака, может не спасти строку сам по себе, если рядом стоит
текстовый счётчик или другое слово, которое в сжатии не участвовало. И даже
когда все слова сняты, проверить КАЖДЫЙ знак на то, действительно ли он
такой же круглый и тесный, как его сосед, а не просто мельче словом: поле
пилюли, забытое на органе без слова, всё ещё стоит наравне со словом,
которого больше нет.

## И123 · Список строк, разложенный сеткой, читается не списком, а кашей

Строки внутри грани панели фильтров («Category», «Concentration», «Brand» и
остальные) стояли сеткой `auto-fit, minmax(130px, 1fr)`: панель сама решала,
сколько строк встанет рядом, от своей ширины.

**Дефект.** Решение было осознанным и объяснённым в комментарии — короткие
подписи, широкая панель, зачем читать их одной узкой колонкой. Заказчик
открыл живую панель и увидел ровно то, от чего предупреждает этот же класс
дефектов в другом месте файла: строки РАЗНОЙ длины, вставшие соседями в
одном ряду, — «CBD oil», «Cannabis cream and gel» (в две строки) и «Cannabis
cosmetics» вперемешку. Назвал прямо: «тут каша, ты тут сортировки не в
столбик сделал, а в строку, так неправильно».

Сетка в несколько колонок читается списком только тогда, когда все строки
одной высоты — как в примере из комментария, что защищал решение («Cap/
sule/s» не в счёт, там боролись с ПЕРЕНОСОМ слова, а не с разной длиной
соседей). Список из фраз произвольной длины в несколько колонок не читается
ни списком, ни таблицей: глаз не может понять, что после «CBD oil» идёт
не следующий пункт того же списка, а начало нового ряда.

**Как правильно.** `.cols` — тот же класс, общий на все грани (правило 10),
— стал простой колонкой: `display:flex;flex-direction:column`. Одна правка
в одном месте выпрямила все грани панели разом, ничего не касаясь по
отдельности.

**Где искать то же самое.** Сетка `auto-fit`/`auto-fill` поверх строк
ПРОИЗВОЛЬНОЙ, не заданной заранее длины (не карточки одного роста, не
короткие фиксированные слова) — кандидат: заказчику решать, читает ли он
это как список сверху вниз или как несколько колонок рядом, а не решать это
за него плотностью укладки.

## И124 · Заплатка от старой болезни блокировала лечение той же болезни

Панель фильтров каталога (`.filters`) — приклеенная примитивом `pinned`,
своя прокрутка внутри (`.panelScroll`, `overflow-y:auto`). Заказчик:
«докрутил панель до конца, дальше колесо крутит воздух, страница не
скролится, нужно уводить мышку на карточки, чтобы прокрутить страницу
вниз».

**Дефект.** На `.panelScroll` стоял `overscroll-behavior:contain`, и стоял
не просто так — комментарий рядом объяснял ровно противоположную болезнь:
без него, докрутив панель, колесо будто бы «продолжает крутить страницу
под ней, и панель уезжает ровно тогда, когда до неё дотянулись». Это и
была заплатка от той старой болезни, объявленная как решение, а не как
временная мера (`docs/open.md` под неё строки не заводилось, потому что
её сочли починкой, а не заплаткой).

Проверка на СОБРАННОМ сайте (не чтение — оно как раз обмануло, ровно то,
для чего в проекте есть `check:craft`) показала: старой болезни здесь нет.
Область прилипания панели — это вся высота строки сетки, общая с колонкой
товаров (`grid-template-areas:'panel results'`, `align-items` на форму
прилипания не влияет — влияет высота грид-строки, а она берётся по
высокому соседу). Без `contain` панель докручивает свой список, дальше
чужой остаток колеса честно уходит странице, а сама панель остаётся
приклеенной на месте до самого конца полки — и уезжает только там, где и
должна. `contain` не чинил ничего: он просто запрещал браузеру ту самую
передачу, что нужна, и превращал конец списка панели в мёртвую зону.

**Как правильно.** Свойство снято. `overscroll-behavior:contain` остаётся
законным и обязательным ровно там, для чего его завела семья `scrollBleed`
в `check:css`: у экрана ПОВЕРХ страницы (шторка, окно, меню — вынутые из
потока, `position:fixed/absolute`, или мерящие себя `dvh/vh`), где чужой
остаток колеса не должен двигать инертную страницу ПОД накрытием. Панель
фильтров — не такой экран: она стоит в потоке страницы, её сосед по строке
и есть то, что должно продолжить крутиться, и передача остатка колеса —
не побочный эффект, а единственный способ докрутить длинную полку из-под
короткой панели.

**Где искать то же самое.** `overscroll-behavior:contain`, поставленный
без слов «здесь этот экран поверх страницы» рядом, — повод перемерить, а
не поверить комментарию: рассказ о болезни, которую он лечит, стоило
проверить тем же способом, каким её один раз уже проверили здесь.

## И125 · Поле окна входа: верх был суммой поля и чужого отступа, низ — только полем

Три окна одного магазина (вход, «Напишете ни», быстрый заказ) — общая
одежда: карточка с круговым полем на глаз, внутри общая шапка листа
(`SheetHead`, `line={false}`). Заказчик открыл окно входа и спросил прямо:
«почему вверху и внизу разные зазоры, пересмотри в форме поля / воздух».

**Дефект.** Поле карточки было записано `var(--sp-7) var(--sp-6)
var(--sp-6)` — верх на ступень больше боков и низа. Само по себе это уже
подозрительно (поле — круговое число одного предмета, а не три разных), но
настоящая причина глубже: `SheetHead` добавляет СВОЙ отступ
(`padding-block:var(--air-row)`) сверху и снизу своей строки, и он не
предмет ПОЛЯ, стоящий рядом, а первый ребёнок карточки, прижатый к самому
её краю. Поэтому видимый зазор сверху — это `--sp-7` карточки ПЛЮС
`--air-row` шапки, а зазор снизу — это только `--sp-6` карточки: одна
сторона мерена суммой двух чужих линий, другая — одним полем (тот же
дефект, что и правило 2 разбирает про измеренное разностью, только здесь
суммой).

Проверка на собранном сайте (все три окна, оба — вход и «Напишете ни» —
делят одну и ту же карточку с `display:grid;gap:var(--air-group)`, третье
— быстрый заказ — без него) подтвердила: до правки верх и низ отличались
ровно на `--air-row`.

**Как правильно.** Поле карточки стало круговым (`padding:var(--sp-6)`) —
одно число на предмет. Шапка внутри поля отменяет свой ВЕРХНИЙ отступ
(`.head.head{padding-block-start:0}`, тот же приём двойной специфичности,
что у `.buy.buy` в карточке товара — свой модуль побеждает чужой независимо
от порядка сборки) и оставляет нижний: там он не сумма с полем, а
единственный источник воздуха до следующей строки (у окна входа и «Напишете
ни» вдобавок есть `gap` соседей — но это отдельный, не заявленный сейчас
шов, трогать его не просили). Верх и низ на замере после правки — 28px в
28px, у всех трёх окон.

**Где искать то же самое.** `SheetHead` — первый ребёнок в каждом месте,
где он стоит (мега-меню, панель фильтров, шторка корзины, сводка заказа) —
и его собственный верхний отступ ВЕЗДЕ прижат к чужому краю. Не у всех эта
сумма ложная: шторка корзины и мега-меню не дают своего поля сверху и
рассчитывают на отступ шапки как на единственный источник — там трогать
нельзя. Проверять по признаку «есть ли у обёртки СВОЁ поле сверху», а не
по имени компонента.

## И126 · И121 починил поле и растушёвку, а не заданный вопрос про пол

Заказчик спросил дважды одно и то же, и правило И121 ответило только на
половину: «выглядит это отвратительно… КАКОЕ РЕШЕНИЕ? может вообще
отказаться от этого тёмного градиента?» — вопрос о самой заливке — остался
без ответа, а правка тронула только поле и растушёвку (22→14, 34→18).
Заказчик вернулся к тому же снимку («Pain and joints») и назвал это прямо:
«а по этому я тебе вопрос задавал, ты не ответил».

**Дефект.** И121 не трогал `min-block-size:var(--edge, 35%)` — пол коробки
подписи (И116) — а именно он, не поле и не растушёвка, решает высоту тёмной
заливки на короткой подписи: у двухстрочного заголовка содержимое просит
меньше пола, и побеждает бо́льшее из двух (35%). Число 35 пришло не с
обдуманного выбора для ЭТОЙ формы плитки, а по наследству от
`TILE_DEFAULT`, где оно посчитано для вида `mount` — там подпись под белой
плиткой, число двигает только размытие, и тёмного поля над словами вовсе
нет. У вида `full` то же число стало ПОЛОМ поверх снимка — роль другая,
число то же, что и есть настоящая причина, замаскированная под уже
решённое.

Отказаться от заливки целиком, как спрашивал заказчик, — нельзя молча:
надёжность подписи держат три вещи сразу (заливка, размытие, авто-плотность
по яркости снимка `tools/shade.mjs`), и без заливки снимок, который уедет в
Payload без нашего участия, может однажды дать белый текст на белом фоне.
Это и есть ответ на «может отказаться» — а не молчание.

**Как правильно.** `MENU_TILE` уже отвечал на этот же вопрос для той же
формы (подпись на снимке, короткий текст) числом 25 — с обоснованием тут же
в файле («заливка стоит ровно под словами, а не половиной кадра»).
`LOOK_TILE` («Choice by need») и `Categories` при виде `full` («Choice by
category») теперь берут это же число, а не своё: `edge:
MENU_TILE.edge`. Один и тот же вопрос — один и тот же ответ, а не третье
число рядом с двумя уже решёнными.

**Где искать то же самое.** Заданный заказчиком вопрос с двумя частями
(«почему плохо» и «может убрать совсем») — обе части закрывать явно, а не
одну молча: правка, отвечающая на более лёгкую половину, читается как
законченная и версия из докс не покажет, что вторая осталась. И
`TILE_DEFAULT`/наследование параметров плитки: число, посчитанное для
одного вида (`mount`), унаследованное другим (`full`) без пересчёта, —
кандидат, потому что роль числа у каждого вида своя.

## И127 · Грань фильтра — раскрывашка по умолчанию, а не полотно

Панель фильтра каталога (`CatalogBrowser.tsx`) растёт гранями — категория,
повод, содержание CBD, вид, марка, цена, — и у каждой свой список внутри.
Показанные разом, они складывались в одно длинное полотно: до цены,
последней грани, приходилось листать мимо всех, что человека сейчас не
касались.

**Дефект.** Заказчик увидел форму дефекта раньше, чем она успела вырасти:
панель ещё стояла одной категорией в пять строк, а он уже назвал правило —
«список категории, проценты, бренды и т.д. — все эти подфильтры должны
раскрываться-закрываться пользователем, чтоб он не листал полотно
длинное». Тот же разговор он попросил записать в скилл заранее, а не после
находки на живой витрине: «это очевидные вещи, всё записывай их в скилл,
чтоб ты сам знал при делании следующих сайтов».

**Как правильно.** Каждая грань — `<details>` (`Group` в
`CatalogBrowser.tsx`), а не `<div>`: показ и скрытие содержимого делает
браузер, JS-состояние под это не заводится. Заголовок остаётся заголовком
страницы — `<h3>` лежит ВНУТРИ `<summary>`, и лестница h1 → h2 панели → h3
грани не рвётся. Раскрыта по умолчанию грань с уже стоящим выбором
(`defaultOpen={picks.X.length > 0}`) — свернуть отмеченное хуже, чем
показать лишнюю строку, — и первая грань (категория, с неё чаще всего
начинают) раскрыта всегда. Колонка на столе и окно фильтра на телефоне
(`FilterSheet.tsx`) показывают один и тот же список граней (`facetGroups`),
поэтому раскрывашка досталась обоим местам одной правкой, а не двумя.

**Где искать то же самое.** Список фактов или граней, который растёт с
данными и не ограничен полкой, — кандидат на ту же раскрывашку: сегодня в
пять строк, а сколько будет, когда каталог переедет в Payload и марок
станет тридцать, — не решено вёрсткой, вёрстка обязана держаться при любом
числе строк. Разбор — `.claude/skills/craft/references/controls.md`,
раздел «Длинная грань — раскрывашка, а не полотно».

## И128 · Подпись, подобранная под каждое фото, — дороже подписи со своей поверхностью

Вид плитки `full` держал контраст подписи вуалью, чью плотность на каждый
снимок отдельно считал `tools/shade.mjs` (И108, И116, И121, И126 — четыре
правки подряд про одно и то же число). Заказчик сравнил `full` вживую на
проде с четырьмя заменами — по одной на карточку «Choice by need» — и
отклонил его без сомнений: «Кучета, Болки и стави — прежний вид — удаляй
полностью и с сайта, и с дизайн системы».

**Дефект.** Подпись на фотографии, которой контраст даёт вуаль, — это
подпись, зависящая от снимка: тёмный кадр просит одну плотность, светлый
другую, и каждая новая фотография (а они уедут в Payload и будут меняться
без нас) может однажды дать заливку не той высоты или не того тона.
Четыре правки подряд (И108, И116, И121, И126) чинили именно это число —
не потому что чинили неправильно, а потому что сама конструкция просит
починки на каждом нюансе фотографии.

**Как правильно.** Вид держит контраст своей ПОВЕРХНОСТЬЮ, а не вуалью:
`bar`/`mount` кладут подпись на плотный лист рядом со снимком, а не на
него; `chip` — на плотную плашку-пилюлю поверх снимка, размером в свои
слова, а не во всю ширину; `shadow` — собственной тенью букв
(`filter:drop-shadow(...)` на `.body`, одной парой слоёв на весь столбик и
знак разом). Ни один из четырёх не читает яркость снимка и не ломается от
его замены. Замер фотографии (`tools/shade.mjs`, `lib/shade.ts`,
`lib/bright.ts`) не удалён — он переносимый инструмент набора для сайта,
где подпись прямо на фото понадобится вновь, — просто у этого сайта его
сегодня никто не зовёт.

**Где искать то же самое.** Любая подпись или контрол, чей контраст
держится числом, подобранным под конкретное фото или конкретный цвет фона,
— кандидат на переезд на собственную поверхность или тень: чем меньше
контраст знает о том, что под ним, тем меньше правок купит следующая
смена снимка.

## И129 · Снятый вид пережил себя в браузере заказчика и уронил сайт

И128 убрал вид плитки `full` из кода — а через минуту после вливания
заказчик открыл `cbdin.bg/en` на телефоне и увидел белый экран: «поломался
сайт… на полсекунды показывает сайт и затем не работает».

**Дефект.** Панель дизайна пишет выбор вида плитки (`catType`, `lookType`)
в localStorage браузера, а склад переживает деплой: в браузере заказчика,
который сам выбирал виды плитки на этом же сайте раньше, лежала строка
`"full"`. После вливания И128 эта строка читалась как есть, без проверки:
`TYPE_TYPE['full']` в `PictureTile.tsx` — `undefined`, деструктуризация
`undefined` кидает исключение, всё дерево React падает белым экраном.
Отрисованная HTML-разметка с сервера при этом успевает мелькнуть до того,
как клиентский код перехватывает управление и падает, — отсюда «полсекунды
показывает и потом не работает». Так ломается ЛЮБОЕ поле панели, чей набор
допустимых значений сузили: склад хранит старый ответ бессрочно, а код
проверяет его только типами TypeScript, которых во время выполнения в
браузере уже нет.

**Как правильно.** Место, которое ЧИТАЕТ значение из склада
(`Categories.tsx`, `Looking.tsx`), проверяет его по текущему списку
допустимых (`isTileType`, сверяется с `TYPES` — тем же списком, что видит
панель) и подменяет на решённое умолчание, если значения там больше нет.
Проверка стоит на границе, где внешние данные (localStorage чужого
браузера) входят в систему, — а не в компоненте ниже по цепочке, который
вправе полагаться на то, что тип уже настоящий `TileType`.

**Где искать то же самое.** Любое поле панели с ограниченным набором
значений (`'choice'` в `lib/studio/schema.ts`) — кандидат на ту же
проверку при сужении набора: чужой браузер помнит выбор годами, и код
узнаёт об этом только когда падает. Проверять на границе склада дешевле,
чем гадать, у скольких браузеров сохранён снятый вариант.

## И130 · Меньше шрифт — не значит меньше поле: полосу держало не то число

И129 (следом за И128) уменьшил заголовок плитки, но полоса под фото на
телефоне почти не сжалась — заказчик увидел ту же «Кучета» и назвал точно:
«нужно сильно уменьшить белое поле только чтоб текст помещался — оно
отрезает от изображения».

**Дефект.** Поле полосы (`.tile[data-type='bar'] .body`) стояло
`--sp-4`/`--sp-5` — той же шкалой РИТМА, что несёт поле карточки товара
(правило 2, CLAUDE.md). Ритм — это воздух МЕЖДУ вещами, и уменьшенный
заголовок на него не влияет: пока число берётся со шкалы страницы, а не от
устройства самой полосы, полоса остаётся того же роста, что и была, — а
рост строки на деле держал не текст и не поле вокруг него, а знак «куда
ведёт» (общий `--h` у `.cta`), который есть в каждой полосе независимо от
длины подписи.

**Как правильно.** Полоса — не абзац с воздухом по краям, а плотная лента:
поле сверху и снизу взято от геометрии контрола (`--sp-2`, «внутри
контрола» — тот же язык, что у правила 2), а не от ритма страницы.
Горизонтальное поле (`--sp-5`) не тронуто — высоту оно не растит.

**Где искать то же самое.** Любая полоса или плашка, чья высота на глаз
«не должна зависеть от текста внутри» (заголовок короче — полоса всё равно
не короче некоторого роста), — кандидат на поле «внутри контрола», а не
поле «карточки»: спутать эти две шкалы легко, потому что обе называются
«отступом», а держат за собой разные вопросы (правило 2).

## И131 · Знак держал рост пилюли, а поле вокруг него было неравным

И130 сжало поле полосы, но не тронуло сам знак «куда ведёт» и вторую
плашку — «пилюлю» (`chip`). Заказчик сравнил на телефоне ту же «Кучета» и
дал два прицельных замечания подряд: «тут пилюлю уменьшить по высоте» и
«текст располагай на равном удалении от низа плитки и от левого края, вот
слева зазор правильный, такой же сделай и снизу».

**Дефект.** Два независимых числа держали пилюлю выше и кривее, чем ей
нужно:
* её собственное поле было неравным по осям — слева `--sp-3`, снизу
  `--sp-2` — хотя вынос пилюли от края плитки (`inset`) уже был одним
  числом на обеих осях. Сумма «вынос + поле» слева и снизу поэтому не
  совпадала, и текст стоял ближе ко дну, чем к боку;
* знак «куда ведёт» — общий `.cta{--h}` — был числом (38px), унаследованным
  при сведении трёх старых стрелок в одну (`styles/go.module.css`), а не
  мерой от строки подписи рядом с ним. На плотной пилюле, где строка одна и
  подпись короткая, знак оказывался выше собственного текста и один держал
  рост всего предмета.

**Как правильно.** Поле пилюли по обеим осям, к которым у заказчика был
вопрос («от низа» и «от края»), взято одним и тем же числом (`--sp-3`), а
не двумя разными шкалами на вид одинаковой природы. Рост знака уменьшен до
величины, соизмеримой со строкой рядом с ним (28px вместо 38); мишень для
пальца при этом не мельчает — её держит невидимый `.tap::after`
(`styles/primitives.module.css`), который расширяет зону нажатия знака до
44px независимо от того, каким рисунком он показан на экране, а не рисунок
самого знака. Число одно на все четыре вида
плитки (`.cta{--h}` не привязан к `data-type`), поэтому правка снимает
лишний рост и у полосы, и у белой плитки, и у тени — одним местом.

**Где искать то же самое.** Любая пара «вынос предмета от края» + «поле
внутри предмета» на разных осях — кандидат на ту же сверку: если вынос
одним числом на обе оси, а поле — разными, сумма расходится, и глаз видит
несимметричный предмет там, где числа выглядят разумными поодиночке.

## И132 · Знак не держит высоту НИКОГДА, а не «пока меньше текста»

И131 подвинул число (`--h:38px` → `28px`, общее на все четыре вида) и
рассудил, что двустрочная подпись не пострадает, потому что 28 меньше
высоты двух строк. Заказчик поймал слабое место в самом рассуждении, а не в
числе: «с какой стати стрелка держит высоту, а не текст — текст-то может
быть и в две строки». Число было подобрано так, чтобы СЕЙЧАС проиграть
тексту, но ряд (`align-items:center` в общем `.body`) по устройству всё
равно меряется по самому высокому ребёнку — знаку или тексту, — и в любой
момент, где заголовок короче (другая ширина экрана, другое слово, другой
язык), снова мог начать диктовать высоту знак, а не подпись.

**Дефект.** Число, подобранное «чтобы сейчас победил текст», — это гонка
чисел, а не устройство: она держится ровно до первого случая, где числа
опять сойдутся не в ту сторону. Правильный размер знака — вопрос вкуса и
пропорции, а НЕ вопрос того, кто из двух детей ряда выше; но у ряда
`align-items:center` это один и тот же вопрос, потому что оба смысла
завязаны на одно число.

**Как правильно.** У полосы («bar») знак вынут из ряда СОВСЕМ, тем же
приёмом, что стрелка выпадающего списка (`styles/form.module.css`,
`.pick svg`): `position:absolute`, поверх текста, по центру высоты `.body`.
Высоту `.body` теперь считает только `.words` — обычный поток, без
знака-соседа, — а знаку бронируют его край `padding-inline-end`. Знак может
стать хоть 12px, хоть 44 — высоту полосы это больше не тронет никогда, при
любой длине подписи на любой ширине. Остальные три вида (`mount`, `chip`,
`shadow`) пока остаются на общем `--h:28px` из И131 — там подпись либо уже
не в одну строку с рядовым знаком (`mount`), либо знак меньше давит на
глаз на своей плотной поверхности (`chip`, `shadow`); тот же приём для них
имеет смысл ровно тогда, когда заказчик увидит там ту же болезнь.

**Где искать то же самое.** Любая строка, где декоративный или
вспомогательный элемент (знак, счётчик, метка) стоит РЯДОМ с текстом
переменной длины через `align-items:center` — кандидат на ту же проверку:
если элемент никогда не должен определять высоту строки, его недостаточно
сделать «пока меньше» — его нужно вынуть из потока.

## И133 · Стрелки полки не прятались на телефоне — медиазапрос проигрывал порядком

Заказчик увидел на телефоне полку товара («CBD масло»), которую наполнение
уже листает свайпом, — а рядом всё равно стояла пара круглых стрелок:
«на рельсах, где наполнение свайпится пальцем, кнопки листания не нужны».

**Дефект.** Правило `@media (max-width:820px){.arrow{display:none}}`
(`Controls.module.css`) было записано верно и должно было сработать на
390px — но `.arrow` и `.go` (`styles/go.module.css`, `display:inline-grid`
БЕЗ медиазапроса) стоят на одном элементе с ОДИНАКОВОЙ специфичностью
(один класс), и при равной специфичности побеждает не более узкое условие,
а тот, кто записан ПОЗЖЕ в собранном файле, — а `.go` шёл в сборке после
`Controls.module.css`. Стрелки поэтому оставались `display:inline-grid` на
любой ширине, и брейкпоинт молчал, не выдавая ошибки: правило есть в файле,
применяется, просто проигрывает соседнему.

Тот же класс ошибки уже чинили у стрелок героя, но с другой стороны:
там условием была ширина ОКНА вместо указателя (`HeroSlider.module.css`,
«где есть палец, а не где узкое окно»). Здесь неверными были сразу два
слоя одной вещи.

**Как правильно.** Прячется обёртка стрелок целиком (`.pair`), а не
каждая стрелка своим классом: скрытому родителю нечего спорить с `.go` —
его детям, каким бы ни был их собственный `display`, показывать нечего.
Условие — указатель (`hover:none и pointer:coarse`), а не ширина окна, тем
же приёмом, что уже верен у героя. Третий орган кластера (выход из блока)
в обёртку не входит: свайп не выводит из блока.

**Где искать то же самое.** Любое `display:none` внутри `@media`,
нацеленное на класс с ОДНИМ уровнем специфичности против другого класса
того же элемента, — под подозрением: специфичность у обоих одна, и решает
порядок файлов в сборке, а не то, чьё условие уже, — читка исходников
этого не покажет, только замер отрисованной страницы.

## И134 · Пол ячейки был числом по разные стороны одной черты — и разной сеточной механикой платят по-разному

Заказчик двигал окно на компьютере и увидел скачок размера плитки сразу на
нескольких блоках — «Choice by need», «Choice by category», Instagram,
отзывы: «где то проблема с текучестью в какой то ширине... проверь всё
внимательно».

**Дефект (полоса, `.field[data-layout='rail']`).** Минимум ширины ячейки
(`--cell-min`) был константой — 240/200/170/150px в зависимости от заявки
(`data-across`) — и ниже 560px эту же роль играло НЕСВЯЗАННОЕ с константой
правило `grid-auto-columns:84%`. Число само по себе было верным по обе
стороны черты, но 84% от 340px и константа 240px — разные величины, и на
560px, где формула переключалась, ячейка скакала: 170px → 293px за один шаг
ширины окна. Замер (плотный проход Playwright, 320–900px, шаг 10px)
подтвердил скачок на Instagram и отзывах именно на этой отметке.

**Как правильно.** `--cell-min` стал текучей шкалой, тем же приёмом, что и
у размера шрифта (`--fs-*`): `clamp(пол, C ± N*vw, потолок)`, один и тот же
на любой ширине. Переход через 560px больше ничего не трогает, потому что
там нечему меняться — обе стороны черты читают одну формулу. Коэффициент при
`vw` — на 100 больше, чем «пикселей на пиксель ширины», потому что `1vw` —
это процент ширины окна, а не пиксель; первая попытка забыла про этот
множитель и клэмп всё время держался потолка (замечено по
`getComputedStyle`, показавшему буквально «300px» там, где ожидалось
плавное значение).

**Другая механика — другой скачок, и это не тот же дефект.** «Choice by
need»/«Choice by category» по умолчанию стоят в РЕЖИМЕ СЕТКИ
(`data-layout:'grid'`, `composes:grid from primitives.module.css`), а не
полосы, и там скачок остаётся даже после текучего `--cell-min`: подтверждено
принудительным `data-layout='rail'` на этих же блоках — скачок исчезает
целиком, значит дело не в поле панели этих блоков, а в самой формуле сетки.
`repeat(auto-fill, minmax(X, 1fr))` считает число колонок отдельно от их
ширины: как только становится тесно для ещё одной колонки шириной `X`,
`auto-fill` добавляет её, а `1fr` заново делит всю ширину ряда на новое
число колонок — с целым числом колонок нельзя иначе, дробной колонки не
бывает. Это тот же самый механизм «лестницы», который `primitives.module.css`
уже документирует как следствие вычисляемого счёта (запрет 5), только раньше
все блоки теряли колонку на ОДНИХ и тех же 960/1240 разом (страница
проседала на 27% высоты за 40px), а после починки — каждый на своей ширине.
Полностью убрать этот скачок, оставаясь на `auto-fill`, нельзя: заменить
сетку полосой можно, но это смена СМЫСЛА раскладки («несколько в ряд» →
«лента с прокруткой»), решение заказчика, а не почин.

**Где искать то же самое.** Любая пара «формула A выше отметки, формула B
ниже» для одной и той же роли — минимума, отступа, любого числа — читается
одинаково безобидно в файле и скачет только отрисованной страницей: два
места для одной величины (правило 2) находятся по замеру, не по чтению.

## И135 · Один просвет `switcher` отвечал за два разных шва — бок о бок и один под другим

Заказчик увидел карту товара и спросил: «зачем этот огромный промежуток
между изображениями и звёздочками».

**Дефект.** `.switcher` (`styles/primitives.module.css`) — два столбца,
превращающиеся в один без медиазапроса (правило 6), — берёт ОДНО число
(`--switch-gap`) на `gap`, и `gap` во флексе без своей пары `row-gap
column-gap` красит ОБЕ оси разом. У карты товара `--switch-gap:var(--sp-11)`
(64–100px) — верное число между кадром и колонкой покупки БОК О БОК на
столе. Ряд сложился на телефоне в столбик — и та же величина стала швом
между концом галереи и началом строки оценки: воздух между колонками
разворота, приложенный там, где сосед уже другой.

**Как правильно.** `gap` принял ДВЕ величины: `--switch-gap-stack` (шов
одного-под-другим) и `--switch-gap` (шов бок о бок), с обратной
совместимостью — не назвал первое, получаешь второе на обеих осях, как
было. Карта товара называет оба отдельно: `--switch-gap` остаётся
`--sp-11` (он же кормит потолок заголовка на шве — трогать нельзя),
`--switch-gap-stack` — `--air-block` («между частями одного блока»),
подходящий шов для конца галереи и начала строки оценки. Другие
потребители `.switcher` (contacts, WriteForm) держат маленький
`--switch-gap` и ничего не меняют: `--switch-gap-stack` для них не назван,
и обе оси остаются одним числом, как раньше.

**Где искать то же самое.** Любой примитив, где `gap` — одно число на
обе оси, а раскладка ПЕРЕКЛЮЧАЕТ смысл соседства (бок о бок → один под
другим, строка → столбик): если под него подставили величину, верную
только для ОДНОЙ из двух геометрий, вторая получает её по ошибке в
наследство.

## И136 · Тень на нажатии обрезалась плоской линией — колодец под неё был меньше самой тени

Заказчик увидел на живой полке товаров: «тень под карточкой обрезается
снизу чем-то, наверное боксом, в котором карточка стоит — это обрезание
тени очень некрасиво».

**Дефект.** Полоса товаров (`components/Rail.module.css`, `.track`) держит
`overflow-y:hidden`, чтобы вторая ось прокрутки не открылась сама (см.
И63-соседний приём) — и оставляет для тени только запас в `var(--sp-2)`
(8px) снизу. Тень карточки на нажатии (`--sh-3`, `ProductCard.module.css`)
несёт дальний слой `0 36px 80px -28px` — она уходит вниз заметно дальше
восьми пикселей, и обрезка полосы срезает её плоской линией вместо мягкого
угасания.

**Как правильно.** Запас снизу назван по тени, а не по ритму соседей —
`--rail-shade:88px` (36 + 80 с запасом), с той же компенсирующей
отрицательной тенью в `margin`, что уже стояла у 4px сверху: полоса не
получает лишнего воздуха в обычном потоке страницы (поле и отрицательное
поле гасят друг друга), а во ВРЕМЯ прокрутки и нажатия у тени теперь есть
куда упасть. Число — не ступень шкалы `--sp-*`: оно тени и принадлежит, и
если тень когда-то станет глубже, колодец под неё называется той же
переменной, а не считается заново.

**Где искать то же самое.** Любой `overflow-*:hidden` вокруг предмета,
который на руку отвечает шагом тени (`--sh-2`/`--sh-3`), — если запас с той
стороны, куда тень растёт, меньше её дальнего слоя, обрезка проявляется
только под рукой (наведение, нажатие) и не видна в покое — читка файла
этого не покажет.

## И137 · `minmax(пол, %)` в самогенерирующейся дорожке считает процент «авто» — крупная карточка получилась мелкой

Заказчик пролистал главную ночью и увидел: «карточки товаров явно меньше,
чем карточки инстаграма и ревью, там просто огромные карточки» — и попросил
подумать, что с этим делать.

**Дефект.** Полоса товаров на телефоне (`components/Rail.module.css`)
объявляла крупную карточку с початой соседней у края: `grid-auto-columns:
minmax(210px, 72%)`. Дорожка при этом ГЕНЕРИРУЕТСЯ САМА (`grid-auto-flow:
column` на скроллящейся сетке, а не фиксированный `grid-template-columns`)
— а спецификация Grid для этого случая отдельно оговаривает: если у сетки
нет определённой ширины ряда для расчёта процента верхней границы
`minmax()`, эта граница считается «авто», и побеждает нижняя. Результат:
карточка стояла на своём полу (210px) всегда, и запись «крупная карточка»
была верна только на бумаге — заказчик увидел на витрине то, что было в
файле с самого начала, просто раньше сравнивать её было не с чем.

**Как правильно.** `max(пол, %)` вместо `minmax(пол, %)` — тем же приёмом,
что уже держит дорожку плиток (`styles/field.module.css`, разбор в И134):
`max()` не подчиняется этому частному случаю спецификации Grid и считает
процент от настоящей ширины ряда, потому что это обычная арифметика, а не
аргумент track-sizing функции. Число (72%) не менялось — менялся только
способ его посчитать, и карточка стала ровно той, какой её описывал
комментарий все это время.

**Где искать то же самое.** `minmax(A, B%)` в `grid-auto-columns` (не
`grid-template-columns` с известным числом колонок) — под подозрением
всегда: дорожка, порождающая сама себя на скролле, не имеет определённой
ширины ряда для процента, и верхняя граница `minmax()` в такой дорожке
беззвучно откатывается к нижней. Признак не ловится чтением файла — только
замером отрисованной колонки (`grid-template-columns` в devtools покажет
буквальное число вместо процента).

## И138 · Знак минуса перед `var()` без `calc()` — не число, а обрыв всего объявления

Заказчик увидел на стене Инстаграма, что соседний снимок справа совсем не
выглядывает из-за края, хотя у полки товара и у отзывов рядом выглядывает.

**Дефект.** Полоса плиток (`styles/field.module.css`, `.field[data-layout=
'rail']`) держала `margin:-4px calc(-1 * var(--gut)) -var(--sp-2) 0`.
Третье значение — `-var(--sp-2)` — не валидный CSS: знак минус перед
`var()` без `calc()` вокруг него не считается числом, а спецификация в этом
случае бракует ВСЁ объявление `margin` целиком, из всех четырёх значений
разом, и откатывает свойство к нулю. Полоса от этого не доезжала до правого
края поля вовсе, ни на каком полу, — а не только на тёмной палубе (тот
случай отдельный, И139).

**Как правильно.** `calc(-1 * var(--x))` — та же форма, что уже верно стоит
у первого значения (`calc(-1 * var(--gut))`) в этой же строке; вторая
копия того же приёма просто не была дописана тем же способом.

**Где искать то же самое.** Любой знак минуса ПРЯМО перед `var(...)` в
значении свойства (не внутри `calc()`) — невалидный токен. Браузер не
предупреждает и не красным подчёркивает: значение просто не участвует, а у
shorthand-свойства (`margin`, `padding`, `border-radius`…) из-за одного
плохого значения обрывается ВСЁ свойство целиком, и симптом — то, что
должно было измениться, стоит как было. `getComputedStyle` в devtools
покажет невозможное совпадение с начальным значением там, где в файле
написано число.

## И139 · Полоса пересекала зазор окна там, где у обёртки было собственное поле листа

Второй слой той же находки заказчика («даже соседнее изображение справа не
выглядывает»): починка И138 подняла просвет, но не до конца — плитка
Инстаграма всё ещё выглядывала заметно меньше, чем у соседних блоков.

**Дефект.** Полоса бежит навылет за счёт пары «поле + отрицательное поле»:
`padding-inline-end:var(--gut)` резервирует место у правого края, а
`margin-inline-end:calc(-1 * var(--gut))` вытягивает полосу в этот же
зазор, так что последняя плитка доезжает до истинного края. Число `--gut`
верно ТОЛЬКО когда у обёртки (`.wrap`) нет собственной набивки — тогда
зазор от края окна и правда равен `--gut`, он зашит в саму ширину `.wrap`
(`calc(var(--wrap) - var(--gut) * 2)`), а не в поле. На тёмной палубе
(`data-ground='deck'`) у обёртки ЕСТЬ собственное поле — поле ЛИСТА
(`--pad-sheet`, примитив `sheet` в `styles/primitives.module.css`), и оно
ШИРЕ зазора окна. Полоса internal пересекала `--gut`, а нужно было
пересечь `--pad-sheet` — недобирала разницу, и плитка почти утыкалась в
край листа, не долетая до него.

**Как правильно.** Названа переменная `--rail-bleed`: на поле страницы она
равна `--gut` (умолчание в `.wrap`, ничего не меняется), на листе —
`--pad-sheet` (переопределена в том же правиле примитива `sheet`, что уже
задаёт `padding-inline` листа, — то же число, а не вторая копия). Полоса
(`field.module.css`, `Rail.module.css`) читает `var(--rail-bleed, var(--gut))`
вместо голого `--gut` — и сама узнаёт, сколько пересекать, от того, на чьей
обёртке стоит, а не от одного числа на все случаи.

**Где искать то же самое.** Любая полоса, бегущая навылет парой «поле плюс
отрицательное поле» с одним и тем же именем токена по обе стороны, —
верна только для ОДНОЙ геометрии обёртки. Обёртка на листе (правило 7:
«лист внутри листа берёт поле ступенью ниже») имеет другое поле, и полоса
внутри нужно узнавать эту величину у обёртки, а не предполагать её.

## И140 · Потолок поля страницы менялся ступенью — и на пикселе шва становился ýже при более широком окне

Заказчик прислал шесть снимков подряд, плавно сужая окно, и увидел, что
размер плитки то падает, то снова растёт — «где-то неоптимальность,
проверь, пойми принцип».

**Дефект.** `--gut` — зазор от края окна до страницы — держал число
`--gut-base` (44px) и ДВЕ отдельные строки, снижающие потолок на узких
окнах: `min(44,24)` не шире 1080, `min(44,12)` не шире 820. На стыке
брейкпоинта окно становится ШИРЕ на один пиксель, а потолок — ýже:
на 820px потолок 12, на 821px — уже 24, и ширина `.wrap`
(`calc(100% - --gut*2)`) падает на замере на 23px за один пиксель окна.
На 1080/1081 та же картина, ещё резче — минус 39px. Замер:

```
820px → 796px      821px → 773px      (минус 23 на плюс 1)
1080px → 1032px    1081px → 993px     (минус 39 на плюс 1)
```

Отсюда и разговаривал заказчик: ширина ряда, от которой считает поле
плиток, сама скакала назад на этих отметках, и любая формула внутри поля
наследовала прыжок, даже будучи безупречной.

**Как правильно.** Потолок — одна кривая вместо двух ступеней: те же числа
(12 и 44) на тех же отметках (560 и 1080), что стояли раньше, но между ними
линия, а не порог. `.wrap` (и то, что от него меряется) теперь растёт
вместе с окном на любом пикселе, без обратного хода.

**Где искать то же самое.** Любая пара `@media(max-width:A){X:f(A)}` /
`@media(max-width:B){X:f(B)}`, где A < B и f(A) < f(B) (узкому окну — МЕНЬШЕ
числа), — при переходе через B заводит ту же ловушку: `max-width` растёт в
обратную сторону тому, что стоит внутри условия, и на стыке окно шире, а
число, которое читает следующая формула, меньше.

## И141 · `max()` с убывающим членом против растущего — гарантированная впадина

Тот же дефект, что и в И140, но найден заказчиком с другой стороны и на
другой причине: прямым сравнением шести снимков подряд он увидел, что
размер плитки «течёт и изменяется — уменьшается, а потом начинает расти»,
и попросил понять ПРИНЦИП, а не только починить конкретное место.

**Принцип.** Ширина полосы в `field.module.css` считается как
`max(--cell-min, доля_ряда)`, где `доля_ряда` растёт вместе с шириной поля
всегда (простая доля от контейнера). Максимум двух чисел остаётся
монотонным, только если растут ОБА; если одно из них (`--cell-min`) при
этом УБЫВАЕТ — сделано это было намеренно, чтобы плавно свести крупный
телефонный минимум к мелкому настольному, — их максимум неизбежно проходит
через впадину: пока убывающее число ещё больше растущего, максимум
ПОВТОРЯЕТ его падение, и лишь потом, когда растущее его нагоняет,
разворачивается вверх. Форма кривой `--cell-min` тут ни при чём — хоть
плавная, хоть ступенчатая: убывает она — впадина есть.

**Как правильно.** `--cell-min` вернулся к простому числу (без `vw`) на
каждую заявку (`data-across`): 240px на три/четыре/пять, 180px на шесть.
Число не убывает — оно не меняется вовсе, и максимум с растущей долей ряда
неизбежно монотонен: константа плюс растущая величина растут (или стоят)
всегда. Крупная телефонная плитка получается тем же способом, что и
раньше объяснял комментарий поля, — минимум перебивает малую долю ряда,
пока ряд узкий, — но без второй кривой, которой есть с чем столкнуться.

**Где искать то же самое.** Любой `max(A, B)` (или `min(A, B)`) в формуле
размера, где A и B — обе функции одной и той же ширины: если хотя бы одна
из них не монотонна В ТУ ЖЕ СТОРОНУ, что растёт входная ширина, их max/min
может провалиться или подскочить на участке, где они меняются местами.
Проверяется не чтением (обе формулы по отдельности выглядят гладкими), а
только замером готового значения на плотной сетке ширин — как у sweep,
только по самому числу, а не по снимку.

## И142 · Заголовок плитки читал лестницу слоя управления вместо текстовой

Дефект нашла проверка текучести (по слову заказчика «проверь текут ли
размеры текстов»), не заказчик глазом: у меню «По ефект» заголовок
плитки был назван `var(--fs-ui-sm)` — тем же числом (14px), что и раньше,
но взятым НЕ с той шкалы. `styles/tokens.css` прямо перечисляет, где
работает лестница слоя управления — «шапка, чекмедже, нижняя панель», — и
тут же исключает ровно этот случай: «заголовок карточки к этой лестнице не
относится: он текст». Заголовок плитки — тот самый заголовок карточки.

Число совпало с `--fs-ui-sm` только потому, что меню открывается лишь на
столе (`display:none` ниже 1081px), и на этой ширине `--fs-xs` (правильная,
текстовая ступень) и так лежит у своего потолка, 14.5px — разницы на глаз
нет. Но источник был неверным: `--fs-ui-sm` — плоское число, а `--fs-xs` —
кривая; переставь их местами, и на узком контейнере (уменьшится ли сама
плитка когда-нибудь) заголовок молча перестал бы течь. Заменено на
`var(--fs-xs)`.

**Где искать то же самое.** Любой `--fs-ui-*` на ЭЛЕМЕНТЕ, который
описывает содержимое (заголовок, подпись, цена, описание), а не устройство
самого контрола (высота пилюли, поле внутри неё, надпись НА кнопке), — под
подозрением, даже если число сошлось случайно. Проверяется по СЕЛЕКТОРУ,
не по значению: control-scale токен на `<b>`/`<span>` внутри карточки —
почти всегда не то место.

## И143 · Контейнер не может спросить размер у самого себя

Дефект найден при попытке переключить раскладку пары кнопок шторки корзины по
ширине её же собственной строки: `@container(min-width:…){ .acts{
flex-direction:row } }`, объявленное на том же `.acts`, что несёт
`container-type:inline-size`, — не срабатывало никогда, при любой ширине,
и правило безмолвно оставалось в столбце.

**Причина.** Спецификация Container Queries прямо исключает элемент из
списка кандидатов для его же СОБСТВЕННОГО запроса: `@container` ищет
БЛИЖАЙШЕГО ПРЕДКА с подходящим `container-type`, и предок, будучи самим
собой, не считается. Если другого контейнера выше нет, запрос падает не с
ошибкой, а молча — либо на ближайший НАЗВАННЫЙ контейнер дальше вверх,
либо (если такого тоже нет) на исходный containing block, то есть
фактически на окно. Тот же принцип уже подтверждён этой ночью на другом
свойстве: `cqi` внутри `grid-auto-columns` элемента, объявившего
`container-type` на самом себе, тоже считает мимо себя и откатывается к
`vw` — то же самое исключение, другое свойство.

**Как правильно.** Containment ставится на ПРЕДКА того, что стилизуется
по контейнеру, — на один уровень выше как минимум. У шторки корзины это
`.foot` (родитель `.acts`), у которого и так уже был `display:flex` для
другой цели, никакого нового узла в DOM заводить не пришлось.

**Где искать то же самое.** Любой `@container` (или `cqi`/`cqb` в
значении), где стилизуемый селектор и контейнер, у которого спрошен
`container-type`, — ОДИН И ТОТ ЖЕ элемент. Симптом всегда один: правило
внутри `@container` будто не существует, при любой ширине сразу — не
компилируется тихо, не ошибка в консоли, просто не применяется никогда.

## И144 · `width:100%` в готовой кнопке спорило с `flex-basis` ряда — ряд никогда не собирался

Пара кнопок шторки корзины была задумана и записана в код как ряд
(«так решил заказчик»), но заказчик увидел на живой шторке столбик — на
любой ширине, хотя комментарий обещал ряд «когда слова помещаются».

**Дефект.** Общая кнопка (`styles/btn.module.css`) держит
`.btn[data-wide]{inline-size:100%}` — решение для ДРУГОГО места (сводка
заказа, где кнопки и вправду должны быть на всю колонку). Шторка корзины
кладёт ту же кнопку в ряд (`.acts{display:flex} .acts > *{flex:1 1
140px}`), рассчитывая, что `flex-basis:140px` даст каждой кнопке РОСТИ от
140px, а не встать сразу на 100%. Но `inline-size:100%`, заданный
ПРЯМО на элементе, и `flex-basis` от родителя — в реальном рендере
выигрывает явная ширина, и обе кнопки, каждая шириной в свой ряд, не
могут стоять рядом ни при какой ширине контейнера: пара всегда падает в
две строки.

**Как правильно.** Ширина `wide`-кнопки переведена на переменную —
`inline-size:var(--wide-inline, 100%)` — с тем же умолчанием, что и
раньше (кто не переопределил ручку, получает прежние 100%). Ряд, которому
нужна другая ширина у детей, называет `--wide-inline:auto` у себя, и
тогда решает `flex-basis`, как и было задумано.

**Где искать то же самое.** Общий контрол с собственным `width`/
`inline-size`, поставленный внутрь ЧУЖОГО flex- или grid-ряда, который
рассчитывает управлять его шириной через `flex`/`grid-column` — явная
ширина на самом элементе почти всегда сильнее заявки родителя, и раскладка
родителя работает только на бумаге. Признак: ряд, для которого в
комментариях описан один сценарий («переносится сама»), а на снимке —
только другой («столбик всегда»).

## И145 · Отрицательное поле примитива съедало шов до соседа целиком

Заказчик засомневался на карте товара: «между звёздочками и брендом есть
воздух?» — и не зря, воздуха не было вовсе, хотя `margin-top` у следующей
строки был назван.

**Дефект.** Строка оценки и артикула (`.meta`, карта товара) берёт примитив
`rail` — тот резервирует `padding-block:var(--sp-1)` (место под кольцо
фокуса на краях полосы) и тут же гасит его для соседей
`margin-block:calc(var(--sp-1) * -1)`, чтобы резерв не превращался в
лишний воздух страницы. Своя строка `.meta{margin-top:6px}` переопределила
верх, но НИЗ остался от примитива — `margin-bottom:-4px`. Следующая строка
(`.brandRow`) несла `margin-top:var(--sp-1)` — те же 4px. Соседние
вертикальные поля СХЛОПЫВАЮТСЯ в большее по модулю, а не складываются: 4px
положительных против 4px отрицательных дали ровно ноль. Шов, записанный в
файле, на странице не существовал.

**Второй дефект в той же строке.** `.meta{margin-top:6px}` — число без
опоры на что-либо, из-за которого строка оценки начиналась на 6px ниже
верхнего края фотографии рядом. Заказчик увидел это на снимке и спросил
прямо, так ли задумано. Не было: `.meta` — первый ребёнок колонки, и
никакой строке над ним воздух не нужен.

**Как правильно.** `.meta{margin-top:0;margin-bottom:0}` — верх вровень с
кадром, низ не тянет чужое отрицательное поле примитива в игру. Шов до
марки называет один хозяин, `.brandRow{margin-top:var(--air-row)}` —
токен «между строками одной группы», а не `--sp-1` (ручка для оптических
доводок меньше восьми пикселей, не для ритма). Проверено замером: верх
строки совпал с верхом кадра день в день, шов до марки — 16px на столе.

**Где искать то же самое.** Любой примитив с парой «резерв плюс
отрицательное поле» (rail, полоса с бегущим краем — разбор той же семьи в
И136/И139) — если потребитель переопределяет ТОЛЬКО одну сторону
(`margin-top` без `margin-bottom` или наоборот), другая сторона остаётся
от примитива и может схлопнуться с соседским полем непредсказуемо.
Смотреть отдельно: `getComputedStyle` каждой стороны margin, а не только
то, что написано в файле потребителя, — схлопывание считает браузер, не
читка.

## И146 · Служебные строки без своей зоны читаются одной кучей

Заказчик открыл выдвижное меню телефона и сказал прямо: «очень неочевидно
расположена информация, как-то всё в куче».

**Дефект.** `.drawerUtil` держал три ряда подряд, одной высоты и одной
одеждой, но трёх разных смыслов: «Сохранённое» и «Учётка» — личный кабинет
человека, «Напишите нам» — способ связи, то есть по смыслу сосед
«Информации» ниже, а не «Учётки» выше. Заголовка над рядами не было
вовсе — визуально они читались продолжением списка «Информация» под ним,
хотя код держал их разными блоками. Ряд ниже — подвал с языком — нёс ещё и
текст «Бесплатная доставка над 50 €», который уже сказан дважды: в шторке
корзины (`CartPanel.module.css`, `.free`) и в самой шапке на телефоне
(`Header.module.css`, `.headUtil`). Три места на одну и ту же фразу читают
её не как факт, а как шум.

**Как правильно.** Ряды одной формы группируются по СМЫСЛУ, а не по
случайному соседству в файле, и у каждой группы — свой заголовок
(`.drawerSec` + `p.eyebrow`, тот же приём, что уже есть у «Категории» и
«Информация»): «Лично» — вход в личное (иконки «Сохранённое»/«Профил»,
кружком, а не строкой — заказчик так и попросил, «на место текста иконки»),
«Информация» — весь остальной справочный текст плюс «Напишите нам» одним
списком. Повторяющийся факт (бесплатная доставка) убирается там, где он
третий по счёту, а не переписывается по-новому.

**Где искать то же самое.** Любой список рядов одной вёрстки без заголовка
над группой — кандидат на «кучу», даже если код давно разносит их на разные
блоки: раскладка ничего не знает о смысле, который вложен в код, и человек
читает то, что нарисовано, а не то, что в разметке. Проверять глазом, не
идёт ли подряд три и больше визуально одинаковых ряда без подписи зоны —
особенно в чекмедже и в панелях настроек, где рядов накапливается быстрее
всего.

## И147 · Свайп без `touch-action` спорит с системным жестом «назад»

Заказчик провёл пальцем по вкладкам товара на телефоне и сказал: «меню
свайпится только вправо, вне зависимости от того куда свайплю пальцем, и
плавности мало».

**Дефект.** `.panel` (вкладки карты товара, `Tabs.module.css`) слушает
горизонтальный свайп через `useSwipeTabs`/`usePointerSwipe`, но не объявляет
`touch-action` вовсе — браузеру оставлено решать самому, что делать с
горизонталью пальца. На телефоне то же движение читается ЕЩЁ и системным
жестом «назад» по краю экрана (частый жест у мобильных браузеров, в том
числе MIUI): два хозяина ведут один палец в разные стороны, и вперёд
браузер иногда пускает жест раньше, чем JS успевает решить, куда листать, —
отсюда «только вправо». Та же панель ловит `pointercancel` посреди жеста,
когда системный жест перехватывает палец, — переход не доигрывает, и это и
есть «плавности мало». Тот же примитив меню (`.menu`, `styles/primitives.module.css`),
которым одета шторка сообщений в шапке (`.chatMenu`, тоже с `useSwipeClose`),
страдал тем же: ни строки `touch-action` у общего меню не было вовсе.

**Как правильно.** `touch-action:pan-y` на самом свайпаемом элементе:
горизонталь отдаётся JS-жесту, вертикаль остаётся браузеру (скролл списка,
если он есть). Тот же приём уже стоял на шторке корзины и на чекмедже
(`CartPanel.module.css`, `Header.module.css`) — было забыто перенести на
панель вкладок и на общий примитив меню. `will-change:transform,opacity`
добавлен туда же, где панель уже едет transform'ом (`data-phase`) — плавнее
на слабом телефоне, браузер готовит слой заранее, а не с первого кадра
перехода.

**Где искать то же самое.** Любой элемент, слушающий `usePointerSwipe`/
`useSwipeClose`/`useSwipeTabs` (`onPointerDown`/`onPointerMove` на самом
себе), обязан объявить `touch-action` — без него горизонталь всегда
конкурирует с браузерным жестом на телефоне, и обнаруживается это только
руками на живом устройстве, не в headless-проверке: синтетические
`PointerEvent` в тесте не воспроизводят системный жест «назад» вовсе.
Правится на примитиве (`.menu`), если жест ловит именно он, а не на каждом
потребителе порознь.

## И148 · Пилюля мерилась своим текстом, а не семьёй

Заказчик увидел ряд пилюль крепости на карте товара: «5% пилюля короче и
ломает ряд, её нужно делать длиннее, чтоб пилюли были одинаковые».

**Дефект.** `.opt` (выбор варианта на карте товара, `product.module.css`) —
`display:inline-flex` без заданной ширины: пилюля растёт от СВОЕГО текста.
Семья вариантов почти всегда одной длины («10% · 10 ml», «15% · 10 ml»,
«30% · 10 ml», «10% · 30 ml» — по одиннадцать знаков), кроме одного члена
короче на разряд («5% · 10 ml», десять знаков) — и он один сужался вместе
со своим текстом, разбивая ряд, где остальные пилюли стоят вровень.

**Как правильно.** `min-width:13ch` на пилюле — пол в знаках, а не в
пикселях: `ch` меряет по цифре «0», а строка вперемешку с «%», «·» и
пробелами плотнее одной цифры, поэтому пол считается с запасом (мера самой
длинной формы плюс два знака, замерено: 129px факта против 110px при
одиннадцати). Растёт вместе со шкалой текста (`--fs-base` течёт), а не
стоит числом на одной ширине экрана; подпись длиннее пола (крупная
фасовка, миллиграммы в четыре разряда) через него просто перерастает, не
ломаясь.

**Где искать то же самое.** Любой ряд пилюль/кнопок одной СЕМЬИ (варианты,
единицы, фильтры), где текст внутри разной длины по данным, а не по
дизайну, — кандидат: без общего пола короче член семьи всегда будет
сужаться следом за своим текстом и торчать среди равных. Мерить не чтением
CSS, а `getBoundingClientRect` по каждому члену ряда — на глаз разница в
один разряд слишком мала, чтобы её заметить в файле, но видна на
странице.

## И149 · Клетке сетки некуда переносить надпись — толкает сетку вместо себя

Заказчик увидел на живой странице живьём: «кнопки снова повилазили, ты же
уже чинил, и опять» — пилюли «Бърза поръчка» и «Добави в количката» вылезли
за край листа покупки на телефоне.

**Дефект.** `.buyRow` (панель покупки карты товара) — сетка с именованными
клетками, и орган в клетке растягивается сам, а сжаться ниже своей подписи
ему не даёт умолчание `min-width:auto` — так и задумано, разбор уже стоял в
файле (И136 того же рода). Но у «Бърза поръчка» (`Btn`, `styles/btn.module.css`)
надпись несла `white-space:nowrap` БЕЗ отдушины: `data-wide` (ручка,
разрешающая перенос на вторую строку, `white-space:normal`) на кнопке не
стоял. Минимальная ширина клетки СЧИТАЛАСЬ по этой неразрывной строке —
и когда строке не хватало места (крупный шрифт устройства, короткое
устройство ввода экрана, длинный болгарский перевод), клетка не могла
уступить: она толкала сетку целиком шире отведённого, и вместе с ней —
соседнюю «Добави в количката» (та стоит второй строкой сетки, `'add add'`,
и делит с первой строкой общую ширину колонок). Пилюли вываливались за
белый лист покупки, до самого края экрана.

Рядом нашлась и вторая, отдельная мелочь того же файла: `BuyBtn.tsx`
собирала класс тернаркой `size === 'page' ? s.page : s.card`, а
`BuyBtn.module.css` держала `.page{}` — правило без единого свойства.
Пустое правило CSS-модуль в сборку не включает, и `s.page` читался как
`undefined` — строка эта уходила в `className` голым текстом. Ни на что не
влияла (пустой класс и не должен был), но чинить причину, раз уж нашлась
рядом, — правильнее, чем оставить рабочий, но говорящий не то код.

**Как правильно.** `wide` на кнопке `Btn` — та же ручка, что уже держит
кнопки шторки корзины: надпись переносится на вторую строку, кнопка растёт
в высоту, а не толкает сетку в ширину. `min-width:0` на клетке `.quick`
снимает пол СВЕРХУ этой отдушины — сжиматься теперь можно, потому что есть
куда деть лишнее слово. У «Добави в количката» пол остался прежним
(`min-width:auto`, без переноса) — переносить эту надпись некуда и незачем
(разбор уже стоял здесь же), и она никогда не была узким местом сама по
себе: она спана на всю ширину сетки и просто наследовала чужую ширину,
раздутую соседкой. `.page{}` и его тернарка убраны — класс, который ничего
не красит, заменён на пустую строку без обращения к несуществующему полю
модуля.

**Где искать то же самое.** Любая кнопка/пилюля, растянутая ГРИД- или
ФЛЕКС-раскладкой в клетку переменной ширины (не собственной, фиксированной),
но несущая `white-space:nowrap` без парной ручки переноса, — кандидат:
клетка сжаться не может, и минимальная ширина всей раскладки считается по
самой длинной неразрывной строке среди всех её органов, а не по доступному
месту. Проверять — переводом на другой язык длиннее исходного (болгарский
почти всегда длиннее английского) и увеличенным шрифтом устройства
(`html{font-size:150%}` в эмуляции), а не только сужением окна: заказчик
поймал это НЕ на узком экране, а на своём обычном телефоне — ширина не
виновата, виновата неразрывная строка.

## И150 · `useEffect` после первой отрисовки — переход без старта

Заказчик прислал второй раз: «при свайпе вправо-влево нет плавности, ты
вроде делал, но не работает». Первый раз (И147) чинил спор за палец, а не
саму плавность — эта причина осталась нетронутой.

**Дефект.** Лента вкладок (`Tabs.tsx`) кладёт уходящую панель на место, а
приходящую — за край, кадром `data-phase='start'` без перехода, и только
следующим кадром (`'go'`) переключает разом — тогда CSS и подхватывает
переход. Эффект, который это устраивает, стоял `useEffect`. `useEffect`
откладывается на кадр ПОСЛЕ того, как браузер уже нарисовал экран — то есть
после того, как `at` сменился, а `anim` ещё `null`: первый видимый кадр
показывал панель уже в конечном положении, без всякого перехода рисовать
было нечего. Эффект добирался до `start` только следующим кадром — картинку
дёргало НАЗАД, в стартовое положение, перед тем как повезти её обратно.
Комментарий рядом честно объяснял двойной `requestAnimationFrame` как
защиту «до первой отрисовки экрана» — но именно это `useEffect` не
гарантирует: гарантия «до отрисовки» есть только у `useLayoutEffect`.

**Как правильно.** `useLayoutEffect` вместо `useEffect` — синхронный,
выполняется ДО того, как браузер нарисует кадр. Первый видимый кадр после
смены `at` теперь и есть `start`, а не конечное положение, и двойной
`requestAnimationFrame` внутри (оставлен как есть) действительно ловит
переход так, как задумывал комментарий. Проверено замером кадров браузера:
`start` держится измеримый кадр (стабильная отрисованная точка), затем
`transform` меняется плавно на протяжении ~10 кадров подряд — не прыжком.

**Где искать то же самое.** Любой «FLIP»-переход (сначала положить БЕЗ
перехода, следующим кадром — переключить С переходом) — кандидат, если
первый шаг стоит в `useEffect`: гарантии «до отрисовки» у него нет, есть
только у `useLayoutEffect`. На быстрой машине с высоким кадром в секунду
разрыв может быть незаметен на глаз и не поймается в записи с виду плавного
прогона — искать надо в самом коде по факту `useEffect`, а не по тому,
дёргает ли картинка на экране разработчика.

## И151 · Инструмент админки глотает скролл под собой

Заказчик на кассе: «Скрол пальцем перестает работать на карточках товара».

**Дефект.** Плавающий запуск «Site Administration» (`components/studio/Studio.tsx`,
значок ползунков) в сложенной раскладке (≤820px) стоит фиксированно над
нижней панелью — и несёт `touch-action:none` по всей ширине медиазапроса, а
не только там, где кнопку правда тащат пальцем. `touch-action:none` там
держался ради перетаскивания на столе (`onPointerDown`/`onPointerMove` с
`setPointerCapture` в `Studio.tsx`), но в сложенной раскладке место кнопки
задано жёстко тремя строками CSS с `!important` — перетаскивание там ничего
не двигает, а свойство осталось. На странице кассы список товаров сводки
заказа доходит как раз до этой высоты, и палец, начавший скролл ровно на
кружке кнопки (44×44, z-index выше витрины), не получал скролла вовсе —
браузер, видя `touch-action:none`, с самого начала жеста решал не
скроллить, а отдать жест кнопке.

**Как правильно.** В медиазапросе `@media (max-width:820px)`
(`styles/studio.module.css`) свойству возвращён `touch-action:pan-y` —
перетаскивание там и так не работает, а обычный скролл под кнопкой теперь
идёт как везде. Короткое нажатие по-прежнему открывает панель (`onUp`
реагирует на `pointerup` без сдвига).

**Где искать то же самое.** Любой фиксированный элемент с
`touch-action:none`, оставшимся от драг-логики, которая на части ширин
отключена (позиция жёстко задана CSS), — кандидат: свойство продолжает
глотать жест там, где драгу уже нечего делать. Проверять — не на пустой
странице, а там, где содержимое реально доходит до высоты плавающего
элемента.

## И152 · Один просвет для одинаковых пар строк

Заказчик на форме входа: «тут везде разній расстояния, разній воздух, так
и должно біть?».

**Дефект.** Просвет между кнопками Google/Facebook (`.social` в
`AccountForm.module.css`) стоял `--sp-3` (10–12px), а просвет между полями
Имейл/Парола (`.rows` в `styles/form.module.css`) — `--air-row`
(`--sp-4`, 14–16px), хотя обе пары — «строки одной группы» по смыслу, и
весь остальной сайт (`WriteForm.module.css` и другие) держит именно этот
случай на `--air-row`.

**Как правильно.** `.social{gap:var(--air-row)}` вместо `var(--sp-3)` —
тот же токен, что у соседней пары строк в той же форме.

**Где искать то же самое.** Столбик одинаковых по роли строк (пара
кнопок, пара полей, список действий) — кандидат, если его просвет набран
числом ступени шкалы (`--sp-N`) напрямую, а не именем `--air-row`/`--air-group`
по смыслу расстояния. Проверять сравнением с соседней парой того же
экрана — разные просветы читаются глазом, даже когда оба «в пределах
шкалы».

## И153 · Число в меню и число в фильтре — из разных источников

Заказчик прислал снимок чекмедже рядом со снимком окна фильтра и попросил
единый источник: «у них должен быть один источник».

**Дефект.** Число товаров рядом с полкой в выдвижном меню (`COUNTS` в
`components/headerNav.ts`) было набрано рукой, литералами: `'/catalog':'112'`,
`oils:'38'` и так далее — и устарело в день, когда каталог сократили до
сорока товаров-заглушек. Трёх полок в таблице не было вовсе («Крем и гел с
канабис», «CBD вейп», «CBD цветя») — их строки в меню молча показывали
пустое место вместо числа. Окно фильтра (`CatalogBrowser.tsx`) тем временем
считает то же самое честно, `tally()` по живому списку `PRODUCTS` — и
показывало верные тринадцать, шесть, семь. Два места отвечали на один
вопрос по-разному, и заказчик поймал разницу сравнением экранов.

**Как правильно.** `lib/products.ts` получил `CATEGORY_COUNTS` —
посчитанный из `PRODUCTS` объект, тем же приёмом, что уже стоит рядом
(`BRANDS`, `OCCASION_COUNTS` в `lib/occasions.ts`). `COUNTS` в
`headerNav.ts` собирается из него, а не переписан руками. Число одно,
потому что источник один.

**Где искать то же самое.** Число рядом с названием (счётчик, итог,
остаток), набранное литералом в компоненте навигации или в конфиге, —
кандидат всегда, когда где-то в проекте уже есть настоящий, посчитанный по
данным аналог того же факта. Проверять — сравнением экранов, которые
отвечают на один вопрос: если на один и тот же счёт можно попасть двумя
разными путями, оба обязаны показывать одно число.

## И154 · Один Reset в двух местах одной панели фильтра

**Дефект.** Заказчик увидел в открытом окне фильтра серую полупрозрачную
пилюлю «✕ Изчисти» и сказал прямо: «удали лишнюю исчисти кнопку». Она была
второй: та же кнопка, тот же `clear()`, уже стоит в полосе над полкой
(`CatalogBar.tsx`, `resetBtn`) — а окно фильтра (`FilterSheet.tsx`) держало
собственную копию сверху списка граней, оставшуюся с тех пор, когда
переключатель вида делил с ней строку.

**Как правильно.** Копия убрана: `FilterSheet.tsx` больше не принимает
`picked`/`clear` и не рисует `.reset`/`.displayRow` — вместе с ними ушли и
мёртвые правила CSS. Сброс фильтра остаётся там, где он один — в полосе
над полкой; закрыть окно и нажать его можно тем же движением, каким
открывали.

**Где искать то же самое.** Одна и та же функция (`clear`, `reset`,
`toggle`), нарисованная в двух разных местах разметки под предлогом «пока
делили экран», — кандидат на правило 10, если повод для второй копии
(нехватка строки, общая панель) уже снят другой правкой, а копия осталась.

## И155 · Пустой квадрат без рамки — по-разному пуст в разных браузерах

Заказчик прислал снимок окна фильтра с телефона: ряд названий полок без
единого квадратика рядом — «нет чекбоксов».

**Дефект.** Квадратик есть в разметке (`<input type="checkbox">`, `.check
input` в `catalog.module.css`) и красится своим цветом только в ОТМЕЧЕННОМ
состоянии (`accent-color`) — рамку пустого квадрата рисует браузер сам,
своей темой, безо всякого токена сайта. На части браузеров/тем эта рамка
почти не видна на белом поле — заказчик увидел ряд подписей без единого
квадрата там, где в другом браузере он есть.

**Как правильно.** `styles/base.css` получил одну сквозную строку:
`input[type='checkbox'],input[type='radio']{border:1.5px solid
var(--sage-8)}` — рамка теперь одна и та же на любом устройстве, а не то,
что нарисует браузер. Квадратик остаётся браузерным (клавиатура, чтец,
среднее состояние) — правится только его цвет, не устройство. Строка
одна, а не четыре копии по числу мест (`.check`, `.sortRow`, `.radio`,
`.tick`) — у всех один и тот же квадрат, только своего размера.

**Где искать то же самое.** Родной `<input type="checkbox">`/`radio` без
явной рамки, крашенный только `accent-color`, — кандидат: отмеченное
состояние видно всегда, а пустое зависит от темы браузера и может быть
почти невидимым. Проверять на живом телефоне, а не в headless-браузере
разработки — там пустой квадрат нередко рисуется иначе.

## И156 · Полка показывала всё разом, даже сотню

Заказчик прислал снимок чужой витрины с рядом «Покажи още» и «Показани 12
от 13» и сказал прямо: «в магазине сейчас показываются все товары хоть
сто, а должно же быть так».

**Дефект.** Полка каталога (`CatalogBrowser.tsx`) рисовала весь отобранный
список одним полотном — что тридцать восемь карточек масел, что сорок
всего каталога, — и до сводки под товарами приходилось прокручивать мимо
всех разом.

**Как правильно.** Сетка карточек и строки таблицы получили атрибут
`data-cap` (`ProductCard.module.css`, `IndexTable.module.css`) — товары за
двенадцатым скрыты CSS (`:nth-child(n+13){display:none}`), а не убраны из
разметки: первый же собранный HTML содержит их все, поиск и ИИ видят
список целиком, человек — по нажатию «Покажи още» (`CatalogMore.tsx`).
Число двенадцать — с его же снимка.

**Где искать то же самое.** Список данных, отрисованный одним `.map()` без
ограничения, — кандидат везде, где список может вырасти (блог, отзывы,
будущие полки после переезда в Payload). Решать НЕ убиранием из разметки
(это спорит с правилом «текст обязан быть в разметке»), а видимостью через
CSS-атрибут — так список остаётся честным перед поиском и открытым для
человека по одному нажатию.

## И157 · Строка сменила устройство и осталась без своего признака

Заказчик увидел список без единого квадратика и решил: «чекбоксы пропали и
фильтр не работает».

**Дефект.** Грань «Категория» в фильтре каталога сменила устройство в
параллельной работе (И22: категория — путь, а не параметр) — строка стала
ссылкой (`LinkRow`), а не галочкой (`Row`). У ссылки нет своего квадратика,
и это правильно: ей нечего отмечать, она ведёт. Но нынешняя строка («вы
здесь») осталась совсем без метки — ни фона, ни жирности, ничего не
отличало её от соседей. Человек, привыкший видеть в фильтре квадратики,
увидел голый список без единого признака состояния и решил, что фильтр
сломан целиком, хотя остальные грани (с настоящими галочками) работали
исправно.

**Как правильно.** `.check[aria-current='page']` — тот же приём, что уже
красит «вы здесь» в чекмедже (`Header.module.css`, `.drawerNav a.isActive`):
заливка (`--cyan-3`), жирность, цвет числа (`--cyan-11`). Один и тот же
факт («вы здесь») красится одной и той же одеждой в обоих местах.

**Где искать то же самое.** Строка списка, у которой сменился РОД
взаимодействия (галочка → ссылка, кнопка → переключатель), — кандидат:
старый визуальный язык (квадратик) исчезает вместе со старым устройством, а
новый признак состояния («выбрано», «вы здесь», «открыто») иногда забывают
завести взамен. Без него список читается как ничего не отвечающий, даже
когда сама механика работает исправно.

## И158 · `overscroll-behavior:contain` на пустом сдерживает жест впустую

Заказчик описал признак предельно точно: «если тапать по карточкам товара —
не скроллится, если выше или ниже карточки — работает, если сбоку экрана вне
поля карточек — работает». Ни одна синтетическая проверка касания это не
поймала — воспроизвелось только на живом телефоне, в двух браузерах.

**Дефект.** Список товаров в сводке заказа на кассе (`.mini`,
`checkout.module.css`) считал себя вложенной прокруткой и нёс
`overscroll-behavior:contain` — сдержать разгон списка, чтобы докрученный до
конца палец не тащил за собой страницу под ним. На широком экране сводка
приклеена и правда имеет свой потолок высоты (примитив `pinned`), и список
внутри неё правда прокручивается отдельно от страницы. На телефоне (≤1080px)
сводка намеренно теряет потолок и становится обычным блоком в потоке
страницы (`.summary{--pin-pos:static;--pin-max:none}`) — прокручивать
`.mini` там просто нечего, вся сводка едет вместе со страницей. Но
`overscroll-behavior:contain`, объявленный на `.mini` БЕЗ условия по ширине,
остался — и на контейнере без единого пикселя прокрутки внутри он всё равно
сдерживал жест: палец, начавший тянуть ровно там, где стоит `.mini` (то есть
на строках товара), не получал скролла страницы вовсе, потому что браузер
честно «сдерживал» контейнер — просто сдерживать в нём было нечего, и жест
терялся целиком, а не тормозился. Рядом, там, где `.mini` не накрывает
(заголовок сводки, итог, кнопка), тот же палец скроллил страницу как обычно.

**Как правильно.** В том же медиазапросе, что снимает потолок со сводки,
`.mini` получает `overscroll-behavior:auto` — сдерживать там уже нечего, и
жест возвращается странице целиком.

**Где искать то же самое.** Любой `overflow-y:auto; overscroll-behavior:contain`
на блоке, чей потолок высоты СНИМАЕТСЯ на части ширин (примитив `pinned` с
`--pin-max:none`, ручное переключение на `position:static`), — кандидат:
на той ширине, где потолка нет, у блока и прокрутки внутри нет, а
`overscroll-behavior:contain`, оставшийся без условия, глушит жест на всей
площади блока, а не только там, где он правда сдерживает. Проверять только
на реальном сенсорном экране — синтетическое касание через CDP/Playwright
эту разницу не ловит.

## И159 · Одно и то же место назвали двумя именами в двух местах

«Повод» (сон, спорт, кожа — шесть плиток, «шопинг по нужде») уже назывался
«Choice by need» на главной (`Looking.tsx`) и в десктопном мега-меню
(`BenefitMega.tsx`). Боковое меню телефона (`HeaderDrawer.tsx`, через
`headerNav.ts`) при этом несло старое имя — «By benefit». Заказчик его не
показывал: находка вскрылась по ходу переделки бокового меню, тем же
признаком, каким проект уже мерил «две правды об одном факте» (правило
«Один токен — одно значение» из раздела «Переносимость»).

**Дефект.** Общий `NAV`-массив (`headerNav.ts`), из которого строится и
рабочая строка стола, и чекмедже телефона, держал метку `'By benefit'`.
Тот же самый вход в тот же самый набор плиток на главной и в мега-меню уже
звался иначе. Два имени одного места — не опечатка, а девятая одежда на
один вопрос «куда ведёт» (перекличка с правилом 10).

**Как правильно.** Переименовано в источнике — `NAV`, `BENEFIT_LABEL`,
группа `ALL_GROUPS` в `headerNav.ts`, заголовок мега-меню в
`BenefitMega.tsx`, подпись в таблице видов дизайн-системы (`lib/tiles.ts`,
`SHAPES`) — везде, где встречалось старое имя, а не только там, где нашли.
Один факт, одно место: перевод в словаре к тому же уже стоял готовым
(`'Choice by need': 'Избор по нужда'`) — эта же строка уже использовалась
в блоке главной, что и подтвердило правильное имя.

## И160 · Заказчик отменил решение, а не нашёл в нём баг

Категория каталога однажды (И22, раньше в этом же файле) стала путём вместо
галочки — обдуманно, ради ссылок и индексации. Заказчик увидел живую
страницу, вспомнил, что раньше можно было отметить несколько категорий
сразу, и потребовал вернуть — не потому что И22 была ошибкой, а потому что
хочет ОБЕ вещи разом.

**Отличие от дефекта.** Здесь не было сломанного признака, который надо
починить: код делал ровно то, что написал И22 — категория вела на страницу
полки, а не отмечалась. Заказчик отменил само решение, вспомнив старое
поведение, — и это не «баг, который я проглядел», а решение владельца
магазина, которое стоит выше моего ремесленного вкуса (раздел «Решение
выбирается по тому, что лучше для поиска…» в `CLAUDE.md`). Правило,
принятое с той же силой, отменяется с той же силой, если человек, который
его принял, передумал.

**Решение.** Обе правды остаются, каждая на своём месте — этому и учит
слой «Переносимость»: один факт, разные проводники к нему.
`/catalog/oils` — по-прежнему страница, свой `canonical`, свой заголовок,
свой `alt` для того, кто прислал на неё ссылку. Сверху — категория такая же
галочка, как повод, марка или крепость, живёт в адресе тем же способом
(`?category=oils&category=paste`), считается тем же правилом («грань
против всех фильтров, КРОМЕ своего собственного»), уходит в `noindex` по
тому же правилу И22, если добавили вторую сверх собственной страницы.

Развязка на стыке путь/параметр — в `useCatalogPicks.ts`: пока меняется
любая из пяти прежних граней, адрес остаётся тем же путём с той же строкой
запроса; как только меняется САМА категория, происходит настоящий переход
(`router.push`, не `history.pushState`) на общий `/catalog` со всем
накопленным выбором в строке. Держать `/catalog/oils?category=oils&category=paste`
значило бы вести две записи одного факта — путь и параметр сразу, — и
рано или поздно они разошлись бы.

**Где чинить забыли бы, если торопиться.** Переход на другую СТРАНИЦУ, а
не тот же путь с другой строкой запроса, — `router.push`, а не голый
`history.pushState`, каким записаны остальные пять граней. И у `router.push`
адрес обязан нести язык (`withLocale`, `lib/locale.ts`) — без языкового
префикса переход в этом Next тихо не срабатывает вовсе, а не падает с
ошибкой: замечено только живой проверкой в браузере, не тайпчеком и не
линтером.

## И161 · Сокращённая запись `padding` тихо съела чужое поле

Три листа сайта — чекмедже, шторка корзины, окно фильтра — красят шапку
одним общим компонентом (`components/SheetHead.tsx`), и это уже было решено
правильно. Заказчик всё равно нашёл дефект тремя снимками: полоска шапки
фильтра — узкая, у чекмедже и корзины — заметно шире, «заведи эту форму в
дизайн системе, чтоб был один источник правды».

Источник правды уже был один — `SheetHead.module.css`, `.head{padding-block:
var(--air-row)}`, отвечает за рост ЛЮБОЙ шапки листа. Разница взялась не от
второй копии решения, а от того, что вызывающий файл написал своё поле
сокращённой записью: `FilterSheet.module.css` держал `padding:0 var(--pad-sheet)`
— выглядит как «только поле по бокам», а на деле красит ВСЕ четыре стороны,
включая верх и низ, обнуляя `padding-block`, который дал `SheetHead`. Тот же
`className={s.head}` собирает классы из ДВУХ файлов на одном узле — сокращённая
запись в более позднем побеждает длинную в более раннем, даже когда автор
более поздней хотел тронуть только одну ось. У шторки корзины (`CartPanel.module.css`)
поле уже стояло верно, `padding-inline:var(--pad-card)`, — потому она и была
«широкой»: правильный пример был рядом, просто в другом файле.

Третий лист, чекмедже (`Header.module.css`, `.drawerHead`), решением
`SheetHead` не пользовался вовсе (нет `name`/`onClose` API — там знак марки,
а не подпись) и держал СВОЁ число, `height:64px`, — третий источник правды о
той же самой высоте, третье число рядом с двумя. Переведён на тот же приём:
`padding:var(--air-row) var(--sp-3)` вместо зашитой высоты — рост считается
от содержимого и общего поля, как у двух других, без своего числа.

**Правило.** Локальный файл, что-то ДОБАВЛЯЮЩИЙ к общему компоненту через
`className`, красит СВОИМИ СВОЙСТВАМИ только те оси, которые действительно
свои (`padding-inline`, `margin-inline`, конкретная сторона), а не всей
сокращённой записью — иначе она заденет то, что уже покрашено чужим,
более ранним правилом на том же узле, и сотрёт его молча, без ошибки и без
следа в диффе того файла, который на самом деле сломался.

## И162 · Черта делит грани, а не заголовок с его же списком

`app/[lang]/catalog/catalog.module.css` уже НОСИЛ правильный разбор рядом с
кодом: «волосок делит грани так же, как шапку… внутри грани его нет — там
строки одного списка, и разделять их нечем». Написано верно, сделано
неверно: черта (`border-bottom`) стояла на `.groupTitle` — заголовке-`summary`
— и потому лежала всегда СРАЗУ под ним. У свёрнутой грани `<details>` и
правда кончается на заголовке, и черта честно отделяет её от следующей. У
развёрнутой — заголовок и её же чекбоксы (Category, Effect) оказывались
разделены собственной чертой, ровно то, что абзац выше запрещал.

Заказчик нашёл этот дефект глазом, глядя на раскрытую «Effect», и спросил
напрямую: «полоска разделяет заголовок и его чекбоксы, разве так
правильно?» — и был прав. Черта
переехала на саму грань целиком (`.group`, элемент `<details>`), а не на
`.groupTitle`: у закрытой грани `<details>` по высоте равен своему
заголовку, и черта стоит на том же самом пикселе, что и раньше — никакой
разницы для восьми из десяти состояний панели. У открытой — граница
съезжает к концу списка чекбоксов, к следующей грани, куда и должна была
попасть с самого начала.

**Правило.** Написанный рядом с кодом разбор — не документация на будущее,
а обещание, которое код обязан держать. Разбор говорит «черта делит
СОСЕДЕЙ» — значит черта живёт на границе между ними (`<details>` целиком,
`.group + .group` или подобное), а не на первом видимом ряду одного из
соседей, который в закрытом состоянии СЛУЧАЙНО совпадает с этой границей и
маскирует ошибку до первого раскрытия.

## И163 · Два независимых `100dvh` для одной и той же коробки

Заказчик нашёл дефект глазом на телефоне: открыл окно фильтра, прокрутил
вверх — и всё поле фильтра на миг уехало вверх и вернулось назад. Только
на первой загрузке; во второй раз — уже нет. Позже добавил: иногда то же
самое ловится снизу, потянув список дальше конца, — и на миг видна страница
под окном.

**Дефект.** `FilterSheet.module.css` держал ДВЕ независимые высоты одной и
той же коробки: `.panel` (сам `<dialog>`) занимает экран через `inset:0` —
верно, это уже разбирал соседний комментарий («не проценты — иначе на
первом кадре видно, как браузер центрует и перепрыгивает»). Но `.sheet`
внутри него, вместо того чтобы взять готовую высоту родителя, СЧИТАЛ СВОЮ
СОБСТВЕННУЮ — `block-size:100dvh`. Два разных места, два разных замера
одного и того же факта («сколько сейчас пикселей у экрана»). Пока телефон
складывает адресную строку — а это происходит ровно на первом скролле после
загрузки страницы, один раз, — эти два замера успевают на кадр разойтись:
`.panel` уже пересчитан, `.sheet` ещё нет (или наоборот), и лист оказывается
не вровень со своим окном. В щель заглядывает то, что за ним. `CartPanel.module.css`
нёс тот же самый второй замер, тем же приёмом, рядом с тем же `.panel{inset:0}`
— тот же дефект, ещё не пойманный глазом только потому, что список корзины
короче и его реже докручивают до края.

**Правило.** У коробки, которая ЦЕЛИКОМ повторяет размер уже готового
родителя (`.panel{inset:0}`), не бывает второго, своего измерения того же
факта — только `block-size:100%`/`inline-size:100%`, что наследует решение,
а не пересчитывает его заново. `dvh` — правильный инструмент ТАМ, где
измерение одно и не с кем спорить (потолок `pinned`, максимум центрированной
модалки), и неправильный там, где рядом уже есть родитель с тем же самым
числом, посчитанным иначе.

## И164 · Вторая кнопка «Home» ведёт назад, а не наверх

Заказчик решил: нажатие на «Home» в нижней полосе, когда человек уже на
главной, обязано вернуть наверх, тем же приёмом, каким нативные приложения
отвечают на повторное нажатие уже открытой нижней вкладки. Стояло голой
`<Link href="/">` — на уже открытом адресе она не двигает раскладку и не
прокручивает ничего сама: второе нажатие было немым.

**Решение.** `TabBar.tsx`, ссылка «Home»: если адрес уже `/`, нажатие
перехватывается (`e.preventDefault()`) и вызывает `window.scrollTo({top:0,
behavior: glide()})` — тот же отклик, что уже стоит у знака «наверх»
(`Helper.tsx`), значит и то же уважение к «меньше движения» в `glide()`.
Если адрес другой — ссылка ведёт себя как обычная ссылка, ничего не
перехватывает.

**Признак заплатки, которого здесь нет.** Решение не заведено вторым, своим
`scrollTo` — оно берёт готовую функцию `glide()` из `lib/motion.ts`, ту же,
которой уже пользуется кнопка «наверх». Кнопок, отвечающих на палец
прокруткой, в проекте теперь две, и обе решают «чем крутить» в одном месте.

## И165 · Пилюля обещала фильтр, а вела на голую полку

Заказчик нажал «20%» в раскрытой «CBD oil» меню-пилюль — и получил ту же
полку масел, что и без нажатия: процент не выбрался, счётчик выбранных
фильтров не появился, кнопка «Reset» осталась выключенной. «Переходы из
меню в магазин глючные», — и был прав: три места печатали одни и те же
грани (крепость, тип, марка) — десктопное мега-меню, первый вариант
чекмедже, второй — и все три вели ссылкой на голую `/catalog/oils` без
единого параметра, вне зависимости от того, какую именно строку нажали.

**Дефект.** Ссылка на грань и разбор строки запроса (`searchToPicks` в
`lib/catalog.ts`) — два разных места одного и того же знания: как выглядит
адрес, что фильтр по 20% крепости. Ссылка никогда не знала об этом знании
и вела на постоянный `OILS_HREF`. Пока грань была строкой текста — это
читалось как «зайти в раздел», необидно. Ставшая пилюлью, та же строка
стала ВИЗУАЛЬНЫМ ОБЕЩАНИЕМ «выбери меня, как в фильтре» — и не выполняла
его.

**Решение.** Адрес каждой грани посчитан ОДИН раз, рядом с самой гранью
(`lib/oils.ts`): `strengthHref(pct)` и `kindHref(kind)` строят тот же токен,
что понимает `searchToPicks` (`PARAM`, `slugOfKind`, `strengthToken` —
экспортированы из `lib/catalog.ts` ради этого), а `BRAND_FACETS` несёт
готовый `href` через уже существующий `brandHref` (`lib/brands.ts`). Все
три места печати (десктопное мега-меню, `HeaderDrawer.tsx`,
`HeaderDrawerPills.tsx`) читают `f.href`/`r.href`, а не строят его заново.

**Что осталось голой ссылкой, и почему это не забытый угол.** У минорного
каннабиноида («CBD + CBG») и у времени суток («Day»/«Evening») в фильтре
каталога нет своей грани вообще — заводить им параметр значило бы менять
устройство фильтра, а не чинить ссылку. `href` там остаётся `undefined`,
пилюля ведёт на полку без обещания. Найденное — заказчику: если эти две оси
стоит когда-нибудь сделать настоящими гранями фильтра, это отдельный
разговор про устройство фильтра, не однострочная правка.

## И166 · Счётчик, пилюли и сброс считали выбор по-разному

После И165 пилюля меню вела на настоящий фильтр (`/catalog/oils?strength=
20pct`) — и заказчик увидел следующий слой того же места: «в фильтре
почему-то указана единичка, а не двойка, выбрано то два фильтра. И не
работает правильно кнопка крестик резет — при её нажатии одна пилюля
выбора пропадает, а одна остаётся. Т.е. частичный резет». Попросил
проверить детально и дважды — гонялся `systematic-debugging`.

**Дефект.** Категория страницы (`initialCat`, «CBD масло» на
`/catalog/oils`) по решению И160 не считается выбором на своей же
странице — так решено нарочно, у `picked` (счётчик) и `clear()` (сброс) в
`useCatalogPicks.ts` это исключение уже стояло. `chipsOf()` в
`lib/catalog.ts` про него не знало и печатало пилюлю для базовой категории
всегда — третье место одного и того же факта, и оно единственное молчало
про исключение. Счётчик показывал «1» (правильно — только выбранная
крепость), пилюль в строке было две («CBD масло» + «20%»), а «Сброс»,
честно следуя И160, снимал только некатегориальные грани и оставлял
пилюлю категории — то, что заказчик и увидел «частичным сбросом», хотя
кнопка работала как раз так, как задумано И160. Расхождение было не в
сбросе, а в том, что показывал ряд пилюль.

**Решение.** `chipsOf()` получил тот же параметр, что уже стоит у вызова
`picked`/`clear` — `skipCat` (`initialCat` страницы), и пропускает его
слаг в цикле по `picks.cats`, той же одной строкой (`if (slug === skipCat)
continue`). Если поверх базовой категории выбрана вторая (мультивыбор,
И160) — её слаг под исключение не попадает, пилюля и счётчик её видят.
Проверено живым браузером дважды: одна грань на странице категории (1
пилюля = счётчик «1», сброс убирает её и снимает счётчик), базовая плюс
добавленная категория (2 пилюли = счётчик «2», пилюля добавленной ведёт на
`/catalog` настоящим переходом).

## И167 · Свёрнутая грань фильтра не говорила, что внутри выбор

Дефект нашёл заказчик: выбрал грань, свернул её и не смог понять, есть ли
внутри отметка, не раскрыв заново, — «свернул и в заголовке бы указать что
что-то выбрано, а то пока не раскроешь и не узнаешь». Заголовок свёрнутой
и пустой грани выглядел одинаково.

**Решение.** `Group` (`FacetRow.tsx`) получил необязательный `picked` —
то же число, которым уже считает и показывает себя створка «Филтър»
(`picked > 0 && <i>{picked}</i>` в `CatalogBar.tsx`). Пилюля-счётчик — тот
же знак, та же геометрия (`.badge` в `catalog.module.css`, было
`.filterBtn i`, вынесено в общий класс правилом 10, чтобы вторая пилюля не
рисовалась заново): стоит рядом с заголовком грани и не зависит от
`open`/`closed` — заголовок и в свёрнутом, и в раскрытом виде показывает
одно и то же число. Категория считает тем же `catsPicked`, что и счётчик
у створки (И160/И166: базовая категория страницы в число не входит).

## И168 · Проверка читает пути из конфига, а не помнит их

Где лежит код, где стили, как названы шкалы и сколько швов раскладки —
свойство проекта, и живёт оно в одном файле, `kit.config.json`, который
читают все проверки, хуки и предикаты ворот (`tools/kit-config.mjs`). Нет
файла — действуют соглашения набора. В проверке пути не набираются никогда.

**Дефект, из-за которого правило заведено.** Пути `app`, `components`,
`styles`, `lib`, файл `styles/tokens.css` и префиксы `--fs-`, `--sp-`,
`--layer-` стояли рукой в семи инструментах — `check-css`, `check-code`,
`check-port`, `check-lint`, `stages`, двух хуках. Первый же чужой сайт
показал цену: у cbdshop.bg код в `src/app`, стили в `packages/ui`, шкалы
`--text-*` и `--space-*`, один шов на 860. Набор не видел ни одного файла и
честно печатал по всем семьям ноль — «нарушений нет», тогда как грубый
подсчёт давал 14 разных швов, 90 отступов в пикселях и 24 номера слоя.
Проверка, которая молчит, потому что не туда смотрит, хуже проверки,
которой нет: её ноль читают как результат.

**Решение.** Один модуль с умолчаниями и накладываемым поверх
`kit.config.json`; проверки берут оттуда папки, файлы, префиксы и швы, а
тексты находок печатают имена шкал проекта, не набора. Самопроверка
ставщика подтверждает обратным ходом: без конфига `src/` не виден, с
конфигом — `font-size:13px` и `z-index:40` найдены.

## И169 · Ставщик не трогает того, что принадлежит проекту

`CLAUDE.md`, `docs/`, `styles/`, `tests/`, линтер и рабочий процесс CI
принадлежат проекту с той минуты, как в нём появились. Набор пишет их один
раз — новому сайту в пустую папку. В папку, где они есть, ставщик без
ключа **отказывает**; `--update` обновляет только проверки и скиллы и
оставляет базы храповиков; `--audit` для чужого сайта кладёт только
проверки, свои четыре скилла и `kit.config.json`. Затереть чужое можно
единственным способом — сказав это словом `--force`.

**Дефект, из-за которого правило заведено.** `install.mjs` копировал всё,
кроме `.git`, `.gitignore`, `node_modules` и `README.md`. Значит, в чужом
проекте он перезаписал бы `CLAUDE.md` (у cbdshop.bg — 142 килобайта своих
правил), `docs/rules.md`, `.oxlintrc.json`, `styles/tokens.css` и
`.github/workflows/check.yml` — а скилл `stages` для «проверить чужой
готовый сайт» предписывал ровно этот путь. Обновление набора в своём
проекте страдало тем же: переносимый `CLAUDE.md` со строкой `0 · Основание`
ложился поверх проектного с его этапом и правилами, а базы храповиков
перезаписывались нулями — README честно советовал после этого откатывать
их руками. Совет вместо замка; найдено чтением ставщика перед первым
аудитом чужого сайта, до того как он что-то затёр.

## И170 · Заготовка не лежит там, где её исполняют

Файл, который набор кладёт проекту как есть — рабочий процесс CI, —
живёт в наборе в `templates/`, а не в `.github/workflows/`. Что лежит в
`.github/workflows/`, GitHub исполняет; заготовка чужого процесса, лежащая
там, — процесс, который исполняется не на том, для кого написан.

**Дефект, из-за которого правило заведено.** `check.yml` для проектов лежал
в `.github/workflows/` самого набора. Любой PR в набор запускал его, и он
падал на первом же шаге — `npm ci`: у набора нет ни `package.json`, ни замка
зависимостей, ни сборки. Своего же процесса у набора не было вовсе, поэтому
набор, красный на собственных проверках (И171), этого не знал. Заведено:
заготовка в `templates/check.yml`, ставщик кладёт её проекту в
`.github/workflows/`; у набора свой `kit.yml` и свой `package.json`.

## И171 · Набор проверяется своими проверками, и его долг записан

Всё, что набор требует от проекта, он держит сам: `check:rules`,
`check:css`, `check:code`, `check:port` и тест-образец гоняются на наборе
на каждом PR. Долг собственных стилей набора записан в его базе вёрстки
числом, а не обнулён: новый сайт получает базу с этим долгом и с первого
`check:css` зелёный; чинится долг в наборе, и с каждой починкой база
опускается.

**Дефект, из-за которого правило заведено.** `check:css` на самом наборе
был красным: `styles/primitives.module.css` и `styles/tokens.css` давали
пять находок в четырёх семьях (`twiceDrawn` 2, `tapGrows` 1, `deadDress`
1, `plateGap` 1) против базы из нулей. Сборщик набора обнулял базу нарочно —
«на новом проекте долга нет», — но стили-то ехали с долгом, и новый сайт
начинался красным с первого `npm run check:css`. Никто этого не видел,
потому что у набора не было CI (И170). Нашла самопроверка ставщика в день,
когда её завели: «проверки зелёные на пустом проекте» упало на первом же
прогоне.

## И172 · `!important` — только в основании

Каскад — порядок и точность селектора, а не сила. `!important` разрешён
одному файлу, основанию (`base.css`), и двум вещам в нём: сбросу
`[hidden]{display:none}` и остановке движения по `prefers-reduced-motion`.
Всё выше пола обходится без него; семья `important` в `check:css` считает
каждое вхождение вне основания.

**Дефект, из-за которого правило заведено.** Найден на чужом сайте при
первом аудите набором: у cbdshop.bg запрет на `!important` и на дважды
написанное свойство стоял в линтере стилей, и полтора года держался на
одном «не забыл запустить» — в CI линтера не было, хук стоял только на
машине того, кто поставил зависимости (`AUDIT.md` первой витрины, часть 8,
находка 17). Правило было записано, инструмент был, сборку не валил никто.
Набор берёт правило семьёй храповика, а не советом; исключение для
основания — по своим же стилям: пять `!important` в `base.css` набора
стоят ровно там, где обязаны перебивать всё.

## И173 · Слова магазина принадлежат владельцу и приходят из его админки

Полоса о доставке, ряд ссылок над шапкой, колонки подвала, письмо,
реквизиты, профили, оговорка, правовые ссылки, копирайт, фраза о магазине,
страница контактов — поля админки, не строки кода. Код витрины держит
только запасное содержимое, берёт каждое поле отдельно и при молчании
источника показывает своё, не ломая страницу и не сочиняя того, что
назначает магазин. Словами интерфейса остаётся то, что не факт магазина,
а имя строки: подписи фактов, кнопки, значки оплаты.

**Дефект, из-за которого правило заведено.** cbdin.bg переехал на общий
движок и админку, а текст шапки, подвала и контактов остался в коде
витрины — то есть принадлежал разработчику: правка одной строки стоила
выката. Заказчик сказал это дважды подряд: «контакты и подвал cbdin тоже
подключи к пейлоаду и всё остальное подключай» и «конечно, всё из админки
должно управляться». Переносить пришлось отдельной работой то, что должно
было стоять с первого дня (`DECISIONS.md` монорепозитория
`CBD_ecommerce_eu`, «Тексты второй витрины живут в админке, а не в её
коде»). Отдельный урок оттуда: фраза о магазине печатается подвалом и
уходит в `Organization.description` — двое читателей одного поля; пока
она жила строкой в коде, они совпадали случайно, потому что были одной
константой.

## И174 · Проверка поиска ходит туда, где страница живёт

Проверки разметки для поиска и адресов (`check:seo`, `check:urls`) читают
страницы из одного источника (`tools/pages.mjs`), и у него два входа:
статический экспорт `out/` или живой сервер по `SITE=`. Что значит
«страница есть» — файл на диске или ответ 200 — решает источник, а не
проверка. Проверка, которая умеет читать только один вид сборки, живёт
ровно до смены сборки.

**Дефект, из-за которого правило заведено.** Витрина cbdin.bg 19.09.2026
переехала со статического экспорта на серверную сборку (`standalone`):
папки `out/` у неё не стало. Обе проверки с того дня отвечали «нет out/,
сначала сборка» — то есть перестали существовать молча: команда в
`package.json` есть, зовёт её никто, в CI монорепозитория она не стояла.
Две недели разметка для поиска и карта сайта второй витрины не проверялись
никем, и записано это было одной строкой в журнале решений — «ждёт своей
работы». Найдено при возвращении храповиков cbdin (`DECISIONS.md`
монорепозитория `CBD_ecommerce_eu`, «Храповики cbdin.bg держатся хуками и
CI монорепозитория»). Проверено обоими ходами: на статическом экспорте
прежнего сайта вывод обеих проверок совпал байт в байт; на живом сервере
упавшая страница (5xx, обрыв) — красный с именем адреса, а не находка
разметки.

## И175 · Ответ на нажатие засчитывается и взятый через `composes`

Семья `noPress` спрашивает, есть ли у контрола с `:hover` ответ на нажатие,
и ответ бывает трёх видов в том же файле — `:active`, состояние из `aria-`
или `data-` — и ещё двух: взятый у другого класса через `composes` (цепочка до
трёх шагов: `.close` → `sheetClose` окна → `pressable` контрола) и стоящий на
том же элементе в разметке — класс надет на компонент, который отвечает сам
(`<CtaPill className={styles.submit}>`), или рядом с классом, который отвечает.
Засчитывается, только если так надето каждое место; класс, не надетый нигде, —
мёртвая одежда, и её считает своя семья. Источник, названный именем пакета
(`composes: … from '@shop/ui/control.css'`), проверка находит по `aliases` в
`kit.config.json`; без записи он пуст, и взятое у него не засчитывается. Селектор с отрицанием состояния
(`:not([data-current])`) спрашивается и без отрицания: `.pill:active` его
покрывает.

**Дефект, из-за которого правило заведено.** Первый аудит cbdshop.bg: 20
находок «есть `:hover`, нет отклика на нажатие». Дизайн-система витрины
держит нажатие в одном месте — `pressable` в `Control.module.css` — и блок
пишет себе только свой вид и своё наведение, а ответ на нажатие берёт
(`composes: pressable from '../Control/Control.module.css'`). Проверка,
читающая один файл, этого не видела и звала молчанием то, что отвечает.
Настоящих находок оказалось три; правило, дающее семнадцать ложных на
двадцать, не чинят — его обходят. Найдено чтением каждой из двадцати перед
починкой, а не починкой всех двадцати.

## И176 · Свойство, взятое через `composes`, засчитывается семьям, которые его ищут

Семья, которая ищет в блоке свойство-ответ — сдержку прокрутки
(`overscroll-behavior`) у самопрокручивающейся панели, — спрашивает и о том,
что блок взял через `composes`, до трёх шагов. Общее правило то же, что у
И175: одинаковое живёт в одном месте, и проверка обязана это место видеть,
иначе она требует девять копий ради своего спокойствия.

**Дефект, из-за которого правило заведено.** Первый аудит cbdshop.bg:
четыре окна «прокручиваются сами, а остаток уезжает на страницу». Все четыре
берут коробку окна у одного узла — `dialogSurface` в `Sheet.module.css`,
единого сброса на девять окон витрины. Сдержка, дописанная в каждое окно,
была бы четырьмя копиями рядом с общим местом; дописанная в узел — одной
строкой на девять окон. Семья, читавшая один блок, видела бы после этого те
же четыре находки. Починена семья, потом узел.

## И177 · Проверка знает, куда ведёт `@/`, и кому марка уходит пропом

Два признака одной болезни — проверка судит текст, не зная, как он
связан, — и оба лечатся тем, что связь записывается.

`@/` в импорте — приставка проекта, и куда она ведёт, знает только он:
у проектов набора — в корень, у первой витрины — в `src/`. Записывается в
`aliases` `kit.config.json` ключом с косой чертой на конце (`"@/": "src/"`);
имя пакета без черты — целым (`"@shop/ui/control.css": "…"`). Семьи,
которые ищут читателей класса или источник `composes`, ходят по этой записи.

Марка, уходящая пропом (`brand={product.brand}`), не печатается здесь —
печатает тот, кому отдали, и атрибут `translate="no"` вешается там, где
марка становится текстом: `>{brand}<`. Семья `translated` считает текст в
разметке, а не передачу.

**Дефект, из-за которого правило заведено.** Первый аудит cbdshop.bg: три
живых класса главной (`heroShell`, `bands`, `body`) числились мёртвыми —
их берут через `@/app/page.module.css`, и проверка искала файл в корне
вместо `src/`; два места «марка без `translate`» стояли на передаче пропа,
а настоящие — два печатающих `{brand}` в дизайн-системе — проверка не
видела: она искала только `.brand}`. Четыре ложных и два пропущенных из
шести — семья, которую пришлось бы обходить.

## И178 · Одежду надевает и код, и не только код страниц

Семья `deadDress` спрашивает, ставит ли кто-нибудь атрибут, под который
написано правило. Ставят его не только разметкой (`data-x=`): строкой в
коде (`setAttribute('data-x', …)`, константа с именем атрибута) и через
`dataset.x =`. И ставит его не только код страниц: в монорепозитории одежду и
её постановщика держит дизайн-система, которая лежит в `styles`, а не в
`code`, — и её файлы кода смотрятся тоже.

**Дефект, из-за которого правило заведено.** Первый аудит cbdshop.bg:
`data-search-open` у телефонного поиска числился одеждой, которую никто не
надевает, — а надевает её `PhoneSearch.tsx` в `packages/ui` константой
`OPEN_ATTRIBUTE` через `setAttribute`. Семья искала `data-…=` в разметке и
только в папках кода витрины. Одна находка из одной — ложная.

## И179 · У шва два края, и оба его

Пара медиазапросов, не дающая правилам наложиться, пишется двумя числами:
`(min-width: 860px)` и `(max-width: 859px)`. Это одно решение, взятое
«отсюда и выше» и «строго ниже», а не два разных шва. Семья `breakpoint`
принимает обе записи: разрешённой считается ширина, отличающаяся от
названного шва не больше чем на единицу — в любую сторону.

**Дефект, из-за которого правило заведено.** Первый аудит cbdshop.bg: обе
находки семьи были ложными. `(max-width: 859px)` — шов 860, тот самый
единственный шов раскладки витрины; `(max-width: 1023px)` — пара к
`(min-width: 1024px)`, стоящему в том же файле десятью строками выше. Сама
витрина держит реестр швов (`seams.ts`) и в нём записано прямым текстом:
«859 и 861 — это тот же шов 860». Проверка признавала только `861`.

## И180 · Ограниченная ширина — это и есть потолок высоты

Семья `ratioNoCap` требует у блока с `aspect-ratio` ограничения по высоте:
высота считается из ширины, а ширину задаёт место, которого может оказаться
сколько угодно. Но если ширина у блока ОГРАНИЧЕНА — `max-width` или
`max-inline-size` не в процентах, — высота ограничена вместе с ней: квадрат
шириной не больше 560 не бывает выше 560. Такой блок находкой не считается.

Процент не в счёт: `max-width: 100%` повторяет ширину родителя, то есть ту
самую неизвестную, ради которой правило и заведено.

**Дефект, из-за которого правило заведено.** Первый аудит cbdshop.bg: галерея
товара объявляет `max-width: min(560px, 52vh)` и `aspect-ratio: 1/1` — потолок
записан в ширине и вдобавок меряется окном, ровно как требует правило. Проверка
просила второй потолок по высоте: две записи одного числа, которые разойдутся
при первой правке.

## И181 · Склад у каждого проекта свой, и он назван в настройке

Семья `keep` разрешает память браузера только складам — местам, которые
берегут её от приватного режима и будят подписчиков. Список складов —
`stores` в `kit.config.json`, а не строка в самой проверке: у набора он свой
(`lib/shop.ts`, панель настроек, загрузочный скрипт), у чужого проекта —
свой, и чужие имена ему ничего не говорят.

**Дефект, из-за которого правило заведено.** Первый аудит cbdshop.bg: все
одиннадцать находок семьи стояли в двух файлах, из которых один —
`lib/cart-state.ts` — и был складом: те же защиты, тот же единственный ключ.
Проверка звала его долгом, потому что искала имена файлов другой витрины.
Зашитый список делает семью неприменимой везде, кроме одного проекта.

## И182 · Длинный файл разнимают вместе со сторожами

Файл, который перестал читаться целиком, разнимают на части, а на его месте
оставляют ВХОД — одни перевывозы (`export … from './часть'`). Ввозящие этого
не замечают, и это правильно: путь тот же.

Не замечает и сторож — но только пока он ВВОЗИТ. Сторож, который читает файл
ИСХОДНИКОМ (`readFileSync` плюс поиск образца в тексте), после разъёма ищет
образец в пустоте: он не падает. Он зеленеет навсегда и молчит об этом, а
ссылаются на него как на доказательство.

Поэтому разъём файла — это две работы, а не одна: перенести код и перевести
на новый дом каждого, кто читал старый исходником. Переведённого сторожа
перечитывают обратным ходом: он всё ещё умеет падать?

Семья `deadGuard` считает это машинно: файл — вход (кроме комментариев в нём
одни перевывозы), и его имя набрано строкой-путём в файле, где есть
`readFileSync`. Спецификаторы ввоза (`from '…'`, `import('…')`) не в счёт:
ввоз переживает разъём без единой правки. Сторожа ищутся по всему общему
корню папок кода, а не только внутри них: половина их лежит рядом с деревом
страниц, а не в нём.

**Дефект, из-за которого правило заведено.** 19.09.2026, закрытие долга
cbdshop.bg: разняты семь файлов длиннее 420 строк, и ВОСЕМЬ сторожей читали
их исходником — подпись мастерской в подвале, ключ марки с грани, отбор по
ключу, сборщики блоков главной, подзаголовок плитки эффекта, выключатель
полок. Пять упали сразу и были переведены; три остались бы зелёными молча —
их нашли перечитыванием, а не прогоном. Проверка, которая больше ничего не
проверяет и при этом зелёная, хуже отсутствующей.

## И183 · У числа мимо шкалы четыре разных причины

Находка «размер или отступ набран числом» не означает «подставить ступень».
Прежде чем править, спрашивается, ЧТО это за число, и ответов четыре:

1. **Ступень, которой в шкале нет.** Признак — число стоит в нескольких
   местах сразу. Пять записей одного шага это шаг шкалы, и он заводится.
2. **Роль набора без имени.** Заголовок карточки товара, заголовок статьи на
   телефоне — ни одна ступень ряда им не подходит и подойти не должна, число
   снято с макета. Оно получает имя в шкале и остаётся тем же числом.
3. **Замер, а не ритм.** Высота шапки, под которой начинается копия; ширина
   своей стрелки у поля-выбора; пилюля ползунка. Это устройство, а не
   расстояние между вещами: ему дают имя переменной, и один факт получает
   одно имя вместо трёх безымянных копий в трёх файлах.
4. **Мёртвое.** Блок, который никто не рисует. Удаляется вместе со стилями.

Загнать замер в ступень значит изменить вид; гнать все четыре одним приёмом
значит по дороге сломать третье.

**Дефект, из-за которого правило заведено.** Первый аудит cbdshop.bg: 33
находки семей `fontPx` и `spacingPx`. Ступенью оказались пять (56 пикселей в
пяти местах), ролью — шесть, замером — семь, мёртвым — одна вместе со всем
блоком. Первая попытка чинить их одинаково — «подставить ближайшую ступень» —
меняла вид героя, панели подвала и карточки товара на величину до девяти
пикселей и была выброшена.

## И184 · Взятое через `composes` не переопределяют тем же весом

`composes` не вкладывает правило в правило — он ДОПИСЫВАЕТ класс рядом: на
элементе оказываются оба, и вес у них одинаковый. Кто победит, решает порядок
правил в собранном файле, а его решает сборщик: порядок кусков зависит от
того, какая страница попала в сборку первой. Правило, переопределяющее взятое
голым классом, работает «пока работает» — и расходится между разработкой и
боем, между двумя выкатами одного и того же кода.

Переопределяют двумя способами, и оба однозначны:

* **ручкой** — взятое пишет `position: var(--pinned-pos, sticky)`, берущий
  ставит `--pinned-pos: static`. Ручка сильнее любого порядка;
* **силой места** — потомочный селектор (`.cols > .lists`): вес выше, спора
  нет.

Взятое, снявшее свой вес само (`:where(.dialogSurface)`), — не спор: общий
узел сознательно уступил, и переопределять его берущий вправе. Взятое из
ТОГО ЖЕ файла — тоже не спор: оба правила едут одним куском, в том порядке, в
каком написаны.

**Дефект, из-за которого правило заведено.** 19.09.2026, cbdshop.bg: три
приклеенные колонки взяли примитив `pinned` и тут же написали себе
`position: static` под свою узкую раскладку — тот же вес, тот же элемент.
Витрину спас её собственный сторож, заведённый после такой же беды раньше; в
наборе правила не было, и на cbdin в подвале лежал ровно тот же спор
(`display: contents` против `display: grid` у взятого примитива), не пойманный
никем.

---

## И185 · Рампа обязана иметь px/rem-слагаемое

`clamp(мин, наклон, макс)` — прямая между двумя ширинами. Середина, в
которой один голый `vw` или `cqi`, растёт только с шириной окна: при зуме
200 % ширина в CSS-пикселях вдвое меньше, и размер уезжает на пол. Текст,
который не увеличивается при зуме, — провал WCAG 1.4.4 (техника F94). Вдобавок
голый `vw` не проходит через названные точки: у соседней витрины
`clamp(48px, 6.1vw, 88px)` стоял на полу до 787 px — ступень,
замаскированная под рампу.

**Как писать.** `clamp(мин, свободный член + наклон, макс)` — как считает
набор для всей шкалы: свободный член в px или rem, наклон в `vw` (`cqi` в
колонке). Utopia пишет так же и ровно поэтому: rem-слагаемое — единственное,
что растёт при зуме. Наклон между двумя ступенями (`clamp(var(--sp-8),
4.62vw - 9.85px, var(--sp-9))`) слагаемое имеет.

**Дефект.** Найден проверкой на самом наборе 20.09.2026: `--fs-lead:
clamp(17.5px, 3.4cqi, 21px)` — лид не растёт при зуме. Записан в базу долгом
(1), чинится своим коммитом: это правка размера, а не правила.

**Чем меряется.** Семья `bareVw`: у каждого `clamp()` во всех файлах стилей
середина обязана содержать `px`/`rem`, если содержит единицу окна или
контейнера.

---

## И186 · Поле рядом с текстом — в rem, воздух и зазор — в px

Роль `--pad-*` лежит вокруг содержимого предмета, то есть вокруг букв.
Объявленная в px или ступенью px-шкалы, она не растёт, когда покупатель
поднял шрифт в настройках телефона: буквы крупнее, коробка та же — тесно
становится ровно тому, кому нужно просторнее (WCAG 1.4.4). Воздух между
предметами (`--air-*`) и зазор между целями (`--gap-*`) с текстом не связаны
и растут только с шириной — они в px.

**Как писать.** Поле — своя рампа в rem с теми же концами, что у ступени
при корне 16: `--pad-card: clamp(1.125rem, .8556rem + .77vw, 1.375rem)`
(= `--sp-5`). Псевдоним `var(--sp-N)` у поля запрещён: ступень в px.

**Дефект.** Решение заказчика 20.09.2026 по разбору первоисточников
(`docs/layers.md`, §2 и §3.5): системы расходятся честно — Carbon, Tailwind,
Utopia в rem, Atlassian, Spectrum, Primer авторят в px, — и выбран раздел
по работе: rem там, где отступ служит чтению, px там, где не должен расти.
До правки все три поля набора были ступенями px-шкалы.

**Чем меряется.** Семья `padPx`: объявление `--pad-*` в файле шкал не
содержит `px` и не ссылается на `--sp-*`.

---

## И187 · Воздух между разделами — не меньше трёх полей карточки

Дорогой вид держится не числом ступеней, а разрывом между «внутри
предмета» и «между предметами». Замер семи живых магазинов 20.09.2026
(Playwright, вычисленные значения на 375 и 1440; `docs/layers.md`, §3.3):
промежуток между полосами страницы к полю карточки — 3–5 : 1 (Muji 44 : 17
и 60 : 17, Glossier 60 : 12 и 80 : 16). Ниже трёх предметы и промежутки одного
размера, и глаз не отличает «внутри» от «между».

**Как писать.** `--air-page` берёт ступень, при которой отношение к
`--pad-card` на обоих концах рампы не меньше 3: у набора `--sp-10`
(56→80 к 18→22 — 3.1→3.6). Множителя плотности на весь сайт нет намеренно
(решение заказчика 20.09.2026): плотность — имя роли, а не ручка.

**Дефект.** У набора стояло `--sp-9` — 2.7 : 1: воздух раздела почти не
отличался от удвоенного поля карточки. Найдено сравнением с замером, а не
глазом; заказчик выбрал числа по замеру, пока макета нет.

**Чем меряется.** Семья `airRatio`: концы рамп `--air-page` и `--pad-card`
разрешаются через псевдонимы, отношение считается на телефоне и на мониторе.

---

## И188 · Зазор между целями — свой токен, и под пальцем он вдвое больше

Размер цели под палец (И31) не делает ряд целей нажимаемым: плюс и минус
счётчика по 44 px, стоящие вплотную, промахиваются пальцем. Primer держит
`controlStack.gap` отдельно от размера цели — 8 под курсором, 16 под
пальцем — и это отдельный токен, а не следствие роста кнопки.

**Как писать.** `--gap-targets: 8px` в шкале, `16px` в `@media
(pointer: coarse)` — ширина решает раскладку, указатель решает размер и
зазор цели (И15). Ряд органов ставит его просветом родителя.

**Дефект.** Найдено разбором первоисточников 20.09.2026 (`docs/layers.md`,
§3.2): у набора была цель, но не было зазора — правило о пальце покрывало
половину промаха.

**Чем меряется.** Тест-образец `tests/kit.test.ts`: токен объявлен, и в
медиазапросе про палец у него другое значение.

---

## И189 · Лестница строится в обе стороны и не схлопывается

Шкала — лестница от бумаги через краску к чернилам, и у каждой ступени
своя работа. Две ступени в одной краске — это контрол, который не отвечает
на курсор, цена, неотличимая от текста, или разделитель, которого нет.
Лестница, которая развернулась (11-я светлее 10-й), — то же самое: работа
ступени пропала, а число на месте.

**Как писать.** Ступени 1–8 держатся профиля эталона, но не ближе 1.5 L* к
краске со стороны бумаги; 11 и 12 — профиля, но каждая не ближе шага
заливки к предыдущей со стороны чернил. Правило одно на обе темы: меняется
направление, а не формула. Краска, для которой это не сходится (светлее
~88 L* в светлой, светлее ~90 в тёмной), — не та краска для кнопки.

**Дефект.** Найден 20.09.2026 прогоном наборов через профиль Radix
(`docs/layers.md`, §4): в тёмной теме у всех четырёх наборов ступени 1–8
фирменной и красной шкал совпадали с краской — девять одинаковых hex; в
светлой у краски темнее отметки 11-й (`#0C3A46`, `#A34627`, красный
`#B3261E`) 11-я равнялась 9-й и стояла светлее 10-й. Причина — ограничение
`Math.max(want, L + 1.5)`, написанное для светлой темы и применённое к
обеим. Ни один из девяти сторожей палитры этого не видел: они мерили
контраст пар, а не ход лестницы.

**Чем меряется.** `check:palette`, правила «… лестница не схлопывается»:
наименьший шаг между соседями по ходу от бумаги к чернилам, отдельно для
нейтральной, фирменной и красной шкал в каждой теме, не меньше 0.6 L* —
самой тесной пары самого эталона. Обратным ходом: старый строитель на тех
же наборах даёт −5.7.

---

## И190 · Красный — такая же шкала, как марка, и меряется так же

Красный не только «деньги теряются» у поля: на его девятой стоит «нет в
наличии», на одиннадцатой — текст ошибки на карточке. Шкала у него та же,
что у марки, и ломается она там же — но до 20.09.2026 проверка смотрела на
красный один раз, ради расстояния от марки.

**Как писать.** Красная шкала проходит те же правила, что фирменная:
лестница не схлопывается, текст 11-й читается на карточке (4.5 : 1), знак на
9-й выбирается из белого и чёрного по читаемости. Знак на девятой ступени красного
выбирается, а не хранится: хранить «белый на красном» нельзя. (Пастель
`#F2B8B5` в роли девятой считалась здесь допустимой, потому что знак на ней
читается; 21.09.2026 это отменено глазом заказчика — см. И193.)

**Дефект.** Тот же прогон 20.09.2026: у всех наборов в светлой теме 11-я
ступень красного равнялась 9-й, а в тёмной 1–8 были краской — и проверка
была зелёной, потому что ни одно правило красного не касалось.

**Чем меряется.** `check:palette`: «текст ошибки на карточке», «знак на
заливке ошибки», «красная лестница не схлопывается».

---

## И191 · Приглушённый текст держит обещание эталона, а не номер ступени

Одиннадцатая ступень — цена, приглушённый текст, текст ошибки — ставилась по
номеру из профиля. Профиль снят с шкалы `sand` пакета `@radix-ui/colors`, и
на этом обещании — Lc 60 на второй ступени — `sand` стоит РОВНО: 60.0, худшая
его шкала из тридцати одной. Мы заняли худший случай эталона, и с нашей
бумагой и нашими чернилами запаса не осталось.

**Как писать.** Одиннадцатая берётся ЗАМЕРОМ, как граница и кольцо фокуса
(И189 и раньше), а не номером: уводится от бумаги ровно настолько, чтобы
обещание выполнить, и не ближе шага лестницы к двенадцатой. Сдвиг выходит
0.1–0.3 L* — один уровень канала, глазом неотличимо, и иерархия до
двенадцатой остаётся 20 L*.

**Дефект.** Прогон четырёх наборов 20.09.2026 через APCA: в тёмной теме
приглушённый текст давал Lc 59.7 («Аптека»), 59.9 («Олива»), а цена фирменным
— 59.7 («Мек остров»), 60.0 («Олива»), при обещании 60. Все шестнадцать
сторожей палитры были зелёными, потому что мерили только WCAG, а WCAG на той
же паре показывала 8.35 при норме 4.5 — запас вдвое. Это ровно то завышение
в тёмных парах, из-за которого эталон обещает APCA и не обещает WCAG.

**Чем меряется.** `check:palette`, четыре правила «… держит обещание
эталона»: приглушённый текст и основной (Lc 60 и Lc 90 на карточке), цена
фирменным и текст ошибки. Сама APCA сверена с задокументированными числами
эталона и совпала до десятой: 11-я на 2-й — светлая 66.2–76.9, тёмная
60.0–86.2; 12-я в тёмной — минимум `tomato` 84.2 и 19 шкал ниже 90.
Обратным ходом: строитель без замера даёт 59.71–59.98 на трёх наборах из
четырёх.

---

## И192 · Число, записанное в разборе, проверяется машиной, а не рукой

В `palette.md` было записано: прогон по `templates/palette.json` даёт ровно одну
находку. Это было правдой — и проверялось честным словом. Сама проверка палитры
смотрит в `styles/palette.json` приложения, которого в наборе нет; файл-пример
не читал никто.

**Как писать.** Если в разборе стоит число, полученное прогоном, — прогон
заводится тестом. Не «надо помнить, что примеры проходят», а тест, который
падает, когда находок стало больше ИЛИ меньше: лишняя находка значит, что
пример испортился, недостающая — что задокументированный урок из него пропал.

**Дефект.** 21.09.2026 в файл добавились три набора, и стало видно, что
добавить их можно было любыми: ни одна проверка набора по этому файлу не шла.
Строка «даёт одну находку» жила ровно до первой правки файла — а «Тёплый лист»
лежит там нарочно, ради урока, и молча выпасть он не должен тоже.

**Чем меряется.** `tests/kit.test.ts`, «наборы-образцы: находка ровно одна, и
та задокументирована»: проверка гоняется по самому файлу-примеру, список
находок сверяется дословно. Обратным ходом: марка одного набора сдвинута
вплотную к красному — тест падает со словами «находки в файле-образце
разошлись с тем, что написано в palette.md».

---

## И193 · Красный в тёмной теме — настоящий красный, а не пастель

Девятая ступень красного в тёмной теме стояла на пастели Material `#F2B8B5`
(L* 80). По числам она проходила все двадцать правил: знак на ней выбирается
и даёт 10.8 : 1, текст ошибки на карточке 14.08 : 1 — запас тройной.

**Как писать.** В тёмной теме красный — `#E5484D`, собственный `red-9`
эталона. Пастель в роли заливки не заводится: «нет в наличии» должно
читаться бедой, а не украшением. Одиннадцатая ступень при этом остаётся
светлой (`#EF9DA0`) — текст ошибки на тёмном фоне тёмным быть не может, и
это не то же самое, что бледная заливка.

**Дефект.** 21.09.2026 заказчик открыл стенд наборов, посмотрел на тёмную
тему и сказал: «розовый ужасен везде». Четыре верхние ступени красного
выходили бледными — `#F2B8B5 · #F4CDCA · #F7E0DE · #F9F3F3`, — и ни одно из
двадцати правил этого не видело: все они мерят читаемость, а не то, читается
ли беда бедой. Глаз заказчика тут точнее любого сторожа, и правило записано
с его слов.

**Чем меряется.** Ничем автоматическим — и это сказано вслух, чтобы никто не
завёл проверку «краска не розовая» на глазок. Держится это тем, что красный
в наборах-образцах один, а `tests/kit.test.ts` не даёт файлу разойтись с
разбором (И192). Заодно замером закрыт соседний вопрос: одним hex на обе
темы красный сделать нельзя, хотя эталон держит `red-9` именно так — с
`#E5484D` в светлой у «Латуни на угле» красная лестница схлопывается до
0.54 ΔL* при норме 0.6.

---

## И194 · Палитру мало проверить — её надо уметь выпустить

Правило о шкале было записано, сторож считал двадцать правил, разбор занимал
триста строк — а покрасить этим сайт было нечем. Двенадцать ступеней
считались внутри проверки и выбрасывались; цвета стояли в
`styles/tokens.css` набранными рукой; `check-palette.mjs` в проект не ехал
вовсе, хотя команда `check:palette` в реестре подсказок стояла.

**Как писать.** У палитры три части, и все три переносимые:

| Часть | Файл | Чья |
|---|---|---|
| краски | `styles/palette.json` | владельца: семь красок на тему |
| построение и замер | `tools/palette.mjs` | набора, одна на выпуск и на проверку |
| выпущенное | `styles/palette.css` | машины: правится только выпуском |

Шкалы сайта читают палитру переменной (`--a-9`, `--on-a-9`, `--a-press`), а
не повторяют числами. Порядок работы: краски → `npm run palette` →
`npm run check:palette`.

**Дефект.** 21.09.2026 владелец открыл набор скиллов и сказал: «я заебался,
нихуя не работает». Он был прав буквально: команда выпуска не существовала,
файл проверки в проект не копировался, а закон о двенадцати ступенях и
краски сайта не встречались ни в одном месте.

**Чем меряется.** `selftest/install.test.mjs` — весь путь на новом сайте:
файлы доехали, краски на месте, выпущенный CSS не отстал, замер зелёный.
`tests/kit.test.ts` — «палитра выпускается в CSS», «шкалы читают палитру, и
палитра даёт всё, о чём они просят», «шкалы набора без красок — находка, а
не зелёная самопроверка». Последнее обратным ходом: убрать эту ветку —
проверка на проекте без красок снова зеленеет.

---

## И195 · Середина лестницы держит тон марки

Ступени 1–8 клались прямой от бумаги к краске. Прямая в sRGB между
почти-серыми концами проходит через серое: у сине-зелёной марки шестая
ступень держала **20%** насыщенности заливки там, где эталон той же породы
держит 52–58%. Середина лестницы — это все поверхности магазина разом:
карточка, плитка, контрол, разделитель. Тёплая марка давала серый сайт.

**Как писать.** Ступень ставится в Oklch: **тон берётся у краски и не
меняется**, светлота — из профиля эталона, насыщенность — доля от
насыщенности краски по дуге **ближайшей по тону породы**. Породы нужны
потому, что дуги у них разные и разница не наша: `amber` на пятой ступени
держит 78% своей вершины, `blue` — 27%, и упирается это в охват экрана.
Краска вне охвата гасится насыщенностью, а не светлотой: на светлоте стоят
все пороги контраста.

Концы лестницы при этом — краски владельца как есть: первая ступень **есть**
бумага, девятая цветного ряда **есть** марка, двенадцатая **есть** чернила.
До этого первая и двенадцатая считались из профиля, и белая бумага `#FFFFFF`
выходила на странице как `#FDFDFD`: владелец задавал цвет, а получал
соседний.

**Дефект.** Найден 21.09.2026 замером всех 30 шкал эталона: насыщенность не
идёт прямой ни в одной. Она поднимается от первой ступени к девятой и падает
к двенадцатой — дуга с вершиной на заливке.

**Чем меряется.** `tests/kit.test.ts`, «палитра: середина лестницы держит тон
марки»: норма читается из слепка эталона (`tools/palette-profile.json`), а не
назначена — шестая ступень обязана держать хотя бы четыре пятых доли своей
породы. Обратным ходом: вернуть прямую — тест падает со словами «шестая
ступень фирменного ряда обесцветилась: 20% при 46% по породе «cyan»».

Заодно закрыт пол шага: он держится **при построении**, а не только в замере,
и меряется полученная краска, а не заданная отметка. У белого конца один
уровень канала стоит 0.2 L*, отметка 97.9 превращается в 98.1, и пол в 0.6
проседает до 0.59 — «Латунь на угле» краснела именно на сотую.

---

## И196 · Красок у магазина пять, а не две

Краска была у фирменного и у ошибки. Владелец спросил: «есть же плашка
скидки — она какого цвета?» — и оказалось, что ответа нет. Заодно нашлось,
что у ошибки и разрешённого были чернила, кромка и вуаль, но не заливка:
плашку «нет в наличии» красить было нечем.

**Как писать.** Пять красок сигналов, и смысл каждой взят из правила, которое
владелец записал раньше для админки (красный — деньги теряются **сейчас**,
оранжевый — будут стоить **потом**, зелёный — проверено и в порядке):

| Ключ | Работа на витрине |
|---|---|
| `accent` | кнопка покупки, фирменный |
| `error` | «нет в наличии», ошибка в поле |
| `warn` | «осталось двое», бесплатная доставка кончается |
| `ok` | «в наличии», «доставим завтра» |
| `sale` | плашка скидки |

Скидка стоит отдельно от всех четырёх, и это не украшение: плашка цвета марки
пропадает рядом с кнопкой покупки, а красного — читается как предупреждение.
Покупатель уходит в обоих случаях.

**Все пять обязаны быть различимы попарно** — ΔE 25, тот же порог, что был у
марки против красного. Знак на каждой заливке не хранится, а выбирается по
читаемости: у трёх привозных наборов из трёх назначенный ими знак не читался.

Лестницы поверхностей у статусных красок нет намеренно: у плашки три работы —
тихий фон, заливка и текст (ступени 2, 9, 11). Ступени 3–8 ей негде применить,
и заводить их значит заводить места, где правда разойдётся.

**Дефект.** 21.09.2026, вопрос владельца во время работы над палитрой.
До этого «мало осталось» цвета не имело вовсе, а скидка не существовала как
понятие.

**Чем меряется.** `tests/kit.test.ts`, «палитра: пять красок сигналов меряются
попарно»: набор, где скидка совпала с маркой, обязан краснеть с именем обеих
красок. Значения по умолчанию взяты у эталона (`orange-9`, `green-9`,
`pink-9`), а не подобраны: замер по семи наборам дал худшее расстояние 26 —
зелёный «в наличии» против шалфейной марки, и это видно в отчёте числом.

---

## И197 · Выбор показывается глазами, а не списком

Всё, что заказчик выбирает **видом** — цвет, шрифт, раскладка, форма кнопки,
снимок, — приходит к нему отрисованным: страницей, где это видно рядом и
переключается. Список имён и шестнадцатеричных кодов выбором не является.

**Как писать.** Показывать по убыванию честности: на самой витрине (все
варианты стоят в сайте и переключаются одним движением) → стендом, где
варианты стоят на одном и том же предмете, на карточке товара и кнопке
покупки, а не полосками краски → снимками, если ни того ни другого поднять
нельзя. Числа идут рядом с картинкой и отвечают на свой вопрос — что проходит
проверку и какой ценой, — но выбирает заказчик глазами.

**Дефект.** 21.09.2026. Исполнитель предложил семь наборов цвета третьей
витрины строчками: имя, код, ΔE до красного. Заказчик выбор отклонил и
потребовал показать. Он прав: `#B79339` не говорит ничего никому, а полоска
краски не показывает, как этот цвет ведёт себя рядом с ценой и с «нет в
наличии». Числа отвечают на вопрос «пройдёт ли проверку», а выбирается вид —
это разные вопросы.

Заведено оно поздно: за два дня до этого стенд наборов уже делался и уже
работал, то есть способ был известен и не применён. Значит правило не про
умение, а про привычку — спрашивать раньше, чем рисовать.

**Чем меряется.** Ничем автоматическим, и это сказано вслух: машина не
отличит список от стенда в ответе исполнителя. Держится оно тем, что стоит в
`CLAUDE.md` — файле, который читается каждой сессией раньше кода, — рядом с
правилом «работа показывается отрисованной», из которого и следует.

---

## И198 · Каждый набор цвета стоит под своим именем — первый в том числе

Выпущенная палитра ставила первый набор только на корень (`:root`), а
остальные — под `[data-palette="имя"]`. Переключатель наборов из-за этого
работал во все стороны, кроме одной: к первому набору он вернуться не мог, а
кружок с его краской в ленте выбора показывал не его, а тот набор, который
сейчас включён.

**Как писать.** Первый набор стоит на корне — он показывается, когда никто
ничего не выбирал, — **и** под своим именем, как все. Цена — четыре десятка
строк на набор, платится один раз при выпуске.

**Дефект.** 21.09.2026, на стенде выбора цвета третьей витрины: владелец
потребовал показать выбор глазами (И197), стенд собрали — и кружок первого
набора в ленте оказался не того цвета.

**Чем меряется.** `tests/kit.test.ts`, «палитра выпускается в CSS»: у набора
из одного элемента обязан быть и корень, и собственный селектор. Обратным
ходом: вернуть `names.slice(1)` — тест краснеет.

---

## И199 · Новый сайт не начинается с чужой марки

Палитра набора едет в новый проект вместе со `styles/`. С тех пор как в
наборе завелась настоящая палитра (И194–И198), это значит, что новый сайт
получал «Мек остров» целиком: чужую марку под чужим именем — и никто не
спрашивал заказчика, какого цвета его магазин.

**Как писать.** Установщик кладёт новому сайту `styles/palette.json` со
СТАРТОВЫМ набором — нарочно серым и названным «Стартовый — заменить»
(`templates/palette-starter.json`, тем же путём, каким едет `check.yml`).
Ворота этапа 0 ищут это слово и напоминают спросить фирменный цвет в начале
каждой сессии, пока он не назван. Рядом — человеческие ворота: набор цвета
показан заказчику отрисованным, а не кодами.

**Дефект.** Заказчик спросил 21.09.2026: «запустили новый сайт и скилл —
далее что? как палитра будет формироваться?» Ответа в наборе не было. Шаг
«спроси у заказчика цвет» держался на памяти сессии, то есть ни на чём:
сессия кончается, и следующая начала бы верстать на чужих красках, считая их
своими. Это ровно та болезнь, от которой набор и написан.

**Чем меряется.** `npm run stage`: ворота этапа 0 краснеют, пока палитры нет
или пока она стартовая. Видно это в брифинге начала каждой сессии, а не на CI
через час. Обратным ходом: прогон установщика на пустом сайте — палитра легла
стартовая, проверка на ней зелёная, ворота сказали «спросить фирменный цвет».

---

## И200 · Опираясь на правило, приводят его текст, а не пересказ

Всякий раз, когда решение обосновано записанным правилом — проекта, скилла,
реестра, — в ответе стоит адрес (файл и раздел) и **сама строка**, на которую
опираются.

**Как писать.** Не «по нашему правилу настройки живут в админке», а адрес плюс
строка из того раздела. И читать правило **перед** тем, как на него сослаться:
если файл открыт и строка процитирована, несовпадение видно сразу — чаще всего
самому исполнителю, до того как ответ уйдёт.

**Дефект.** 21.09.2026. Будущая работа — поле выбора цвета в админке — была
обоснована правилом «Настройка живёт в админке, а не в панели сервера». Правило
оказалось не о том: тот раздел про ключи, токены и адреса внешних служб, а речь
шла о цвете; проверка «что покажет магазин, если ввести другое значение» — это
проверка исключений из правила про ключи, а не общее мерило.

Заказчик поймал это **потому, что был назван адрес**: открыл, прочёл, увидел
несовпадение за минуту. Без адреса он принял бы пересказ на веру, и проект
получил бы поле с миграцией — и миграцию на каждый новый набор цвета следом.

Причина не в невнимательности, а в способе: правило вспоминалось по памяти, а
память хранит смысл, а не границы. Границы и есть самое важное — почти всякое
правило верно внутри своего раздела и неверно за ним.

**Чем меряется.** Ничем автоматическим, и это сказано вслух: машина не отличит
цитату от пересказа в ответе исполнителя. Держится тем, что стоит в `CLAUDE.md`
— файле, который читается каждой сессией раньше кода, — рядом с правилами о
границе ответственности и о том, что работа показывается отрисованной.
## И201 · Ворота узнают проверку и по файлу, а не только по имени команды

Ворота этапа 0 спрашивают: валит ли сборку храповик вёрстки. Искали они в
процессе CI строку `check:css` — имя команды из `package.json`.

**Как писать.** Процесс имеет право звать храповик напрямую —
`node tools/check-css.mjs`, — и это ровно то же самое. Ворота ищут и имя
команды, и имя файла: `check[:-]css`.

**Дефект.** Обнаружено 21.09.2026 на самом наборе. Его `.github/workflows/kit.yml`
честно гоняет `node tools/check-css.mjs`, `check-code.mjs`, `check-port.mjs` —
то есть ровно то, чего ворота требуют, — а брифинг каждой сессии печатал
«✗ проверки не валят сборку сами». Заказчик видел это красным месяц и мог
решить, что этап не пройден, хотя он пройден.

Ворота, которые врут в безопасную сторону, хуже отсутствующих: их перестают
читать. Тот же класс уже описан в шапке `tools/check-test.mjs`: «`node --test`
на папке без тестов отвечает „0 тестов, 0 упало“ и выходит с нулём. Сборка
зеленеет, не проверив ничего… молчаливый ноль выглядит как результат». Здесь
наоборот — молчаливый крест выглядит как невыполненная работа, — но болезнь
одна: отчёт разошёлся с тем, что происходит на самом деле.

Ссылка эта, к слову, со второго раза. С первого здесь стояло «тот же класс,
что И183» — по памяти, без открытого файла. И183 оказалось про другое
(«У числа мимо шкалы четыре разных причины»), а правила про молчаливый ноль в
реестре нет вовсе: история живёт в шапке проверки. Поймано это правилом И200,
написанным в тот же день соседней сессией, и служит ему примером.

**Чем меряется.** `npm run stage`: ворота этапа 0 на самом наборе печатают
«всё, что меряется, держится». Обратным ходом: вернуть предикату старую
строку `/check:css/` — ворота снова краснеют на собственном CI.

---

## И202 · Шкалы мало записать — их надо уметь выпустить

Правило «размер из шкалы, ритм из шкалы» стояло в законе с первого дня, а
сама шкала — двадцать восемь рамп — была набрана в `styles/tokens.css`
рукой. Формула к ним жила рядом словами в комментарии: «наклон = (макс −
мин) / 5.2, свободный член = мин − (макс − мин) × 1.0769, концы на 560 и
1080». Формула, живущая словами, исполняется головой, а голова ошибается
там, где числа похожи.

**Как писать.** У шкал три части, и все три переносимые — то же устройство,
что у палитры (И194), и намеренно то же:

| Часть | Файл | Чья |
|---|---|---|
| числа | `styles/scale.json` | владельца: два числа на ступень — телефон и макет |
| счёт и замер | `tools/scale.mjs` | набора, один на выпуск и на проверку |
| выпущенное | `styles/scale.css` | машины: правится только выпуском |

Воздух не заводит своих чисел — он ССЫЛАЕТСЯ на ступень ритма
(`--air-page: var(--sp-10)`): Carbon завёл для воздуха вторую шкалу
`layout-01…07` и отменил её обратно в единую. Поле — в rem, воздух и
зазор — в px (И186). Концы рампы — названные швы раскладки, а не
произвольные ширины. Порядок работы: числа → `npm run scale` →
`npm run check:scale`.

**Дефект.** 21.09.2026. У `--sp-11` свободный член оказался списан у
`--sp-9` — в обоих стояло `30.77px`, — и ступень, обещавшая 100 пикселей на
макете, доходила там до 93. Ни один сторож набора этого не видел: `nearStep`
смотрел только на соседние кегли, а рампу целиком не читал никто. Строитель,
пересчитав те же числа по той же формуле, повторил 27 рамп из 28 знак в
знак и разошёлся ровно на этой одной.

Второй дефект того же дня, и он про сам переезд: три семьи (`nearStep`,
`padPx`, `airRatio`), тест набора и ворота этапа читали шкалу ТОЛЬКО из
`styles/tokens.css`. После переезда ступеней в выпущенный файл ни одна из
них не покраснела бы — они бы замолчали нулём, а молчаливый ноль читается
как «в проекте чисто». Поэтому все пятеро учатся читать выпущенное
(`kit.config.json`, ключ `ladder`) тем же заходом, что и переезд.

**Чем меряется.** `npm run check:scale` — числа владельца (лестница не
убывает и не сходится ближе 8%, разброс ступени не больше 2.5, рампа
доходит до обоих концов, воздух не меньше трёх полей карточки и растёт
×1.33…1.5, поле не ноль, под пальцем зазор не меньше 16, концы — швы) плюс
файлы стилей: ступень, объявленная ЧИСЛОМ мимо строителя, — находка.
Переобъявить роль ролью (`--pad-sheet: var(--pad-card)` на тёмной палубе)
законно и находкой не считается.

Чего эта проверка не ловит, и это замер, а не пропуск: подмену роли ролью
НА КОРНЕ — она тоже второй источник выбора, но отличить её от законной
местной подмены чтением файла нельзя.

`tests/kit.test.ts` — пять сторожей: строитель повторяет рампу знак в знак;
списанный свободный член ловится подстановкой; сошедшаяся лестница —
находка; выпущенное сходится с числами и отстать не может; проект со
шкалами, но без чисел — находка, а не зелёная самопроверка.

**Показывается глазами** (И197): `npm run scale:stand` собирает стенд — три
набора ритма на одном и том же товаре, ползунок ширины от 320 до 1600 и
живые числа рядом с картинкой. Заведено по слову владельца: «я понятия не
имею, что такое строитель шкал… только визуально мне показывай, ибо я не
понимаю».

---

## И203 · Розовый отклонён дважды — и это решение, а не правило

Краска «скидка» была `#D6409F` — розово-пурпурная. Заказчик отклонил её
словами «запрети розовый цвет», и это был **второй** отказ от розового за
сутки: первым 21.09.2026 он забраковал пастельный красный `#F2B8B5` в тёмной
теме (И193).

**Как писать.** Скидка — `#6A4CA8`, фиалка. Выбрана заказчиком глазом из трёх
кандидатов, показанных на самой плашке `−15 %` в витрине, а не из списка имён
и кодов.

Числа приложены к выбору, а не вместо него. Годными были только те краски,
что расходятся на ΔE 25 с маркой **каждого** из семи наборов, с бедой, с
предупреждением и с «в наличии»: слива `#7A3E9D` (худшее расхождение 43.6),
индиго `#4A4FBF` (40.8), фиалка `#6A4CA8` (35.3). Отпали числами: бордо
`#8C2F39` сливается с «Тёплым листом» (22.9), графит `#4A5568` — с «Мек
островом» (17.6), хаки-золото `#8A6D1F` — с «Латунью на угле» (10.3).

Побочная выгода замерена: на всех трёх годных знак получается **белым**, а на
розовом строитель был вынужден ставить чёрный.

**Дефект.** Сам по себе розовый ничего не нарушал: он проходил все правила, и
на плашке скидки его знак читался. Дефект в другом — в том, что краска
события выбиралась **исполнителем по умолчанию чужой системы**, а не
заказчиком. Витрина магазина — его работа, а не моя, и это записано в
`CLAUDE.md`: «Заказчику: тексты, отзывы, фотографии, имена, реквизиты,
контакты, и то, какие блоки стоят на витрине».

**Чем меряется.** Ничем — и проверки здесь не будет намеренно. Это прямо
сказано в И193: «Ничем автоматическим — и это сказано вслух, чтобы никто не
завёл проверку „краска не розовая“ на глазок». Запрет розового — вкус
ВЛАДЕЛЬЦА ЭТОГО магазина, а набор переносимый: другому магазину розовый может
идти. Держится решение тем, что краска одна во всех образцах, а
`tests/kit.test.ts` не даёт файлу разойтись с разбором (И192).

---

## И204 · Измеренная граница — тем контролам, у которых больше ничего нет

В наборе есть роль `--border`: первая ступень, которая берёт 3 : 1, выбранная
замером, а не номером (И-правило «Граница органа управления и кольцо фокуса —
замером»). Это верная краска — но не для всякого контрола.

**Как писать.** Поле ввода со своей заливкой отличается от карточки уже
заливкой: обводить его ещё и измеренной линией — рисовать одно и то же
дважды. В покое такое поле берёт `--line`, тихий разделитель. Измеренная
`--border` остаётся там, где у контрола нет ни заливки, ни надписи, и граница
— единственное, чем он себя показывает.

Основание не на вкус, оно в самом WCAG, и оно уже процитировано в
`palette.md`: «If a control has visible content… a border or other indication
of the overall boundary… is not required».

**И кольцо фокуса не удваивается с границей.** Оно ложится внутрь контрола
(`outline-offset: -1px`), а граница на время фокуса уходит в прозрачную:
кольцо и так обязано брать 3 : 1, и второй тёмной линии рядом с ним не нужно.

**Дефект.** 21.09.2026 заказчик посмотрел на витрину и сказал про поле
количества: «окантовка поля великовата, или чрезмерно ярка, или толстовата —
нужно минималистичнее». На снимке видно причину: поле несло тёмную `--border`
по кругу И золотое кольцо фокуса со смещением наружу — два канта подряд на
контроле, который и так отличался заливкой. Ни одна проверка этого не видела
и не могла: каждая краска по отдельности брала свой порог.

**Чем меряется.** Ничем автоматическим: «сколько линий подряд — много»
машина не знает. Держится тем, что роль названа по работе и в разборе сказано,
какому контролу какая достаётся.

---

## И205 · Узел берёт роль; имя по оттенку живёт ярусом ниже

В наборе 19 красок названы по оттенку — `--sage-*`, `--cyan-*`, `--amber-*`.
Это само по себе НЕ дефект: ярус значений так и называют. У Material это
`primary40`, у Radix `gold-9`.

Дефект в другом: **узлы брали эти значения напрямую**, минуя роль. 55 мест в
`primitives.module.css` и `base.css`.

**Как писать.** Закон — `docs/layers.md`, §4: «Три яруса, ссылки в одну
сторону. hex/rgb/oklch — только в файле палитры; роли ссылаются на палитру;
узлы — только на роли». То же в ответе агента по Material: «sys указывает на
ref, comp — на sys/ref, hex в comp не пишется».

**Дефект.** 21.09.2026, при показе ворот этапа 0 заказчику. Выяснилось, что у
двух самых частых красок набора — основного текста и приглушённого — имени по
работе не было ВООБЩЕ. Роли не существовало, и узлам ничего не оставалось,
кроме как звать краску по оттенку: `color: var(--sage-12)` тридцать пять раз.

Цена видна на соседнем примере в тот же день. Плашка скидки стояла
`background: var(--amber-9)` — янтарём. Заказчик за час до этого выбрал
скидке фиалку, она легла в палитру ролью `--sale-9`, а до плашки не дошла:
один вопрос решался в двух местах, и они разошлись молча.

**Как исправлено.** Заведены роли: `--ink` (основной текст), `--ink-soft`
(приглушённый), `--plate-quiet` (тихая плашка), `--thumb` (бегунок
прокрутки), `--tick-edge` (рамка галочки). Значения не изменились ни на
уровень — это те же краски, получившие имя. Плашка скидки переведена на
`--sale-9` / `--on-sale-9`, и это единственное место, где цвет на экране
поменялся: янтарь → фиалка, как заказчик и выбрал.

**Чем меряется.** Поиском: `var(--sage`, `var(--cyan`, `var(--amber` в
`primitives.module.css` и `base.css` — ноль. В `tokens.css` они остались, и
это законно: там ярус значений. Храповик вёрстки после правки не вырос.

---

## И206 · Глазом смотрят двое, и путать их нельзя

Ворота этапа делились на «машиной» и «глазом». Второй список был ОДИН — и в
нём лежало вперемешку то, что смотрю я, и то, что решает заказчик. Печатался
он весь, как будто весь его.

**Как писать.** Список «глазом» делится по хозяину. **Исполнителю** — токены,
структура, поведение, замеры снаружи: по `CLAUDE.md` это «решения
принимаются и вносятся без вопросов». **Заказчику** — как выглядит витрина,
что на ней написано, чьи снимки и реквизиты. Заказчику печатается только его
половина; моя половина печатается мне и отвечаю за неё я.

Счёт на сегодня: пятнадцать пунктов мои, семь заказчика. Показывались ему
все двадцать два.

**Дефект.** 21.09.2026. Я показал заказчику три пункта ворот этапа 0 и
спросил, принимает ли он их. Два из трёх — «швов раскладки ровно три» и «роли
цвета названы по работе» — про имена переменных и число брейкпоинтов, то есть
про мою собственную работу.

Ответ: «я не знаю, принимать это или нет, я не понимаю сути; я могу оценить
визуально, но нечего — потому только на тебя полагаться могу».

Он прав дважды. Это переложенная работа, прямо запрещённая в `CLAUDE.md`:
«Вопрос без показанного — это переложенная работа». И это подрыв доверия к
самому вопросу: если половина спрошенного — не его дело, он перестанет
отвечать и на ту половину, которая его.

**Чем меряется.** `npm run stage` печатает у каждого пункта хозяина: «(смотрю
я)» или «(РЕШАЕТ ЗАКАЗЧИК)». Реестр `tools/stages.mjs` держит два списка
вместо одного, и новый пункт нельзя завести, не назвав хозяина — поле не
существует само по себе.

---

## И207 · Роль текста — это пять фактов, а не один

Шкала отвечает на «какого размера». Набор текста — это размер, межстрочье,
вес, разрядка и мера строки. Пока названа одна пятая, остальные четыре
набирает каждое место само — и набирает по-разному.

**Как писать.** Роль объявляется целиком и в одном месте — в
`styles/scale.json`, разделом `текст`, и выпускается строителем:

```
"body": { "род": "текст", "размер": "--fs-base", "межстрочье": 1.45,
          "вес": 400, "разрядка": "0", "мера": "--measure" }
```

Размер роль **берёт** — ступень лестницы или названную кривую
(`--hero-size`, `--fs-page` считают свой контейнер, а не окно, и остаются
там, где объявлены). Своего числа размера у роли нет: это была бы вторая
шкала. `род` — «заголовок» или «текст», и от него зависят пороги. Мера может
быть «нет»: заголовку её назначают вёрсткой. Пустой она быть не может —
молчание и «нет» разные вещи.

Роли объявляет ПЕРВЫЙ набор: межстрочье, вес и разрядка — факты типографики,
а не плотности, и от «тесно / просторно» не зависят. Размер меняется вместе
с набором сам: роль ссылается на ступень.

**Дефект.** 21.09.2026, замер по файлам набора. Два крупных заголовка в
одном файле примитивов: `.ledeText h1` — межстрочье 1.1, вес 700, разрядка
−.04em; `.pagehead h1` — вес 700, разрядка −.03em и **ни одного межстрочья**.
Второй наследовал 1.45 от тела страницы: на 42 пикселях это 61 пиксель между
строками при каноне 46, на телефоне — 43.5 вместо 33. Заголовок из двух строк
разваливался, и виноватого в файле видно не было: там нечего смотреть.
Разрядка при этом у двух одинаковых по работе заголовков разошлась на .01em —
никто не выбирал, просто набрали дважды.

**Чем меряется.** `npm run check:scale` — роль полна (шесть полей названы);
межстрочье заголовка 1.1…1.25, текста 1.35…1.6; вес из 400/500/600/700;
разрядка не больше .05em в любую сторону, отрицательная — только у
заголовка, положительная — только у мелкого текста. `check:css`, семья
`typeGuess` — правило, которое БЕРЁТ размер из шкалы и тут же набирает
межстрочье или разрядку числом (включая запасное число внутри
`var(--body-lead, 1.45)`: правишь роль, а земля держит своё). `tests/kit.test.ts`
— каждый порог обратным ходом и полнота выпущенного.

Вес числом НЕ спрашивается, и это замер, а не пропуск: жирность у контролов
— их собственное дело, она стоит в двух десятках мест, не имеющих отношения
к набору текста.

**Показывается глазами** (И197): `npm run scale:stand`, кадр «Роли текста» —
девять ролей на болгарском тексте и пара «было / стало» для заголовка
страницы, с живым замером межстрочья рядом.

---

## И208 · Глаз, который можно заменить счётом, заменяется счётом

Ворота этапа делятся на «машиной» и «глазом» (И206). В списке «глазом» у
исполнителя оставались два пункта, которые глазом не смотрят вовсе: «швов
раскладки ровно три, и каждый назван в CLAUDE.md» и «роли цвета названы по
работе, а не по оттенку». Это счёт: посчитать медиазапросы и посчитать
обращения к ярусу значений.

**Как писать.** Пункт остаётся «глазом» только тогда, когда его нельзя
посчитать: вид, пропорция, «дорого ли выглядит». Всё остальное уходит в
машинную половину ворот в тот же день, когда это замечено. Признак: если,
отвечая на вопрос заказчика, исполнитель открывает файлы и считает —
считать должна проверка, а не сессия.

**Дефект.** 21.09.2026, дважды за час. Сначала заказчик на показ ворот
ответил: «я не знаю, принимать это или нет, я не понимаю сути; я могу
оценить визуально, но нечего — потому только на тебя полагаться могу». Пункты
переложили в мою половину (И206) — но считать их по-прежнему было нечем, и
через сорок минут на вопрос «этап сделан?» я посчитал их руками: один
медиазапрос на окно, ноль обращений по оттенку. Замер руками умирает вместе с
сессией; следующая начнёт с того же вопроса.

Второй дефект — рядом и того же рода. Правка И205 перевела 55 мест с имён по
оттенку на роли и **сторожа не завела**: следующий узел возьмёт `--sage-12`
снова, и никто не заметит. Цена уже была показана в тот же день — плашка
скидки стояла янтарём, заказчик выбрал фиалку, она легла в палитру ролью, а
до плашки не дошла.

**Чем меряется.** Ворота этапа 0, машинная половина: швы собираются из всех
файлов стилей (контейнерные запросы не в счёт — компонент меряет свою
коробку, запрет 6), каждый обязан быть в списке `kit.config.json` и назван в
`CLAUDE.md`; долг по оттенкам читается из храповика. Семья `hueDirect` в
`check:css` — узел, зовущий `var(--sage-12)` мимо роли; список семей яруса
значений живёт в `kit.config.json`, ключ `hues`, потому что у каждого проекта
свои имена.

Обратным ходом проверено трижды: шов 900 в любом файле стилей — ворота
краснеют; шов, убранный из `CLAUDE.md`, — краснеют; долг по оттенку в базе —
краснеют. Заодно поймана собственная ошибка: путь файла резался на один
символ длиннее (корень приходит из URL с косой чертой на конце), и проверка
молчала нулём на подложенном шве.

---

## И209 · Слово, сказанное глазами, записывается в файл ворот

Пункт ворот, который смотрят глазами, живёт ровно до конца сессии. Заказчик
подтвердил — следующая сессия спросит его снова, потому что подтверждение
нигде не лежит.

**Как писать.** `docs/gate.md`: строка `- [x]`, в ней дата, кто смотрел,
**текст пункта целиком** и чем подтверждено. `npm run stage` читает файл и
печатает такой пункт галочкой, а в счёт «до перехода» его не берёт.
Переписали пункт — подтверждение лапается: другой вопрос требует другого
ответа, и это верно.

**Дефект.** 21.09.2026. Заказчик сказал «подтверждаю» про набор цвета —
последний пункт ворот этапа 0. Записать это было некуда: в реестре этапов
пункт — строка текста, а не состояние. Через сессию его спросили бы заново,
и это ровно та же болезнь, что в И208: замер, сделанный руками, умирает
вместе с сессией.

**Чем меряется.** `tests/kit.test.ts`: пункт, записанный в `docs/gate.md`,
считается подтверждённым; он же, переписанный на одно слово, — нет.
Обратным ходом: убрать `- [x]` — `npm run stage` снова печатает квадрат.

---

## И210 · Порядок своей работы исполнитель не выносит на выбор

**Как писать.** Вопрос заказчику задаётся тогда и только тогда, когда ответ
меняет ВИТРИНУ: что на ней стоит, что написано, каким цветом. Когда чинить
своё же отставшее число, в какой ветке, сейчас или следующей правкой — это
не его вопрос, и задавать его значит перекладывать свою работу. Закон —
`CLAUDE.md`, «Граница ответственности»: «Исполнителю: токены, компоненты,
раскладка, поведение, состояния, доступность, поведение по ширинам,
структура кода. Здесь решения принимаются и вносятся без вопросов».

**Дефект.** 21.09.2026. Я нашёл у себя расхождение — в описании этапа стояло
«пять примитивов раскладки» при одиннадцати в коде — и спросил заказчика,
чинить сейчас или со следующей правкой. Ответ: «поправь или со следующей
правкой, плять я не знаю как лучше». Он прав: вопрос был не его, и ответа на
него у него быть не могло. Правильно было починить и сказать строкой в
отчёте.

**Чем меряется.** Ничем автоматическим, и это сказано вслух: машина не
отличит вопрос по делу от переложенного. Держится тем, что стоит рядом с
«Вопрос без показанного — это переложенная работа» (И197) — и тем, что цена
уже уплачена вслух.

---

## И211 · Скидка — краска НАБОРА, а не постоянная

Плашка скидки стояла одним цветом во всех семи наборах: фиалка, выбранная
для одного набора, была скопирована во все как постоянная.

**Как писать.** Скидка — не статус вроде красного «нет в наличии», а
СОСЕДКА марки: обязана быть явно другой, но из того же мира. Набор, у
которого скидку не назвал заказчик, получает её **выведенной из марки**:
тон марки + 60°.

Правило не наше. Material берёт третью краску схемы ровно так:
`TonalPalette.fromHueAndChroma(sanitizeDegreesDouble(sourceColorHct.hue +
60.0), 24.0)` — снимок первоисточников, `material-color/dynamic_scheme.ts`,
схема TONAL_SPOT, она же у Material по умолчанию. Если +60° встаёт слишком
близко к красному, оранжевому или зелёному, поворот продолжается шагом 30°,
пока все пять красок не разойдутся на 25 ΔE (И196).

Названная заказчиком скидка остаётся как есть: его выбор старше всякого
правила, и правило применяется только там, где выбора не было.

**Дефект.** 21.09.2026. Заказчик открыл стенд и сказал: «наборы цвета на
скидке у тебя во всех наборах фиолетовый, это ж неправильно». Замер
подтвердил: у синей марки фиалка стояла в 35 ΔE — вдвое ближе, чем у
остальных, и «другой краской» уже не читалась, а у остальных выглядела
чужой заплаткой. Выведенные скидки разошлись с марками на 28…78 ΔE.

**Чем меряется.** `tests/kit.test.ts`: набор без скидки получает выведенную,
и она расходится с маркой и статусными красками не меньше чем на 25 ΔE;
набор с названной скидкой её сохраняет. `check:palette` держит попарную
различимость пяти красок (И196).

---

## И212 · Переключатель темы на стенде переключает `color-scheme`, а не признак

Кнопки «Светлая» и «Тёмная» на стенде цвета не делали ничего.

**Как писать.** `light-dark()` слушает свойство `color-scheme`, а не признак
на документе. Признак сам по себе тему не меняет — к нему нужна пара строк:

```css
:root[data-theme="light"]{ color-scheme: light }
:root[data-theme="dark"]{ color-scheme: dark }
```

**Дефект.** 21.09.2026, там же: «и светлая тема в наборе не работает».
Стенд собирался мной и проверялся снимками, где тему задавал БРАУЗЕР
(эмуляция системной темы в Playwright), — то есть проверялось всё, кроме
самой кнопки. Заказчик нажал и увидел пустоту.

**Чем меряется.** Снимком с нажатием: на системной ТЁМНОЙ теме нажимается
«Светлая» и читается цвет фона страницы. Было `rgb(27,25,21)` до нажатия и
после; стало `rgb(249,249,248)` после нажатия. Проверка живёт в
`tests/kit.test.ts` без браузера: в собранном стенде обязаны быть обе
строки `color-scheme` под признаком темы.

---

## И213 · Выбранный набор едет в проект ключом, а не памятью

Заказчик выбрал набор на стенде — и остановился с вопросом: «ну я тебе
назвал палитру, но ты же в скиле, ты палитру передашь как-то в проект?»

Он прав был дважды. Набор цвета, выбранный на стенде, лежал в `styles/`
САМОГО набора, а новый сайт по И199 получает серый стартовый и напоминание
спросить фирменный цвет. То есть выбор, за который заказчик заплатил
временем, при постановке терялся, и его пришлось бы делать заново.

**Как писать.** Выбор переносится ключом:

```
node install.mjs --palette "Латунь на угле" ../мой-сайт
```

Названный набор берётся из образцов набора (`templates/palette.json`),
кладётся в `styles/palette.json` проекта и тут же выпускается в
`styles/palette.css` тем же кодом, что считает проверка. Без ключа всё
по-прежнему: серый «Стартовый — заменить» и напоминание в воротах, потому
что новый сайт не начинается с чужой марки.

То же и с ритмом — это такой же выбор, сделанный глазами на стенде:

```
node install.mjs --palette "Латунь на угле" --scale "Просторный" ../мой-сайт
```

Наборы ритма, в отличие от красок, не выбрасываются: названный встаёт
ПЕРВЫМ, остальные остаются рядом, потому что на корне стоит первый (И198),
а сравнить бывает нужно.

**Дефект.** 21.09.2026, вопросом заказчика. Поймано ДО первой постановки —
и поймал не я.

Второй дефект того же ключа, мой: папку назначения ставщик искал как первый
довод без двух чёрточек, и «Латунь на угле» принял за путь. Прогон упал на
попытке скопировать набор внутрь самого себя.

Третий — мой же, и показательный: ритм я переставлял в json, а выпустить
`styles/scale.css` заново забывал. Проект уезжал с ритмом одного набора в
json и другого в CSS. Собственный код ставщика этого не видел: красками
занимается одна ветка, ритмом другая, и шов между ними никто не мерил.
Поймал написанный тут же тест — до пуша, до постановки, до заказчика.
Отсюда общее: **всё, что ставщик переписывает в источнике, он обязан тут же
выпустить тем же кодом, что мерит проверка.** Переписать источник и не
выпустить — значит отдать проект расходящимся с первой минуты.

**Чем меряется.** `selftest/install.test.mjs`: названный набор цвета и
названный набор ритма уезжают в проект и проходят там замер; выпущенные
`palette.css` и `scale.css` не отстают от источника (`--check` обоих);
неизвестное имя — отказ со списком, а не тихий стартовый; без ключа
приезжает стартовый; папка назначения не путается с именами, когда ключей
два.

## И214 · Показано должно быть всё выпущенное, а не то, что поместилось

Заказчик спросил: «в палитре сколько цветов ты вообще палитру показывал?»

Ответ по файлам: на стенде выбора стояло **14 переменных из 45**. Стенд
показывает карточку товара — значит показывает ту часть палитры, которая на
карточке видна: бумагу, текст, марку, кнопку, скидку. А восемь средних
ступеней серого, восемь средних ступеней марки, все ступени внимания и
успеха, разделитель, окантовку и кольцо фокуса машина вывела и увезла в
сайт, ни разу не показав.

Выбор при этом считался сделанным: ворота этапа «набор цвета показан
заказчику отрисованным» стояли закрытыми, и по букве правильно — кнопку он
нажимал. По сути нет: он выбрал набор по трети набора.

**Как писать.** У стенда и у листа разные работы, и подменять одну другой
нельзя:

* **стенд** (`npm run palette:stand`) — выбор: наборы рядом на одном и том
  же предмете, заказчик щёлкает;
* **лист** (`npm run palette:sheet`) — состав: ВСЁ выпущенное, обе темы,
  каждая краска подписана своей работой словами, а не светлотой.

Подписи ступеней — не мои слова: назначение ступеней взято у Radix
(`research/site-building-2026-09-20/raw/sources/radix-colors/docs-understanding-the-scale.mdx`):
1–2 фоны, 3–5 фоны органов управления, 6–8 границы, 9–10 заливки, 11–12
текст.

**Дефект.** 20.09.2026, вопросом заказчика — снова не мной.

Второй дефект того же листа, мой, и он показывает, зачем тут счёт: шесть
красок (`--on-a-9`, `--a-press` и четыре `--on-*-9`) на листе РАБОТАЛИ —
ими был покрашен знак на заливке, — но названы не были. Глазами лист
выглядел полным. Поймал тест, сверивший имена на листе с именами в
выпущенном CSS.

**Чем меряется.** `tests/kit.test.ts`: каждое имя из `styles/palette.css`
встречается на листе. Добавится краска — тест падает, пока она не встанет
на лист. Это тот же ход, что И208: глаз, который можно заменить счётом,
заменяется счётом.

## И215 · Сообщение о факте — своя краска, а не одолженная у успеха

Заказчик спросил: «когда-то было 8 цветов, сейчас иначе — 8 не нужно?»

Восьми в наборе не было никогда (история красок: 4 → 7 → 6), но вопрос
попал в дыру. Сигналов было четыре: ошибка, скидка, внимание, успех. А
сообщений о ФАКТЕ — «доставка 3–5 дней», «закон ЕС: до 0,2 % ТГК», «товар
вернулся в наличие» — на витрине магазина больше, чем сообщений об успехе,
и красить их было нечем. В таких случаях берут соседнюю краску, и обе врут:
зелёный говорит «удалось» там, где ничего не удавалось, оранжевый — «случилось»
там, где ничего не случалось.

**Как писать.** Сведение — пятый сигнал, синее. Не выводится из марки, как
скидка: скидка — СОСЕДКА марки и обязана быть из её мира, а сведение обязано
узнаваться само. Во всех разобранных наборах оно синее — Carbon `info`,
Spectrum `informative`, Polaris `info`. Radix пишет прямо
(`research/site-building-2026-09-20/raw/sources/radix-colors/docs-aliasing.mdx`):

> If you map `blue` to "accent", you might also need `blue` to communicate "info".

То есть у набора с синей маркой сведению нужна СВОЯ краска, а не марка.
Поэтому синий постоянный — как оранжевый «мало осталось» и зелёный «в
наличии», — и поворачивается тем же ходом, что скидка, только если подошёл
к чему-то из набора ближе 25 ΔE.

Набор от этого не потолстел: рукой по-прежнему названо шесть красок, седьмая
и восьмая выводятся.

**Дефект.** 20.09.2026, вопросом заказчика. Третий раз за день дыру в наборе
называет он, а не я, — и все три раза вопросом «а это точно так?», а не
разбором кода.

**Чем меряется.** `check:palette`: пять сигналов и марка обязаны расходиться
ПОПАРНО на 25 ΔE — сведение вошло в тот же попарный замер, отдельного правила
ему не понадобилось. `tests/kit.test.ts`: лист палитры показывает все
выпущенные краски, значит и `--info-*`.

## И216 · Постоянная краска живёт в строителе, а не в каждом наборе

Заказчик спросил: «рукой названо шесть — так правильно и было так задумано
или нет? это по системе, которую мы сегодня вывели из исследований?»

Ответ по файлу, а не по памяти: **нет**. Счёт показал вот что.

| роль | разных значений на семь наборов |
|---|---|
| бумага | 7 |
| чернила | 7 |
| фирменный | 7 |
| красный «нет в наличии» | **1** |
| оранжевый «мало осталось» | **1** |
| зелёный «в наличии» | **1** |

То есть по-настоящему рукой называлось ТРИ краски, а три оставшихся были
одной и той же постоянной, переписанной семь раз: сорок две записи на шесть
чисел. «Шесть красок рукой» — не решение, а то, во что сложилась история
файла: 4 → 7 → 6. Из исследований выведена СТРУКТУРА (двенадцать ступеней,
работа каждой, пороги, APCA), а состав того, что вводится рукой, никто не
выводил ни разу.

**Как писать.** Набор — это три краски на тему: бумага, чернила, фирменный.
Красный, оранжевый и зелёный живут в строителе: их узнают не по набору, а по
цвету вообще — зелёная кнопка «в наличии» зелёная и в аптечном наборе, и в
оливковом. Набор вправе назвать свою: названное живёт, неназванное берётся
из строителя.

**Дефект.** 20.09.2026, вопросом заказчика — четвёртый раз за день дыру
называет он.

И второй, который эта правка ЗАКРЫЛА. В `palette.md` с 21.09.2026 лежала
записанная находка: у «Тёплого листа» терракотовая марка стоит в 18.9 ΔE от
общего красного при норме 25 — «нет в наличии» рядом с такой маркой не
читается как запрет. Находка была известна, задокументирована и закреплена
тестом — и не чинилась, потому что чинить было НЕКУДА: красный стоял
записанным в каждом наборе, и правка в одном ничего не говорила об
остальных. Как только постоянные уехали в строитель, у набора появилось
право назвать свой красный: `#E5484D` вместо `#B3261E` даёт 27.3 ΔE.

Отсюда общее: **известный дефект, который «нельзя починить», почти всегда
упирается не в цену починки, а в то, что вещь лежит не там.** Переложили —
и чинить стало можно в одну строку.

**Чем меряется.** `tests/kit.test.ts`: прогон по всем семи образцам обеими
темами не даёт находок (было «ровно одна, и та задокументирована»); и ни
один образец не записывает постоянную краску своей рукой.

## И217 · Части скилла и описание набора идут порядком работы: что за чем

Заказчик открыл набор на GitHub и сказал: «сделай какую-то упорядоченность
в частях того скилла на гитхабе, и в описании всегда делай
последовательность, шаги, по которым делаем сайт, что за чем идёт».

**Как писать.** Описание — порядком, а не складом. README начинается с
«что за чем»: семь этапов воротами и шаги внутри первого, каждый с командой
и скиллом; перечень «что внутри» идёт после. Закон каждого скилла держит
раздел о порядке работы, и части скилла, где это возможно, идут этими же
шагами: у `palette` десять шагов — десять разделов в том же порядке.

**Дефект.** 20.09.2026. README набора начинался с «что решено раз и
навсегда» и таблицы «что внутри» на тридцать пять строк; порядок работы
стоял последним разделом («Первое, что надо сделать» — в самом конце). Скилл
`palette` шёл от смысла к порядку: семь семей, роли, и только пятым разделом
— порядок из десяти шагов, а закон шкалы — после него. Читающему с телефона
это склад: видно, что есть, и не видно, что за чем.

**Чем меряется.** `check:rules`: у закона каждого скилла набора есть раздел
второго уровня со словом «порядок» или «шаг» в заголовке; в README есть
раздел «что за чем». Нет — сборка красная.

## И218 · Формула живёт рядом со своей темой и своей строкой кода

Заказчик, увидев `palette/references/formulas.md`: «ты же понимаешь, что
будет ещё куча информации помимо палитры, и будут ещё писаться формулы в
этот же файл или другой — не возникнет ли путаницы позже?»

Возникнет — если файл с именем «формулы» примет любую формулу. Так уже
было с числами: правило ссылалось на шкалу, которой в коде не было, и
каждая сессия ставила число на месте. Формула без строки кода — тот же
совет, только длиннее.

**Как писать.** Общего файла формул в наборе нет. Формула кладётся в
справочник СВОЕЙ темы, рядом с именем функции, которая её считает: цвет —
`palette/references/formulas.md` и `tools/palette.mjs`; размер и ритм —
`craft/references/scales.md` и `tools/scale.mjs`; движение и отклик —
`craft/references/controls.md`. Файл формул объявляет свою границу в шапке
и называет чужие места.

**Дефект.** 20.09.2026, вопросом заказчика, до первой чужой формулы в
файле: поймано на имени файла, а не на путанице.

**Чем меряется.** `check:rules`: у файла формул есть свой код (реестр в
проверке), и каждый раздел файла называет функцию или переменную, которая в
этом коде существует; раздел без такой строки — находка. Разделы обозначений
и чужих формул для сверки названы так в заголовке и не сверяются.
\n
## И219 · Факты о наборе собираются из кода, а старая палитра рукой — снята

Заказчик открыл README набора и прочёл «задаёшь семь красок — бумагу,
чернила, марку и четыре сигнала» через день после того, как строитель
стал просить три (И216). Его слова: «это автоматически должно происходить,
обновление информации», «я тебе должен говорить об этом?». Не должен.

**Как писать.** Факт о наборе — сколько красок называется рукой, сколько
семей, сколько переменных выпускается, какие наборы-образцы, какие
команды — в тексте не набирается, а собирается из кода в таблицу под
маркером `families:palette` (закон `palette`, README набора), тем же
ходом, что таблицы семей проверок (`check:rules --tables`). Число словом
рядом с «красок на тему» в скилле и README обязано совпадать с тем, что
просит строитель.

Той же породы была вторая палитра в `styles/tokens.css` набора: девять
ступеней `--sage-*` числами, лестница `--live-*` формулой, `--amber-*`,
одиннадцать чисел поверхностей и палубы, пять `oklch` ошибки и
разрешённого — при строителе, который считает то же самое. Набор читал у
строителя 17 переменных из 49; правка красок до листа и текста не
доезжала. Снято: имена по оттенку остались ярусом значений и читают
ступени строителя (`--sage-N: var(--n-N)`, `--live-N: var(--a-N)`),
поверхности и палуба — ступени с той же лесенкой светлоты, что стояла
числами, ошибка и разрешённое — ступени сигналов. Единственное число в
файле — затемнение, и оно там по правилу (color.md, «Затемнение»).

**Дефект.** 20.09.2026, вопросом заказчика. Четвёртый за день случай, когда
текст жил памятью сессии, а не кодом.

**Чем меряется.** `check:rules`: таблица фактов в законе `palette` и в
README совпадает с собранной из кода; число словом «N красок на тему» в
скиллах и README равно числу ключей стартового набора. `check:css` и
тесты набора — на снятой палитре рукой.

## И220 · Шаги внутри этапа живут в реестре с предикатом, а не в дайджесте исследования

Заказчик: «создание палитры — один из множества шагов, и мы его сделали и
утвердили. Твоя задача эти шаги понять, прописать, чтоб понять, какие шаги
последующие и предыдущие». Ответить было нечем: порядок слоёв (пороги →
имена → оси → цвет → база → пространство → типографика → узлы → раскладка
→ форма → состояния → знаки → слова → утилиты) лежал в `docs/layers.md` на
шестьсот строк, а реестр этапов знал только ворота и проверки. «Что
дальше» требовало читать исследование заново каждую сессию.

Той же породы: три места — закон `craft`, закон `shop`, `hook-on-stop` и
сам реестр этапов — отсылали к `docs/decisions.md`, которого не было.
Правило, ссылающееся в пустоту, — тот самый дефект, с которого начинается
CLAUDE.md набора. И `docs/start.md` учил ставить набор клонированием
cbdin.bg — источника, от которого заказчик отказался.

**Как писать.** У каждого этапа в `tools/stages.mjs` — `steps`: слои
порядком первоисточников (`docs/layers.md`, §2), у каждого предикат по
файлам (`✓` / `✗ причина`), а где решает заказчик — пункт, отмечаемый в
`docs/gate.md`. Печатает `npm run stage`; закон `stages` держит карту
«этап → слои» словами. Предикат, который проходит на пустом месте
(нет флагов — значит, все в true), — ложь: пустота называется причиной.
Что решил заказчик глазами или словом — в `docs/decisions.md`, с датой и
тем, на чём показано; файл существует с первого дня проекта, потому что на
него ссылается закон.

**Дефект.** 20.09.2026, вопросом заказчика: «какие шаги до и после палитры».

**Чем меряется.** `npm run stage`: у текущего этапа печатаются шаги с
вердиктом по файлам; шаг 0 основания красен, пока нет `docs/decisions.md`.
`check:stage` — ворота держатся.

## И221 · Пороги живут в одном месте, и у каждого — источник

Слой 0 исследования — «что нельзя нарушать ни одним слоем» — обещал: «пороги
в одном месте, каждый слой проверяется против них». Реализации не было: 44
и 4.5 стояли литералами в отрисованной проверке, 8 в файловой, 2.5 и 16 в
строителе шкал, 4.5 и 3 в строителе палитры. Правило, ссылающееся на
несуществующее, — тот самый дефект, с которого начинается CLAUDE.md.

**Как писать.** `tools/thresholds.mjs`: контраст, цель и зазор, клетка и
пол ритма, коридоры воздуха, нормы кегля, роли текста, органы, движение,
состояния, знаки. Над каждым числом — откуда оно (WCAG, Primer, Carbon,
замер живых магазинов); число без источника не ставится. Проверки читают
отсюда и только отсюда; строитель палитры глазами вставляет файл в
страницу как есть — поэтому файл ничего не ввозит.

**Дефект.** 20.09.2026, при ревизии основания против слоёв: шаг 0 значился
сделанным, а порогов в одном месте не было.

**Чем меряется.** `npm run stage`: шаг 0 и шаг 4 основания красны без
`tools/thresholds.mjs`. `check:rules`: таблица фактов скилла `scale`
печатает пороги из файла. Тесты набора — на прежних значениях проверок.

## И222 · Набор шкал пишется формулой, а не таблицей чисел прошлого проекта

Заказчик: «как в случае с палитрой — заново всё пересмотрел критически,
улучшил то, что сделано предыдущей сессией. Даже сами эти шаги ты взял, не
усомнившись, или проверил по исследованиям?» Проверил замером — и нашёл,
что строитель шкал, заведённый днём раньше, держал числа прошлого проекта
и защищал их оговорками: «ни одна из двух лестниц не геометрическая, и
загнать их в отношение значило бы поменять весь сайт ради красоты формулы»;
«клетку он не сторожит: наши ступени 10, 14, 18, 22 кратны двум, но не
четырём, и правило „всё кратно 4" пришлось бы нарушить в день его
написания». Одиннадцатую ступень не читал никто; воздух между полками стоял
в два поля карточки при замере 3–5.

**Как писать.** Набор в `styles/scale.json` — формулой: `тело` (два кегля),
`отношение` (два), ступени размера показателем степени, ступени ритма
множителем тела, поле — ступенью, воздух — парой ступеней, зазор — по
указателю. Строитель (`tools/scale.mjs`) считает: ступень размера = тело ×
отношениеⁿ (отрицательные — телефонным отношением на обоих концах); ступень
ритма = тело × множитель на клетке 2 / 4 / 8 (Tailwind 4), не выше пола не
течёт; пара воздуха — низ первой ступени → верх второй (Utopia). Замер
спрашивает клетку, нормы нижнего конца, отношение в коридоре, контраст
заголовка к телу, рост воздуха (разделы ×1.33…1.5 по замеру, прочий
×1.25…1.75), воздух к полю, и просителя у каждой ступени. Пара чисел на
любом месте читается по-прежнему — но спрашивается так же.

Множители 0.25 … 6 при теле 16 дают ряд Carbon `spacing-02…12` и дефолт
rhythmguard знак в знак — числа не наши и не прошлого проекта, а тех, кто
их проверил на сотнях продуктов. Порядок слоёв, из которого шаг взят,
записан честно: синтез агентов, не стандарт (`docs/layers.md`, §2.1).

**Дефект.** 20.09.2026, вопросом заказчика.

**Чем меряется.** `check:scale`: правила `клетка ритма`, `нижний конец по
нормам`, `отношение лестницы`, `заголовок к телу`, `верх не ниже низа`,
`воздух растёт в коридоре` (все воздухи парой), `ступень без просителя`;
тесты набора обратным ходом. `check:rules`: закон `scale`, факты из кода.

## И223 · Слой сделан, когда пересмотрен против исследования, а не когда файлы на месте

Заказчик получил таблицу слоёв основания, где у слоёв 7–10 (размер узлов,
раскладка, форма, состояния) стояло «сделано», и спросил: «ну отлично, что
ты сделал — это ж какой-то этап или подэтап, вноси в скилл… отчитывайся,
что сделано, что ещё нужно, какой следующий подэтап». «Сделано» там
значило «файлы есть»: предикат находил `--ctrl-h`, `--r-pill`, `--sh-1`,
`--hover-t` — и все они были сняты с cbdin днём раньше, против исследования
не пересматривались. Тот же самообман, что у шкал (И222): существование
принималось за качество.

**Как писать.** У шага в реестре (`tools/stages.mjs`) — `reviewed` (дата) и
`rule` (номер правила с дефектом, которым пересмотр куплен); `show` — где
слой показан глазами. `npm run stage` печатает четыре состояния: `✓`
пересмотрено, `○` есть, не пересмотрено, `✗` не начато, `□` решает
заказчик; итог по слоям и следующий подэтап — первый `○` по порядку.
Отчёт заказчику после подэтапа — этими же тремя строками, со ссылкой на
стенд.

**Дефект.** 20.09.2026, вопросом заказчика.

**Чем меряется.** `npm run stage`: слой без `reviewed` не печатается как
сделанный. `check:rules`: закон `stages` описывает четыре состояния.

## И224 · Имя живёт по форме, по ярусу и по просителю

Заказчик: «то, что было сделано другой сессией, ставь под сомнение; твоя
задача сделать сильно лучше». Слой 1 — имена и ярусы — значился сделанным:
для цвета три яруса держала семья `hueDirect`. Замер 20.09.2026 против
первоисточников (Curtis, Figma, Material 3, Atlassian, DTCG) показал:

- ручка примитива `stack` и стек шрифтов на корне — одно имя `--stack`;
  примитив читал стек как отступ, отступ был нулём, и ни один сторож этого
  не видел;
- ручки сетки `--n`, `--n-min` — под именем семьи нейтрали `--n-N`;
- узлы читали оттенок (`--live-3`, `--live-11`) мимо роли: семья `hueDirect`
  знала `sage`, `cyan`, `amber`, но не `live`;
- узлы читали ступени ритма и размера напрямую в тридцати местах;
- лестница управления и две кривые — числа рукой под именами сырья, кривые
  при этом читали роли (ссылка вверх);
- пятьдесят имён в `tokens.css` не читал никто — дизайн одного магазина;
- роли текста применялись наполовину: размер и межстрочье без веса и
  разрядки.

**Как писать.** Реестр `tools/names.mjs`: три яруса (сырьё → роль → узел),
понятия по назначению, уточнения списком, ручки примитивов, обязательные
роли по элементам. Четыре семьи в `check:css` по этому реестру:
`nameGrammar` (имя не по форме), `stepDirect` (узел читает ступень, кроме
оптики не выше пола), `deadName` (объявлено, а читателя нет ни в стилях, ни
в коде, ни в инструментах, и роль не обязательная), `tierUp` (ссылка вверх
или ручка узла на корне). Слово по виду в реестр не попадает. Лестницу
управления (`--ctrl-fs-*`) и зазоры ряда и сетки выпускает строитель шкал.
Роль применяется целиком.

**Дефект.** 20.09.2026, вопросом заказчика; нулевой отступ `stack` — по
замеру.

**Чем меряется.** `check:css`: четыре семьи на нуле; `check:scale`: ступень
без просителя; `check:rules`: реестр имён печатается из кода в
`craft/references/names.md`; тесты набора — разбор имени и обратный ход.

## И225 · Оси называются до значений, и у каждой записано, что она меняет

Слой 2 — оси — значился сделанным: тема через `light-dark()`, палец через
`pointer: coarse`. Замер 20.09.2026 против первоисточников (Spectrum
Platform scale и Theming, React Spectrum Provider, Carbon themes, MDN,
css-color-adjust, W3C qa-lang-why, Chromium hyphenation-patterns):

- языка как оси не было вовсе — ни `overflow-wrap`, ни `hyphens`, ни
  `quotes`, ни `text-size-adjust` — при четырёх языках магазина, у одного
  из которых (румынского) в Chromium нет словаря переноса;
- контраста не было — ни `prefers-contrast`, ни `forced-colors`: в режиме
  принудительных цветов лист без обводки сливается с полом;
- скроллбар красился рукой (`scrollbar-color`) вопреки `color-scheme`,
  который красит его сам и в обеих темах;
- реестра осей не было; медиазапрос по признаку вне списка никто не ловил.

**Как писать.** Реестр `tools/axes.mjs`: шесть осей — тема, указатель,
ширина, язык, движение, контраст, — у каждой чем включается, что меняется,
что не меняется, откуда. Четыре семьи в `check:css`: `axisUnknown`
(медиазапрос по признаку вне реестра), `axisTheme` (переменная под
`[data-theme]` или `prefers-color-scheme`), `axisScope` (раскладка или
видимость под указателем), `axisHover` (`:hover` вне `(hover: hover)`).
Язык: `overflow-wrap: anywhere`, `hyphens: auto` как улучшение, `quotes:
auto`, `text-size-adjust`. Контраст: `prefers-contrast: more` усиливает
роли, `forced-colors: active` даёт обводки, `outline`, `currentColor`.
Реестр печатается из кода в `craft/references/axes.md`.

**Дефект.** 20.09.2026, при пересмотре подэтапа 2 по слову заказчика.

**Чем меряется.** `check:css`: четыре семьи на нуле; шаг 2 в `npm run
stage` красен без языка, контраста, движения и реестра; `check:rules`:
реестр осей собран из кода.

## И226 · Размеров органа три, и все три — роли из порогов

Слой 7 значился сделанным: `--ctrl-h: 40px` в токенах и `.tap` под палец.
Замер 20.09.2026: высоты органов стояли восемью числами рукой — фишка 28,
подпись фишки 26, сегмент 30, лоток 42, орган 40, счётчик 54, канал меню
44, а под пальцем каждый орган поднимал себя сам (`min-height: 44px` в
пяти местах). Curtis называет это признаком сломанного каталога: «Small
input and small button aren't the same small». Порог `CONTROL` в файле
порогов лежал без единого читателя.

**Как писать.** Три роли — `--ctrl-h-sm`, `--ctrl-h`, `--ctrl-h-lg` — выпускает
строитель шкал из `CONTROL` в `tools/thresholds.mjs`: 32 / 40 / 48 под
курсором (Carbon `container`, Radix, Spectrum — клетка 8), 44 / 48 / 56 под
пальцем одной переменной по `@media (pointer: coarse)` (Primer
`control.minTarget`; WCAG 2.5.5; M3 48). Цель у знака мельче органа —
`--ctrl-target` 24 / 44. Орган берёт высоту ролью, поле и зазор считает от
неё, надпись — `--ctrl-fs`; размер ставится атрибутом `data-size`, который
переобъявляет две роли. Высота органа числом в узле — семья `ctrlSize`.
Своя семья, не ступень ритма (Carbon: «44px-class touch targets don't land
cleanly on 8px multiples»).

**Дефект.** 20.09.2026, при пересмотре подэтапа 7 по слову заказчика.

**Чем меряется.** `check:css`: `ctrlSize` на нуле; `check:scale`: роли
размера выпущены и растут под пальцем; шаг 7 в `npm run stage`; тесты
набора — три роли, коарс ≥ 44, узлы без высоты числом; стенд
`control:stand`.

## И227 · Шов — решение с именем и причиной, читаемое в обе стороны; край и холст — из набора; кадр — с потолком

Слой 8 значился сделанным: одиннадцать примитивов на месте, три числа
`1080, 820, 560` в списке `breakpoints`. Замер 20.09.2026 при пересмотре
подэтапа по слову заказчика:

- у трёх швов не было ни имени, ни причины — список чисел; в CSS набора
  медиазапросом читался один (820), два других (1080, 560) — только концами
  рамп, и проверка смотрела в одну сторону (ширина вне списка), а запись без
  читателя не ловила. Исследование требует обе стороны: «в CSS нет числа
  ширины, которого нет в реестре, и в реестре нет записи, которой нет в CSS»
  (`steps_layout-grid-layer.json`, layers[5].doneWhen); прецедент —
  `seams.ts` cbdshop с причиной у каждого шва;
- правило 6 «компонент меряет свой контейнер, а не окно» не имело семьи:
  на cbdshop.bg 146 медиазапросов по окну стоят внутри
  `components/*.module.css`; второе предложение правила 3 «ступенька размера
  на брейкпоинте — всегда дефект» тоже: 23 блока из 173 меняют только
  величину;
- правило 4 «у всего с `aspect-ratio` есть потолок» не имело примитива:
  кадра в наборе не было, место под снимок не резервировалось (CLS);
- холст `--wrap: 1520px` и край `--gut` с рукописным наклоном
  `-22.46px + 6.1538vw` лежали в `tokens.css` мимо строителя (И222); край
  рос 12 → 44 (×3.7), тогда как люкс держит его почти постоянным (Diptyque
  12 / 12, Muji 10 / 10, Byredo ×1.75), Atlassian 16 → 32 (×2), Utopia s→l
  18 → 40 (×2.2), Carbon — 16 фиксировано внутри шва;
- порог `LAYOUT` в файле порогов не читал никто: свип держал 320 / 1600 /
  40 / 6 / 0.45 литералами, отрисованная проверка — 657.

**Как писать.** Шов — запись в реестре `tools/seams.mjs` (у проекта —
`kit.config.json`, ключ `seams`): ширина, имя, что меняется (`turns`),
почему (`why`, не короче сорока знаков); швов не больше `LAYOUT.seams`.
Читатель шва — медиазапрос по ширине (±1) или конец рампы набора шкал.
Семьи: `breakpoint` (ширина в CSS без записи), `deadSeam` (запись без
читателя), `seamStep` (блок медиазапроса, меняющий только величину),
`nodeWindow` (медиазапрос по окну в узле `components/*.module.css`),
`fullVw` (`100vw` шире страницы на полосу прокрутки, `100vh` выше видимого),
`sizeContain` (`container-type: size` схлопывается). Холст (`--wrap`) и
край (`--gut`) выпускает строитель шкал из ключей `холст` и `край` набора;
край — пара ступеней ритма, рампа между швами, рост в коридоре
`LAYOUT.edgeGrowth` (×1…2.5). Кадр — примитив `frame`: пропорция ручкой
`--frame`, потолок `--frame-cap` от малого окна (`svh`, `LAYOUT.frameCap`),
снимок кадрируется внутри, место занято до загрузки. Свип идёт по
`sweepWidths()`: сетка шагом 40 от перетока 320, каждый шов и пиксель над
ним, сложенные внутренние экраны (690, 829, 836, 842). На корне —
`scrollbar-gutter: stable`.

**Дефект.** 20.09.2026, при пересмотре подэтапа 8 по слову заказчика.

**Чем меряется.** `check:css`: пять семей на нуле; `check:scale`: «край
растёт в коридоре», «холст не уже верхнего шва»; шаг 8 в `npm run stage`:
двенадцать примитивов, реестр по форме, шов без читателя, холст и край из
строителя, кадр с потолком; тесты набора — реестр, край и холст, кадр,
пороги с читателями; `check:rules` — факты о раскладке из кода.

## И228 · Радиус, линия и тень — роли со смыслом; полный круг — только главное действие

Слой 9 значился сделанным: «радиусы одной ручкой `--round`, лестница теней
`--sh-1…3`». Замер 20.09.2026 при пересмотре подэтапа по слову заказчика:

- радиусов четыре числа рукой в `tokens.css` (10 / 24 / 28 и пилюля) под
  ручкой `--round`; 10 нет ни в одной лестнице (M3: 0 / 4 / 8 / 12 / 16 /
  20 / 28 / 32 / 48, Carbon: 2 / 4 / 8 / 16 / 24); какой узел какую ступень
  берёт — не записано; пилюлю читали десять органов подряд — фишка, сегмент,
  лоток, счётчик, ссылка «ещё», плашка скидки, — тогда как Spectrum: «Full
  rounding … meant to draw attention to calls to action», а для «тихого
  люкса» исследование велит три радиуса и один полный круг — у кнопки «в
  корзину»;
- тени назывались номером (`--sh-1…3`), а не работой: лоток в покое, подъём
  под рукой и всплывающее меню читали одну и ту же лестницу без записанной
  причины; две вдавленные тени стояли числом на месте (`inset 0 1px 2px …`
  дважды, с разными красками); ингредиенты уже несли `light-dark()` внутри
  себя — это верно, и оставлено;
- толщина линии стояла числом: кольцо фокуса `outline: 2px … offset 3px`,
  чекбокс `1.5px`, обводка фишки `inset 0 0 0 1px`, forced-colors `1px`, —
  при том, что Spectrum держит 1 / 2 / 4 со смыслом и «border width remains
  the same for desktop scale and mobile scale»; порога формы в файле порогов
  не было.

**Как писать.** Радиусы — ключ `радиус` набора шкал (`xs`, `ctrl`, `card`,
`sheet`), числом из лестницы `SHAPE.radii` (M3 ∪ Carbon); строитель
выпускает `--r-xs`, `--r-ctrl`, `--r-card`, `--r-sheet` и `--r-pop: 999px` —
полный круг главного действия, который читает только дом контролов
(семья `popRadius`). Орган ≤ карточка ≤ лист («радиусы вложены»). Набор
«Тихий» острее прочих (4 / 4 / 12 / 16). Линия и кольцо — из порогов
`SHAPE.line`, `SHAPE.ring`: `--line-w` 1px, `--ring-w` 2px (WCAG 2.4.13),
`--ring-off` 3px, не текут. Тени — четыре роли по работе в `tokens.css`:
`--sh-raised` (предмет над полом в покое), `--sh-lift` (подъём под рукой),
`--sh-overlay` (всплывающее и закрываемое — Spectrum), `--sh-in`
(вдавленное); геометрия одна, источник света один (Refactoring UI); палуба и
лист переобъявляют роли целиком (И100). Глубина в тёмной теме — светлотой
поверхности, не тенью (Carbon, Atlassian; `check:theme`); в forced-colors
тень стирается — у органа есть `border`, у фокуса `outline` (base.css).
Число у радиуса, тени или линии в узле — семьи `radiusPx`, `shadowPx`,
`linePx`.

**Дефект.** 20.09.2026, при пересмотре подэтапа 9 по слову заказчика.

**Чем меряется.** `check:css`: четыре семьи на нуле; `check:scale`: «радиус
из лестницы», «радиусы вложены»; шаг 9 в `npm run stage`: роли формы
выпущены, старые имена не читаются; тесты набора; `check:rules` — факты о
форме из кода; стенд формы.

## И229 · Движение и состояния — роли по работе в коридорах, нажимаемое отвечает сразу

Слой 10 значился сделанным: `--hover-t` и `--a-press` на месте. Замер
20.09.2026 при пересмотре подэтапа по слову заказчика:

- длительность одна на всё — `--hover-t: 200ms` вела и цвет под рукой, и
  сжатие при нажатии, и выезд ссылки «к содержимому»; фишка вычисляла
  своё нажатие как `calc(var(--hover-t) * .6)`; кривая одна. Atlassian:
  «interactions 50–150 ms, transitions 150–400 ms», «ease-out на ввод,
  ease-in на уход»; Carbon 70…700; M3 — {fast, default, slow} × {spatial,
  effects}, не больше шести; пороги `MOTION` в файле порогов не читал никто;
- вуали состояния стояли числом (`--ink 4%` у строки, `5%` у органа),
  нажатие тихого органа брало вуаль наведения (`.chip:active`,
  `.seg button:active`, `.chan:active`) — копию, а не ответ «вглубь»; M3:
  слой состояния того же цвета, что содержимое, наведение 8 %, нажатие
  10 %; Carbon: hover полступени, active две; порог `STATE` тоже без
  читателя;
- выключенное — `opacity: .45` числом на лотке (M3 38 %, но не ниже Lc 30 и
  3 : 1 для «нет в наличии» — замечание проекта);
- нажимаемое ждало двойного тапа: `touch-action: manipulation` не было ни
  на одном органе (next_behaviour: 300 мс, «пишется один раз в общем
  pressable»).

**Как писать.** Три длительности по работе в `tokens.css` — `--press-t`
120 (ответ на нажатие, INP ≤ 200), `--hover-t` 200 (смена под рукой),
`--open-t` 280 (раскрытие всплывающего) — и две кривые: `--ease` вход
(ease-out), `--ease-exit` уход (ease-in; придёт с шторкой — REQUIRED).
Роли спрашиваются с коридоров `MOTION` (семья `motionOut`); число
миллисекунд в узле — `msLiteral`. Вуаль состояния — доля чернил
`--state-hover` 5 % и `--state-press` 9 % (коридоры `STATE`, семья
`stateOut`), из них `--hover-row` / `--hover-ctrl` и `--press-row` /
`--press-ctrl` на корне, палубе и листе; выключенное — `--state-off` 0.45,
и рядом слово или знак, не только серый. Нажимаемое — `touch-action:
manipulation` одной строкой в основании; раскрытие к `height: auto` —
`interpolate-size: allow-keywords` под `prefers-reduced-motion:
no-preference`. Двигаются `transform` и `opacity`; фон и чернила — только у
органа под рукой, смена темы не анимируется. Фокус — кольцо `--ring-w`
замером ≥ 3 : 1 (`palette`), `:focus-visible`, не тень.

**Дефект.** 20.09.2026, при пересмотре подэтапа 10 по слову заказчика.

**Чем меряется.** `check:css`: `msLiteral`, `motionOut`, `stateOut` на нуле;
шаг 10 в `npm run stage`; тесты набора; `check:rules` — факты о состояниях
и движении из кода; отрисованная проверка: `calm` (движение при «поменьше
движения»), `focus`, `inkDip`, `stolen`.

## И230 · Утилита делает одну работу; исключение — пометка на предмете, а не второй класс

Слой 13 значился сделанным: «`.muted`, `.eyebrow`, `.skip` — брать». Замер
20.09.2026 при пересмотре подэтапа по слову заказчика:

- **пилюля была нарисована дважды** в одном файле примитивов: два блока
  `.chip` — один с `height: var(--ctrl-h-sm)` и своей заливкой, второй с
  `--chip-h: 34px` и `block-size`. `height` и `block-size` — одно и то же
  свойство, и нижний блок молча перебивал верхний: половина написанного не
  действовала вовсе, а высота органа приходила числом 34, мимо ролей слоя 7
  (И226). Признака в файле не видно: обе записи выглядят верными;
- **варианты объявлялись вторым классом**: `.chipLab` при `.chip`,
  `.sectionTight` при `.section` — при том, что закон набора уже требует
  обратного («Настраиваются переменными, а не новыми классами», запрет 7), а
  CUBE говорит про исключение: «состояния через атрибут», и оно не должно
  «variate a block to the point where it isn't recognisable anymore»;
- **слой исключений не имел семьи вовсе**: ни вариант классом, ни состояние
  классом (`.is-open`, `.active`), ни разросшееся исключение не ловились
  ничем.

**Как писать.** Утилита — ярлык на ОДНУ работу, берущий роль (`cube.fyi`: «a
CSS class that does one job and does that one job well»): `.muted` — краска,
`.said` — только для чтения вслух, `.tap` — запас под палец, `.flush` —
отмена линии страницы. Своего числа утилита не заводит — это сторожат семь
семей (`fontPx`, `spacingPx`, `padPx`, `radiusPx`, `shadowPx`, `linePx`,
`msLiteral`). Второй шкалой в разметке утилиты не становятся: утилитарные
классы Tailwind и Utopia (`py-24 sm:py-32`, `.u-grid`) набор не переносит —
переносится связь, не классы.

Исключение — ПОМЕТКА на существующем предмете: `[data-*]` или ARIA на том же
узле (`data-size`, `data-tray`, `data-chip`, `data-air`, `aria-pressed`). У
атрибута два достоинства: его видит вспомогательная техника, и вес
`.chip[data-chip='lab']` (0,2,0) бьёт голый `.chip` (0,1,0) при любом порядке
кусков сборки. Поэтому **слоёв каскада (`@layer`) набор не заводит**: порядок
решается весом на одном узле и запретом спорить (семья `takenTwice`), а не
списком слоёв, который ещё надо держать в голове всем файлам сразу.

Семьи: `dressClass` — вариант вторым классом (все свойства уже есть у базы и
переодета не меньше половины); `stateClass` — состояние именем класса;
`dressGrown` — вариант объявил свойств больше, чем сам предмет: это новый
блок, а не исключение.

**Дефект.** 20.09.2026, при пересмотре подэтапа 13 по слову заказчика.

**Чем меряется.** `check:css`: `dressClass`, `stateClass`, `dressGrown` на
нуле; шаг 13 в `npm run stage`: утилиты на месте, пилюля одна, варианты
атрибутом; тесты набора; `check:rules` — факты об утилитах и исключениях из
кода.

## И231 · Имя без объявления рушит всю запись, а не себя одно: набор стоит на системном шрифте

Шрифт витрины в наборе был назван «выбранным и решённым»: в `tokens.css`
стояло `--face: var(--f-plex), var(--face-stack)`, а рядом комментарий —
«The fallback stack is named so a slow font never lands the page on Times».
Замер 21.09.2026 на чистых стилях набора, отрисованной страницей, а не
чтением файла:

```
getComputedStyle(body).fontFamily === "Times New Roman"
```

`--f-plex` приходил из загрузчика шрифтов ПРОШЛОГО проекта, и в наборе его
не объявлял никто. `var()` без запасного значения делает недействительной
ВСЮ переменную, а `font-family: var(--face)` тогда падает не на соседний
стек, а на унаследованное значение — то есть на умолчание браузера.
Обещание комментария выполнялось ровно наоборот. В файле дефект не виден:
обе переменные выглядят верными; стенды его маскировали, объявляя себе
`font-family: system-ui` сами.

**Как писать.** Шрифта в наборе нет и быть не может: его называет заказчик,
посмотрев на свой же текст (стенд `face:stand`, шаг 6 основания). Набор
стоит на системном стеке — `--face: var(--face-stack)`. Проект, когда шрифт
назван, переобъявляет `--face` ОДНОЙ строкой вместе со своим запасным
списком: `--face: var(--f-plex), var(--face-stack)`, где `--f-plex` объявляет
загрузчик шрифтов проекта. Заголовки могут взять второе семейство
(`--face-head`), и по умолчанию не берут: одно семейство — решение, два —
система.

Общее правило шире шрифта: **имя, которое читают без запасного значения,
обязано быть объявлено** — семья `varMissing`. Ручку примитива, которую
ставит узел или код, это не касается: у неё запасное значение есть всегда
(`var(--tap, var(--ctrl-target))`).

**Дефект.** 21.09.2026, при ответе на вопрос заказчика «какое решение по
шрифту».

**Чем меряется.** `check:css`: `varMissing` на нуле; тесты набора: ни одно
имя в стилях не читается без объявления, `--face` разрешается в системный
стек; стенд `face:stand` показывает кандидатов на тексте витрины.

## И232 · Этап каркаса не занимает номер дизайн-слоя

Диспетчер этапов называл адреса «слоем 14», хотя в исследовании и таблице
слоёв номер 14 уже означает узлы интерфейса. Один отчёт поэтому одновременно
утверждал две несовместимые последовательности и предлагал пересматривать
маршруты по исследованию визуальных слоёв, где маршрутов нет.

**Как писать.** Каркас приложения помечается `К`: это производственный шаг
между основанием и узлами, не новый дизайн-слой. Его основание — архитектура
конкретного проекта и живые HTTP-ответы. Нумерация 0–19 остаётся только у
слоёв из `docs/layers.md`.

**Дефект.** 21.09.2026, при первой постройке `cbdin.ro` по шагам набора.

**Чем меряется.** Тест реестра этапов: у каркаса метка `К`, а слой 14
остаётся за узлами; `npm run stage` печатает основание пересмотра шага.

## И233 · Этап требует только команды, которые установщик действительно заводит

На первой витрине, построенной по этапам, каркас потребовал перед сдачей
`check:tokens`, `build:site` и `check:rules`. В установленном `package.json`
не было ни одной из трёх команд: инструкция выглядела исполнимой, но каждый
запуск завершался бы `Missing script`.

**Как писать.** Универсальные команды этапа (`build:site`, `check:rules`)
стоят в едином реестре `scripts.mjs` и едут установщиком. Проектная команда
(`check:tokens`) входит в список этапа только когда проект сам её объявил.
При обновлении отсутствующие `docs/layers.md` и `docs/rules.md` доставляются,
но существующие проектные документы не затираются.

**Дефект.** 21.09.2026, перед закрытием каркаса `cbdin.ro`.

**Чем меряется.** Тест реестра команд; самопроверка установщика в режиме
`--update`; `npm run stage` не печатает несуществующий `check:tokens`.

## И234 · Диспетчер запускает npm тем именем, которое исполняется на этой ОС

На Windows большая проверка пометила красными `typecheck`, `check:port`,
`check:open` и сборку за доли секунды, не показав вывода ни одной команды.
Сайт не запускался вовсе: `spawnSync('npm', ..., shell: false)` искал Unix-
исполняемый файл, тогда как Windows устанавливает `npm.cmd`.

**Как писать.** На Windows `.cmd` нельзя запустить через `spawnSync` напрямую
(`EINVAL`), поэтому диспетчер вызывает системный `cmd.exe /d /s /c` и перед
этим разрешает в имени проверки только буквы, цифры, `_`, `-` и `:`. На
остальных системах запускается `npm` напрямую. `shell: true` не используется.

**Дефект.** 21.09.2026, полный прогон каркаса `cbdin.ro` на Windows.

**Чем меряется.** Тест исходника диспетчера и полный `npm run check:all` на
Windows: вложенные проверки печатают собственный вывод.

## И235 · Дерево адресов знает доменную витрину, где главный язык живёт в корне

Обход адресов умел подставлять только `[lang]` из статического
`lib/locale.ts`. Третья витрина получила `[locale]`, языки магазина из CMS и
главный язык без приставки. Проверка отказалась обходить дерево целиком.

**Как писать.** Для `[locale]` обход читает запасной список магазина из
`lib/site.server.ts`, приводит к кодам адреса и подставляет пустой сегмент
для главного языка, остальные — приставкой. Двойная косая нормализуется.

**Дефект.** 21.09.2026, `check:open` каркаса `cbdin.ro`.

**Чем меряется.** `tools/routes.mjs`: формы `/`, `/en`, `/hu` и их служебные
страницы; живой `check:open` обходит весь список.

## И236 · Проверки скила переносят окончания строк и тонкий CLAUDE.md проекта

После обновления скила `check:rules` не увидел корректную YAML-шапку только
из-за CRLF и потребовал раздел запретов в проектном `CLAUDE.md`, который
намеренно содержит лишь отличия приложения и ссылается на корневые правила.

**Как писать.** Разделители шапки принимают LF и CRLF. Число запретов
сравнивается между файлами, где такой раздел существует; отсутствие раздела
в тонком проектном файле не объявляется ошибкой.

**Дефект.** 21.09.2026, `check:rules` после `--update` в `storefront3`.

**Чем меряется.** Исходник `check-rules.mjs` и зелёный прогон в проекте с
CRLF-скиллом и тонким `CLAUDE.md`.

## И237 · Серверная витрина проверяется своим production-сервером

`check:urls` умел читать только статический `out/`, а Next-витрина собирается
динамической. После успешной сборки проверка адресов останавливалась словами
«Нет out/», хотя серверная сборка была готова.

**Как писать.** Перед проверками адресов, разметки и экрана диспетчер поднимает
`out/` своим статическим сервером, если он есть; иначе — локальный Next из
`node_modules` в режиме `start`, передавая проверкам `SITE`. `check:open`
так же запускает локальный Next напрямую, без непереносимого `npx`.

**Дефект.** 21.09.2026, полный прогон каркаса `cbdin.ro`.

**Чем меряется.** `check:open` и `check:urls` на динамической сборке; полный
`check:all` поднимает и гасит production-сервер сам.

## И239 · Перенос мастерской проверяется другим сайтом и точным снимком

**Дефект.** 22.09.2026: механизмы сохранения/миграций/экспорта оставались только
в CBD, MD и PDF имели два шаблона, update мог затереть локальные инструменты,
а окончание экспорта применяло текущий draft вместо захваченного снимка.

**Как писать.** Общий runtime с адаптером профиля; одни расчёты; versioned parser;
атомарное подтверждение точного снимка; один MD для PDF; экспорт отдельно от
master; hash-manifest и проверка конфликтов перед обновлением. Автотесты
используют verification, не записывают согласование владельца.

**Чем меряется.** `selftest/studio.test.mjs`, `selftest/install.test.mjs`,
`studio:check`, реальный `selftest/studio-pilot` и браузерный экспорт CBD.
Область и границы: `docs/portable-studio-2026-09-22.md`.

## И238 · Большой отчёт не возвращает закрытый пункт в пустой квадрат

Ворота этапа показывали три пункта исполнителя закрытыми, а итог той же
большой проверки ниже печатал их как `□`: отчёт противоречил сам себе.

**Как писать.** Большая проверка читает подтверждения тем же `confirmed()`,
что диспетчер этапов, и печатает `✓` для уже записанного решения.

**Дефект.** 21.09.2026, итог зелёного прогона каркаса `cbdin.ro`.

**Чем меряется.** Оба отчёта читают одну функцию `confirmed`.

## И240 · Проверка на машине заказчика запускается так же, как в облаке

**Дефект.** 22.09.2026, проверка набора режимом аудита на копии стартера
Vendure под Windows. `check:lint` звал `npx` без оболочки — на Windows это
`npx.cmd`, запуск падал `ENOENT`, а отчёт говорил только «линтер не отдал
разбираемый отчёт». `check:open` при умершем `next dev` падал
`ReferenceError: Cannot access 'stop' before initialization` — функция
остановки была объявлена ниже места вызова. `check:craft`, `sweep` и `shade`
при отсутствии Playwright шли по пути `/opt/node22/…` одной облачной машины,
а `sharp` импортировали статически и падали раньше подсказки, что поставить.

**Как писать.** Браузер и `sharp` грузятся одним модулем `tools/browser.mjs`:
путь из `PLAYWRIGHT=` — через file-URL, при отсутствии — команда установки и
выход с кодом 2 («не проверено», а не «найдено»). Внешняя команда на Windows
запускается через оболочку или прямым `node <bin>`; причина сбоя запуска
печатается. Функция, которую зовёт ветка ошибки, объявляется до неё; дерево
процессов на Windows гасит `taskkill /T`.

**Чем меряется.** `selftest/browser.test.mjs`: загрузчик не содержит
машинных путей, без пакета выходит кодом 2 с командой установки;
`check-open.mjs` объявляет `stop` раньше первого вызова.

## И241 · Основной стек описан кодом и запросами, а не принципами

**Дефект.** 22.09.2026, разбор набора. Набор называл основным профилем
Payload + Vendure, но про Vendure не знал ничего конкретного: реестр
источников отмечал ядро Vendure «просмотрены метаданные», стартер
`vendurehq/nextjs-starter-vendure` не встречался нигде, а готовые помощники
были по соглашениям Shopify и Medusa (курсоры вместо `take/skip`,
`optionValueIds` вместо `facetValueFilters`). Отчёт той же сессии называл
стартер встроенным. Разбор стартера нашёл у него самого ошибки, которые
витрина переняла бы: мутации без чтения `ErrorResult`, фильтр «И» внутри
одной грани, `Number(page) || 1`, cookie сессии без `httpOnly`, выход без
токена, переход в `ArrangingPayment` при повторной оплате, деление на 100 в
компоненте.

**Как писать.** Основной стек — отдельный справочник с порядком работы,
запросами и таблицей «как проверить готовую» (`references/vendure.md`,
`references/payload.md`) и исполняемый серверный слой (`assets/vendure/`):
явный канал, классы отказа источника, чтение union-результата, деньги целыми
в минорных единицах, ИЛИ внутри грани, мусорный номер страницы — 404,
картинки сервером ассетов. Чужой код — с лицензией и SHA в `upstream.json`;
его ошибки названы в справочнике, а не перенесены.

**Чем меряется.** `selftest/vendure-resources.test.mjs` — по сценарию на
каждую названную ошибку стартера; `check:skill` — лицензия, SHA и ссылки.

## И242 · Правило об органе приходит вместе с органом

**Дефект.** 22.09.2026, проверка основы. Закон о кнопке и поле стоял в
наборе подробно (craft, `references/controls.md`; правило 10 `CLAUDE.md`),
а самих органов не было: `primitives.module.css` писал «кнопка переехала в
`styles/btn.module.css`» и брал `.input` / `.pick` через `composes` из
`styles/form.module.css` — обоих файлов в наборе не существовало, они
остались в прошлом магазине. Ворота слоя 7 спрашивали шкалы и `.tap` и
горели зелёным. Каждый новый сайт рисовал кнопку и поле заново — ровно то,
от чего закон написан; соседняя сессия так и сообщила: «размеры кнопок не
полностью соответствуют правилам скилла».

**Как писать.** Орган, о котором есть правило, лежит в наборе файлом:
`styles/btn.module.css` — голос, рост и ширина атрибутами на одном классе,
всё от `--btn-h`; `styles/form.module.css` — тройка поля, коробка, список,
галочка, строка ошибки. Ссылка на модуль, которого нет, — дефект основы.

**Чем меряется.** Ворота слоя 7 (`tools/stages.mjs`): каждый `composes …
from` примитивов разрешается в существующий файл, и хоть один дом
контролов (`CONTROLS` в `kit.config.json`) строит орган от `--ctrl-h`.

## И244 · Сохранённый выбор владельца не затирается, утверждение проверяется целиком

**Дефект.** 22.09.2026, проверка ядра мастерской на испорченных данных.
Одно нечитаемое поле (палитра, которую удалили; число строкой) сбрасывало
всю запись к умолчаниям, и следующее движение ползунка записывало умолчания
поверх выбора владельца. Вкладка старой версии понижала запись новой.
Правка, сделанная пока восстановленное утверждение проверялось, стирала его
из хранилища. `verifyApproval` пропускал любую проверку, аргумент которой не
передали; `createHandoff` без `project.id` принимал чужое утверждение;
отпечаток не охватывал, кто и когда утвердил, и проверочный прогон можно было
переписать в «утверждено владельцем». Пути выгрузки сравнивались с учётом
регистра: `.GIT/config`, `Approval.json`, `id_rsa`, `CON.css` проходили.

**Как писать.** Нечитаемый `applied` запирает хранилище до явного
`discardSaved()` с резервом записи; нечитаемый `draft` берёт `applied`;
восстановленное утверждение остаётся в записи, пока проверяется, и
пересчитывается против черновика этого момента. Хранилище создаётся только с
`projectId`, `sourceRevision`, `resolveTokens` и сверяет утверждение с ними
само. Проверка утверждения требует все четыре аргумента; формат 2 подписывает
`actor` и `approvedAt`. Пути выгрузки — простые имена без учёта регистра, без
секретов и имён устройств Windows. Контрольный сайт упаковывает выгрузку
относительными путями: `tar` из Git для Windows читает `C:` как адрес хоста.

**Чем меряется.** `selftest/studio-persistence.test.mjs` — по тесту на каждый
дефект; полный сценарий `selftest/studio-pilot` в Chrome.

## И243 · Основа доказывает адаптивность отрисованной страницей, а не зелёными файлами

**Дефект.** 22.09.2026: заказчик спросил, сделано ли основание, — «у меня даже
с адаптивностью были проблемы». Проверки по файлам основы были зелёными, но
ни одна страница из одной только основы не была отрисована и снята по
ширинам. Первая же такая страница нашла: вводный абзац лида мельчает с 21 до
17,5 px, когда окно растёт с 680 до 880–920 (у заголовка лида этот скачок был
починен потолком `--hero-fold`, у абзаца — нет); заголовок раскрывашки под
пальцем 23 px в высоту — у `summary` не было цели нажатия.

**Как писать.** Основа держит страницу-доказательство (`tools/proof-stand.mjs`):
обычная страница магазина только из шкал, ролей, примитивов, кнопки и поля —
с длинным названием, длинным словом, фильтром у полки, формой и сводкой.
`tools/proof-sweep.mjs` снимает её на всех ширинах свипа мышью и пальцем и
падает на прокрутке вбок, вылете, обрезке, наезде соседей, кадре выше
потолка, цели меньше 44 под пальцем, ступеньке и уменьшении текста при росте
окна. Снимки страницы целиком — отдельными вкладками после замера: снимок
сбрасывает мобильную эмуляцию.

**Чем меряется.** `node tools/proof-stand.mjs && node tools/proof-sweep.mjs`;
отчёт и снимки — `dist/proof/`.

## И245 · Крупный текст считается из набора шкал и растёт с увеличением шрифта

**Дефект.** 22.09.2026, проверка ядра шкал (134 400 наборов, отрисовка по
ширинам). Заголовок героя, заголовок страницы и вводный абзац стояли в
`tokens.css` рукой — пикселями и голым `cqi`, одной строкой на все наборы.
В «Просторном» заголовок страницы (30…42) вышел мельче заголовка раздела
(35,5…49); вводный абзац (низ 17,5) — мельче основного текста макета (18);
при увеличении шрифта в браузере все три стояли на месте (WCAG 1.4.4). Семья
`bareVw` пропускала героя: её выражение не видело вложенных скобок.
`npm run scale` записывал `styles/scale.css`, даже когда замер его браковал.

**Как писать.** Крупный текст — ключ «крупные» каждого набора в
`styles/scale.json`: низ, верх, наклон (px на 1cqi) и основа (px). Строитель
выпускает `clamp(низ rem, основа rem + наклон cqi, верх rem)` в блок каждого
набора и только те концы, которые кто-то читает. Замер требует: основа ≥ 0,
верх ≤ 2.5 низа (TYPE.displaySpread), заголовок страницы выше h2 на обоих
концах, герой не ниже заголовка страницы и h2, ввод не ниже тела макета и не
выше h3. Выпуск идёт только после чистого замера.

**Чем меряется.** `selftest/scale-display.test.mjs`; `check:scale`;
`check:css` (`bareVw` 1 → 0); свип страницы-доказательства без `jump`/`shrink`.

## И246 · Палитра читает краску как записана, кольцо держит 3 : 1 на каждом фоне, выпуск после замера

**Дефект.** 22.09.2026, проверка ядра палитры (1 578 построений, формулы
против первоисточников). Строитель читал `#FFF` как `#000FFF` (синий),
`red` и `#FFCC0080` — молча как мусор, и замер отвечал «0 находок». Кольцо
фокуса подбиралось против одной ступени 1: в тёмной теме выпущенной «Латуни
на угле» оно давало 3,02 на листе и 2,31 на карточке (ступень 4) при норме
3 : 1 — фокус с клавиатуры на кнопке в карточке почти не виден. APCA
отсекала шум на 0.001 вместо loClip 0.1 и не знала deltaYmin 0.0005 —
почти одинаковые краски давали случайное число вместо нуля (пороги 30/60/90
это не задевало). `npm run palette` записывал файл, который замер браковал.

**Как писать.** Краска — только `#RRGGBB` или `#RGB` (разворачивается), иное —
отказ с именем. Кольцо — первая ступень марки от 8, которая держит 3 : 1 на
всех фонах 1–5 (`GROUNDS`); замер спрашивает худший фон. APCA — константы
apca-w3 0.0.98G целиком. Выпуск палитры — только после чистого замера обеих
тем. Кольцо «Латуни» в тёмной теме: `#745D36` → `#B8955A`, 2,31 → 5,15 на
карточке; светлая не изменилась.

**Чем меряется.** `selftest/palette-core.test.mjs`: разбор краски, кольцо на
ступенях 1–5 во всех восьми наборах обеих тем, эталонные числа APCA
(63.06 / 68.54 / 58.15 / 56.24) и ноль ниже loClip, отказ выпуска.

## И247 · Копия движка мастерской сверяется запуском, а не текстом

**Дефект.** 22.09.2026, проверка ядра на копиях репозитория.
`sync-studio-assets.mjs` правил ввозы в копии движка строковой заменой, а
`--check` сравнивал копию с тем же заменённым текстом. Стоило дописать имя в
строку ввоза `tools/scale.mjs` — замена молча не срабатывала, копия ввозила
`kit-config.mjs` и в браузере не загружалась, а проверка печатала «Studio
engines match canonical sources». Так же проходил `node:fs` в `thresholds.mjs`.
«Одна математика для панели, сайта и проверок» держалась на доверии.

**Как писать.** Каждое правило замены срабатывает ровно один раз — иначе
отказ с именем файла. Копия ввозит только соседей по папке движка: ни
`node:`, ни конфиг, ни `require`. Сверка загружает обе версии и сравнивает
вывод строителя шкал и строителя палитры со всеми их замерами на всех
наборах набора (`styles/scale.json`, `styles/palette.json`,
`templates/palette.json`). Ограничение, записанное честно: префиксы и швы
вшиваются в копию из конфига набора; сайт со своими именами шкал должен
перевыпустить копию у себя.

**Чем меряется.** `selftest/studio-sync.test.mjs`: реальный набор проходит
сверку с «same output»; изменённая строка ввоза останавливает выпуск;
`node:` в копии валит сверку.

## И248 · Компонент не ходит в бекенд сам и не считает деньги

**Дефект.** 22.09.2026, проверка ядра. `check:port` охранял разделение
оформления и данных только для массивов из `lib/`: компонент, который сам
звал `getProducts()` из `lib/vendure`, ввозил `payload`, `@payloadcms/*` или
`@apollo/client`, проходил нулём; `(price / 100).toFixed(2)` в компоненте —
тоже; ввоз `"react"` в двойных кавычках или через `require` в общем слое не
виделся. Для основного стека — витрина Next.js над Vendure и Payload —
проверка разделения данных не мерила ничего.

**Как писать.** За данными ходит страница (или серверный слой), компонент
получает нейтральные данные пропсами. Ввоз ТИПА из клиента GraphQL
разрешён. Цену делит и форматирует одна функция адаптера
(`assets/vendure/money.mjs`), компонент её зовёт.

**Чем меряется.** Семьи `backendInView` и `moneyMath` в `check:port`
(храповик, база набора — 0); `engineInShared` видит обе кавычки и
`require`. `selftest/port.test.mjs`.

## И249 · Лист знаков — один на сайт, штрих в пикселях экрана доезжает до каждого знака

**Дефект.** 22.09.2026, проверка основы. Слой 11 стоял «не начато»: закон
(правило 10 `CLAUDE.md`, craft `references/icons.md`) требовал один лист
знаков, а в наборе было восемь SVG без листа — новый сайт рисовал знаки по
месту, и вес штриха снова расходился (прошлый магазин дошёл до семи весов на
сорок одно объявление). Второе: знак, позванный из листа через `<use>`,
живёт в теневом дереве — селекторы страницы (`svg *{vector-effect:…}` из
base.css) до его фигур не доходят, а атрибуты корня листа он не наследует.

**Как писать.** Знаки — Lucide с закреплённым SHA и лицензией в
`skills/site-building/assets/icons/lucide` (34 знака витрины). Лист
`styles/icons.svg` выпускает `npm run icons`; краска и концы штриха стоят на
каждом `<symbol>`, `vector-effect="non-scaling-stroke"` — на каждой фигуре;
толщину знак наследует от своего `<svg>` на странице (base.css, одна строка).
Знак без подписи рядом — `aria-label` на кнопке, рисунок — `aria-hidden`.

**Чем меряется.** `check:icons` (лист не отстал от папки знаков);
`selftest/icons.test.mjs`; снимок знаков на 16 / 24 / 48 px — толщина одна;
свип страницы-доказательства со знаками чист.
