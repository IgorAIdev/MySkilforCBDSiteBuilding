/**
 * Имена семей отрисованной проверки — один список на проверку и на сборщик
 * набора.
 *
 * Заведено по счёту, и счёт этот ТРЕТИЙ. Первый раз список разошёлся у
 * проверки кода (`code-families.mjs`), второй — у проверки вёрстки
 * (`css-families.mjs`), и оба раза болезнь была одна: сборщик набора пишет
 * новому проекту пустую базу, а список семей у него свой, второй экземпляр.
 * Семьи прибавлялись в проверке, база набора оставалась прежней — и новый
 * проект получал файл, молчащий о половине того, что меряется. У этой
 * проверки в базе набора стояло девять семей из тридцати одной.
 *
 * Отдельным файлом, потому что сборщику нельзя ВВОЗИТЬ проверку: она
 * поднимает браузер прямо при чтении.
 */

export const CRAFT_FAMILIES = [
  'placeholder', 'measure', 'lane', 'target', 'contrast', 'collision',
  'weight', 'jump', 'name', 'heads', 'dress', 'clip', 'swipe', 'stretch',
  'broken', 'spill', 'focus', 'wrap', 'anchor', 'covered', 'outline',
  'marker', 'sticky', 'theme', 'coarse', 'calm', 'ladder', 'wideCtrl',
  'lopsided', 'sunk', 'stolen', 'inkDip', 'markInk', 'field', 'alone',
  'catalogueColumns', 'twoAir', 'twiceLift', 'sheetSize',
]

/** Вектор — адрес SVG: файл `.svg` или `data:image/svg+xml`. Семья
 *  `weight` меряет отданные ПИКСЕЛИ, а у вектора их нет: рисунок один на
 *  любой размер, байты не растут с местом. Его `naturalWidth` — не вес, а
 *  размер по умолчанию (150 у SVG без своих `width`/`height`), и сравнивать
 *  его с местом значит считать долгом то, чего нет (И264). Регулярным
 *  выражением, потому что уезжает в страницу строкой: замер импортировать
 *  не умеет. */
export const VECTOR = /^data:image\/svg\+xml[;,]|\.svg(?:[?#]|$)/i

/** Пустая база: ноль по каждой семье. На новом проекте долга нет, и первое
 *  же нарушение обязано валить сборку. */
export const emptyCraftBaseline = () =>
  Object.fromEntries(CRAFT_FAMILIES.map((k) => [k, 0]))

/** Что ловит каждая семья — человеческими словами. Один текст на проверку
 *  (отчёт) и на скилл (таблица в `references/checks.md` собирается отсюда
 *  командой `check:rules --tables`). Пока таблица в скилле набиралась рукой,
 *  в ней было двадцать четыре строки при тридцати одной семье. */
export const CRAFT_LABELS = {
  placeholder: 'незаполненный факт в тексте ([NAME], [COMPANY], [Stand-in clip.]) — считается фактом, а не страницами',
  measure: 'мера текста уже колонки (заголовок в половину ширины)',
  lane: 'полоса под орган отнята у всего текста: боковые поля съели больше четверти ширины',
  target: 'цель нажатия меньше нормы на телефоне (44×44, с data-tap — 24×24)',
  contrast: 'контраст ниже порога',
  collision: 'соседние блоки ближе 8px',
  weight: 'снимок отдан вдвое крупнее места (нет srcset)',
  jump: 'картинка без width/height — вёрстка прыгнет',
  name: 'орган без имени (ни текста, ни aria-label, ни alt)',
  heads: 'лестница заголовков: пропуск уровня или не один h1',
  dress: 'одно действие в двух одеждах: выход из блока рисуется по-разному',
  clip: 'текст, срезанный своим же блоком (nowrap, overflow:hidden)',
  swipe: 'шторка за краем экрана не закрывается движением (правило И8)',
  stretch: 'снимок растянут: своя пропорция не та, что на странице',
  broken: 'снимок не доехал — пустое место там, где картинка',
  spill: 'элемент вылез за правый край страницы (кто именно)',
  focus: 'фокус не виден ничем — клавиатура теряет место',
  wrap: 'подпись контрола сложилась в две строки',
  anchor: 'ссылка внутрь страницы: цели нет или цель уедет под шапку',
  covered: 'заголовок страницы начинается под приклеенной шапкой — первой строки не видно',
  outline: 'контур поверх силуэта: заливка и обводка на одном пути',
  marker: 'маркеры списка у навигации или ряда — «1.» и точки на витрине',
  sticky: 'приклеенный блок выше окна ноутбука — низ не увидеть никогда',
  theme: 'контраст ниже порога в ТЁМНОЙ теме',
  coarse: 'цель нажатия меньше 44 при пальце на широком окне (планшет)',
  calm: 'движение осталось при системной настройке «поменьше движения»',
  ladder: 'размер заголовков не по лестнице: уровень ниже крупнее верхнего, или жирное спорит с h1',
  wideCtrl: 'орган шире меры строки (кнопка в полэкрана) — у колонки нет потолка',
  lopsided: 'рваная правая кромка: у соседей по колонке разные правые вертикали',
  sunk: 'орган утонул в своём полу: заливка кнопки неотличима от того, на чём она стоит',
  stolen: 'мёртвая зона: часть органа не отзывается — указатель перехватывает сосед',
  inkDip: 'краска пропадает ПО ХОДУ перехода: контраст проваливается посередине, слово мигает',
  markInk: 'знак в контроле покрашен не тем, чем слово рядом: чужое правило достало до знака внутри взятой кнопки',
  field: 'лист без поля: буква лежит на краю собственного пола (в файле не видно — поле взято токеном)',
  alone: 'одна в ряду: сетка сложилась в столбик — карточка заняла ряд целиком',
  catalogueColumns: 'товарный каталог нарушает плотность своего режима: без боковой панели не 4–5 или шире 340px, с панелью не 3–4 или шире 380px',
  twoAir: 'шов между блоками оплачен дважды: сверху поле одного, снизу воздух другого — просвет вдвое больше задуманного',
  sheetSize: 'лист набора рисует предмет не в его настоящем размере: образец и витрина разошлись',
  twiceLift: 'под одной рукой поднимаются двое: орган ползёт по тому, на чём стоит, потому что подъём добавили оба',
}
