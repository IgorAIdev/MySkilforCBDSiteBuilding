/**
 * Пары «образец на листе набора — тот же предмет в магазине».
 *
 * Лист набора существует затем, чтобы по нему принимали решения. Предмет,
 * нарисованный на листе не в том размере, в котором он стоит на витрине,
 * превращает лист в чужой рисунок: решают по одному, покупатель видит другое.
 * В файлах признака нет никакого — и лист, и витрина безупречны по
 * отдельности, расходятся они только на отрисованной странице.
 *
 * Каждая строка говорит: где предмет живёт в магазине, где его показывает
 * лист, в каком окне мерить и что обязано совпасть ('w', 'h', 'fs').
 * Совпадать может не всё: у карточки товара рост зависит от длины названия.
 *
 * Разбор и дефекты — в скилле, references/sheet.md.
 */

export const SHEET_SAMPLES = []

/** Насколько можно разойтись, прежде чем это дефект. */
export const SHEET_SLACK = 2

/** Допуск складки height / width: 0.05 не прячет найденные 0.72 → 0.85. */
export const SHEET_AR_SLACK = 0.05
