/**
 * Имена семей проверки вёрстки — один список на проверку и на сборщик набора.
 *
 * Заведено по счёту, и счёт этот второй: ровно та же ошибка уже была у
 * проверки кода и уже была починена файлом `code-families.mjs`. У вёрстки её
 * не заметили, и она прожила дольше.
 *
 * Список жил дважды: в самой проверке (`check-css.mjs`, одиннадцать семей) и
 * в сборщике набора (`kit.mjs`, пять). Семьи прибавлялись в проверке —
 * `halfRole`, `nearStep`, `inlinePx`, `zIndex`, `focusGone`, `noPress`, — а
 * база нового проекта оставалась с пятью. То есть новый сайт получал файл,
 * молчащий о ШЕСТИ из одиннадцати проверок: не «ноль нарушений», а «про эти
 * шесть тут ничего не сказано».
 *
 * Отдельным файлом, потому что сборщику нельзя ВВОЗИТЬ проверку: она
 * запускается прямо при чтении, и сборка набора обернулась бы прогоном
 * проверки по чужому проекту.
 */

export const CSS_FAMILIES = [
  'fontPx', 'spacingPx', 'breakpoint', 'ratioNoCap',
  'halfRole', 'nearStep', 'motion', 'inlinePx',
  'zIndex', 'focusGone', 'noPress', 'liftOnPress', 'stickyCap', 'scrollBleed', 'railY',
  'colLadder', 'proseCols', 'twiceDrawn', 'deadEffect', 'tapGrows', 'dressLayout',
  'twoOwners', 'barNoScroll', 'deadDress', 'plateGap', 'barTwice', 'groundGlue',
  'important', 'takenTwice', 'bareVw', 'padPx', 'airRatio', 'typeGuess', 'hueDirect',
  'nameGrammar', 'stepDirect', 'deadName', 'tierUp',
  'axisUnknown', 'axisTheme', 'axisScope', 'axisHover',
]

/** Пустая база: ноль по каждой семье. На новом проекте долга нет, и первое
 *  же нарушение обязано валить сборку. */
export const emptyCssBaseline = () =>
  Object.fromEntries(CSS_FAMILIES.map((k) => [k, 0]))

/** Что сторожит каждая семья — человеческими словами. Один текст на
 *  проверку (она печатает его в отчёте) и на скилл (таблица в
 *  `references/checks.md` собирается отсюда командой
 *  `check:rules --tables`). Пока подпись жила в проверке, а таблица в
 *  скилле набиралась рукой, они разошлись: в заголовке стояло
 *  «четырнадцать» при восемнадцати семьях. */
/** Выражение «узел зовёт краску по оттенку»: `var(--sage-12)`. Семьи
 *  яруса значений приходят из `kit.config.json` — у каждого проекта свои
 *  имена, и помнить их проверке нельзя. */
export const hueRx = (hues) => new RegExp(String.raw`var\(\s*--(${hues.join('|')})-\d+`, 'g')

export const CSS_LABELS = {
  fontPx: 'font-size в px (правило 1: размер из шкалы --fs-*)',
  spacingPx: 'отступ в px (правило 2: ритм из шкалы --sp-*)',
  breakpoint: 'брейкпоинт вне трёх разрешённых (правило 3)',
  ratioNoCap: 'aspect-ratio без max-block-size (правило 4)',
  halfRole: 'роль переопределена наполовину: знак сменили, поверхность нет',
  nearStep: 'соседние ступени шкалы ближе 8% — глаз их не различает',
  motion: 'движение: двигает раскладку, дольше 500ms или ease-in',
  inlinePx: 'число в разметке: инлайновый стиль мимо шкалы (правила 1 и 2)',
  zIndex: 'z-index числом: имя из --layer-* или верхний слой (<dialog>, popover) (правило 8)',
  focusGone: 'кольцо фокуса снято и не заменено — клавиатура теряет место',
  noPress: 'есть :hover, нет отклика на нажатие — на телефоне контрол молчит',
  liftOnPress: 'нажатие поднимает предмет — на телефоне он подпрыгивает под пальцем',
  stickyCap: 'приклеенное рукой вместо примитива pinned: колонка без потолка от окна (правило 9)',
  scrollBleed: 'панель прокручивается сама, а остаток прокрутки уезжает на страницу под ней',
  railY: 'полоса едет не только вбок: одна ось прокрутки молча делает второй такой же',
  colLadder: 'лестница колонок на медиазапросах вместо auto-fit (правило 5)',
  proseCols: 'сплошной абзац разрезан на колонки — читается сверху вниз один раз',
  twiceDrawn: 'контрол нарисован дважды: знак и его ответ живут в одном месте (правило 10)',
  deadEffect: 'vector-effect на самом svg — не наследуется, молча не работает (писать «svg *»)',
  tapGrows: 'запас под палец вырос коробкой — орган поднял всё, что стоит с ним в строке',
  dressLayout: 'вид предмета пишет свою раскладку — одна и та же, но у каждого вида своя копия',
  twoOwners: 'одно свойство ведут двое — показ и переход одной вещи: на снятии показа она схлопывается кадром',
  deadDress: 'одежда за атрибутом, которого никто не ставит: вид выбран, а его отвергнутые братья остались в стилях и в разметке',
  barNoScroll: 'ползунок одет на том, кто не крутится: прокрутка переехала, одежда осталась',
  groundGlue: 'пол склеивает соседей: при одном значении настройки два блока сливаются, при другом остаются двумя',
  barTwice: 'одежда ползунка написана дважды: общая запись отменяет рисующую, и браузер рисует свой',
  plateGap: 'лист не объявил себя полом: белая карточка на тёмной палубе, а роли внутри неё остались палубными',
  takenTwice: 'взятое через composes и берущий его спорят об одном свойстве при равной силе — победителя выберет порядок кусков сборки',
  important: '!important вне основания: каскад перебит силой, а не порядком — следующая правка перебьёт его ещё одним',
  bareVw: 'рампа без px/rem-слагаемого: голый vw/cqi не растёт при зуме — провал WCAG 1.4.4',
  padPx: 'поле рядом с текстом в px: буквы выросли по настройке телефона, поле — нет (правило 2, роли --pad-*)',
  airRatio: 'воздух страницы к полю карточки меньше 3 : 1 — предметы и промежутки одного размера, ритма нет (замер люкс-магазинов)',
  typeGuess: 'роль текста набрана наполовину: размер из шкалы, а межстрочье или разрядка — числом на месте',
  hueDirect: 'узел зовёт краску по оттенку (--sage-12) мимо роли — ярус значений ему не виден (правило трёх ярусов)',
  nameGrammar: 'имя не по форме: понятие не из реестра или уточнение не из списка (tools/names.mjs) — через месяц его никто не найдёт',
  stepDirect: 'узел читает ступень (--sp-6, --n-12, --fs-sm) напрямую — сырьё только для ссылок из роли (Figma, M3, Curtis)',
  deadName: 'имя объявлено, а не читает его никто в наборе и не просит список ролей — число про запас',
  tierUp: 'ссылка вверх по ярусам: сырьё читает роль, роль читает ручку узла, ручка объявлена на корне',
  axisUnknown: 'медиазапрос по признаку, которого нет в реестре осей (tools/axes.mjs) — ось не названа, значит не проверяется',
  axisTheme: 'переменная объявлена под [data-theme] или prefers-color-scheme — тема ставит только color-scheme, цвет живёт в light-dark()',
  axisScope: 'по указателю или наведению меняется раскладка или видимость — это эвристика об устройстве, ей меняют цель и отклик, не содержимое',
  axisHover: ':hover вне @media (hover: hover) — на телефоне залипает после касания',
}
